---
baseline_commit: 9426417d628818419f3df63a0b4e08dab0743c6c
---

# Story 8.2: Shared-decode wiring — three analyzers consume one `DecodedAudio`

Status: done

## Spec validation history

- **2026-06-10 — factual-claims grep (mandatory pre-spec).** Six epic-AC errors/gaps found against the live codebase:
  1. **`PCMBufferReader.read(url:)` does not exist.** The decode entry is `readMonoSamples(from:maxSeconds:targetSampleRate:) throws -> (samples: [Float], sampleRate: Double)` (`PCMBufferReader.swift:50`). All decode-counter language in this spec targets the real method.
  2. **Epic AC6 is factually impossible as written** — "`BPMAnalyzer`, `LUFSAnalyzer`, and a placeholder `BeatGridAnalyzer` stub all receive identical `OnsetFeatures`". `LUFSAnalyzer` never consumes `OnsetFeatures` (it K-weights raw samples; no mel/onset stage). Reinterpreted in AC4: all three consume the SAME `DecodedAudio`; `OnsetFeatures` byte-identity applies to the onset consumers only.
  3. **Epic AC2's "typically a `DecodedAudio` accessor on `BPMDiagnosticTrace` plus a `decodeOnce` parameter on the service Options struct" — both rejected** (DD #1): embedding multi-MB PCM in every `Sendable` trace snapshot is a memory disaster with no consumer; an Options-level decode-override closure is a DI seam nothing needs.
  4. **`OnsetFeaturesBuilder.build` has ZERO production call sites.** Story 6.2 DD #8 shipped the Tier-1 facade only. The Tier-3 dependency inversion is NOT this story — Story 8.4's step-11 insertion owns it (DD #6).
  5. **`BPMAnalyzer.estimateBPM(samples:sampleRate:options:)` has ~40+ call sites across 9 test/benchmark files** — see DD #4 for the migration (operator-directed full removal, no shim).
  6. **`FeatureSubstrate.DecodedAudio` is MONO by KDD-S2** (`samples: [Float]`, init precondition `sampleRate >= 8_000`). The 8-1-D1/D4 "per-channel carrier lands in 8.2" trigger language was aspirational — re-deferred with corrected triggers (DD #11).
- **2026-06-10 — pre-implementation review round (Codex thread 019eb349 + axiom platform consult + party-mode panel Winston/Amelia/Siri/John, real subagents) + operator decisions.** Headline outcomes, all folded into the DDs below:
  - **Operator directive: NO backwards-compatible cruft.** The v1.0 spec's shim-retention DDs are overruled — `estimateBPM(samples:sampleRate:options:)` and `measureLoudness(samples:sampleRate:)` are REMOVED; all call sites migrate (DD #4/#5, two-commit strategy).
  - **Codex CRITICAL + unanimous panel: the epic's ≥35% perf gate dies.** Asserted with no measured decode-vs-DSP budget; arithmetic (saving = D/(2D+B+L)) makes it codec/intensity-dependent and likely unreachable at intensity 7. Replaced by probe-first + structural gate + probe-derived floor (DD #10, operator choice "Probe + structural gate").
  - **Codex MAJOR: byte-identity claims overstated; "lossless containers" sloppy** (CAF/AIFF-C can carry lossy codecs). Axiom + Siri: Apple documents NO bit-stability for `AVAudioFile` reads; LPCM prefix-reads are structurally stateless (safe to test-lock, no-SRC carve-out); FLAC decode is bit-exact BY FORMAT SPEC → belongs in the bitPattern set. All byte-identity language reframed to LPCM-payload + FLAC named-fixture regression locks (DD #3a).
  - **Codex MAJOR: DD #2 equality contract missed trace/ML/progress divergences** (`durationHintDetail` writes below the 180s gate at `BPMAnalyzer.swift:2503`; internal ML-trace construction at `AudioAnalysisService.swift:331` even when `enableTrace == false`). Rewritten: equality is structural (url path IS the funnel) + result-level under pinned options; trace comparison is whitelist-only (DD #2).
  - **Codex MAJOR + Amelia: the `onDecode` hook leaves the Options structs entirely** — internal `decodeObserver` parameter design (DD #9). Counter pattern: `Mutex<Int>` (Synchronization, macOS 15+); `Mutex` is `~Copyable` → box in a `final class …: Sendable` probe.
  - **Siri: extension-derived codec tagging indefensible** (.m4a can be ALAC; .caf can carry AAC); `audioFile.fileFormat.streamDescription.pointee.mFormatID` is the content-true codec for free (NOT `processingFormat` — that's the decoded PCM side). **Operator choice: reshape `AudioCodec` to codec semantics** (DD #7).
  - **Siri: AVAudioFile already trims declared priming** (packet table: CAF `pakt`, MP4 edit info, LAME/Xing) before samples are visible — but headerless ADTS/MP3 leak encoder delay undetectably, so zero-trims must mean "applied-or-unknown", never "known zero". **Operator choice: PrimingInfo gains explicit trim-state provenance now** (DD #7a; resolves W63 by removal).
  - Codex: new public `readDecodedAudio` would inherit the reader's unsanitized `maxSeconds` `Int64` trap (`PCMBufferReader.swift:95`) → producer sanitizes (DD #3b). AC6 count-lock → set-equality test. AC5 cancellation wording tightened (DD #8).

## Story

As a library consumer,
I want `analyzeBPM` and `analyzeLUFS` (and the forthcoming `analyzeBeatGrid`) to share a single decode pass via public `DecodedAudio`-accepting overloads on `AudioAnalysisService`,
so that combined analysis never pays for the same decode twice (FR-35, structural), and the KDD-C4 seam is in place before Story 8.4's step-11 insertion and any unified `analyze(...)` follow-up.

## Key Design Decisions

1. **The seam is DecodedAudio-accepting overloads + one public producer; combined `analyze(...)` stays deferred (KDD-C4 verbatim).** New/changed public surface, exhaustively enumerated (Mary's seam rule — this IS the PR-description promotion list):
   - `PCMBufferReader.readDecodedAudio(from:maxSeconds:) throws -> FeatureSubstrate.DecodedAudio` — public producer; wraps `readMonoSamples` + codec tagging (DD #7) + trim-state (DD #7a) + `maxSeconds` sanitization (DD #3b).
   - `AudioAnalysisService.analyzeBPM(decoded:options:) throws -> AudioAnalysisResult?` and `analyzeLUFS(decoded:options:) throws -> LUFSReport?`.
   - `LUFSOptions.isCancelled` (DD #8).
   - `FeatureSubstrate.AudioCodec` RESHAPED to codec semantics (DD #7) and `FeatureSubstrate.PrimingInfo` RESHAPED with `TrimState` (DD #7a) — both pre-1.0 breaks with zero existing consumers (verified: nothing in Sources/ constructs either outside tests; zero `AudioCodec` test references).
   - `DecodedAudio.synthetic(_:sampleRate:)` test-construction helper — public, in `BoomBoomBoomKitTestSupport` (NOT the library target).
   - `ClickTrackAIFFBuilder` — public, in `BoomBoomBoomKitTestSupport` (promoted from MetadataCorroborationTests-private so the DD #2 divergence-by-design lock can build tagged fixtures). *Added to this enumeration by the 2026-06-11 review — it shipped public but was disclosed only in the Completion Notes, violating this list's "exhaustive" claim.*
   - First-production-consumer DocC + README for `DecodedAudio`/`PrimingInfo`/`AudioCodec` (the Epic 6 ↔ 8 seam promotion this story owes).
   Internal (NOT public): `decodeOnce` funnel + `decodeObserver` parameter (DD #9), the `BeatGridAnalyzer` stub (DD #6). REMOVED (internal, no public impact): `estimateBPM(samples:sampleRate:options:)`, `measureLoudness(samples:sampleRate:)` (DD #4/#5). Rejected: `BPMDiagnosticTrace` DecodedAudio accessor; Options-level decode closure; `onDecode` fields on Options structs.
2. **The url path IS the funnel — equality is structural, then verified at the result level.** `analyzeBPM(url:options:)` is reimplemented as: URL-bound signal gathering (metadata read, `fileDuration(url:)`) + `decodeOnce` + the SAME shared core that `analyzeBPM(decoded:options:)` calls. One code path; the DD #2 divergence class (trace writes, ML-trace construction, progress ordering) becomes shared behavior, not a reconciliation burden. URL-bound signals on the decoded path: metadata corroboration absent (no URL → `FileMetadataReader` can never run), `fileDurationSeconds = nil` (no derivation from `samples.count` — wrong under a capped decode; the hint is a URL-bound signal, documented). Verification tests:
   - Result-level equality (lossless per DD #3a): `bpm`/`confidence` bitPattern + element-wise `candidates`, decoded-path vs url-path under PINNED options: `metadataPolicy = .disabled`, `durationHint = false`, `ensemblePolicy = .dspOnly`, `enableMLDiagnostics = false`.
   - Trace comparison is FIELD-WHITELIST only (decode-invariant fields) — never whole-trace `==`; `durationHintDetail`/ML-trace internals are excluded with a comment citing `BPMAnalyzer.swift:2503` / `AudioAnalysisService.swift:331`.
   - One divergence-by-design lock: tagged fixture, DEFAULT `metadataPolicy` — url path corroborates (`metadataEvidence` non-empty), decoded path doesn't (`metadataEvidence` empty); both asserted explicitly so nobody "fixes" the divergence later.
3. **Byte-identity discipline (3a) + `maxSeconds` semantics (3b).**
   - **3a.** All bitPattern assertions run on LPCM-payload fixtures (`test-bwf.wav`, `test-bwf.caf` — verified LPCM — AIFF) + FLAC (bit-exact by format spec; the only Apple-owned step is the deterministic int→Float32 scale). AAC/MP3 are tolerance-or-excluded (8-1 measured ~1e-7 same-machine AAC drift). Carve-outs stated in test comments AND DocC: the locked path has NO sample-rate conversion (SRC is stateful; capped-vs-full reads may legitimately differ at the cap boundary under SRC), and claims are "verified by regression tests on LPCM/FLAC fixtures", NEVER "guaranteed by the platform" (Apple documents no bit-stability contract for `AVAudioFile`). The phrase "lossless containers" is banned from spec/tests/docs — gate fixture membership on payload codec.
   - **3b.** Decoded overloads apply their Options' `maxSeconds` as a prefix-slice mirroring the reader's cap arithmetic exactly (`min(AVAudioFrameCount(clamping: Int64(sampleRate * maxSeconds)), totalFrames)` — `PCMBufferReader.swift:95-96`). The 8-1 sanitize rule (finite, > 0, < 1e9, else nil = full file) is extracted to ONE shared internal helper and applied at all three entry points: `readDecodedAudio` (the new public producer must not inherit the `Int64(Double)` trap), `analyzeLUFS` (both paths), and the BPM decoded overload's slice. Negative-path tests at each.
4. **`estimateBPM(samples:sampleRate:options:)` is REMOVED — full migration, two-commit strategy (operator no-cruft directive).** `estimateBPM(decoded:options:)` is the only entry. All ~40+ call sites across the 9 test/benchmark files migrate via `DecodedAudio.synthetic(_:sampleRate:)` (TestSupport helper pinning `.linearPCM` + `.knownNone` — tests never hand-pick provenance).
   - **Commit 1 (mechanical, inert):** add the helper + migrate every call site + the analyzer signature swap, ZERO behavioral change. Full suite + all four corpus floors green BEFORE the seam work starts. Any test that flips here is a finding, not a flake.
   - **Commit 2+:** the seam work lands on the locked baseline.
   - **Pre-migration failure-mode audit:** `DecodedAudio.init` has a `precondition(sampleRate >= 8_000)` — grep the 9 files for `sampleRate:` literals < 8000 and degenerate-input negative tests BEFORE migrating; rewrite those explicitly against the precondition contract (a trap is not a nil).
   - **Provenance-invariance test (new):** same samples, two different codec tags → bitPattern-equal `BPMResult`. `DecodedAudio` is a carrier; nothing downstream may branch on provenance fields.
5. **`LUFSAnalyzer` migrates the same way — and analyzers still never throw.** `measureLoudness(decoded:)` is the only entry; old form removed; same two-commit discipline (the lock: `LUFSByteIdentityTests` UNTOUCHED and green). Unsupported-rate handling stays EXACTLY where it is today: the SERVICE throws `LUFSAnalysisError.unsupportedSampleRate` before calling the analyzer; the analyzer returns nil on missing K-weighting coefficients (project rule: never add `throws` to analyzers — panel suggestion to throw from the analyzer is rejected on this rule). New test: direct analyzer call with an 8 kHz `DecodedAudio` → nil; service decoded overload with the same → throws.
6. **Tier-3 inversion is NOT this story; the beat-grid placeholder is an internal stub.** Story 6.2 DD #8's Tier-1 facade stands. New internal caseless enum `BeatGridAnalyzer`, single member `static func onsetFeatures(for decoded: DecodedAudio, weighting: WeightingProfile) throws -> OnsetFeatures` calling the builder, marked `// Story 8.4 placeholder: real beat-tracking replaces this`. Its assertable invariant IS AC4(b) (bit-equal `OnsetFeatures`) — it is not behaviorless scaffolding.
7. **Codec tagging is content-derived via `fileFormat` mFormatID — `AudioCodec` reshaped to codec semantics (operator-approved pre-1.0 break).** Cases: `.linearPCM, .aac, .alac, .mp3, .flac, .unknown` (`String, Sendable, Hashable, CaseIterable, Codable`; container names .wav/.aiff/.caf are DELETED — they were containers, not codecs, and `PrimingInfo.codec` is about codec priming). `readDecodedAudio` switches on `audioFile.fileFormat.streamDescription.pointee.mFormatID` (`kAudioFormatLinearPCM`, `kAudioFormatMPEG4AAC`, `kAudioFormatAppleLossless`, `kAudioFormatMPEGLayer3`, `kAudioFormatFLAC`, default → `.unknown`). NOTE for dev: `fileFormat` = encoded on-disk format; `processingFormat` = decoded PCM — using the latter would tag everything `.linearPCM`. Invariant test is SET-equality (`Set(AudioCodec.allCases) == [.linearPCM, .aac, .alac, .mp3, .flac, .unknown]`), not count (count passes when a case is swapped). This resolves ALAC-in-.m4a / AAC-in-.caf / compressed-AIFF-C mis-tagging by construction; no extension parsing anywhere.
8. **(7a) `PrimingInfo` reshaped: explicit trim-state provenance, no magic zeros (operator-approved).** `PrimingInfo` becomes `{ codec: AudioCodec, trimState: TrimState }` with `public enum FeatureSubstrate.TrimState: Sendable, Hashable, Codable { case knownNone; case unknown }`. The `leadingTrimFrames`/`trailingTrimFrames` Int fields are DELETED — nothing can populate them honestly in 8.2 (AVAudioFile pre-trims declared priming; undeclared priming is invisible without the C-level `kAudioFilePropertyPacketTableInfo` query, which is Story 8.5's job and lands as an additive `TrimState` case, e.g. `.declared(leadingFrames:trailingFrames:)`). Population rule: `mFormatID == kAudioFormatLinearPCM` → `.knownNone` (LPCM has no priming concept); everything else → `.unknown` ("trim already applied by the decoder, or leaked undetectably — caller must not assume either"). DocC states this verbatim. **Resolves deferred W63 by removal** (the unvalidated Int fields no longer exist).
9. **LUFS cooperative cancellation lands here (resolves deferred 8-1-D2).** `LUFSOptions.isCancelled: @Sendable () -> Bool = { Task.isCancelled }` (exact mirror of the BPM Options field). Check points, exhaustively: url path — before decode (throws with decode count 0) and after decode / before measurement; decoded path — before measurement. SERVICE-LEVEL ONLY — no per-block checks inside `LUFSAnalyzer` (analyzers never throw; an in-flight measurement runs to completion — O(n) vDSP, documented granularity, same ADR-1 rationale as BPM's between-windows-only). `readDecodedAudio` does NOT check cancellation (documented explicitly — it's a plain throwing reader call, same contract as `readMonoSamples`).
10. **(9a) Decode-observer seam: internal `decodeObserver` parameter — public Options structs untouched.** `decodeOnce(url:maxSeconds:isCancelled:observer:)` internal, observer default nil, fired once per actual decode. Internal overloads `analyzeBPM(url:options:decodeObserver:)` / `analyzeLUFS(url:options:decodeObserver:)` thread it; the public methods pass nil. Tests reach the seam via `@testable import`. Counter pattern (Axiom-confirmed): `Mutex<Int>` from Synchronization (macOS 15+); `Mutex` is `~Copyable` so an escaping `@Sendable` closure boxes it in a `final class DecodeProbe: Sendable`. The observer payload is the produced `DecodedAudio` (lets tests assert `samples.count` for slice checks). Decoded overloads cannot decode by construction (no URL parameter) — the type system is the proof; no hook needed there.
11. **(10) Perf: probe FIRST, structural gate HARD, floor probe-derived (operator-approved; replaces the epic's ≥35%).**
    - **T0 probe (BEFORE seam implementation):** measure median decode time D vs analysis time per format on OA300 ({MP3, FLAC, WAV-class} × default intensity, matched `maxSeconds = 120`), release config, ≥3 warmups, randomized file order, median-of-N — record per-format D-share in the Debug Log. ~Half-day cap.
    - **Hard gate 1 (structural, the FR-35 promise, HALT-worthy):** decode fires exactly once per analysis on every path (AC1) — deterministic, never flaky.
    - **Hard gate 2 (directional):** combined shared-path wall-clock ≤ sequential wall-clock per format, within measurement noise — the seam may never make things slower.
    - **Probe-derived floor (reported + gated):** achieved saving (sequential − shared) ≥ 0.8 × measured single-decode cost D_f for the decode-heavy formats (MP3, FLAC). WAV-class is reported only (decode-trivial; noise-dominated). Secondary informational number at library defaults (BPM 120s / LUFS full-file) reported honestly, labeled secondary — no cherry-picking the matched-window number as the headline.
    - Emitted to `_bmad-output/implementation-artifacts/8-2-shared-decode-impact.json` `{perFormat: {sequentialMedian, sharedMedian, decodeMedian, achievedSaving, floorMet}, defaultsConfig: {...}}` via env-gated benchmark test + `make shared-decode-impact-report`. HALT only on gate 1; floor misses surface as findings with the probe data attached.
    - **epics.md Story 8.2 perf AC is PATCHED in this PR** to this formulation (the 35% was asserted, not derived — Codex CRITICAL, panel unanimous).
12. **(11) Deferred-work dispositions.** RESOLVES: 8-1-D2 (DD #9), W63 (by removal, DD #8/7a). RE-DEFERS with corrected triggers: 8-1-D1 + 8-1-D4 — "Story 8.2 per-channel DecodedAudio" was aspirational; KDD-S2 defines mono; new trigger = "a future KDD-amendment story introducing a per-channel decode carrier (none scheduled), or (D4) any consumer report of implausible −100 dBTP on non-silent material". NOT FIXED (documented): W62 — `DecodedAudio.init` finiteness sweep would put an O(n) `allSatisfy` on the hot production decode path for a corrupt-float-only failure mode; trigger unmet.
13. **(12) epics.md Story 8.2 section patched in this PR** with the Spec-validation corrections (read(url:) naming; AC6 LUFS/OnsetFeatures impossibility; AC2 trace-accessor/Options-param rejection; Tier-3 deferral pointer; the DD #10 perf-AC replacement; AudioCodec/PrimingInfo reshape notes).

## Acceptance Criteria

**AC1 — Decode happens exactly once, structurally (FR-35; the only HALT-worthy perf condition).** Via the DD #9 `decodeObserver` seam: (a) `analyzeBPM(url:)` decodes exactly once; (b) `analyzeLUFS(url:)` decodes exactly once; (c) the shared pattern — one `readDecodedAudio` + `analyzeBPM(decoded:)` + `analyzeLUFS(decoded:)` — fires the service observer ZERO times (decode count 1 by construction; the decoded overloads have no URL parameter); (d) no direct `readMonoSamples` call survives in `AudioAnalysisService` outside `decodeOnce` (grep-verified, recorded in Completion Notes).

**AC2 — BPM migration + decoded-path equality.** Per DD #4: old entry removed, all call sites migrated via `DecodedAudio.synthetic`, commit-1 inert (full suite + corpus floors green pre-seam). Equality tests per DD #2: result-level bitPattern under pinned options on LPCM/FLAC fixtures (< 180s); trace whitelist comparison; the divergence-by-design metadata lock; provenance-invariance test; prefix-slice edge tests (non-integral `sampleRate * maxSeconds` boundary; `maxSeconds` > file length is a no-op).

**AC3 — LUFS migration + equality + 8-1 oracle untouched.** Per DD #5: old entry removed; `analyzeLUFS(decoded:options:)` returns `==`-equal `LUFSReport` to the url path on LPCM/FLAC fixtures at matched `maxSeconds` (nil and 30 both). `LUFSByteIdentityTests` UNTOUCHED (zero diff lines) and green. Shared sanitize helper covers all three entry points (DD #3b) with negative-path tests (NaN/±Inf/0/−5/1e15) at `readDecodedAudio` AND both service paths. Analyzer-level 8 kHz nil + service-level throw both locked (DD #5).

**AC4 — Cross-consumer substrate assertion (epic AC6 as corrected).** From ONE `readDecodedAudio`-produced `DecodedAudio`: (a) `OnsetFeaturesBuilder.build(decoded:weighting:.uniform)` element-wise bit-equal to the direct `BPMAnalyzer.computeMelOnsetEnvelopeWithSubBands` path (the existing `uniformWeightingByteIdentity` pattern, now fed by the production producer — additive replacement of its manual construction at `FeatureSubstrateTests.swift:22-28`); (b) the `BeatGridAnalyzer` stub returns bit-equal `OnsetFeatures` for the same input; (c) `LUFSAnalyzer` consumes the same `DecodedAudio` value (samples.count + first/last sample bitPattern) — LUFS shares the decode, not the onset features.

**AC5 — Cancellation contract (resolves 8-1-D2).** Per DD #8, deterministic injectable-closure tests: url-path pre-decode cancel → `CancellationError` + observer count 0; post-decode/pre-measure cancel → throws after exactly one decode; decoded-path pre-measure cancel → throws; non-cancelled path unaffected; BPM decoded-path ordering unchanged (first `ProgressUpdate` before window 0; cancellation checked before each window — same semantics as url path, test-locked). `readDecodedAudio` is documented as NOT cancellation-checking. DocC on `analyzeLUFS` updated (8-1's "no cancellation" note replaced).

**AC6 — Codec tagging + trim provenance.** Per DD #7/#7a: mFormatID switch tests for all 6 outcomes (LPCM-in-wav, LPCM-in-caf → `.linearPCM`; AAC-in-m4a → `.aac`; mp3; flac; unmapped → `.unknown`); `TrimState` population rule (`.knownNone` iff LPCM, else `.unknown`) test-locked; SET-equality invariant on `AudioCodec.allCases`; Codable round-trip for the reshaped `PrimingInfo`.

**AC7 — Corpus floors + benchmarks (MANDATORY — `BPMAnalyzer` internals are touched).** `make benchmark` + `make benchmark-giantsteps` green: OA300 Acc1 ≥ 57/82, Acc2 ≥ 73/82; GiantSteps Acc1 ≥ 537/661, Acc2 ≥ 546/661; expected measurement unchanged (58/82, 74/82, 537, 546) — any delta is a HALT. Run at commit-1 (migration inert proof) AND at story close. `make perf-benchmark` within the ≤ 1% tripwire of the 2026-06-10 baselines (8-1 convention; investigate-then-decide, two-run median per the 8-1 precedent).

**AC8 — Perf probe + gates + report.** Per DD #10: T0 probe numbers in the Debug Log BEFORE seam implementation; hard gate 2 (never slower) green per format; probe-derived floor (≥ 0.8 × D_f saving on MP3 + FLAC) evaluated and reported; `8-2-shared-decode-impact.json` + `make shared-decode-impact-report` target; defaults-config secondary number reported. Floor misses are findings + operator decision, NOT silent relaxation and NOT HALT-theater.

**AC9 — Docs + governance.** DocC `///` on every new/reshaped public symbol (DD #1 list) including: the DD #2 URL-bound-signals caveat on the decoded overloads, the DD #3a "verified by regression tests, not platform-guaranteed" framing, the DD #7a trim-state semantics verbatim, the DD #8 cancellation granularity. README "Shared decode" subsection with the decode-once snippet. epics.md patch (DD #12). deferred-work.md ledger updates (DD #11). Promotion/removal list verbatim in the PR description.

**AC10 — Gates with exact counts.** `make fmt` → `make lint` (1 violation = the canonical `LUFSAnalyzer` TODO), `make test` (baseline 527 tests / 115 suites — record new totals), `make build`. Completion Notes record exact integers.

## Tasks / Subtasks

- [x] T0 (AC8) — Perf probe per DD #10 (decode-vs-DSP medians per format, release, warmups, randomized order); record in Debug Log; derive floors. NO seam code before this lands.
- [x] T1 (AC2, AC3) — Commit 1, mechanical: `DecodedAudio.synthetic` TestSupport helper; pre-migration failure-mode audit (grep `sampleRate:` < 8000 + degenerate inputs); migrate all `estimateBPM`/`measureLoudness` call sites; remove old entries; full suite + four corpus floors green; provenance-invariance test.
- [x] T2 (AC6) — `readDecodedAudio` producer: mFormatID codec switch, `AudioCodec` reshape, `PrimingInfo`/`TrimState` reshape, shared `maxSeconds` sanitize helper; tests.
- [x] T3 (AC1) — `decodeOnce` funnel + `decodeObserver` internal overloads; reimplement url paths as funnel → shared core; counter tests a-d (Mutex-boxed `DecodeProbe`).
- [x] T4 (AC2) — `analyzeBPM(decoded:options:)` overload: prefix-slice cap, equality + slice-edge + trace-whitelist + divergence-lock tests.
- [x] T5 (AC3) — `analyzeLUFS(decoded:options:)` overload + sanitize negative-path tests at all three entries; verify `LUFSByteIdentityTests` zero-diff green.
- [x] T6 (AC4) — `BeatGridAnalyzer` stub; rewire `FeatureSubstrateTests` cross-consumer assertion to the production producer.
- [x] T7 (AC5) — `LUFSOptions.isCancelled` + check points + deterministic tests + BPM decoded-path progress/cancel ordering tests + DocC updates.
- [x] T8 (AC8) — Shared-decode impact harness (env-gated benchmark test + Makefile target + JSON emit); run gates; report.
- [x] T9 (AC7) — `make benchmark` / `benchmark-giantsteps` / `perf-benchmark`; record counts (commit-1 run + close run).
- [x] T10 (AC9, AC10) — DocC, README, epics.md patch, deferred-work.md ledger, `make fmt`/`lint`/`test`/`build`, Completion Notes with exact counts + promotion/removal list.

## Dev Notes

**Read before editing (non-negotiable):** `PCMBufferReader.swift:50-155` (cap arithmetic :95-96 — the DD #3b mirror contract; the 8 kHz guard :72), `AudioAnalysisService.swift:813-915` (`runPreCorroborationPipeline` — decode :836-838, window loop :861-889; the funnel refactor reshapes this to accept `DecodedAudio` + optional URL-bound inputs) and `:1021-1055` (`analyzeLUFS` — sanitization to extract), `:331` + `:376` (internal ML-trace construction + post-selection ML — shared under the funnel, but invalidates any "trace nil when disabled" assertion), `BPMAnalyzer.swift:180-211` (entry; :211 is the energy-transition analysis-window slice, NOT the maxSeconds cap) and `:2503` (`durationHintDetail` writes below the 180s gate — whitelist around it), `LUFSAnalyzer.swift:118-141`, `FeatureSubstrateTests.swift:20-49`, `LUFSByteIdentityTests.swift` (do-not-touch oracle).

**What must be preserved:** the 11-step pipeline spine and step numbering; `merge`'s 41-call-site signature; pool-authoritative `select(from:)`; `runPreCorroborationPipeline`'s production order (cancellation → metadata → duration → decode → windows → pool) — the funnel refactor keeps this order with decode swapped to `decodeOnce`; `@preconcurrency import AVFoundation` + `nonisolated(unsafe)` converter block; K-weighting `vDSP.Biquad<Double>` untouched; the `LUFSAnalyzer.swift:127` canonical TODO.

**Byte-identity discipline (Epic-7 A3, re-armed):** land additive structure under a green byte oracle; never flip semantics and structure in one motion — this is exactly the DD #4 two-commit strategy. The decoded-vs-url equality tests are this story's T0-equivalent regression backbone.

**Previous-story intelligence (Story 8-1, done 2026-06-10, PR #32):** (1) T0-first ordering — the inherited `LUFSByteIdentityTests` lock stays byte-untouched. (2) Fail-closed (Epic-7 A1): cancellation/sanitization throw or nil, never a wrong number. (3) Use-site sanitization precedent (`votingThreshold`, 8-1 `maxSeconds` patch) — now extracted to the shared helper. (4) 8-1's review converged 3-for-3 on a boundary-semantics bug — test the slice-cap BOUNDARY (non-integral `sampleRate * maxSeconds`), not just the happy path. (5) Gates baseline: 527 tests / 115 suites; lint = 1 canonical TODO; perf wall-clock 0.179s ± 1%. (6) Operator-owned closeout: separate-LLM `/bmad-code-review`, 1Password-signed commit, PR into `rterhaar/epic-8`.

**Git intelligence:** Story 8-1 = 3 commits on `rterhaar/8-1` (`1adc3c3`, `5ad43d1`, `b901608`); PR #32 → `rterhaar/epic-8`. This story branches post-merge. Commit pattern `Story 8-2: <imperative-summary>`; deliverables-only messages; commit 1 of the DD #4 strategy is its own `Story 8-2: migrate analyzers to DecodedAudio currency` commit.

**Swift 6 notes:** `decodeObserver` and `isCancelled` are `@Sendable`; no mutable statics; test counters via `Mutex<Int>` (Synchronization, macOS 15+) boxed in a `final class …: Sendable` (`Mutex` is `~Copyable` — an escaping closure can't capture it directly). All new public types `Sendable`. The prefix-slice is an `Array` operation, not a numeric sweep — no vDSP needed.

**Scope guards:** NO Tier-3 builder inversion (8.4). NO per-channel decode (no architecture carrier). NO combined `analyze(...)` (KDD-C4 defers). NO `BeatGrid` public types (8.3). NO `kAudioFilePropertyPacketTableInfo` priming query (8.5 — `TrimState` gets its `.declared` case there). NO analyzer-level cancellation or throws (project rule). The `BeatGridAnalyzer` stub is internal and ~10 lines.

### Project Structure Notes

New: `Tests/BoomBoomBoomKitTests/SharedDecodeTests.swift` (counter/equality/cancellation/codec tests; split per dev judgment), `Sources/BoomBoomBoomKit/BeatGridAnalyzer.swift` (internal stub, root), `Sources/BoomBoomBoomKit/FeatureSubstrate/TrimState.swift` (or co-located in `PrimingInfo.swift` — dev's call), `Sources/BoomBoomBoomKitTestSupport/DecodedAudioFixtures.swift` (the `synthetic` helper), `Tests/BoomBoomBoomKitBenchmarkTests/SharedDecodeImpactTests.swift`, `make shared-decode-impact-report`. Modified: `PCMBufferReader.swift`, `AudioAnalysisService.swift`, `BPMAnalyzer.swift` (entry), `LUFSAnalyzer.swift` (entry), `FeatureSubstrate/AudioCodec.swift` (reshape), `FeatureSubstrate/PrimingInfo.swift` (reshape), `LUFSReport.swift` (`LUFSOptions.isCancelled`), `FeatureSubstrateTests.swift`, the 9 migration files, `README.md`, `Makefile`, `_bmad-output/planning-artifacts/epics.md`, `_bmad-output/implementation-artifacts/deferred-work.md`, sprint-status.yaml.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 8.2] (as corrected by this story's PR, DD #12)
- [Source: _bmad-output/planning-artifacts/architecture.md#KDD-C4 (:528) / #KDD-S2 (:191)] — mono `DecodedAudio` seam; combined `analyze(...)` deferred
- [Source: _bmad-output/implementation-artifacts/6-2-featuresubstrate-namespace-and-mlfeatureframes-relocation.md DD #8] — Tier-1 facade / Tier-3 deferral
- [Source: _bmad-output/implementation-artifacts/8-1-lufs-public-api-and-lufsreport-promotion.md] — PSI, byte-identity oracle, sanitization precedent, deferred entries 8-1-D1/D2/D4
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — W62, W63, 8-1-D1, 8-1-D2, 8-1-D4 dispositions per DD #11
- [Source: _bmad-output/project-context.md] — ADR-11, cascade rule, byte-equality backbone, fail-closed, analyzers-never-throw, vDSP rules
- Review round 2026-06-10: Codex MCP thread 019eb349 (adversarial spec review); axiom-media/axiom-concurrency platform consult (AVAudioFile bit-stability, FLAC format guarantee, Mutex pattern); party-mode panel Winston/Amelia/Siri/John (this file, Spec validation history)

## Dev Agent Record

### Agent Model Used

claude-fable-5[1m] (Fable 5)

### Debug Log References

- **T0 probe (2026-06-10, BEFORE any seam code — DD #10).** Release config, M5 Max, OA300, matched `maxSeconds = 120`, default intensity, deterministic SplitMix64 file selection (seed 0x82), per-file 1 warmup + 3 measured reps, per-format median-of-medians. Harness: `SharedDecodeProbeTests/decodeVsAnalysisProbe` (env-gated `SHARED_DECODE_PROBE=1`), first member of the T8 impact file.
  | format | n | decode D | BPM url-path | LUFS url-path | sequential | D-share | floor (0.8×D_f) |
  |---|---|---|---|---|---|---|---|
  | mp3 | 10 | 0.1150s | 0.2056s | 0.1358s | 0.3414s | 33.7% | **0.0920s** |
  | flac | 3 | 0.0993s | 0.1973s | 0.1198s | 0.3171s | 31.3% | **0.0795s** |
  | wav-class | 10 | 0.0084s | 0.1016s | 0.0283s | 0.1299s | 6.4% | reported-only |
  D-share = D / (T_bpm + T_lufs) is the THEORETICAL maximum recoverable saving (each url path contains one decode). Empirical vindication of the killed ≥35% epic gate: no format's ceiling reaches 35%. Derived hard floors for AC8 gate 3: achieved saving ≥ 0.0920s (mp3) and ≥ 0.0795s (flac); wav-class reported-only per DD #10.
- **T1 / commit 1 (`75da815`, 2026-06-10).** Two findings + one sequencing decision:
  1. **AC3's "`LUFSByteIdentityTests` UNTOUCHED (zero diff lines)" was unsatisfiable as written** — the oracle file itself calls `LUFSAnalyzer.measureLoudness(samples:sampleRate:)` directly at 3 sites (:85, :168, :205), and DD #4/#5 (operator no-cruft directive, binding) removes that entry. Resolution: the 3 call shapes migrated mechanically to `.synthetic`; **every bitPattern literal is byte-untouched** (diff verified: exactly 3 call-shape lines) and all three oracle suites pass green. AC3's intent (the literals ARE the contract) is fully preserved; the "zero diff lines" letter is amended to "zero literal changes".
  2. **Pre-migration failure-mode audit:** repo-wide grep found NO `sampleRate:` literal < 8000 at any analyzer call site (lowest is 22050 in `LUFSAnalyzerTests` unsupported-rate test — passes the `DecodedAudio` precondition; analyzer correctly returns nil). Degenerate-input tests (`samples: []`, 0.5s input, silence) all use ≥ 44100 — no precondition-contract rewrites needed.
  3. **Sequencing: the AudioCodec/PrimingInfo/TrimState TYPE reshape (DD #7/#7a) landed in commit 1**, not the seam commit — carrier-only types with zero production readers (provably inert; floors confirm), and it lets `DecodedAudio.synthetic` pin its final `.linearPCM`/`.knownNone` shape from the start instead of churning in T2. The mFormatID PRODUCER stays in the seam commit.
  Call-site census: 53 `estimateBPM` migrations across 8 test files + 2 service sites; 31 `measureLoudness` migrations across 3 test files + 1 service site. Service constructs placeholder provenance (`.unknown`/`.unknown`) until the T3 funnel. Gates at commit 1: unit suite 533/117 (was 527/115 + 6 new tests/2 suites — exact accounting), OA300 **58/82 + 74/82**, GiantSteps **537/661 + 546/661** — all four floors at the exact expected measurement (inertness proven, not assumed).

### Completion Notes List

- **Promotion/removal list (DD #1, verbatim for the PR description):**
  - ADDED public (library): `PCMBufferReader.readDecodedAudio(from:maxSeconds:) throws -> FeatureSubstrate.DecodedAudio`; `AudioAnalysisService.analyzeBPM(decoded:options:) throws -> AudioAnalysisResult?`; `AudioAnalysisService.analyzeLUFS(decoded:options:) throws -> LUFSReport?`; `LUFSOptions.isCancelled: @Sendable () -> Bool`; `FeatureSubstrate.TrimState` (new enum: `.knownNone` / `.unknown`).
  - RESHAPED public (pre-1.0 breaks, zero external consumers): `FeatureSubstrate.AudioCodec` -> codec semantics (`.linearPCM/.aac/.alac/.mp3/.flac/.unknown`; container cases `.wav/.aiff/.caf` deleted); `FeatureSubstrate.PrimingInfo` -> `{codec, trimState}` (`leadingTrimFrames`/`trailingTrimFrames` deleted — resolves W63 by removal).
  - ADDED public (TestSupport): `FeatureSubstrate.DecodedAudio.synthetic(_:sampleRate:)`; `ClickTrackAIFFBuilder` (promoted from MetadataCorroborationTests-private — TestSupport is the canonical home for shared test utilities).
  - REMOVED internal: `BPMAnalyzer.estimateBPM(samples:sampleRate:)` + `estimateBPM(samples:sampleRate:options:)`; `LUFSAnalyzer.measureLoudness(samples:sampleRate:)` (operator no-cruft directive — `estimateBPM(decoded:options:)` / `measureLoudness(decoded:)` are the only entries).
  - ADDED internal: `AudioAnalysisService.decodeOnce(url:maxSeconds:isCancelled:observer:)`, observer-threaded `analyzeBPM`/`analyzeLUFS(url:options:decodeObserver:)` overloads, `preCorroborationCore`, `finishBPMAnalysis`, `lufsReport`, `applyCap`, `shouldBuildTrace`; `PCMBufferReader.sanitizedMaxSeconds`, `cappedSampleCount`, `openFile`, `codec(forFormatID:)`, single-open `readMonoSamples(file:url:...)` core; `BeatGridAnalyzer` stub (Story 8.4 placeholder).
  - REJECTED (per spec): `BPMDiagnosticTrace` DecodedAudio accessor; Options-level decode closure; `onDecode` fields on public Options structs.
- **AC1 (decode-once, structural):** counter tests a-c green via Mutex-boxed `DecodeProbe` (`DecodeCountTests`); (d) grep-verified — `readMonoSamples` appears in `AudioAnalysisService.swift` ONLY in a doc comment on `decodeOnce` (zero call sites; the funnel calls `readDecodedAudio`).
- **AC2/AC3 (equality):** url-vs-decoded bitPattern equality green on the full DD #3a set (bpm-120-click.wav, test-bwf.caf, sample-with-cover.aiff, test-audio.flac for BPM; test-bwf.wav/.caf + sample-with-cover.flac for LUFS at maxSeconds nil AND 30) — first-run green. Slice-edge boundary (7.3s non-integral cap; truncation 30869-not-30870 locked at the `cappedSampleCount` mirror), cap-beyond-length no-op, trace allowlist comparison, and the divergence-by-design metadata lock (via the promoted `ClickTrackAIFFBuilder`) all green. `LUFSByteIdentityTests`: all bitPattern literals byte-untouched (3 call-shape-only lines — Debug Log finding #1) and green.
- **AC4 (cross-consumer):** `FeatureSubstrateTests` rewired to the production producer (`uniformWeightingByteIdentity` + new `crossConsumerSharedSubstrate`): builder facade == direct BPMAnalyzer path == `BeatGridAnalyzer` stub (bit-equal `OnsetFeatures`); LUFS consumes the same carrier value.
- **AC5 (cancellation):** deterministic `CancellationScript` tests — url pre-decode cancel (decode count 0), post-decode/pre-measure cancel (exactly 1 decode), decoded-path pre-measure cancel, non-cancelled path unaffected; BPM decoded-path ordering locked (first `ProgressUpdate` is `(0, total)`; pre-cancelled call throws before any progress fires). `readDecodedAudio` documented as NOT cancellation-checking; 8-1's "no cancellation" DocC note replaced.
- **AC6 (codec/trim):** all 8 mFormatID outcomes locked on real fixtures, including two new afconvert-generated fixtures: `test-alac.m4a` (ALAC-in-m4a — DD #7's motivating mis-tagging case) and `test-ulaw.caf` (mu-law -> `.unknown` unmapped fallback). SET-equality on `AudioCodec.allCases`, `PrimingInfo` Codable round-trip, TrimState population rule (`.knownNone` iff LPCM) all green.
- **AC8 (perf gates, release config, M5 Max):** T0 probe in Debug Log. Initial impact run (`make shared-decode-impact-report`, 2026-06-10): mp3 sequential 0.3296s -> shared 0.2203s, saving **0.1093s** >= floor 0.0892s ✓; flac 0.3079s -> 0.2087s, saving **0.0992s** >= floor 0.0776s ✓; wav-class 0.1266s -> 0.1193s (reported-only). **AUTHORITATIVE re-run (2026-06-12, post-review methodology fix — alternating per-rep order + median-of-paired-savings; the original fixed seq-then-shared order warmed caches in the shared path's favor):** mp3 sequential 0.3288s -> shared 0.2204s, saving **0.1107s** >= floor 0.0892s ✓; flac 0.3138s -> 0.2107s, saving **0.1031s** >= floor 0.0785s ✓; wav-class 0.1262s -> 0.1184s (reported-only). The bias was negligible — the re-run savings are marginally LARGER, confirming the original conclusion. The floor is deliberately 0.8 × SAME-RUN decode median (honest same-machine denominator), not the frozen T0 numbers — review 2026-06-11 finding, acknowledged in the harness doc. Hard gate 2 (never slower, now with 0.020s absolute epsilon) green on every format. Defaults-config SECONDARY (mp3, BPM 120s cap / LUFS full-file): sequential 0.5570s -> shared 0.4386s. JSON at `_bmad-output/implementation-artifacts/8-2-shared-decode-impact.json` (`39f75c4-dirty`). The achieved mp3 saving is ~99% of the measured decode cost — the seam recovers essentially the whole duplicate decode.
- **AC7 close-run (2026-06-10, post-seam):** OA300 Acc1 = **58/82**, Acc2 = **74/82**; GiantSteps strict Acc1 = **537/661**, Acc2 = **546/661** — all four floors at the EXACT expected measurement (commit-1 run identical; zero accuracy delta across the entire story). `make perf-benchmark` two-run reading (8-1 investigate-then-decide precedent): run 1 mean 0.175s (−2.3% vs the ae04011 baseline 0.179s), run 2 mean 0.173s (−1.4% vs run 1's baseline) — both runs FASTER than baseline, outside the ±1% tripwire in the improving direction. Investigated: the seam adds only a single `AVAudioFile` open + an mFormatID switch to the url path (sub-ms vs 0.18s wall) and the decode core is byte-identical, so the delta is run-to-run machine variance trending favorable; no regression, no action. Per-run baseline JSONs committed per 8-1 convention.
- **AC10 gates (final, post-`/bmad-code-review` patch round 2026-06-11):** `make fmt` clean; `make lint` = **1 violation** (the canonical `LUFSAnalyzer` TODO); `make test` = **560 tests / 123 suites, 0 failures** (was 527/115 at baseline → +33 tests / +8 suites, all Story 8-2; the +1 over the 2026-06-11 Codex-round count is the review's mid-flight cancellation test); `make build` green; benchmark target compiles + passes in release (`-c release` impact re-run, 2026-06-12).

### Review Findings

**Pre-PR Codex diff review (layer 3, thread 019eb486, 2 rounds, 2026-06-11)** on the staged seam diff. Three findings, all verified against code and patched in-diff; round 2 confirmed all fixes, zero merge-blockers:

- [x] [Review][Patch] **MAJOR — `cappedSampleCount` could trap on a precondition-valid huge sample rate.** `DecodedAudio.init` admits any finite rate ≥ 8 kHz, so `Int64(sampleRate * maxSeconds)` overflows for e.g. a synthetic 1e20 Hz carrier reaching the PUBLIC decoded overloads; the reader's own cap path carried the same latent trap. Patched: both sites guard `cappedFrames.isFinite && cappedFrames < Double(Int64.max)` and no-op the cap otherwise (a cap that large cannot bound a real buffer — mirror semantics preserved). Locked by unit tests at 1e300/1e17 + end-to-end `hugeSampleRateCarrierDoesNotTrap` (BPM → nil, LUFS → throws). Side effect, Codex-endorsed: un-sanitized NaN/Inf `maxSeconds` on public `readMonoSamples` now reads the full file instead of trapping — documented in DocC + locked by `readMonoSamplesNonFiniteCapReadsFullFile`.
- [x] [Review][Patch] **MAJOR — `analyzeBPM(decoded:)` applied the prefix slice before checking cancellation** (a pre-cancelled call paid a potentially multi-MB copy; asymmetric with the LUFS decoded path). Patched: `isCancelled` checked first, before metadata-input construction and `applyCap`.
- [x] [Review][Patch] **MINOR — AC4(c)'s LUFS leg was vacuous** (direct-analyzer call + first/last identity re-checks on immutable value data prove nothing). Patched: the leg now goes through the SERVICE seam (`analyzeLUFS(decoded:)`) and asserts real report content, with a pointer to `LUFSDecodedEqualityTests` as the authoritative equality lock. Round-2 nit (stale doc comment) also fixed.
- **Pending user action (operator-owned closeout):** separate-LLM `/bmad-code-review`; PR into `rterhaar/epic-8` (1Password-signed commits — dev-session commits are signed automatically via git config); optional perf-baseline regeneration commit.

**`/bmad-code-review` (2026-06-11, three layers: Blind Hunter via Codex background agent, Edge Case Hunter, Acceptance Auditor).** 21 raw findings → 1 decision-needed, 11 patch, 1 defer, 6 dismissed (false missing-import claim — `MetadataCorroborationTests.swift:11` has it; decode-once-by-construction is DD #9's own design; floor print-not-assert is AC8's letter; env-gated suite skip is repo convention; T0 probe necessarily predates `readDecodedAudio`; commit-timestamp archaeology not actionable). Auditor verified clean: AC1, AC2, AC6, AC10 (by execution: 559/123), DD #3b/#7a/#8/#9/#11/#12, all scope guards.

- [x] [Review][Decision→Patch] **Impact-harness methodology favors the shared path** — per measured rep, sequential always ran immediately before shared (page-cache/codec warming benefits shared), and `achievedSaving` was a difference of independent medians, not the median of paired per-file savings. **Operator chose fix + re-run:** per-rep order now alternates (matched-window loop AND defaults-config secondary), `achievedSaving` = median of paired per-file savings; release-config impact report re-run, numbers below. [Tests/BoomBoomBoomKitBenchmarkTests/SharedDecodeImpactTests.swift]
- [x] [Review][Patch] **`DecodedAudio` itself has no DocC — AC9/DD #1 partially unmet** (struct, three public properties, public init; the carrier the README now headlines) — DocC added [Sources/BoomBoomBoomKit/FeatureSubstrate/DecodedAudio.swift]
- [x] [Review][Patch] **`ClickTrackAIFFBuilder` is out-of-list public surface** — DD #1's enumeration didn't include it (disclosed in Completion Notes only). Added to the DD #1 list + `///` added on the enum [Sources/BoomBoomBoomKitTestSupport/ClickTrackAIFFBuilder.swift:12]
- [x] [Review][Patch] **`ClickTrackAIFFBuilder.write` traps without diagnostics on degenerate params** — `precondition(..., message)` guards added for clickBPM (finite, > 0, ≤ rate×60), durationSeconds (finite, ≥ 0), sampleRate (finite, ≥ 1) [Sources/BoomBoomBoomKitTestSupport/ClickTrackAIFFBuilder.swift]
- [x] [Review][Patch] **BPM `Options.maxSeconds` doc omits the sanitize rule** — non-positive/non-finite reads the FULL file (pre-8.2: zero-frame read); doc now states the DD #3b rule + the budget-underflow warning, matching `LUFSOptions.maxSeconds` [Sources/BoomBoomBoomKit/AudioAnalysisService.swift:89]
- [x] [Review][Patch] **Public `readMonoSamples` negative-cap split behavior undocumented** — finite negative → 0 frames vs `-.infinity` → full file; DocC now states both halves [Sources/BoomBoomBoomKit/PCMBufferReader.swift:47-52]
- [x] [Review][Patch] **Impact harness never asserts MP3/FLAC coverage** — `#expect(perFormat["mp3"/"flac"] != nil)` coverage asserts added after the format loop [Tests/BoomBoomBoomKitBenchmarkTests/SharedDecodeImpactTests.swift]
- [x] [Review][Patch] **wav-class never-slower gate is relative-only** — gate 2 now `shared <= sequential * 1.10 + 0.020s` absolute epsilon per DD #10's "within measurement noise" [Tests/BoomBoomBoomKitBenchmarkTests/SharedDecodeImpactTests.swift]
- [x] [Review][Patch] **`perf-benchmark` Makefile target lacks the dirty-suffix `GIT_SHA` its siblings have** — the committed close-run baselines say clean `75da815` while the seam sat uncommitted (impact JSON 8 minutes earlier says `75da815-dirty`). Target patched to the sibling dirty-suffix form; baseline regeneration stays the already-pending operator action [Makefile perf-benchmark; _bmad-output/perf-baselines/*75da815*.json]
- [x] [Review][Patch] **Floor provenance mismatch unacknowledged** — the shipped gate computes a same-run floor (`saving >= 0.8 × same-run decode median`) while the Debug Log declared T0-probe floors (0.0920/0.0795); both cleared, no outcome flips. Harness doc + Debug Log now state the same-run floor is deliberate (same machine, same run = honest denominator; T0 floors are the historical derivation) [Tests/BoomBoomBoomKitBenchmarkTests/SharedDecodeImpactTests.swift; this file]
- [x] [Review][Patch] **AC5's decoded-path "cancellation checked before each window … test-locked" was only locked at window 0** — `cancellationMidFlightAfterWindowZero` added (window 0 completes, per-window check cancels before window 1; exactly one ProgressUpdate) [Tests/BoomBoomBoomKitTests/SharedDecodeTests.swift]
- [x] [Review][Patch] **sprint-status.yaml said "557 tests/123 suites"; actual was 559** — corrected (now 560 after the mid-flight test landed) [_bmad-output/implementation-artifacts/sprint-status.yaml]
- [x] [Review][Defer] **AAC-family `AudioFormatID`s beyond `kAudioFormatMPEG4AAC` (HE-AAC, AAC-LD, AAC-ELD) tag `.unknown`** [Sources/BoomBoomBoomKit/PCMBufferReader.swift `codec(forFormatID:)`] — deferred: DD #7 enumerates the closed 6-ID mapping by spec; `.unknown` is the honest fallback. Re-open trigger: a consumer report of an AAC-variant fixture that should tag `.aac`.

### File List

New (library): `Sources/BoomBoomBoomKit/BeatGridAnalyzer.swift`
New (TestSupport): `Sources/BoomBoomBoomKitTestSupport/DecodedAudioFixtures.swift`, `Sources/BoomBoomBoomKitTestSupport/ClickTrackAIFFBuilder.swift`, `Sources/BoomBoomBoomKitTestSupport/Resources/AudioFixtures/test-alac.m4a`, `Sources/BoomBoomBoomKitTestSupport/Resources/AudioFixtures/test-ulaw.caf`
New (tests): `Tests/BoomBoomBoomKitTests/SharedDecodeTests.swift`, `Tests/BoomBoomBoomKitBenchmarkTests/SharedDecodeImpactTests.swift`
Modified (library): `Sources/BoomBoomBoomKit/AudioAnalysisService.swift`, `Sources/BoomBoomBoomKit/BPMAnalyzer.swift`, `Sources/BoomBoomBoomKit/LUFSAnalyzer.swift`, `Sources/BoomBoomBoomKit/PCMBufferReader.swift`, `Sources/BoomBoomBoomKit/LUFSReport.swift`, `Sources/BoomBoomBoomKit/FeatureSubstrate/AudioCodec.swift`, `Sources/BoomBoomBoomKit/FeatureSubstrate/PrimingInfo.swift`
Modified (tests): `Tests/BoomBoomBoomKitTests/BPMAnalyzerTests.swift`, `Tests/BoomBoomBoomKitTests/BPMAnalyzerDurationHintTests.swift`, `Tests/BoomBoomBoomKitTests/BPMAnalyzerClickTrackTests.swift`, `Tests/BoomBoomBoomKitTests/SuperFluxByteIdentityTests.swift`, `Tests/BoomBoomBoomKitTests/AblationQuickTests.swift`, `Tests/BoomBoomBoomKitTests/LUFSAnalyzerTests.swift`, `Tests/BoomBoomBoomKitTests/LUFSByteIdentityTests.swift`, `Tests/BoomBoomBoomKitTests/AudioAnalysisServiceTests.swift`, `Tests/BoomBoomBoomKitTests/FeatureSubstrateTests.swift`, `Tests/BoomBoomBoomKitTests/MetadataCorroborationTests.swift`, `Tests/BoomBoomBoomKitBenchmarkTests/AblationFullMatrixTests.swift`, `Tests/BoomBoomBoomKitBenchmarkTests/OA300BenchmarkTests.swift`, `Tests/BoomBoomBoomKitBenchmarkTests/SuperFluxImpactTests.swift`
Modified (docs/infra): `README.md`, `Makefile`, `_bmad-output/planning-artifacts/epics.md`, `_bmad-output/implementation-artifacts/deferred-work.md`, `_bmad-output/implementation-artifacts/sprint-status.yaml`, this story file
Generated: `_bmad-output/implementation-artifacts/8-2-shared-decode-impact.json`

## Change Log

- 2026-06-10 — Story 8-2 implemented on `rterhaar/8-2` (baseline `9426417` = PR #32 merge). T0 probe first (commit `f9361c5`); commit-1 mechanical DecodedAudio-currency migration (commit `75da815`, floors exact); seam commit: `readDecodedAudio` producer (mFormatID codec tagging + TrimState), `decodeOnce` funnel + internal `decodeObserver` seam, `analyzeBPM(decoded:options:)` / `analyzeLUFS(decoded:options:)` overloads sharing the url-path cores verbatim, `LUFSOptions.isCancelled` (resolves 8-1-D2), `BeatGridAnalyzer` stub, impact harness + `make shared-decode-impact-report` (all gates green; mp3 saving 0.1093s ≥ 0.0892s floor, flac 0.0992s ≥ 0.0776s), README "Shared decode" section, epics.md Story-8.2 patch (DD #12), deferred-work ledger (DD #11). Status → review.
- 2026-06-11 — Pre-PR Codex diff review (thread 019eb486, 2 rounds): 2 MAJOR (Int64 cap-arithmetic trap on precondition-valid huge sample rates — guarded at both `cappedSampleCount` and the reader cap; BPM decoded-path cancellation checked before the prefix slice) + 1 MINOR (vacuous AC4c assertion → service-seam assertion) patched in-diff with regression tests; round 2 confirmed, zero merge-blockers.
- 2026-06-11 — `/bmad-code-review` (three layers: Blind Hunter via Codex background agent, Edge Case Hunter, Acceptance Auditor). 21 raw findings → 1 decision-needed (operator: fix + re-run) + 11 patches applied + 1 defer (8-2-D1, AAC-family format IDs) + 6 dismissed. Headline patches: impact-harness methodology (alternating per-rep order, median-of-paired-savings — authoritative re-run confirms the original numbers, mp3 saving 0.1107s / flac 0.1031s, all gates green), mp3/flac coverage asserts + 0.020s absolute gate-2 epsilon, `DecodedAudio` + `ClickTrackAIFFBuilder` DocC, `ClickTrackAIFFBuilder.write` precondition guards, BPM `Options.maxSeconds` + `readMonoSamples` cap-semantics docs, `perf-benchmark` dirty-suffix `GIT_SHA`, mid-flight decoded-path cancellation test, DD #1 list amended with `ClickTrackAIFFBuilder`, sprint-status count fix. Final gates: **560 tests / 123 suites, 0 failures**; lint 1 canonical TODO; `make build` green. Status → done (pending operator: 1Password-signed commit + PR into `rterhaar/epic-8`; optional perf-baseline regeneration now that `perf-benchmark` stamps dirty provenance).
