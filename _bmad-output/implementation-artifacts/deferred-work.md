# Deferred Work

## Deferred from: code review of 6-2-featuresubstrate-namespace-and-mlfeatureframes-relocation (2026-05-28)

`/bmad-code-review` 3-layer pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor). Auditor confirmed all 8 ACs + 12 DDs SATISFIED. Hunter layers surfaced API-hardening and diagnostic-accuracy concerns. 4 patches kept active in story spec; 1 decision-needed (DD #5 ↔ DD #10 contradiction on `DecodedAudio` error type) pending operator. 12 items deferred below.

- **W62 — `DecodedAudio.init` doesn't validate `samples` finiteness.** `Sources/BoomBoomBoomKit/FeatureSubstrate/DecodedAudio.swift:14-36`. NaN/Inf in `[Float] samples` propagates through vDSP → FFT → `vvlogf` → MLFeatureFrames finiteness guard → throws inside BPMAnalyzer's `try?` → builder reports as `featurizationFailed`. Builder-level message is now accurate after the same-session patch, but the substrate-intake boundary still accepts garbage silently. Edge #2 / Blind #5 (2026-05-28). **Re-open trigger:** a story that needs substrate-intake to reject NaN/Inf at the boundary (e.g., a calibration story that depends on bit-pattern-meaningful audio), OR a consumer-side report of "featurization failed" being attributed to the wrong layer. Fix when re-opened: add `guard samples.allSatisfy({ $0.isFinite })` to `DecodedAudio.init` and throw `FeatureSubstrateError.invalidDecodedAudio(reason:)` (the case introduced if the DD #5 ↔ DD #10 decision lands on extending DD #10's spirit).

- ~~**W63 — `PrimingInfo` accepts negative / `Int.max` trim frames with no validation.**~~ **RESOLVED BY REMOVAL (Story 8-2 DD #7a, 2026-06-10).** The `leadingTrimFrames`/`trailingTrimFrames` Int fields no longer exist — `PrimingInfo` was reshaped to `{codec: AudioCodec, trimState: TrimState}` (`.knownNone` iff LPCM, else `.unknown`; no magic zeros, nothing to validate). Story 8.5's declared-trim query lands as an additive `TrimState` case (e.g. `.declared(leadingFrames:trailingFrames:)`) — THAT story owns non-negativity validation for the new payload.

- **W64 — `SubBandWeights` accepts NaN / Inf / negative weights.** `Sources/BoomBoomBoomKit/FeatureSubstrate/SubBandWeights.swift:12-29`. No validation on the 3 `Float` weight fields. `.subBandEmphasis` is unimplemented in Story 6.2 (builder throws `weightingNotYetImplemented`), so the bad-input path is unreachable today. Edge #4 (2026-05-28). **Re-open trigger:** the calibration story that wires `.subBandEmphasis` into `OnsetFeaturesBuilder.build`. Fix when re-opened: add finiteness + non-negativity guards in `SubBandWeights.init` (throwing init), AND extract+validate the payload in `OnsetFeatures.init`'s `weighting` switch.

- **W65 — `OnsetFeatures.init` and `MLFeatureFrames.init` duplicate the invariant-validation matrix verbatim.** `Sources/BoomBoomBoomKit/FeatureSubstrate/OnsetFeatures.swift:70-143` reimplements ~85% of `MLFeatureFrames.swift:646-755`'s guards (positive dimensions, overflow-checked multiply, count match, size cap, finiteness, fmin/fmax/scale validation, non-empty version). Two sources of truth for the same guard list — they will drift. Blind #6 (2026-05-28). **Re-open trigger:** the next story that adds or modifies an invariant on either type (e.g., the W64 follow-up that adds `SubBandWeights` finiteness, OR a future Parameters tightening). Fix when re-opened: extract a shared `internal static func validateLogMelInvariants(...)` helper (or have `OnsetFeatures` carry an `MLFeatureFrames` internally instead of duplicating the validation).

- **W66 — `OnsetFeatures.init` doesn't validate `hopSize <= fftSize`.** `Sources/BoomBoomBoomKit/FeatureSubstrate/OnsetFeatures.swift:109-112`. Guards check both are positive but allow `hopSize > fftSize` (degenerate non-overlap). The producer (`BPMAnalyzer.computeMelOnsetEnvelopeWithSubBands`) never emits such a state today, so the invariant is academic. Edge #7 (2026-05-28). **Re-open trigger:** a producer story emits `OnsetFeatures` via a path that isn't `BPMAnalyzer.computeMelOnsetEnvelopeWithSubBands`, OR a consumer relies on `(fftSize, hopSize)` for frame-time mapping. Fix when re-opened: add `guard hopSize <= fftSize` to `OnsetFeatures.Parameters` validation.

- **W67 — `AudioCodec` enum + `PrimingInfo.codec` field have no consumer reading them for behavior.** `Sources/BoomBoomBoomKit/FeatureSubstrate/AudioCodec.swift:10-19` and `Sources/BoomBoomBoomKit/FeatureSubstrate/PrimingInfo.swift`. Both ship `public` per DD #2 file map, but `OnsetFeaturesBuilder.build` doesn't branch on codec; no codec-specific trim or processing logic exists. Forward-facing per FR-21 substrate intent. Blind #12 (2026-05-28). **Re-open trigger:** the first story that adds codec-specific feature handling (most likely the calibration story OR an MP3-decoder-priming-aware variant). Fix when re-opened: either remove the unused fields if the story decides codec-aware processing isn't load-bearing, OR document the consumer pattern.

- **W68 — `builderFailurePathsThrow` test (b) asserts "something threw `FeatureSubstrateError`" not "cap fired specifically".** `Tests/BoomBoomBoomKitTests/FeatureSubstrateTests.swift:301-326`. The cap-fire test lowers `MLFeatureFrames._withTestingMaximumLogMelDataCount(100)` and asserts that the builder throws `FeatureSubstrateError`. But the builder's `featurizationFailed` is reached by ANY `MLFeatureFrames.init` throw, not just the cap path. The test's pass depends on `try?` swallowing the cap-specific `MLTechniqueError`; if a future change makes `try?` swallow a different error (overflow, finiteness), the test still passes. Blind #3 (2026-05-28). **Re-open trigger:** W62/W65 lift — once invariant validation is centralized and errors are propagated discriminated, this test should assert the specific failure mode. Fix: add a discriminator to `FeatureSubstrateError.featurizationFailed(underlying: MLTechniqueError?)` or similar, propagate the actual cause from BPMAnalyzer.

- **W69 — `featureSetVersion` accepts whitespace-only / special-character strings.** `Sources/BoomBoomBoomKit/FeatureSubstrate/OnsetFeatures.swift:133-137` and `Sources/BoomBoomBoomKit/FeatureSubstrate/MLFeatureFrames.swift:263-266`. The `!isEmpty` guard accepts `" "`, `"\n"`, `"v1,malicious"`, `"\""`. Benchmark-log parsers expecting `featureSetVersion: "v1"` see escaped quotes and silently misclassify. Edge #11 / Blind #15 (2026-05-28). **Re-open trigger:** a benchmark/ablation report parser breaks on a malformed version tag, OR a story tightens FR-21 train/runtime parity contract. Fix: regex `^[A-Za-z0-9._-]+$` OR `.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty == false`.

- **W70 — `OnsetFeatures.Parameters` is `Codable` but accepts NaN / Inf — `JSONEncoder.encode` throws at serialization boundary, not at construction.** `Sources/BoomBoomBoomKit/FeatureSubstrate/OnsetFeatures.swift:15-36`. `Parameters.init` has no validation; `Parameters(... melFmin: .nan, ...)` succeeds; `JSONEncoder.encode` throws `EncodingError.invalidValue` opaquely deep in the serialization stack. The validation lives on `OnsetFeatures.init` (the wrapper), not on `Parameters.init` (the standalone serializable). Edge #10 (2026-05-28). **Re-open trigger:** a consumer constructs `Parameters` standalone (without immediately wrapping in `OnsetFeatures`) and round-trips via Codable, OR an ablation-report serializer surfaces opaque JSON encoding failures. Fix: move the finiteness guards onto `Parameters.init` (throwing init) and call them from `OnsetFeatures.init` as well.

- **W71 — `OnsetFeatures.init` doesn't extract + validate the `SubBandWeights` payload from `.subBandEmphasis`.** `Sources/BoomBoomBoomKit/FeatureSubstrate/OnsetFeatures.swift:101-104`. The `switch weighting { case .uniform, .subBandEmphasis: break }` is exhaustiveness-only — it doesn't bind or validate the associated `SubBandWeights`. Combined with W64 (`SubBandWeights` unvalidated), `OnsetFeatures(weighting: .subBandEmphasis(SubBandWeights(kickBandWeight: .infinity, ...)))` succeeds and stores the bad weights. Edge #8 (2026-05-28). **Re-open trigger:** the calibration story that wires `.subBandEmphasis`. Fix when re-opened: extract the `SubBandWeights` in the switch arm and forward to `SubBandWeights.validate()` (W64 follow-up).

- **W72 — AC #3 spec wording stale pre-DD #10.** `_bmad-output/implementation-artifacts/6-2-featuresubstrate-namespace-and-mlfeatureframes-relocation.md:144`. AC #3 prose says "throws `MLTechniqueError.invalidFeatureShape`" — that wording predates DD #10's introduction of `FeatureSubstrateError`. Implementation correctly throws `FeatureSubstrateError`; the AC text is just stale. Acceptance Auditor (2026-05-28). **Re-open trigger:** the standalone mid-Epic-6 architecture-amendment commit between Story 6.2 close-out and Story 6.3 spec-create (operator already scheduled per Story 6.2 Dev Notes "Pending Architectural Amendment" subsection). Bundle this AC-text tightening with the 6 architecture.md divergences listed there.

- **W73 — AC #1 spec wording on sampleRate location.** `_bmad-output/implementation-artifacts/6-2-featuresubstrate-namespace-and-mlfeatureframes-relocation.md:140`. AC #1 lists `sampleRate ∈ {44100, 48000, 96000}` among the `OnsetFeatures.init` throwing-init invariants, but DD #5 partitions that check to `DecodedAudio.init`; implementation correctly places it on `DecodedAudio.init`. Acceptance Auditor (2026-05-28). **Re-open trigger:** same as W72 — the mid-Epic-6 architecture-amendment commit. Tighten the AC #1 prose to say "DecodedAudio.init enforces sampleRate ∈ {44100, 48000, 96000}".

## Deferred from: code review of 6-1-signalparticipation-contract-and-pool-stage-1-adapter (2026-05-27)

`/bmad-code-review` 4-layer pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor + Codex Blind Hunter MCP, threadId `019e6bc4-5f4e-7632-a794-e8a03f6d1c77`). 4 reviewers converged on the Hashable+NaN contract violation (decision-needed in the spec). 15 items deferred below; 4 patches kept active in the spec for the same-session fix pass; 2 decisions pending user input.

- **W47 — File-metadata branch collapses per-enabled-source state.** `Sources/BoomBoomBoomKit/AudioAnalysisService.swift:466-503`. When `MetadataPolicy.enabledSources = [.id3TBPM, .vorbisBPM]` and only one source has evidence, `signalParticipationTrace` cannot distinguish "ID3 absent, Vorbis absent, tmpo not applicable" from "metadata subsystem absent." The current Stage 1 emits one `.fileMetadata` entry per accepted tag OR a single `.absent` entry when no tag survived parse. Codex M2 (2026-05-27). Re-open trigger: a consumer story needs per-source visibility (e.g., debug UI surfaces "which metadata sources contributed"), OR Story 6.5 `SignalWeights.fileMetadata` lands and forces per-source weight assignment. Fix when re-opened: emit one `SignalParticipationTraceEntry` per `MetadataSource` in `policy.enabledSources`, with `.present(...)` for sources that contributed and `.absent` for sources whose tag form was absent from the file. ~10 LOC in the pool-construction path. _(Clarified 2026-05-30: `SignalWeights.fileMetadata` landing in Story 6.5a did NOT fire this trigger — it is a single family-level scalar, not a per-`MetadataSource` weight, so it does not force per-source emission. Re-opens only on a consumer needing per-source visibility OR a story making weights per-`MetadataSource`. Same clarification covers W55.)_

- ~~**W48 — `.abstained(.sourceSpecific("stage1-eval-deferred"))` magic string in public trace surface.**~~ **RESOLVED 2026-05-30 (Story 6.5b rider).** Lifted to the named constant `AbstainReason.mlEvalDeferred = "ml-eval-deferred"` (`Sources/BoomBoomBoomKit/SignalPool/AbstainReason.swift:23`; the literal also got the 6-3-D1 Stage-neutral rename `stage1-eval-deferred` → `ml-eval-deferred`), consumed at the ML-participation construction (`.abstained(.sourceSpecific(AbstainReason.mlEvalDeferred))`). No bare string literal remains in `Sources/`. Codex L3 (2026-05-27).

- **W49 — Accepted metadata tags with NaN/zero/negative/Inf `parsedBPM` stored unchecked.** `Sources/BoomBoomBoomKit/AudioAnalysisService.swift:489-501`. `WeightedSignal(bpm: tag.parsedBPM, ...)` trusts the parse-stage `MetadataPolicy.valueRange` filter (default 30…300). A consumer-supplied policy with wider range OR a parse path that survives but produces non-finite `parsedBPM` lands nonsense in the trace. Edge #5 (2026-05-27). Re-open trigger: a consumer reports a `WeightedSignal(bpm: .infinity)` in the trace, OR Story 6.5 calibrates metadata confidence (at which point the guard is cheap to add). Mirror `MetadataCorroborator.apply:164` finite-positive guard.

- **W50 — Parse-rejected metadata tags silently dropped from `signalParticipationTrace`.** `Sources/BoomBoomBoomKit/AudioAnalysisService.swift:475-477`. The `tag.rejectionReason == nil` filter discards `sentinel-zero` / `out-of-range` / `non-numeric` rejections without emitting `.abstained(.confidenceBelowFloor)` or similar. Information IS preserved in `AudioAnalysisResult.metadataEvidence` (separate field), so the loss is trace-surface-only. Edge #6 (2026-05-27). Re-open trigger: a consumer needs the unified pool's per-source view to mirror `metadataEvidence` exactly, OR Stage 2+ adds richer abstain semantics.

- ~~**W51 — Pre-corroboration ordering means decision-phase rejections aren't reflected in pool.**~~ **RESOLVED-IN-PART 2026-05-30 (Story 6.5b, DD #4).** The construction-time-knowable half is taken: `MetadataCorroborator.signalParticipationEntries` now emits `.demoted(reason: .sourceSpecific("intra-file-conflict"))` instead of `.present` when `input.conflictDetected` (`SignalPool/MetadataCorroborator.swift:432-435`), so an intra-file conflict IS reflected in the pool. **Residual by-design gap (closed as wontfix):** the skepticism-penalty / `dsp-disagreement` demotion stays `MetadataBPMEvidence`-only because it is a post-corroboration outcome NOT knowable at pool-construction time; re-opens only if a consumer needs the pool to mirror post-decision state. Blind #7 / Edge #15 (2026-05-27).

- ~~**W52 — `BPMResult` reconstruction enumerates 4 named fields; future fields silently dropped.**~~ **RESOLVED 2026-05-29 (Story 6.4b, DD #9).** Added `extension BPMResult { func with(bpm:confidence:trace:) }` that forwards `candidates` (and the other fields) from `self`; all post-merge / inlined-ensemble rebuild sites route through it, so a new `BPMResult` field is forwarded automatically rather than silently zeroed. Blind #8 (2026-05-27).

- **W53 — `.beatGrid` `SignalSource` case has no producer and no contract assertion.** `Sources/BoomBoomBoomKit/SignalPool/SignalSource.swift:11`. The case ships in Stage 1 anticipating Epic 8 Story 8.x. Today: zero producers, zero tests asserting future producers must emit it. Edge #8 / Blind #2 (2026-05-27). Re-open trigger: Epic 8 Story 8.x — that story should add a `SignalSource.allCases` exhaustiveness test that fails when a new case lacks a producer in `buildStage1SignalPool` (or its Stage 2+ successor).

- **W54 — Codable round-trip lossy for non-canonical NaN bit patterns.** `Tests/BoomBoomBoomKitTests/SignalPoolTests.swift:61-107`. `nonConformingFloatEncodingStrategy = .convertToString(nan: "NaN")` collapses all NaN inputs to the `"NaN"` string; decoding produces the canonical `Double.nan` (quiet NaN). The test's `bitEqual(s1.bpm, s2.bpm)` assertion passes because the test only feeds canonical NaN — non-canonical NaN payloads would silently canonicalize and the assertion would still pass. Edge #11 (2026-05-27). Re-open trigger: a story introduces NaN-bit-pattern-meaningful payloads (e.g., NaN tagging for signaling-vs-quiet distinction), OR the AC #7 contract is tightened to "exact bit-pattern preservation across all NaN values." Today the test serves as "doesn't crash on NaN/Inf" coverage — accurate framing.

- **W55 — Multiple ID3 frames in one file produce N `.present` `.fileMetadata` entries with no per-source dedup.** `Sources/BoomBoomBoomKit/AudioAnalysisService.swift:489-501`. A consumer counting `entries.filter { $0.source == .fileMetadata }.count` cannot distinguish "two sources confirmed BPM" from "one source emitted two conflicting frames." Edge #14 (2026-05-27). Re-open trigger: W47 lift (per-enabled-source emission) — the cleanest form is one entry per `MetadataSource` regardless of how many frames each contributed, dedup'ing within source.

- ~~**W56 — `WeightedSignal.source` redundant with `SignalParticipationTraceEntry.source`; no agreement enforcement.**~~ **RESOLVED 2026-05-30** (post-Epic-6, on `develop`; re-surfaced by Copilot on the Epic-6 umbrella PR #17, matching the original Blind #19 finding). Both types carry `SignalSource`; a `.dsp` entry wrapping a `.ml` signal was type-valid but nonsensical. **Fix:** added an `init`-time `precondition` in `SignalParticipationTraceEntry.init` that a `.present`/`.demoted` participation's embedded `WeightedSignal.source` matches the entry's canonical `source` (`.absent`/`.abstained` carry no signal → unchecked). **Chose the precondition over dropping `WeightedSignal.source`** because that field is part of the type's Codable provenance contract (round-trip-locked by `SignalPoolTests`'s `SignalParticipation` Codable test via `s1.source == s2.source`), so it is not dead data — enforcing agreement is the correct fix, not removal. All six production constructions satisfy the invariant (DSP→`.dsp`, fileMetadata→`.fileMetadata`, ML participation is always `.absent`/`.abstained` at pool-build), so the precondition is inert on every current path. Positive regression test `SignalPoolTests.traceEntrySourceMatchesEmbeddedSignalSource` pins the matching-source shape (the trap case is not `#expect(throws:)`-catchable per the project `precondition` limitation). `make test` 483/105 green; `make lint` clean. No mutex/semaphore needed — all involved types are immutable `Sendable` value types and the check reads only its local arguments.

- **W57 — `WeightedSignal.confidence` mixes uncalibrated scales.** _(re-pointed 2026-05-30)_ `Sources/BoomBoomBoomKit/BPMSelectionPolicy.swift:521-525` (DSP, `Double(candidate.score)`, can exceed 1.0) + `Sources/BoomBoomBoomKit/SignalPool/MetadataCorroborator.swift:421-424` (metadata, 1.0 saturation). Downstream aggregation across `WeightedSignal.confidence` from different sources is meaningless until calibration. Blind #5 (2026-05-27). **Owner corrected Story 6.5 → Story 7.7 (FR-25 confidence calibration):** Story 6.5 delivered per-source *weighting* (`SignalWeights`) but explicitly NO cross-source *calibration* (epics.md Story 6.5 AC: "no automatic cross-source calibration in v1.0"). The live weighted ensemble compares CLAMPED `[0,1]` confidences, so the uncalibrated-scale concern is now confined to the trace-surface `WeightedSignal.confidence`, not live selection.

- **W58 — `weight` and `contribution` independently storable in `SignalParticipationTraceEntry.init` with no `contribution == confidence * weight` invariant.** `Sources/BoomBoomBoomKit/SignalPool/SignalParticipationTraceEntry.swift:18-28`. Any caller can construct an entry that lies about its own math. Today the single caller obeys; no enforcement. Blind #11 / Edge #12 (2026-05-27). Re-open trigger: Story 6.5 lifts `weight` from hardcoded 1.0 — at that point either add a `static` factory `make(source:participation:weight:)` that computes contribution internally, OR add an init-time precondition.

- **W59 — Codable test only exercises `.sourceSpecific` variant of `AbstainReason` / `DemotionReason`.** `Tests/BoomBoomBoomKitTests/SignalPoolTests.swift:79-83`. The three other `AbstainReason` cases (`.policyDisabled`, `.inputBelowMinimum`, `.confidenceBelowFloor`) and the `.implausibleForContext` `DemotionReason` case are not exercised. Synthesized Codable on enums is well-tested by the stdlib, so coverage gap is theoretical. Blind #13 (2026-05-27). Re-open trigger: a Swift Testing parameterization story lands, OR a regression where one enum case silently fails to encode. Cheap when re-opened: add a `@Test(arguments: AbstainReason.allCases ...)` once `AbstainReason` conforms `CaseIterable` (a separate decision since `.sourceSpecific` has an associated value).

- **W60 — `SignalParticipation` lacks `CustomStringConvertible`; trace entry description falls back to synthesized debug form.** `Sources/BoomBoomBoomKit/SignalPool/SignalParticipation.swift`. `SignalParticipationTraceEntry.description` interpolates `participation` and Swift synthesizes verbose output like `present(BoomBoomBoomKit.WeightedSignal(bpm: 128.0, confidence: 0.8, source: BoomBoomBoomKit.SignalSource.dsp))`. Blind #17 (2026-05-27). Debug-only impact. Re-open trigger: a benchmark log harness needs stable serialization of pool entries (precedent: `ClickCorrelationEntry`'s `description` shape is consumed by ablation log parsers).

- ~~**W61 — `UnifiedSignalPool` has no non-empty invariant.**~~ **RESOLVED 2026-05-30 (Story 6.5b, DD #7).** Chose the **Optional** path over a precondition (library safety): `BPMSelectionPolicy.select(from:)` returns `PoolSelection?` and returns `nil` on the empty-window case rather than constructing an empty pool that would trap. The pool is built unconditionally on the populated path. Edge #18 (2026-05-27).

## Deferred from: code review of 5-7-app-store-submission-readiness (2026-05-24, PR #11 GitHub Copilot pass)

GitHub Copilot's automated review of PR #11 (rterhaar/5-7-fixes → develop) surfaced 4 inline comments on commit `3f9ceb9` (the fourth-pass patch round). Three exactly matched W17/W19/W20 deferred entries from the fourth-pass review; one was new. Codex plan-review (threadId `019e5bde-f555-7c22`) pushed back on deferring three one-liners and recommended patching all four. All four applied in PR #11 commit `99ce476`; W17/W19/W20 marked CLOSED at the original entries (fourth-pass section). New entry below.

- **W21 (CLOSED 2026-05-24) — `scripts/demo-bump-build.py` fixed temp filename `project.pbxproj.tmp` races between concurrent invocations.** GitHub Copilot finding #1 (pr-comment `3295289509`). The pre-PR-#11 atomic-write pattern (`tmp = PBXPROJ.with_suffix(".tmp"); tmp.write_text(...); os.replace(tmp, PBXPROJ)`) used a single fixed temp path; two concurrent `make demo-bump-build` invocations would race on writing the same tempfile before `os.replace`, with the loser silently overwriting the winner's intermediate content. Fourth-pass review missed this. Closed in PR #11 commit `99ce476` (P1 patch): switched to `tempfile.NamedTemporaryFile(dir=PBXPROJ.parent, prefix=".pbxproj.", suffix=".tmp", delete=False)` for per-invocation unique tempfiles; added `try/finally` cleanup so exception paths don't leak temp files; added `shutil.copymode(PBXPROJ, tmp_path)` to preserve original pbxproj mode (`0644`) since `NamedTemporaryFile` defaults to `0600` and `os.replace` carries the source's mode. **Acknowledged limitation:** this fixes the temp-file collision but does NOT serialize the full read-modify-write race — two concurrent invocations can each read version N and both write N+1, losing one bump. Single-operator workflow does not exercise this; if a future scenario does (parallel CI bumping versions), an `fcntl.flock` on `PBXPROJ` at start would close it. Operator chose not to add the lock pre-emptively (AskUserQuestion 2026-05-24).

## Deferred from: code review of 5-7-app-store-submission-readiness (2026-05-23, PR #9 Copilot pass)

GitHub Copilot review of PR #9 (post-push, after the 14 review patches + 2 Codex Blind Hunter deltas + 6 Codex diff-review patches had already landed in commit `764a0f7`). 6 comments triaged: 1 doc-rewording patch applied inline (README `demo-archive`/`demo-build-sandboxed` asymmetry), 2 spec contradictions patched inline (Task 2.3 `SystemBootTime` evidence + File List `Info.plist` entry), 1 rejected as false-positive (develop-only `Demo/README.md` link to internal `_bmad-output/**` spec — won't break because the README itself never ships to main per `CLAUDE.md` §Release Process), 2 deferred below as test-coverage gaps.

- **W46 — `analyze(url:autoStarted:true)` / `.onOpenURL` LaunchServices path has no regression test for the C1 security-scope balance fix.** `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift:200-201` (`let didStart = url.startAccessingSecurityScopedResource(); let shouldStop = didStart`). Story 5-7's W43 closure corrected the start/stop balance on the LaunchServices-delivered code path, but existing smoke tests (`AnalysisViewModelSmokeTest.swift`) only exercise the drop-style `analyze(url:)` path with `autoStarted: false` defaulted; the `.onOpenURL` → `handleOpenURL(url)` → `analyze(url:autoStarted:true)` route is uncovered. A regression that re-introduces the prior `stop()`-without-`start()` mispairing (or that flips the `shouldStop` conditional) would still pass the smoke suite. Surfaced by GitHub Copilot on PR #9 (2026-05-23). Re-open trigger: a user reports `.fileReadFailed(sandboxDenied: true)` on a LaunchServices-delivered URL (which would suggest the heuristic mis-fired because the start/stop got rebalanced incorrectly), OR a future story tightens the open-URL / drop-URL test seam. Fix when re-opened: extract the start/stop pairing into an injectable `SecurityScopeAccessor` protocol (or thread a test-only hook), then add a test that programmatically invokes `viewModel.handleOpenURL(<test fixture URL>)` and asserts `start()` was called exactly once and `stop()` was called exactly once when `didStart == true`. Est. ~15 LOC for the test plus a small seam (~5 LOC in the production code). Kept separate from W45 (sibling exportTrace gap) so each can close independently if a future story tightens one path before the other.

## Deferred from: code review of 5-7-app-store-submission-readiness (2026-05-23, second pass)

`/bmad-code-review` Acceptance Auditor + Edge Case Hunter layers (leveraging the prior `/code-review` xhigh 5-angle pass). One deferred finding; thirteen patches kept active in the story spec for the same-session fix pass; one decision-needed pending user input. Codex Blind Hunter dispatched in background — its delta findings will be appended on completion.

- **W45 — exportTrace lacks `isFileURL` guard AND lacks regression-test coverage for the C2 security-scope balance fix.** `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift:586-604`. Two coupled gaps in the same function:

  (a) **Missing `isFileURL` guard.** NSSavePanel can return URLs from `NSFileProviderExtension` providers (Dropbox, OneDrive, iCloud Drive) where `panel.url` is non-nil but the URL targets a virtualized location. `startAccessingSecurityScopedResource()` may return true (provider grants scope) but `Data.write(to: url, options: .atomic)` fails because the `.tmp` companion can't be created under the provider mount. The error surfaces as `.traceExport(detail: ...)` with non-actionable NSCocoaErrorDomain copy. `validateDropPayload` has the analogous `guard url.isFileURL` at `:314` — the export path doesn't. NOT introduced by Story 5-7 (C2 fix did not amplify the gap); inherited from Story 5-4 export pipeline. Est. 3 LOC.

  (b) **Missing regression test for the start/stop pairing** (added 2026-05-23 per PR #9 Copilot feedback). Story 5-7's W44 closure balanced `startAccessingSecurityScopedResource()` / `stopAccessingSecurityScopedResource()` in `exportTrace()`, but the existing `AnalysisViewModelSmokeTest.swift` only exercises `writeTraceJSON(to:)` directly — `exportTrace()` itself is bypassed because the test cannot drive `NSSavePanel` interactively. A regression that removes or mispairs the `didStart` / `stopAccessingSecurityScopedResource()` calls would still pass the smoke suite. Same fix approach as W46: a `SecurityScopeAccessor` seam or test-only injection point lets the start/stop pairing be asserted without an NSSavePanel UI harness.

  Re-open trigger (either gap): a user reports a cryptic trace-export failure on a cloud-provider Save destination, OR a future story tightens the export pipeline error UX, OR a future story introduces the security-scope seam needed by W46 (in which case the same seam covers gap (b) here for free). Fix when re-opened: apply (a) inline (`guard url.isFileURL else { error = .traceExport(detail: "Destination must be a local file..."); return false }` immediately after `panel.url` unwrap) AND wire (b) through the same seam W46 introduces.

## Deferred from: code review of 5-7-app-store-submission-readiness (2026-05-23)

Carry-over from Story 5-6 PR #8 Copilot review. Story 5-7 owns sandbox refinement scope (KDD #3 sandbox/file-access entitlement) — both findings closed in this story's development.

- **W43 — Copilot C1: `.onOpenURL` `autoStarted` path forced `stopAccessingSecurityScopedResource()` without matching `start...` — CLOSED 2026-05-23 (Story 5-7 sandbox refinement).** `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift:190-191` (line numbers corrected per Story 5-7 review patch — pre-patch citation of `:171-172` was drift from earlier file state). Fix: defensive `startAccessingSecurityScopedResource()` for both `autoStarted` paths. Apple's documented contract (corrected per Story 5-7 review patch): `start...` returns `true` (refcount++) for any security-scoped URL and `false` only for non-scoped URLs — independent of who 'already granted access'. Typical LaunchServices delivery (`.onOpenURL` / Dock drop / Finder Open With / `open -a`) uses non-scoped file URLs (the sandbox extension is kernel-vended, not URL-mediated) — so `start` returns `false` on them, but the *reason* is non-scoping, not "already-granted". Drop-delivered and bookmark-restored URLs are scoped; `start` returns `true` and the defer-stop balances. `autoStarted` parameter retained because the sandbox-denial heuristic at `:274` (`!autoStarted && !didStart` → `.fileReadFailed(sandboxDenied: true)`) needs to distinguish "you dropped a file we couldn't access" from "system delivered this and analysis failed for other reasons." Re-open trigger: if Apple changes the start/stop refcount semantics in a future macOS, OR if a fresh-clone user reports `.fileReadFailed(sandboxDenied: true)` on a LaunchServices-delivered URL.
- **W44 — Copilot C2: `exportTrace()` called `stopAccessingSecurityScopedResource()` without matching `start...` — CLOSED 2026-05-23 (Story 5-7 sandbox refinement).** `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift:586-589` (line numbers corrected per Story 5-7 review patch — pre-patch citation of `:561` pointed inside `writeTraceJSON`, a different function). Fix: same defensive pattern as W43. Capture `let didStart = url.startAccessingSecurityScopedResource()`, guard the stop call on `if didStart`. NSSavePanel-returned URLs are PowerBox-granted security-scoped URLs (corrected per Story 5-7 review patch — the pre-patch "PowerBox typically returns false (no refcount needed)" framing understated the original underflow): `start...` returns `true` with a refcount that must be balanced, and the pre-fix bare `defer { stop }` underflowed the sandbox extension's retain count on every save. Under sustained use that can manifest as later saves denying write with NSFileWriteNoPermissionError. Re-open trigger: same as W43.

## Deferred from: code review of 5-6-end-user-ui-redesign (2026-05-23)

`/code-review` xhigh-effort 5-angle + Codex blind-hunter pass produced 15 verified F-IDs (F01-F15). Triage + bucket routing per `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-23.md` (v3). Per-story snapshot at `_bmad-output/implementation-artifacts/5-6-end-user-ui-redesign.md` §Review Findings. Entries below are the project-wide search target.

- **W33 — F01: PRODUCT_BUNDLE_IDENTIFIER + INFOPLIST_KEY_CFBundleDisplayName rename to BoomBoomBoom (RE-OPENED 2026-05-24 via Story 5-8)** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.xcodeproj/project.pbxproj:412` + `:404`. Story 5-7 KDD #4 ("Bundle ID stays `com.robbyt.BoomBoomBoomKitDemo`") and KDD #5 ("App name stays `BoomBoomBoomKitDemo`") forbid this rename; reverted in Story 5-6 Phase 2 surgical revert. Re-open trigger ("a future story explicitly authorizes the rename via a KDD update in Story 5-7 or a successor App-Store-renaming story") fired 2026-05-24 during `/bmad-party-mode` rename-scoping discussion: operator wants the final consumer-facing name (`BoomBoomBoom`) committed before first App Store Connect submission so the bundle ID isn't permanently welded to the wrong identity (per Siri's authoritative Apple-platform note that App Store Connect treats bundle IDs as immutable post-first-upload). Successor story: **5-8-demo-rename-and-app-store-submission** owns the rename scope decision (a/b/c per the party-mode discussion: display-name-only vs bundle-ID+display vs full path/scheme rename), the KDD #4 + #5 amendments to Story 5-7, the bundle ID availability check, and the deferred App Store Connect submission cycle that was OUT-OF-SCOPE in Story 5-7.
- **W34 — F04: EmptyStateView fills only upper half — AC #12 accepted violation** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/EmptyStateView.swift:23`. `.frame(maxWidth: .infinity, maxHeight: .infinity)` competes with parent `Spacer()` (`ContentView.swift:46-48`) for vertical extent; SwiftUI splits 50/50. AC #12 promised "fills the main pane". Story 5-6 closes with this AC violation explicitly accepted; resolution lands in Story 5-6b. Fix: drop `maxHeight: .infinity`, let parent Spacer own vertical centering. Est. 1 LOC.
- **W35 — F05: AppIcon.icon directory + 2 MB clock-man.png + icon.json + ASSETCATALOG_COMPILER_APPICON_NAME rename** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AppIcon.icon/` + `Demo/.../project.pbxproj:388,413`. Story 5-7 AC #1 owns the app icon set; KDD #1 authorizes stub artwork. Reverted in Story 5-6 Phase 2. Story 5-7 dev agent may regenerate fresh stub OR optionally reuse the reverted `clock-man.png` artifact. Re-open trigger: Story 5-7 execution.
- **W36 — F07: StrategyBackground neutral gradient uses static Color(white:) — AC #6/#7 accepted violation** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/StrategyBackground.swift:66`. Non-dynamic colors break in Dark Mode; every other case uses semantic anchors. AC #6 demands 4.5:1 metadata contrast in both Light and Dark mode; AC #7 demands solid-fill in increased-contrast. Both contracts violated for `.none` case. Story 5-6 closes with this AC violation explicitly accepted; resolution lands in Story 5-6b. Fix (Apple-docs MCP corrected): use TWO different dynamic anchors — `Color(nsColor: .windowBackgroundColor)` paired with `Color(nsColor: .underPageBackgroundColor)`. The `nsColor:` argument label is REQUIRED; bare `Color(NSColor.windowBackgroundColor)` resolves to the asset-catalog `Color(_ name:)` overload and silently fails. Same color for both anchors = flat fill, not a gradient.
- **W37 — F08: MACOSX_DEPLOYMENT_TARGET set to 15.6 on all 6 demo configs — accept-as-known-issue (user-authorized 2026-05-23).** `Demo/.../project.pbxproj` (6 configs) + `Package.swift:6`. Original v3 proposal recommended revert to 15.0 per Codex's "splitting silently breaks library consumers on 15.0-15.5" concern. User overrode: demo enforces 15.6 minimum; Package.swift stays at `.macOS(.v15)`. Decoupling rationale: the demo binary is distributed via App Store as a separate end-user product (Story 5-7 framing); library SPM consumers integrate `BoomBoomBoomKit` directly through Package.swift and are unaffected by the demo's deployment target. Demo's 15.6 floor only affects end-user installs of the App Store demo binary. Story 5-7 KDD #9 reframed accordingly (demo at 15.6 documented; Package.swift independently at .v15 because demo binary is separate from library SPM distribution). Re-open trigger: a future change that re-couples demo + library platform targets (e.g., bundling the library inside the demo's App Store binary as the sole distribution channel).
- **W38 — F10: DEAD_CODE_STRIPPING=YES on test target — accept-as-known-issue (user-authorized 2026-05-23).** `Demo/.../project.pbxproj` (6 instances across app target Debug/Release, test target Debug/Release, project Debug/Release). Codex flagged "Swift Testing reflection-based `@Test` discovery risk" and recommended `DEAD_CODE_STRIPPING = NO` on the test target; v3 proposal promoted to Bucket 1 for Phase 2 removal. User overrode: the setting was suggested by Xcode and reads as recommended; user prefers Xcode's recommendation over the theoretical Swift Testing risk. Empirical check: post-Phase-2 `make demo-test` invocation count is 78, matching Story 5-6 dev close-out baseline — no observed regression. Re-open trigger: any future test-count drop attributable to dead-code stripping (would suggest the theoretical risk has materialized), OR an Xcode point release that changes the reflection-discovery contract.
- **W39 — F11: Drop target safe-area mismatch — gradient extends behind title bar, dropDestination doesn't** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/ContentView.swift:55`. `.contentShape(Rectangle())` + `.dropDestination` on outer ZStack lack `.ignoresSafeArea()`; child StrategyBackground has it. Drops on the visible-gradient strip behind the title bar silently fail. Deferred to Story 5-6b. Fix: move `.ignoresSafeArea()` to ZStack OR stop gradient at safe area. Decide based on visual verification.
- **W40 — F12: Toolbar Diagnostics Button has no .accessibilityValue for toggle state** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/ContentView.swift:87`. VoiceOver always announces "Diagnostics, button" regardless of inspector visibility. Deferred to Story 5-6b. Fix (Apple-docs MCP): `.accessibilityValue(Text(inspectorPresented ? "Shown" : "Hidden"))` — wrap in `Text(...)` to match the currently-indexed Apple doc surface (the `String` overload exists in the SDK and compiles today, but the `Text` overload is the documented form).
- **W41 — F13: EmptyStateView caption uses .tertiary instead of spec'd .secondary — KDD #5 accepted violation** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/EmptyStateView.swift:21`. Lower contrast than spec; may drop below WCAG AA 4.5:1 on the F07-buggy neutral background. Story 5-6 closes with this KDD violation explicitly accepted; resolution lands in Story 5-6b. Fix: `.tertiary` → `.secondary`. Est. 1 LOC.
- **W42 — F14: EmptyStateView missing .accessibilityElement(.combine) + .accessibilityHint** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/EmptyStateView.swift:12`. VoiceOver announces headline + caption as two separate elements with no drop-affordance explanation. Deferred to Story 5-6b. Fix (Apple-docs MCP): `.accessibilityElement(children: .combine)` + `.accessibilityHint("Drag an audio file here to analyze")` on outer VStack. "Drag" verb (not "Drop") matches macOS HIG + AppKit drop-target VoiceOver vocabulary (Finder, Mail attachments).
- See also: **W28** closed inline in Story-5-3 review section (Picker camelCase rawValues fix via Story 5-6 F06).

## Deferred from: code review of 5-3-parameter-controls (2026-05-20)

- **W24 — `cancelInFlightActive` test brittleness: race between natural completion and cancel propagation** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemoTests/AnalysisViewModelSmokeTest.swift:228-253`. Calls `viewModel.analyze(url:)` then synchronously `viewModel.cancelInFlight()` on a short synthetic fixture (`bpm-120-click.wav`, ~1-2s). The 30s polling deadline accommodates natural completion of the short fixture; the `#expect(detectedBPM == nil)` assertion still catches a false-pass, but a busy CI could see a flaky FAIL if natural completion wins the race on a fast machine before the library polls cancellation at window-boundary 0. Codex partially refuted (no suspension point between the two MainActor calls). Mitigation when flaky: switch to a longer fixture (e.g. `Submerged_Lament.mp3`), OR insert `try await Task.yield()` between `analyze(url:)` and `cancelInFlight()` to force the detached DSP to start. Re-open trigger: this test flakes on CI.
- **W25 — Cancel propagation latency UX: "Cancelling…" can show for 30+ seconds on long files at high intensity** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift` `cancelInFlight` + library window-boundary polling. ADR-1 dictates between-windows cancellation in the library; the demo cooperates correctly. On `intensity: .thorough` with `windowSizes: [30, 60, 90]`, a cancel mid-90s-window holds the task open for up to 30s with "Cancelling…" copy visible. The UX is "frozen" from the user's perspective. Not a defect of Story 5-3; it surfaces the library's existing cancellation cadence. Re-open trigger: user reports the perceived freeze on a long file, OR a story decides to add per-step cancellation polls in the library.
- **W26 — Pasteboard env-gated test has no diagnostic on restricted-pasteboard CI environments** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemoTests/AnalysisViewModelSmokeTest.swift:678-702`. Test is opt-in via `BBBKIT_RUN_PASTEBOARD_TEST=1` and writes a sentinel snippet to `NSPasteboard.general`. In a restricted-TCC CI runner (or sandboxed CI agent), `setString` returns false and the test fails with `copyConfigToPasteboard() == true` violated, without diagnosing that pasteboard access was denied (vs the snippet being wrong). Today this is a developer-local opt-in; risk surfaces only if CI policy ever sets the env var. Fix: probe `setString` with a one-byte sentinel during test setup and skip if the round-trip fails, OR record `viewModel.errorMessage` via `Issue.record` when the success assertion violates.
- **W27 — GroupBox containing Slider may capture mouse events that interfere with file-drop onto controls section** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/ContentView.swift` controls section + outer-VStack drop chain. Spec Task 2.8 claims "a user dragging onto the slider still drops successfully" but this depends on SwiftUI gesture-system precedence (drop is system-level, Slider drag is gesture-level). Auto-mode tests cannot exercise window-drop. Re-open trigger: AC #11 manual smoke (Task 6) reveals that drops onto the controls section silently fail.
- **W28 — Picker labels show camelCase rawValues (`maxConfidence`, `windowVoting`) — CLOSED 2026-05-23 (Story 5-6 F06, commit `6ca4ae8`).** `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/ContentView.swift:177` (was `:109` at Story 5-3 spec time). Per Story 5-3 DD #3 the rawValue strings were emitted verbatim to match public API symbol names so the user could map UI→code mentally. Acceptable for a developer demo; the re-open trigger ("demo evolves into a more polished release shape") fired with Story 5-6's end-user UI redesign. Closure: `Text(strategy.rawValue)` → `Text(AnalysisViewModel.humanize(strategy))` per Story 5-6 review finding F06 (`_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-23.md` §4.1 Change 3). `humanize()` produces `"max confidence"`, `"window voting"`, etc. Re-open trigger: humanize() output changes meaning, OR a usability study surfaces a different mapping preference.
- **W29 — Cancel button can flash "Cancel" between `isCancelling: false → isAnalyzing: false` propagation order** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/ContentView.swift` cancel button. Two independent observed properties (`isCancelling`, `isAnalyzing`) drive one button's text via `viewModel.isCancelling ? "Cancelling…" : "Cancel"`. The view may briefly render "Cancel" between `isCancelling: false` and `isAnalyzing: false` if @Observable's update batching doesn't fire them on the same frame. One-frame artifact at most. Re-open trigger: AC #11 manual smoke (Task 6) observes the flash, OR a downstream story tightens the cancel-flow state machine.
- **W30 — `manualReRunUsesCurrentOptions` test coverage gap; Story 5-4 trace-surface hook — CLOSED 2026-05-20 (Story 5-4).** Story 5-4 landed the atomic `LastRunDiagnosticSnapshot` (DD #5) carrying a narrow `RunOptionsSnapshot.mergeStrategy` field. `manualReRunUsesCurrentOptions` was tightened to add `#expect(viewModel.lastRunSnapshot?.runOptions.mergeStrategy == .dedup)` after the existing `detectedBPM != nil` assertion (AC #8). A regression that silently honors a stale `.maxConfidence` snapshot now fails the assertion.
- **W31 — `FormatError` taxonomy + view-model banner-message typing** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift` `FormatError` enum + `errorMessage: String?` banner field. Two coupled issues surfaced by the Codex post-patch review thread `019e474a-00d6-79c1-80a1-0b34a0b22ff7`: (1) `FormatError.nonFinite` is reused for both NaN/±Infinity AND finite-but-out-of-range values (P4 + P5 patches 2026-05-20). The case name is slightly misleading but renaming to `.invalidRange` would churn 9 existing NaN/Inf tests + the W20 deferred-work entry without behavioral gain. Inline polish to the display copy at `ContentView.swift:202` ("invalid numeric value" replacing "non-finite value") covers it for now. (2) `copyConfigToPasteboard` clears the stale `"Could not copy to clipboard"` banner via string-equality match (`errorMessage == "Could not copy to clipboard"`). Brittle under localization: a future localized translation would silently stop clearing. Re-open trigger: localization lands, OR a downstream consumer needs to distinguish NaN/Inf from out-of-range. Fix when re-opened: introduce a typed `BannerError` enum with cases like `.pasteboardCopyFailed`, `.dropRejection(DropError)`, `.analysisFailure(...)`, and a `displayText` mapping; replace `errorMessage: String?` with `bannerError: BannerError?`. Pre-1.0 (per project-context.md "Public API Discipline") — no BC concern since the surface is demo-internal.
- **W32 — `copyConfigToPasteboard` wipes user's clipboard on `setString` failure** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift:442-457`. `NSPasteboard.general.clearContents()` runs BEFORE `pasteboard.setString(snippet, forType: .string)`. AppKit requires the clear before any owner-change setString call, so the order is mandatory. But if `setString` returns false, the user's prior clipboard contents have already been destroyed and the failure-path `errorMessage = "Could not copy to clipboard"` cannot restore them. Risk profile is low today: `NSPasteboard.general` is an in-process write that rarely fails in practice; only realistic failure mode is a restricted-TCC sandboxed environment (which the demo is, but with pasteboard-write entitlement). Surfaced by GitHub Copilot re-scan 2026-05-20 (PR #5 comment `3277526465`). Fix when re-opened: snapshot `pasteboard.pasteboardItems` BEFORE `clearContents()` (capturing each item's `types` plus `data(forType:)`/`string(forType:)` payloads), call `clearContents()`, attempt `setString`, and on `didCopy == false` rebuild the prior items by allocating a new `NSPasteboardItem`, `setData(_:forType:)` for each captured (type, data) pair, and `pasteboard.writeObjects([...])`. Non-trivial — multiple types per item, binary data, UTI propagation. Re-open trigger: user reports lost clipboard contents from Copy Config, OR a story migrates the pasteboard surface to a higher-stakes payload (e.g., user-curated trace export). Lower-effort interim: drop `clearContents()` entirely if a future test confirms `setString` is idempotent across owner-changes on macOS 15+ — but this would break the AppKit-documented contract.

## Deferred from: code review of 5-2-core-analysis-flow (2026-05-19)

- **W20 — `NaN` / `Infinity` in `bpm` / `confidence` / `elapsedSeconds` renders as `"nan BPM"` / `"nan%"` — CLOSED 2026-05-19 (Story 5-3).** `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift` gains a static `formatResultRow(fileName:bpm:confidence:effectiveIntensity:elapsedSeconds:) -> Result<BPMResultRow, FormatError>` helper (Story 5-3 DD #15 / AC #7). Any of `bpm`/`confidence`/`elapsedSeconds` set to NaN or ±Infinity returns `.failure(.nonFinite)`; `ContentView.displayState` maps that to `.errorOnly("Internal error: analysis returned a non-finite value. This is a library bug — please file an issue.")`. `BPMResultRow` was promoted from `ContentView`'s private struct to a nested type on `AnalysisViewModel`; `FormatError` is an internal enum with cases `.missingFields` (mixed nil/non-nil pattern) and `.nonFinite`. Cleanly testable via `@testable import` — 10 new tests cover happy path + NaN×3 + ±Inf×6 + missing-fields. Extends Story 5-1 W5 with Story 5-2's user-visible result-row surface, hardened + automated in Story 5-3.
- **~~W21~~ — OBSOLETED 2026-05-19** by D1 resolution → P5: `CFBundleDocumentTypes` block removed from Info.plist entirely. No UTI registration remains; the `.m4b` / `.mp4` UX divergence no longer applies.
- **W22 — `make demo-lint` only scans `--include='project.pbxproj'`, missing `.xcconfig` and `xcshareddata` leak paths** — `Makefile:73`. The team-ID leak guardrail covers pbxproj canonical output but a developer setting `DEVELOPMENT_TEAM` in a checked-in `.xcconfig` (CI convention) or `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.xcodeproj/xcshareddata/WorkspaceSettings.xcsettings` would bypass the gate. Story 5-2 ships no xcconfig files, so the immediate risk is zero. Fix when adding xcconfig-based CI configuration: extend the `--include` pattern to `--include='*.xcconfig' --include='project.pbxproj' --include='WorkspaceSettings.xcsettings'`.
- **~~W23~~ — OBSOLETED 2026-05-19** by D1 resolution → P5: no `CFBundleDocumentTypes` entries remain; `CFBundleTypeName` keys are moot.
- **AC #12 manual GUI smoke tests (Tasks 8.2-8.8) pending user action** — `_bmad-output/implementation-artifacts/5-2-core-analysis-flow.md:466-475`. Auto-mode cannot drag files into SwiftUI windows; sprint-status acknowledges. Tasks: drop `.wav` → result, drop `.txt` → error, drop multi → multi error, drop A then B mid-analyze → "Cancelling…" copy, Finder Open With registration, Dock-icon drop, drop `.txt` during analyze → red banner mixed-state. User to execute and record outcomes verbatim in Completion Notes before final commit (Task 9.5).

## Deferred from: code review of 5-1-demo-app-project-scaffold pass 2 (2026-05-19)

- **W12 — Stuck `isAnalyzing` reachability profile broader than W1 originally described — CLOSED 2026-05-19 (Story 5-3).** `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift`. Story 5-2 implemented the per-task UUID discriminator (DD #5) that made `isAnalyzing = false` reachable on user-cancel; Story 5-3 surfaces a visible Cancel button (AC #4) wrapping the new `cancelInFlight()` method, which mirrors the cancel-all-prior cascade — iterate `inFlightTasks`, insert each UUID into `pendingCancellations` before invoking `task.cancel()`. Critical DD #12 invariant: `cancelInFlight()` MUST NOT reassign `currentTaskID` so the cancelled current task's `.failure(is CancellationError)` arm correctly resets `isAnalyzing = false` (Branch A, `currentTaskID == taskID`); Branch B (Cancel followed by `analyze()`) achieves the inequality naturally via the subsequent `analyze()` reassigning the field. Both branches verified by unit tests (`cancelInFlightIdle`, `cancelInFlightActive`) plus AC #11 manual smoke. The previously-theoretical non-supersession cancel paths (window close, scene-phase, sheet dismissal) remain covered by the Story 5-1 `deinit` backstop.
- **W13 — Test target's transitive linkage to `BoomBoomBoomKit` via implicit SPM/Xcode auto-resolve is fragile** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.xcodeproj/project.pbxproj:135`. The test target declares only `BoomBoomBoomKitTestSupport` as a package product dependency. The smoke test imports `BoomBoomBoomKit` directly (line 1 of `AnalysisViewModelSmokeTest.swift`); this resolves because TestSupport transitively re-exports, AND because `BUNDLE_LOADER = $(TEST_HOST)` lets the test bundle resolve against the app's symbol table at runtime. Works today on Xcode 26.3. A future Xcode major that tightens implicit-module-visibility semantics could break this without warning. Fix when a contributor reports `module 'BoomBoomBoomKit' not found` in the test bundle: add `BoomBoomBoomKit` to the test target's `packageProductDependencies` explicitly.
- **W14 — `make demo-build-sandboxed` cryptic errors on machines without (or with multiple) signing identities — CLOSED 2026-05-19 (PR #3 Copilot review).** `Makefile:57-65`. Was: with `CODE_SIGN_STYLE = Automatic` + `DEVELOPMENT_TEAM = ""` in pbxproj, xcodebuild emits `No Account for Team ""` errors. The Makefile help text said "Requires a configured signing identity (Xcode > Settings > Accounts, or DEVELOPMENT_TEAM env var)" but the target body did not propagate the env var into the xcodebuild command line. Closed inline by threading `DEVELOPMENT_TEAM=$(DEVELOPMENT_TEAM)` into the xcodebuild invocation: `DEVELOPMENT_TEAM=ABC1234DEF make demo-build-sandboxed` now works as the help text claims. Help comment updated to reflect the working invocation pattern. Multiple-identity case + `command -v xcodebuild` preflight remain part of W11.
- **W15 — `elapsedSeconds` measures `Task` creation→completion wall-clock, not pure DSP wall-clock** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift` `started = ContinuousClock.now` is captured by the outer `Task { ... }` body, not by the inner `Task.detached`. On a busy MainActor the outer Task may sit in the scheduler queue before the detached child launches; the displayed elapsed includes that scheduling latency. Minor; the smoke test only asserts `> 0`. Fix when a UI surface uses elapsed for a performance dashboard: move `started` capture inside `Task.detached`'s body, or surface a separate "DSP wall-clock" field exported by `AudioAnalysisResult`.
- **W16 — `S85RR68YT7` team-ID leak guardrail timed to Story 5-2 — CLOSED 2026-05-19 (Story 5-2).** `Makefile` gained three targets: `demo-lint` (grep over `Demo/**/project.pbxproj` for the team-ID shape `DEVELOPMENT_TEAM[[:space:]]*=[[:space:]]*"?[A-Z0-9]{10}"?[[:space:]]*;` — covers BOTH quoted and unquoted Xcode-emitted forms per Codex H8), `demo-fmt` (sibling of `make fmt` covering the `Demo/` tree per Codex M11), and `pre-commit` (aggregate: `fmt` + `demo-fmt` + `lint` + `demo-lint`). Recipe inversion verified during Story 5-2 dev: plant `DEVELOPMENT_TEAM = S85RR68YT7;` (unquoted) → `make demo-lint` exits non-zero with file:line for all 6 build configurations; same with quoted form `DEVELOPMENT_TEAM = "S85RR68YT7";`; revert → exits 0. NOT a `.git/hooks/pre-commit` snippet (hooks aren't committed by default, `hooksPath` config dance is fragile across machines). `make pre-commit` is the canonical on-demand pre-PR gate.
- **W17 — Cancellation propagation latency is window-boundary-bounded (multi-second at intensity 7+) — CLOSED 2026-05-19 (Story 5-2).** UI now renders "Cancelling previous analysis…" copy alongside the activity indicator while one or more prior tasks are draining (`viewModel.isCancelling && viewModel.isAnalyzing`). Mechanic: `AnalysisViewModel.pendingCancellations: Set<UUID>` (observed) tracks each superseded task by per-task UUID; the cancel-all-prior cascade inserts each prior task's UUID into the set BEFORE invoking `task.cancel()`, and the fire-and-forget cleanup Task removes the UUID once the cancelled task acknowledges (cooperative-cancel hits the next window boundary). `isCancelling` is a computed property derived from the set's emptiness; @Observable invalidates the view when the underlying set mutates. The "latest-selection-wins" wording in Story 5-1 DD #16-E is now accurate at the UX level — users see explicit visual feedback during the multi-second drain window.
- **W18 — `ENABLE_TESTABILITY = YES` on Debug only; project's `defaultConfigurationName = Release` is a trap** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.xcodeproj/project.pbxproj:359` (Debug) vs no Release entry. `make demo-test` defaults to Debug (xcodebuild test default), so today it works. Any tool that doesn't override (some IDE plugins, archive-then-test workflows, hypothetical performance-test parity Release runs) will pick the project's `defaultConfigurationName = Release` and fail with `module 'BoomBoomBoomKitDemo' not built for testing`. Fix when a contributor reports the error: either set `ENABLE_TESTABILITY = YES` on the app target's Release config too (harmless for a non-shipping demo), OR change the test target's per-target `defaultConfigurationName` to Debug, OR add a CI lane that exercises both configurations.

## Deferred from: code review of 5-1-demo-app-project-scaffold PR #3 Copilot pass (2026-05-19)

- **W19 — `AnalysisViewModel.deinit` cancels in-flight tasks; preempts Story 5-2+ multi-window/sheet retrofit — APPLIED INLINE 2026-05-19.** `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift`. Defensive backstop landed in 5-1: when the view model deallocates, every member of `inFlightTasks` is `.cancel()`-ed; each task's `withTaskCancellationHandler.onCancel` flips its captured `Atomic<Bool>` and the library exits at the next window boundary. Plain non-isolated `deinit`; iteration is safe because `Task<Void, Never>` is `Sendable` and `Task.cancel()` is nonisolated (Codex thread `019e4257` validated; SE-0327 / SE-0371 references). `inFlightTasks` also gained `@ObservationIgnored` since it is operational state, not display state. Re-open trigger: if Story 5-2's drop UI surfaces a scenario where the deinit ordering interacts badly with SwiftUI scene-phase transitions (W12), revisit; otherwise closed.

## Deferred from: code review of 5-1-demo-app-project-scaffold (2026-05-18)

- **W1 — `isAnalyzing` early-return paths leave the flag stuck-true — PARTIALLY-CLOSED 2026-05-19 (Story 5-2).** The original spec listed two paths: `guard !Task.isCancelled` early return + `case .failure(is CancellationError): return` arm. Story 5-2 implemented option (b) from the original fix list: a per-task UUID discriminator (`AnalysisViewModel.currentTaskID: UUID?` + per-task `taskID` captured at Task creation). The Task body's MainActor-re-entry now (1) RUNS UNCONDITIONALLY to the result switch — the inherited `guard !Task.isCancelled` early return is REMOVED (Codex C1 — it made the W1 fix unreachable); (2) handles `.failure(is CancellationError)` FIRST and gates the `isAnalyzing = false` reset on `currentTaskID == taskID`; (3) gates ALL other observed-state writes on the same equality. Net: when the user cancels the current task (deinit / hypothetical Story-5-3 Cancel button), `isAnalyzing` correctly reaches `false`; when the task is superseded by a newer `analyze(url:)`, the successor task already set `isAnalyzing = true` to its own value and the prior task returns without mutating state. Remaining open edges (why "partially" not fully closed): the SwiftUI scene-phase / window-close / sheet-dismissal flows that propagate parent-task cancellation to the analyze Task without a follow-up `analyze(url:)` call are now W12's concern; the deinit backstop already handles view-model deallocation correctly via the same UUID gate (`currentTaskID == taskID` after deinit-fired cancel correctly resets `isAnalyzing`, though the view model itself is going away). Re-open W1 if Story 5-3+ surfaces a scenario where `isAnalyzing` lingers true after a non-supersession cancel path.
- **W2 — `options: AudioAnalysisService.Options` is `public var`; Story 5-3 picker mutation creates UI/run-config divergence during in-flight analysis — CLOSED 2026-05-20 (Story 5-4).** Story 5-3 delivered the first two of W2's three mitigations (snapshot-at-launch via `var opts = options` + visible Re-analyze button). Story 5-4 lands the third mitigation: the result-row caption line `Result captured at: intensity <X>, <humanized merge strategy>` (`Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/ContentView.swift` `resultView` per DD #4), drawing values from `viewModel.lastRunSnapshot.runOptions`. Visible whenever `detectedBPM != nil AND lastRunSnapshot != nil`, independent of the trace disclosure's expanded state. The caption surfaces the divergence the moment a user adjusts a slider/picker after the result lands. AC #13 closes the entry.
- **W3 — `CFBundleDocumentTypes` not declared on the app bundle — CLOSED 2026-05-19 (Story 5-2).** Closure depends on TWO commits landing together: (a) `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/Info.plist` added with the 6-UTI `CFBundleDocumentTypes` array (`public.wav` Default + 5 Alternates: `public.aiff`, `public.mp3`, `org.xiph.flac`, `public.mpeg-4-audio`, `com.apple.coreaudio-format`); pbxproj flipped `GENERATE_INFOPLIST_FILE = NO` + `INFOPLIST_FILE = BoomBoomBoomKitDemo/Info.plist` on both Debug + Release app-target configs; a `PBXFileSystemSynchronizedBuildFileExceptionSet` was added so the synchronized root group excludes `Info.plist` from the Resources copy phase. R2 fallback (hand-written partial Info.plist) was taken instead of `INFOPLIST_KEY_CFBundleDocumentTypes` because that build-setting form only accepts simple top-level values, not nested arrays-of-dictionaries. (b) `.onOpenURL` modifier on `ContentView` body wired to `viewModel.handleOpenURL(url)`; without it, `CFBundleDocumentTypes` declares the app as a handler but LaunchServices document-open events (Dock-icon drops / Finder Open With / `open -a`) are silently dropped because `.dropDestination` does NOT receive them (Codex C2). Built Info.plist verified via `plutil -p` — 6 entries with correct `CFBundleTypeRole = Viewer` + `LSHandlerRank Default/Alternate` + `LSItemContentTypes`. Manual Dock-icon + Finder Open-With verification deferred to Task 8 in close-out (`make demo-build-sandboxed` run on RT's machine).
- **W4 — `MACOSX_DEPLOYMENT_TARGET = 15.0` inherited project-level on the app target, explicit on the test target** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.xcodeproj/project.pbxproj:280,338` (project Debug/Release) vs `:418,439` (test Debug/Release). App target's per-target configs (Debug at :355, Release at :387) do NOT redeclare. Inheritance is the standard Xcode mechanic; `xcodebuild -showBuildSettings` resolves to 15.0 on the app target. AC #1 wording is strictly literal, functional reading is correct. Fix when refactoring build settings: add explicit `MACOSX_DEPLOYMENT_TARGET = 15.0;` to the app target's two configs for symmetry; OR document the inheritance choice in a Project Structure Notes section.
- **W5 — NaN/Inf propagation from `AudioAnalysisResult.bpm` / `.confidence` into view-model observed state** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift:97-99`. The view model trusts the library's `AudioAnalysisResult` and assigns `value.bpm` / `value.confidence` directly to `detectedBPM` / `confidence`. SwiftUI numeric formatters render NaN as "nan" but do not crash. Library responsibility: if `AudioAnalysisService.analyzeBPM` can ever return a non-finite `bpm` or `confidence` on degenerate input, that's a library bug. Fix when adding result-display UI in Story 5-2+: defensive `value.bpm.isFinite` check at the view-model boundary, OR a library-level invariant.
- **W6 — `BoomBoomBoomKitML` linked but never imported in any source file; possible "unused dependency" linker diagnostic in stricter configurations** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.xcodeproj/project.pbxproj:91,111,153,491-498`. DD #4 explicitly justifies the no-import-but-link choice to prove product-list integrity for Stories 5-3 / 5-4. Modern SPM + Xcode handle this silently today. Fix when introducing dead-symbol stripping or when SPM emits "unused dependency" warning: add a no-op `import BoomBoomBoomKitML` in a stub file, OR remove the dependency until Story 5-4 actually needs it.
- **W7 — `.swiftlint.yml` exclusion paths folded into Task 0 hygiene commit beyond literal Task 0 scope** — `.swiftlint.yml:14-19` (3 new excluded paths for transitively-installed `.venv` + nested `.build` trees). Task 0.4 spec scope was `.gitignore` only; dev folded swiftlint exclusions in the same hygiene commit (subject: "Story 5-1: gitignore prep + swiftlint exclusions for Demo/ tree"). Documented in Completion Notes #4 — fixes 156 spurious lint violations that were masking real ones. Defensible scope creep with honest commit subject. Future stories: pre-flight hygiene fixes that affect lint output may continue this pattern, but tools-CI changes should appear in their own story.
- **W8 — Spec text said `.flac` UTType constant; Apple's catalog lacks one** — `_bmad-output/implementation-artifacts/5-1-demo-app-project-scaffold.md:56,222` (spec wording) vs `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/AnalysisViewModel.swift:22` (`flacUTType = UTType("org.xiph.flac")!`). Dev resolved correctly by applying the DD #16-CAF named-helper pattern symmetrically. Spec text was wrong; dev's resolution is right. Fix: amend AC #5 + DD #16-B wording post-merge to reference `flacUTType` instead of `.flac`. Documented in Completion Notes "Build-time discoveries" #1.
- **W9 — No CI / pre-commit lint guarding against `S85RR68YT7` team-ID leak across all `Demo/` pbxproj content** — `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.xcodeproj/project.pbxproj`. DD #7 mandates `DEVELOPMENT_TEAM = ""` and forbids the team ID from leaking onto main. The bare `*.xcodeproj/` gitignore guardrail was removed per Task 0.2 (correctly — pbxproj must be tracked). Net effect: developers can now `git add` an Xcode-regenerated project with their team ID populated, and nothing prevents the commit. Fix when adding a CI lane: `grep -rn 'DEVELOPMENT_TEAM = "S85RR68YT7"' Demo/` returns non-zero → fail. Could also live in `Makefile` as `make demo-lint`.
- **W10 — `Demo/**/build/` gitignore pattern uses globstar `**`; works on modern git, may not match in legacy environments** — `.gitignore:19-20`. git ≥ 1.8.2 (2013) supports globstar in `.gitignore`. Modern contributors are unaffected. If a contributor on an ancient git installation works on the repo, the `build/` directory may not be ignored. Fix when a contributor reports `build/` showing in `git status`: replace with non-globstar pattern `Demo/BoomBoomBoomKitDemo/build/`.
- **W11 — `make demo-build` / `make demo-test` / `make demo-build-sandboxed` lack pre-flight check for `xcodebuild` and `DEVELOPER_DIR`** — `Makefile` (the three xcodebuild-wrapping targets; PP7 amendment 2026-05-19: extended to cover the `demo-build-sandboxed` target added by D4 patch 2026-05-18 which inherits the same gap). Running on a machine with Command Line Tools only (no Xcode app) produces a cryptic `xcodebuild: command not found` or, if it's installed, may target the wrong SDK / toolchain when multiple Xcode installs coexist. `make demo-build-sandboxed` additionally needs `DEVELOPMENT_TEAM=<your-team>` propagated to xcodebuild — the Makefile help text mentions the env var but the target body does NOT thread it. Fix when CI lanes proliferate: add a `command -v xcodebuild >/dev/null 2>&1 || (echo 'Xcode required; install from App Store'; exit 1)` guard at the top of each target, plus optionally honor `XCODE_PATH` / `DEVELOPER_DIR` envs. For `demo-build-sandboxed`: append `DEVELOPMENT_TEAM=$(DEVELOPMENT_TEAM)` to the xcodebuild invocation so the user's env var actually reaches xcodebuild.

## Deferred from: code review of 4-7-spectral-flux-onset-dsp-variant (2026-05-17)

- **W1 — `.full` preset auto-includes `.superFluxOnset` via `Set(allCases)`** — `Sources/BoomBoomBoomKit/DSPTechnique.swift:524`. Codex Blind Hunter flagged this as High severity: consumers selecting `TechniqueSet.full` get the regressing variant despite the Branch B inert-ship outcome. Acceptance Auditor confirmed this is intentional architecture per the doc-comment ("`.full` auto-includes via Set(allCases) (consistent with existing precedent for `.full` definition)"). Re-open trigger: if a downstream story changes `.full` semantics (e.g., to "all proven-helpful techniques" rather than "all techniques"), evaluate whether `.superFluxOnset` should be explicitly excluded. Today's invariant is `TechniqueSet.full == Set(DSPTechnique.allCases)`, locked by `TechniqueSet.allDSPCombinations().count == 256`.
- **W2 — Byte-identity baseline JSON portability** — `Tests/BoomBoomBoomKitTests/Fixtures/4-7-byte-identity-baseline.json`. The fixture pins exact `Double.bitPattern` values captured on Apple M5 Max. A contributor on Intel hardware, on a future macOS Accelerate update that selects different vDSP/vvlogf kernel dispatch, or on a future Apple silicon revision may see byte-different output that is algorithmically identical. Pattern is pre-existing (Story 3-3, 3-6 use the same shape) — not Story 4-7-introduced. Fix when a contributor hits a false positive: add `#if arch(arm64)` gate, capture per-architecture fixtures, OR migrate to relative-tolerance assertions (`|bpm - reference| < 1e-5`).
- **W3 — `computeSuperFluxOnsetEnvelope` lacks `onPostVvlogf` test seam** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:211-218`. Baseline `computeMelOnsetEnvelopeWithSubBands` exposes an `onPostVvlogf: (Int, UnsafePointer<Float>) -> Void` parameter for Story-4-5-style parity testing; SuperFlux variant does not. The shared helper `computeLogMelFramesAndRetention` already accepts the closure (line 331) but the SuperFlux caller never passes one (lines 219-225). Fix when SuperFlux needs parity testing: thread the parameter through (cheap; helper already supports it).
- **W4 — NaN/Inf coverage on `vDSP_vswmax` path** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:707-742`. A single NaN bin in the reference frame now poisons 2 output bins via replicate-pad + max-filter (vs baseline's 1-bin exposure). `vDSP_vthres` behavior on NaN is technically platform-defined; project tests do not exercise NaN/Inf input. Pre-existing exposure in the wider pipeline (FFT zero-magnitude → `vvlogf(0)` → -inf); not Story 4-7-introduced. Fix: add a fixture story for the new failure mode (NaN at refFrame[0] propagates to two adjacent bins via replicate-pad).
- **W5 — SuperFlux + `.subBandNormalization` composition unit-test gap** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:651-654`. Docstring states the composition was not empirically validated before Story 4-7; the 256-combo ablation matrix covers it but `make test` (no `OA300_CORPUS_PATH`) does not. Fix: add a unit test that runs both techniques against a synthesized fixture and asserts the result is finite and within a reasonable BPM range.
- **W6 — Numeric-distinctness invariant #12 uses generic click-track instead of crafted bin-level fixture** — `Tests/BoomBoomBoomKitTests/SuperFluxOnsetEnvelopeTests.swift:1521-1572`. Spec AC #1 invariant #12 called for a fixture with deliberate bin-level energy asymmetry (k=63,65 at 1.0, k=64 at 0.0) where baseline produces `diff[64] = curr[64] - 0.0` but SuperFlux produces `diff[64] = curr[64] - 1.0`. Actual test uses a 4-second 144 BPM click; spirit met (bit-distinct), letter missed. Fix: replace the click fixture with the spec-prescribed bin-level fixture; current test passes only because of accumulated drift across bins.
- **W7 — Replicate-pad scalar loops over 130 floats per frame** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:255-257`. Three `for k in 0..<r` / `for k in 0..<melBands` loops inside the per-frame loop violate the project rule "every bulk numeric operation on audio buffers must use vDSP". At 130 elements and 600 frames per 12s window the cost is negligible, but the precedent matters. Fix: replace with `memcpy` or `vDSP_mmov` calls; or migrate to `vDSP_vsmsa` with an explicit padding pattern.
- **W8 — `acc1Correct` silently returns false for `expected <= 0`** — `Tests/BoomBoomBoomKitBenchmarkTests/SuperFluxImpactTests.swift:897-900`. Ground-truth entries with `bpm: 0.0` (sentinel for "unknown") are silently dropped from `namedDnBImproved`/`controlsPreserved` metrics. The brutal-corpus-gate aggregate may misreport on a corpus with sentinel-valued targets. Fix: add a `case unknown` discriminator in the row classification; track sentinel-counts separately.
- **W9 — `namedDnBSet.contains(trackIDStem) || .contains(filename)` doesn't handle path-separator-containing IDs** — `Tests/BoomBoomBoomKitBenchmarkTests/SuperFluxImpactTests.swift:878-881`. A future `4-dnb-triplet-targets.json` schema_version 4 with `track_id` like `"Faraday/Bunker"` and `track.filename = "Bunker.mp3"` matches neither path. `namedDnBTotal` drops from 4 to 3, AC #4 ratio misreported. Fix: define canonical matching contract; current double-fallback hides schema drift.
- **W10 — Hardcoded `let r = 1` duplicated across 5+ sites** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:233` + comment at :229-232 + `DSPTechnique.swift:511` + `CLAUDE.md` + tests. No single source of truth. Fix when a future story enables r as a parameter: hoist to `private static let superFluxRadius = 1` and reference everywhere; until then, the dup is documented in the comment.
- **W11 — `paddedRef` not re-zeroed between frames** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:241,255-257`. The buffer is allocated once outside the loop at length 130; per-frame loops overwrite all 130 slots, but only because the three replicate-pad ranges exhaustively cover the buffer for `r=1`. If a future change parameterizes r and the slot ranges drift, stale tail bytes from the previous frame contaminate the max-filter. Fix when introducing parameterized r: add `assert(r == 1)` at the top of `computeSuperFluxOnsetEnvelope`, OR re-zero `paddedRef` with `vDSP.clear(&paddedRef)` per frame.
- **W12 — Smoke-combo array lacks duplicate-detection invariant** — `Tests/BoomBoomBoomKitBenchmarkTests/AblationFullMatrixTests.swift:723-735`. `SmokeAblationInvariantTests` parameterizes over `smokeCombos.map(\.1)` (a `[TechniqueSet]`); Swift Testing dedup-by-Hashable collapses duplicates. The separate `smokeLaneSize == 16` test catches deletion but not duplication. Fix: add `#expect(Set(smokeCombos.map(\.1)).count == smokeCombos.count)` to surface accidental duplicates.
- **W13 — `SmokeAblationInvariantTests` failure-message identifier loses smoke-combo display name** — `Tests/BoomBoomBoomKitBenchmarkTests/AblationFullMatrixTests.swift:730`. Failure message interpolates `combo.label` (recomputed from `TechniqueSet`) — e.g., `.baseline.label == "vote+fine"`, not the smoke-combo's tuple `.0` display label. Debugging which smokeCombos slot is the offender is harder than necessary. Fix: change `arguments:` to take the full tuple, then interpolate `\(displayLabel) [\(combo.label)]` in the failure message.



- **NaN propagation through `SubBandEnergies` via `vDSP_maxv`** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:239-251` calls `vDSP_maxv` on per-band onset envelopes; if upstream FFT/normalization ever produces a NaN, the value flows into `SubBandEnergies(kick:snare:crack:hihat:)`. Synthesized `Equatable` returns false for NaN-bearing instances vs themselves (Float NaN semantics), violating reflexivity. Pre-existing DSP behavior, not story-introduced; the legacy `[String: Float]` dict had identical surface. Fix when shipping a story that touches `Equatable` semantics on evidence types: either store as `Optional<Float>` with `.none` for non-finite, or document NaN-unsafety explicitly with a guarded `Equatable` impl.
- **Sub-bands count != 4 silently emits 0 for trailing bands** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:239` `for (i, band) in onsetResult.subBands.enumerated() where !band.isEmpty` paired with `switch i { case 0/1/2/3 ... default: break }`. If `subBands.count` is 1/2/3, missing fields silently default to 0; readers cannot distinguish "unsupplied" from "measured zero". Pre-existing behavior (legacy dict had identical conflation); typed migration faithfully preserves observable semantics per DD #2 ("`.zero` preserves that semantic without forcing every reader through `if let`"). Fix when subBand count becomes runtime-variable.
- **Hardcoded 1.20x perf-benchmark threshold is Apple M5 Max-specific** — `Tests/BoomBoomBoomKitBenchmarkTests/PerformanceBenchmarkTests.swift:542` `mlMockOnAbstainMaxRatio: Double = 1.20` was measured on M5 Max. Apple M2/M3 dev machines may legitimately measure ratios in 1.20-1.25 range, causing false-positive perf-gate failures. Cross-platform threshold work was not authorized by Story 4-3b. Fix when a non-M-series-Max contributor hits a false positive: add `PERF_THRESHOLD_OVERRIDE` env var, or maintain per-hardware threshold table.
- **`analyze-time-profile.py` XML schema/recursion robustness** — `_bmad-output/scripts/analyze-time-profile.py` aggregates xctrace `<deduplicated_symbol>` references via recursive resolve. Edge cases: (a) ref/id cycle in malformed XML triggers RecursionError; (b) deep ref-chain (>1000 elements) hits Python default recursion limit; (c) empty XML produces silent zero report (total_weight_ns == 0); (d) future Xcode versions may shift schema (different tag names than `row`/`weight`/`tagged-backtrace`) → silent zero output without operator notice. Fix when re-running profile against newer Xcode: add iterative resolve, raise recursion limit, exit non-zero on empty input or schema mismatch.
- **`profile-trace-build-cost.sh` hardening for non-default environments** — `_bmad-output/scripts/profile-trace-build-cost.sh`. Six edge cases under `set -euo pipefail`: (a) line 80 `xcodebuild -version` aborts on CommandLineTools-only environments — partial metadata file written; (b) line 89 `git rev-parse` aborts when run outside a git checkout (extracted tarball); (c) line 46 `find ... | head -1` silently picks first of multiple `.xctest` bundles when stale debug + release coexist; (d) line 72 `grep SharedFrameworks | head -1` picks first of multiple Testing.frameworks when Xcode + Xcode-beta both installed → potential ABI mismatch; (e) line 72 `grep SharedFrameworks` returning empty under pipefail aborts script with truncated diagnostic; (f) xctrace exits non-zero mid-recording (codesign / TCC reject after partial capture) leaves orphaned `.trace` bundle in `_bmad-output/perf-baselines/` — gitignored so silent disk growth across reruns. Fix: add `trap 'rm -rf "$TRACE_OUT"' ERR`; guard each command with `2>/dev/null || echo unknown`; fail explicitly when bundle/framework counts > 1.
- **`profileLongLoop` fixed temp filename race** — `Tests/BoomBoomBoomKitTests/MLTechniquePerfTests.swift:108` writes to `<tempDir>/ml_technique_profile_long.wav`. Two concurrent profiling invocations or two test-runner workers collide on the same path. Pre-existing pattern in `MLTechniquePerfTests`; not introduced by Story 4-3b. Fix when adding parallel profiling test: use `UUID().uuidString` suffix, or `FileManager.default.createTemporaryDirectory()`.
## Deferred from: Story 4.3 spec finalization (2026-05-05)

- **Story 4.4 input — DD #14 tag-bias risk for ML-after-corroboration ordering** — The Story 4.3 pipeline ordering `merge → MetadataCorroborator.apply → MLTechnique.evaluate → combine → AudioAnalysisResult` means ML evaluates against the post-corroboration trace (`candidatesAfterBoost`). When a track has tag-corroborated DSP candidates, MetadataCorroborator may have boosted `dspWinner.confidence` to near the 0.95 ceiling. The 0.95 cap was designed under the assumption "the merged DSP candidate is the final word"; once ML enters as a competing voter, the cap acts as a thumb on the scale. Story 4.4's `EnsemblePolicy` enum should consider whether to consume `dspWinner.confidence` raw or to discount tag-boosted confidence before the ML comparison (e.g., `tagBoostDiscount: Double` on policy cases, or a separate `dspConfidenceForEnsemble: Double` computed pre-corroboration and threaded forward). The alternative — ML-before-corroboration ordering — was rejected because it would require either changing `MetadataCorroborator`'s signature or running corroboration twice. Story 4.4 author MUST surface this risk in their spec's KDD block at promotion time. Reference: Story 4.3 DD #14 (Winston party-mode review 2026-05-04). Files: `Sources/BoomBoomBoomKit/AudioAnalysisService.swift` (analyzeBPM body), `Sources/BoomBoomBoomKit/MetadataCorroborator.swift` (corroboration boost logic).

  - **Story 4.4 disposition (2026-05-08):** Surfaced and documented in Story 4.4 DD #6, the ``EnsemblePolicy/highestConfidence`` per-case DocC, and `EnsembleCombiner` source comments. NOT mitigated in 4.4 — the bias is unobservable today (no real `MLTechnique` ships until Story 4.5; tuning a discount against a model that doesn't exist would be guesswork). The deferred-work entry stays open with a re-open trigger: revisit when EITHER (a) Story 4-5 BNNS ablation report shows >5% of `.highestConfidence` DSP-vs-ML disagreements are on tag-corroborated tracks where DSP confidence is at or near the 0.95 ceiling, OR (b) Story 4-5 / 4-6 metadata-on vs metadata-off stratified accuracy diverges by >2 OA300 Acc1 tracks under `.highestConfidence`. If neither triggers after 4.5+4.6, close without mitigation. Mitigations to consider then: `tagBoostDiscount: Double` field on `Options` (parallel to `votingThreshold`), or `dspConfidenceForEnsemble: Double` computed pre-corroboration and threaded forward.

- **Story 4.5 inheritance — HALT trigger (e) MLEvaluation field-shape adequacy** — Story 4.5 BNNS ablation must surface a viable `EnsemblePolicy` case from current `MLEvaluation` shape (`bpm: Double` + `confidence: Double` + `modelIdentifier: String?`) OR halt before merging 4.5. The risk: 4.3 → 4.5 → 4.4 sequence creating a hidden circular dependency where 4.4 cannot ship without first widening `MLEvaluation` (e.g., adding `alternateCandidates`, `featureSetVersion`, `featureSummary`). Story 4.5 dev agent MUST run `jq -e '.proposed_policy_cases | length >= 1' <4.5-ablation-summary.json>` and document at least one viable EnsemblePolicy case in 4.5 Completion Notes; if zero, HALT (e) fires and Project Lead is surfaced BEFORE 4.4 starts. Story 4.5 author MUST copy this trigger verbatim into the 4.5 spec's HALT block at promotion time. Reference: Story 4.3 HALT (e) (Winston party-mode round 2 + John finalization 2026-05-04). Files: Story 4.5 spec at promotion time; `_bmad-output/implementation-artifacts/4-5-bnns-mltechnique-conformance.md` (when filed).

- **Story 4-3b — Trace-Build Cost Budget + `subBandEnergies` Typed Migration (READY-FOR-DEV 2026-05-05)** — Investigation of the ~22% structural trace-build overhead surfaced during Story 4.3 implementation, plus typed-struct migration of the last surviving stringly-keyed trace field. The Codex C-modified resolution to Story 4.3 AC #7 second-gate moved the perf gate to corpus-grain venue at 1.30x (post-source-changes measured 1.054x — well within threshold). Story 4-3b will profile via `xctrace`, migrate `subBandEnergies: [String: Float]` → typed `SubBandEnergies` struct (kick/snare/crack/hihat fields, Story 3-3b precedent), apply profile-named optimizations, and tighten the threshold against the measured floor + 10% headroom. Spec at `_bmad-output/implementation-artifacts/4-3b-trace-build-cost-budget.md`. Sprint status: `4-3b-trace-build-cost-budget: ready-for-dev`. Reference: Story 4.3 Codex finalization (party-mode 2026-05-05).

## Deferred from: code review of 4-2-effective-intensity-and-graceful-ml-degradation (2026-05-04)

- **Story 4.5/4.6 forward-compat: when level 8-10 DSP semantics diverge from level 7, evolve the test layer for `effectiveIntensity`** — Story 4.2's `computeEffectiveIntensity` reports the effective DSP-level depth as 7 when intensity 8-10 is requested without an `MLTechnique`, but the pipeline still runs at the requested intensity unchanged (DD #1 reporting-only architecture). Today this is byte-identical to level 7 because of switch-default delegation in `AnalysisIntensity.swift:64,76,83` (the doc-comment on `effectiveIntensity` warns about this explicitly). When Stories 4.5 (BNNS) and/or 4.6 (CoreML) introduce explicit `case 8 / 9 / 10` arms in `AnalysisIntensity.techniqueSet` / `windowSizes` / `progressiveThreshold`, the reporting-only architecture starts to lie: a request for 9 with no ML will still report 7 but actually execute the new level-9 DSP pipeline. The test layer must evolve concurrently — either: (a) cap requested intensity at the pipeline boundary inside `runPreCorroborationPipeline` (mutating `options.intensity` to `dspOnlyMaxIntensity` when ML is missing — explicitly rejected by Story 4.2 DD #1 to preserve forward-compat with Stories 4.5/4.6 themselves), OR (b) add behavioural assertions on `BPMDiagnosticTrace` that verify the pipeline received the capped intensity (requires propagating effective-intensity into the trace), OR (c) add a test that fixture-compares 8-10-no-ML output against 7-no-ML output bytewise (verifies behavioural equivalence at the result level, not pipeline level). Story 4.5 author should pick the option that fits their changes. Source: bmad-code-review of Story 4.2 (2026-05-04) — Codex H1 ("`effectiveIntensity` may be a reporting fiction"), Codex M2 ("reliance on switch-default coincidence"), Codex M4 ("tests assert reporting fields but not pipeline behaviour"), Edge Case Hunter ("untested mid-range 2-6 + no behavioural pipeline test"). Files: `Sources/BoomBoomBoomKit/AudioAnalysisService.swift` (`computeEffectiveIntensity`, `analyzeBPM` reporting block at line 251), `Sources/BoomBoomBoomKit/AnalysisIntensity.swift:58-83` (switch defaults), `Tests/BoomBoomBoomKitTests/AudioAnalysisServiceTests.swift` (`EffectiveIntensityTests` suite).

## Deferred from: Story 4.2 spec review (2026-05-04)

- **Promote `AudioAnalysisResult.degradationReason` from `String?` to typed enum** — Story 4.2 ships `degradationReason: String?` per AC #2's example shape (`"Requested intensity 9 requires BoomBoomBoomKitML package. Running at intensity 7 (DSP-only)."`). When a SECOND degradation path emerges (candidate examples: `mlPackageVersionMismatch`, `requestedFeatureUnavailable`, `requestedFeatureRequiresOSVersion`), promote to typed enum: `enum DegradationReason: Sendable { case mlPackageMissing(requested: AnalysisIntensity, effective: AnalysisIntensity); /* + new cases */ }`. Pre-1.0 / no-BC framing accepts the breaking change at promotion time. Today's `String?` shape is sufficient for the single ML-missing path. Story 4.2 explicitly directs consumers needing control flow to `AudioAnalysisService.maximumSupportedIntensity(mlTechnique:)` rather than string-parsing the message field, so the future enum promotion does not strand any documented consumer pattern. **Concomitantly required:** refactor `AudioAnalysisService.degradationMessage(requested:effective:)` (today hardcoded to ML-missing wording — `"...requires BoomBoomBoomKitML package..."`) into per-case message handling, most naturally a `DegradationReason.localizedDescription` computed property after enum promotion. The two changes ship together; doing only one leaves either a stranded helper (still hardcoded to ML-missing path despite an enum carrying multiple cases) or a stranded enum (typed cases with no message-rendering path). Reference: Story 4.2 DD #2 + Codex review 2026-05-04 (thread `019df4cc-2ca5-70f0-8d0e-54259fd97767`) + Edge Case Hunter pass 2026-05-04. Files: `Sources/BoomBoomBoomKit/AudioAnalysisService.swift` (`AudioAnalysisResult`, private `degradationMessage` helper introduced by Story 4.2 Task 3.3).

- **Migrate `AudioAnalysisResult` from synthesized memberwise init to explicit `public init(...)` with named parameters or builder pattern** — Story 4.2 brings the field count to 7 (`bpm, confidence, candidates, trace, metadataEvidence, effectiveIntensity, degradationReason`). Apple's precedent (`URLResponse.init` 4 params, `URLRequest.init` similar) suggests 7 is on the high end but tolerable. When the field count exceeds ~10, OR when field-reordering becomes a recurring story-level cost (every reorder breaks positional construction at the single internal call site in `AudioAnalysisService.analyzeBPM`), migrate to explicit named-args init or builder pattern. Migration mechanics: replace synthesized memberwise with `public init(bpm: Double, confidence: Double, ...)` matching the current shape, then evolve forward by adding new fields with default values to the explicit init. Synthesized memberwise is the Swift idiom for small value types — the migration is a cost-driven decision, not a stylistic one. Reference: Story 4.2 DD #6 + Codex review 2026-05-04. Files: `Sources/BoomBoomBoomKit/AudioAnalysisService.swift` (`AudioAnalysisResult`).

## Deferred from: code review of 3-5-configurable-window-voting-policy (2026-04-29)

- **2% BPM tolerance clustering is non-transitive** — `Sources/BoomBoomBoomKit/CandidateMergeStrategy.swift:394-398`. Pre-existing in original `windowVoting`; 100/102/104 BPM windows split into separate clusters depending on insertion order. Revisit if drifting-tempo tracks become a corpus blind spot.
- **Hardcoded baseline numbers brittle across non-Apple-Silicon platforms** — `Tests/BoomBoomBoomKitBenchmarkTests/OA300BenchmarkTests.swift:422-424` and `GiantStepsBenchmarkTests.swift:121-123`. AC #4 design decision; per-platform IEEE 754 stable on current Apple targets only.
- **FP equality on summed confidences short-circuits DD#16 tiebreaker chain at 1-ulp boundaries** — `Sources/BoomBoomBoomKit/CandidateMergeStrategy.swift:218-222`. `lhs.summed != rhs.summed` only fires the tiebreaker chain on EXACT equality; near-ties pick the larger sum. Per-platform deterministic; consistent with DD#17.
- **Threshold-gate `>=` boundary at exact `maxConf == effectiveThreshold == 1.0` untested** — `Sources/BoomBoomBoomKit/CandidateMergeStrategy.swift:254`. Code uses `>=` (accept), but no test exercises the exact-equality boundary.
- **`benchmarkVotingPolicies` Acc1 denominator counts all `audioData` even when a track produces 0 windows** — `Tests/BoomBoomBoomKitBenchmarkTests/OA300BenchmarkTests.swift:405`. Same pattern as `benchmarkMergeStrategies`; pre-existing.
- **`windowVotingDefaultPolicyMatchesBaseline` silently treats `nil` analyzeBPM result as a 0 hit** — `Tests/BoomBoomBoomKitBenchmarkTests/OA300BenchmarkTests.swift:453`. A transient AVFoundation nil could fail "Acc1 must equal 57/82" without surfacing the actual cause; consider asserting `trackBPMs` has no nil entries before the Acc tally.
- **AC #10 API-surface check uses typed `let` references that don't actively fail on third overload** — `Tests/BoomBoomBoomKitTests/BPMAnalyzerTests.swift:1469-1471`. Author acknowledges in code comment; spec wording demanded a stronger reflection-based or symbolgraph-extract approach.
- **`-0.0` threshold not normalized to canonical `+0.0` inside `resolveThresholdGated`** — `Sources/BoomBoomBoomKit/CandidateMergeStrategy.swift:247`. `min(max(-0.0, 0.0), 1.0)` yields `-0.0` per IEEE 754 `max` semantics. Behaviorally equivalent (`-0.0 == 0.0` numerically); only matters for code that prints/hashes the internal `effectiveThreshold`.
- **`mergeByWindowVoting` lacks `precondition(results.count >= 2)`** — `Sources/BoomBoomBoomKit/CandidateMergeStrategy.swift:152`. Doc-comment claims the invariant but no runtime assertion. Currently safe because the dispatcher short-circuits count==1 before reaching the helper.

## Deferred from: code review of 3-4-duration-derived-bpm-hint (2026-04-28)

- **TOCTOU race between `fileDuration` and `readMonoSamples`** — `Sources/BoomBoomBoomKit/AudioAnalysisService.swift:179` opens `AVAudioFile(forReading:)` once for duration, then again for PCM read inside `PCMBufferReader.readMonoSamples`. If the file is replaced between the two opens, the duration-derived hint is computed against a different file than the DSP candidates. Accepted by Story 3-4 DD #9 (two opens are acceptable). Race window sub-millisecond on local files. Revisit if a future story consolidates the reader API to return `(samples, sampleRate, fileDuration)` atomically.
- **Bar-count candidates recomputed per window** — `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:363` (step 9.7 invocation) calls `applyDurationHint` once per analysis window, which recomputes `applyDurationHintBarCounts` even though the inputs (`fileDurationSeconds`, `durationHintMinFileSeconds`) are identical across windows. ≤ 6 divisions per window — sub-1% perf impact today. Hoist into `AudioAnalysisService` (precompute once and pass into each window) when intensity 6+ multi-window paths get heavier or when bar-count math grows.

## Deferred from: code review of 3-3a-public-api-harmonization (2026-04-27)

- ~~**MLTechnique tuple-typed return/parameter bypasses Sendable enforcement**~~ **RESOLVED BY STORY 4.3** (2026-05-05). Story 4.3 replaced the labeled-tuple protocol with `public struct MLEvaluation: Sendable { bpm: Double; confidence: Double; modelIdentifier: String? }` and `public protocol MLTechnique: Sendable { func evaluate(trace: BPMDiagnosticTrace) -> MLEvaluation? }`. The defect was reframed during Story 4.3 from "bypasses Sendable enforcement" to "nominal-type / DocC / evolvability defect" — labeled tuples ARE structurally `Sendable` per SE-0302, but cannot conform to `Equatable`/`Hashable`/`Codable`, cannot be DocC-documented, and cannot be extended. The migration target is unchanged. Pre-1.0 / no-BC framing accepted the breaking protocol-shape change without a deprecation cycle. `MLCandidate` struct deferred per Epic 4 planning session 2026-05-04. Files: `Sources/BoomBoomBoomKit/DSPTechnique.swift` (post-Story-4.3 protocol body), `Sources/BoomBoomBoomKitTestSupport/MockMLTechnique.swift` (public mock conformances).

## Deferred from: code review of 3-3-click-track-cross-correlation (2026-04-26)

- Trace-key collision on `%.1f` BPM rounding in `BPMDiagnosticTrace` dictionaries. `BPMAnalyzer.swift:1970` writes `clickCorrelationDetail[String(format: "%.1f", cand.bpm)] = bestNCC`; two candidates rounding to the same one-decimal label collapse into one entry. Same pattern in `harmonicRatioDetail` (`BPMAnalyzer.swift:309-314`) and `subBandVoteDetail` (`BPMAnalyzer.swift:325-329`). Address as a cross-cutting trace-API change (e.g., key by candidate index plus formatted BPM, or switch to `[(bpm: Double, score: Float)]`), not in this story.
- Ablation matrix runs all 128 combos concurrently in a single `withTaskGroup` without batching (`AblationFullMatrixTests.swift:73`). Runs in 77.5 s on M5 Max in practice; revisit when the matrix grows further or runs on resource-constrained CI.

## ~~Non-Octave BPM Disambiguation (3:2, 3:1 ratios)~~ RESOLVED

**Deferred from:** Phase 3 batch planning (2026-03-26)
**Resolved by:** Story 3-1 (2026-04-22)

3:2 and 3:1 ratio detection added to `resolveOctaveAmbiguity` as trace-only (populates `harmonicRatioDetail` in `BPMDiagnosticTrace`). Behavioral resolution (modifying winner BPM for triplet pairs) deferred to Story 3-6 (metadata corroboration) because sub-band voting was calibrated for 2:1 octave pairs and causes regressions on DnB tracks when applied to 3:2/3:1 pairs. Zero regression: OA300 Acc1=69.5%, Acc2=89.0%; GiantSteps Acc1=81.1%, Acc2=82.5%.

## Story 3-1b: Harmonic Ratio Behavioral Resolution — RE-SCOPED INTO Epic 4 (resolution conditional on Story 4.5 outcome)

**Re-scoped by:** Epic 4 planning session 2026-05-04. The 4 named DnB triplet tracks (Charly, Faraday_Bunker, Yin Yang Audio, HEFT_Anagram 6) are folded into Story 4.5 as named regression targets with quantified T2 gates (≥2 of 4 resolved within ±0.5 BPM strict Acc1, no per-track regression, asserted floors hold). The DSP-only resolution strategies that previously failed (comfort-zone override, sub-band vote for 3:2/3:1 pairs, hybrid) confirmed the Epic 3 retro finding that DSP single-window Acc1 ceiling on OA300 is 55/82 — these 4 tracks are not recoverable by additional DSP-only technique tuning at default intensity. ML is the architecturally-correct lever.

**Conditional resolution by Story 4.6 branch outcome:**
- **Branch A (BNNS resolves ≥3 of 4)** or **Branch B (exactly 2)** → 3-1b is effectively resolved by 4.5/4.6 work; this entry can be moved to RESOLVED at Epic 4 close-out.
- **Branch C (BNNS resolves <2)** → 3-1b returns to active deferred status pending the Story 4.5b research-spike outcome (model architecture / training-data / feature-engineering investigation). This entry stays in the open list under that scenario until the spike's findings re-define the path forward.

Story 4.7 (spectral-flux DSP variant) is an additional upstream lever that may help these 4 tracks even before BNNS — track that story's named-DnB outcomes alongside this entry.

**Note for future stories:** the internal `HarmonicRatioEvidence` struct (Story 3-3b promoted to public) provides typed data (ratio, fastBPM, slowBPM, winnerBPM) that Story 4.5 BNNS feature engineering can consume directly via `BPMDiagnosticTrace`. The public `harmonicRatioDetail` trace field also surfaces this for diagnostic purposes.

**Original deferral rationale (preserved for context):**

**Original deferral rationale (preserved for context):**

**Deferred from:** Story 3-1 code review (2026-04-22)
**Reason:** Three resolution strategies attempted; all caused Acc1 regression (69.5% -> 63-66%)

### Strategies Tried (all regressed)
- Comfort-zone override (80-160 BPM preference): Acc1 dropped to ~66%
- Sub-band vote for 3:2/3:1 pairs: Acc1 dropped to ~63%
- Hybrid (vote + comfort fallback): Acc1 dropped to ~65%

### Validation Targets (now Story 4.5 named-track regression set)
- **Primary:** Charly (expected=160.0, detected=106.2, ratio=1.507)
- **Secondary:** Faraday_Bunker (113.5/170.0), Yin Yang Audio (113.2/170.0), HEFT_Anagram 6 (113.4/170.0)
- All are DnB tracks with 3:2 triplet patterns

## Confidence Semantics for Merge Strategies

**Deferred from:** Multi-window candidate merging code review (2026-03-26)
**Reason:** Design decision, not a bug -- revisit after ablation data

Currently all merge strategies report `max(confidence)` across all windows. For clustered strategies, the winning BPM may come from a low-confidence window while a high-confidence window contributed a different candidate. Consider per-cluster confidence (max confidence among windows contributing to the winning cluster) after ablation shows which strategy works best.

**Files:** `CandidateMergeStrategy.swift`

## Post-Disambiguation Merge Strategies

**Deferred from:** Merge strategy ablation (2026-03-27)
**Reason:** Ablation showed all clustering strategies hurt Acc1 vs maxConfidence

The 6 clustering-based merge strategies (dedup, quorum, average, median, weightedAverage, union) all perform worse than `maxConfidence` (59.8% vs 69.5% Acc1). Root cause: they merge raw candidates from `BPMResult.candidates` which are pre-disambiguation values. Each window's `BPMResult.bpm` has already been through sub-band voting and octave disambiguation, but the `.candidates` array contains the raw top-N peaks before that step.

To make merge strategies useful, they would need to operate on the *final disambiguated BPM* from each window rather than the raw candidates. This would mean:
1. Collect `(bpm: result.bpm, confidence: result.confidence)` from each window (post-disambiguation)
2. Cluster/vote on those final values
3. Return the winner

This is a fundamentally different approach from the current candidate-level merging.

**Files:** `CandidateMergeStrategy.swift`, `AudioAnalysisService.swift`
**Validation:** OA300 benchmark, targeting Acc1 > 69.5%

## Deferred from: code review of 1-1-internal-buffer-reuse-in-bpm-pipeline (2026-04-03)

- **F4: Unnecessary Array copy from PipelineBuffers.windowed** — Both `computeFourierTempogram` and `refineCandidates` copy `pb.windowed` into a new `[Float]` via `Array(UnsafeBufferPointer(...))`. The `vDSP_dotpr` calls could use the pointer directly, avoiding the heap allocation. Optimization for a future performance pass.
- **F5: Integer underflow in reserveCapacity** — `(samples.count - fftSize) / hopSize + 1` yields negative intermediate when `samples.count < fftSize`. No crash (reserveCapacity treats negative as 0), but intent unclear. Pre-existing.
- **F6: Windowed onset truncation** — Windowing always uses only the first `windowLength` elements of the onset envelope. Pre-existing behavior, not caused by buffer reuse changes.

## Deferred from: code review of 2-1-giantsteps-tempo-dataset-integration (2026-04-06)

- **Failure table expected field** — When a track is an Acc2-hit against `tempo2` only, the failure record stores `expected: track.bpm` rather than the matched `tempo2`. Cosmetic -- does not affect accuracy counts, only the failure table display.
- **isAcc2Match divergence** — GiantSteps `isAcc2Match` now includes {1,2,1/2,3,1/3} factors (MIREX-compliant) while OA300 and DAWOracle still use {1,2,1/2} only. Story 2-2 scope.
- **isAcc1Match division by zero** — `abs(detected - expected) / expected` divides by zero when `expected == 0`. Pre-existing across all 3 benchmark files. Current data excludes 0-BPM tracks, so not triggered.
- **Duplicated acc1/acc2 logic** — `runBenchmark` and `benchmarkByGenre` implement the same dual-tempo matching independently. Pre-existing pattern.

## Deferred from: code review of 2-2-dual-tolerance-accuracy-reporting (2026-04-13)

- **Division by zero in `isAcc1Match` when `expected == 0`** — All 3 benchmark files divide by `expected` with no guard. `classifyError` is the only caller that checks `expected > 0`. Spec notes as "Forward Hazard: Epic 2.4 Ground-Truth Validation." Pre-existing (also noted in Story 2-1 review).
- **Negative or zero tolerance not guarded** — No validation that `tolerance > 0` in `isAcc1Match`/`isAcc2Match`. Zero tolerance requires exact float equality; negative always returns false. No current call site passes bad values.
- **`benchmarkByGenre` calls `runBenchmark` but discards meaningful use of returned metrics** — Runs full corpus then recomputes genre-level stats from separate `runBenchmarkDetailed` run. Pre-existing pattern.

## Deferred from: code review of 2-3-performance-benchmark-infrastructure (2026-04-17)

- **Baseline file grows unbounded** — `BaselineStore.append` rewrites the full JSON array on every run. No rotation, pruning, or size cap. Daily CI runs produce multi-MB files within a year. `PerformanceBenchmarkTests.swift:206-218`.
- **Baseline path edge cases** — `PERF_BASELINE_DIR` is passed through `URL(fileURLWithPath:)` with no `~` expansion; `$(CURDIR)/_bmad-output/perf-baselines` in the Makefile may not match the committed repo path when benchmarks run from a `git worktree`. Environmental. `PerformanceBenchmarkTests.swift:582-584`, `Makefile:12`.
- **Fingerprint filename collisions** — Whitespace-differing chip brand strings (`"Apple M5 Max"` vs `"Apple  M5 Max"`) collapse to the same sanitized name; consecutive `_` are not collapsed; only major OS version is encoded so macOS 26.0/26.1/26.5 share one history file. `PerformanceBenchmarkTests.swift:182-192, 220-230`.
- **Hardware edge cases** — `physicalMemoryGiB` integer-truncates to 0 on sub-1-GiB systems; no cap on chip-string length (PATH_MAX on pathological VMs); non-ASCII/emoji chip names pass the Unicode-aware `isLetter`/`isNumber` sanitizer. Won't hit on macOS 15+ Mac hardware. `PerformanceBenchmarkTests.swift:117-119, 182-192`.
- **Virtualization edges** — `ContinuousClock` is documented monotonic but `Duration.components.seconds` can individually be negative; no `max(0, …)` guard. Paused/resumed VMs could surface this. `PerformanceBenchmarkTests.swift:417-420`.
- **Single-track corpus** — If only one track is available, it is labeled warmup, `okDurations` is empty, accuracy is computed but silently discarded, no baseline written. Dev-tooling edge. `PerformanceBenchmarkTests.swift:285-311`.
- **`recordedAt` second-precision + `Date()` non-monotonic** — ISO8601 output omits fractional seconds; two runs in the same second share a timestamp. `records.last` is positional, not chronological, so NTP-induced clock skew can make "last" not be "most recent wall-clock". `PerformanceBenchmarkTests.swift:543-547`.
- **Filesystem error reporting cluster** — Read-only baseline dir, existing-file-at-dir-path, and `.atomic` writes on Dropbox-synced volumes all get caught by `do { try append } catch { print("Warning:") }` and the test passes. Consider `Issue.record` so CI surfaces these. `PerformanceBenchmarkTests.swift:207-218, 609-613`.
- **`isAcc1Match` tolerance is asymmetric** — `abs(detected - expected) / expected` means a match at `detected = 2×expected` and a match at `expected = 2×detected` use different denominators. Matches OA300/DAWOracle suites intentionally; design choice. `PerformanceBenchmarkTests.swift:62-64`.
- **Diagnostic output via `print`** — Hardware header, per-track table, aggregate footer, and Δ line all go to stdout; Swift Testing captures stdout only via raw logs. `Issue.record` or test attachments would preserve output durably. `PerformanceBenchmarkTests.swift:329-395`.
- **`swift test --filter` is substring-match** — A future suite named `PerformanceBenchmarkTestsFoo` would be picked up by `make perf-benchmark`. Theoretical. `Makefile:16`.
- **Two committed records at same `gitSHA: "a97bad2"`** — Both initial records were generated locally at the same commit 53s apart; a future policy decision (CI-only producer vs local-canonical) determines whether this is an issue. `Apple_M5_Max-26.json:655,695`.
- **`GIT_SHA=unknown` silently degrades** — When run outside a git checkout, delta line prints `(SHA unknown, …)`. Working as designed per AC #4 + Dev Notes, but useless for comparing old records. `Makefile:14-15`, `PerformanceBenchmarkTests.swift:548`.
- **`sysctlbyname` post-call size race / byte truncation** — Size returned from second call isn't used to trim the buffer before decoding; theoretical race between size-probe and size-read. `PerformanceBenchmarkTests.swift:71-79`.
- **`FileManager.fileExists` passes unreadable files** — Files with mode 000 or SIP-protected paths pass the filter; `analyzeBPM` throws inside `try?`; track silently becomes `.failed` with no diagnostic. Printing the failing filename + error string would close the loop. `PerformanceBenchmarkTests.swift:269-272, 482-484`.
- **`accuracy.total` and `wallClock.trackCount` reuse the same integer with different semantics** — Only a concern if the out-of-scope accuracy snapshot feature (see decision-needed on story) is kept. Cosmetic. `PerformanceBenchmarkTests.swift:368, 381-395`.
- **All-tracks-failed soft-fail** — If every track throws, no baseline is written but the test reports success with `Acc1=0/N`. Consider a smoke gate like `#expect(oa300Acc1 > 0)`. `PerformanceBenchmarkTests.swift:437-454`.
- **OS version with no digits → fingerprint `-0.json`** — Won't hit on real macOS but superseded by the patch that switches to `ProcessInfo.processInfo.operatingSystemVersion.majorVersion`. `PerformanceBenchmarkTests.swift:220-230`.
- **Debug-mode committed baselines** — Permitted by AC #7 and documented as "Forward Hazard: Release-Mode Numbers" in the story Dev Notes. A future story should decide whether to ship `-c release` in the Makefile target or a separate `make perf-benchmark-release` pair. `Apple_M5_Max-26.json:647,687`, `Makefile:16`.

## Deferred from: code review of 2-3-performance-benchmark-infrastructure (2026-04-18)

**Reason:** Real findings from the third-pass code review; either pre-existing (not caused by Story 2-3 diff) or cosmetic/theoretical enough to defer to a later hygiene story.

- **`loadRecordsRejectsNonV2` unit test uses synthetic-v1 JSON** — The crafted JSON includes `hardware{}`/`wallClock{}`/`accuracy{}` keys a realistic v1 record would not have. Real historical v1 records fall through the generic `.malformed` path, not the `schemaVersion` guard. Guard itself is correct; only test coverage is misleading. `Tests/BoomBoomBoomKitBenchmarkTests/PerformanceBenchmarkTests.swift:+1262-1280`.
- **Malformed-file recovery relies on implicit cross-function state** — After renaming a corrupt file and setting `previousRecord = nil`, the code falls through to `BaselineStore.append`, which internally re-calls `loadRecords` against the (now-missing) renamed file. Works today; any future edit to `append`'s preamble silently breaks recovery. `PerformanceBenchmarkTests.swift:+1157-1171`.
- **`BaselineStoreUnitTests.tmpFile()` defer-cleanup inconsistency** — `loadRecordsMissingFile` never writes, so no leak today, but the pattern differs from every other test in the suite (which `defer`s cleanup). A future write-adding edit silently leaks tmp files across CI runs. `PerformanceBenchmarkTests.swift:+1240-1245`.
- **Makefile `perf-benchmark` `trap` tiny-window leak** — Retired. Story 2-6 removed the staging/trap flow entirely; this item is no longer applicable.

## Deferred from: code review of 2-4-oa300-corpus-expansion-with-genre-diversity (2026-04-18)

**Reason:** Findings from the code review that are either pre-existing, environmental, or cosmetic enough to defer to follow-up hygiene rather than gate Story 2-4 from moving to `done`.

- **Test runs `JSONDecoder().decode()` twice in `throwsWhenGenreMissing`** — Belt-and-suspenders: the `#expect(throws: DecodingError.self)` block plus a second `do/catch` to extract the `keyNotFound` key both decode the same JSON. Could consolidate once Swift Testing supports typed associated-value matching. Cosmetic. `Tests/BoomBoomBoomKitTests/CorpusTracksDecodingTests.swift:56-69`.
- **`convert-rekordbox-export.py` CLI has no `args.output` hardening** — No parent-dir validation; `-o -` creates a literal file named `-` instead of routing to stdout; trailing-slash path surfaces `IsADirectoryError` as a bare traceback. Pre-existing style; script is not meant to be re-run per Task 3.6. `Tests/BoomBoomBoomKitTests/Fixtures/convert-rekordbox-export.py:278-281`.
- **No `encoding="utf-8"` on `open(args.output, "w")` or `sys.stdout`** — Under `LANG=C`/`LC_ALL=POSIX`, `json.dump(..., ensure_ascii=False)` can raise `UnicodeEncodeError` mid-output because the fixture contains non-ASCII (`Xiûa`, smart apostrophes). Environmental; macOS default locale is UTF-8. `Tests/BoomBoomBoomKitTests/Fixtures/convert-rekordbox-export.py:278, 283-284`.
- **Duplicate `.wav` / `.mp3` pairs in OA300 fixture count twice in per-genre accuracy** — E.g., `5. Darkgray Heart_Beating Heart Of The Summer Sun (robbyt Remix)` has both `.wav` and `.mp3` variants as separate rows. Pre-existing corpus data-quality issue, not introduced by this story but now doubled in Story 2-5's per-genre buckets. `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/oa300-ground-truth.json`.
- **Completion Notes' "145/145 unit tests green" and GiantSteps `Acc1=81.1%, Acc2=82.5%` are unverifiable from the diff** — Trust-but-verify via `make test` and `make benchmark-giantsteps` before flipping status to `done`. Verification-only, not a code change. `_bmad-output/implementation-artifacts/2-4-oa300-corpus-expansion-with-genre-diversity.md:282-284`. (Note: `make test` re-run during review returned 149/149 green — claim upheld. GiantSteps re-run not repeated but unchanged code path.)
- **`--merge-from <json>` flag for `convert-rekordbox-export.py`** (Gemini refinement #1) — Preserve `genre` on re-run by reading the existing ground-truth JSON, extracting per-filename genre tags, and re-applying them to the newly generated rows. Currently the script warns loudly that re-running destroys genre; this would close the footgun. Spec explicitly lists the extension as out-of-scope (line 254-255); pick up if the script ever needs to re-run.
- **`Genre` wrapper struct with `RawRepresentable` / `ExpressibleByStringLiteral`** (Gemini refinement #4) — Replace `OA300Track.genre: String` with a typed wrapper exposing `static let drumAndBass: Genre = "drum-and-bass"` for common labels while staying string-compatible (preserves AC #2's "no library change required to add a genre"). Drift-guard test (`fixtureGenresAreWithinAllowedTaxonomy`) already catches typos in practice. Revisit once Story 2-5's reporter makes the ergonomic benefit (autocomplete, compile-time checks on common buckets) concrete.

## Deferred from: Story 2-5 genre-stratified accuracy reporting (2026-04-19)

**Reason:** Forward hazards noted during Story 2-5 implementation that are explicitly out-of-scope for this story but should be picked up later.

- **`BoomBoomBoomKitTestSupport` organization review** — Post-2-5, TestSupport holds 5 public helper files: `AccuracyMatchers`, `CorpusTracks`, `AudioFixtures`, `TestSignalGenerators`, and `GenreAccuracyReporter`. Five is the threshold where a table-of-contents or subdirectory split starts to pay off; at 7+ a README explaining the public-surface contract becomes table stakes. Revisit organization (subdirectories, README, public-API/SemVer contract) when the count exceeds 7. `Sources/BoomBoomBoomKitTestSupport/`.

## Deferred from: code review of 2-5-genre-stratified-accuracy-reporting (2026-04-19)

**Reason:** Review findings that are real but either blocked on a post-commit action or reflect coverage gaps the original AC set did not require.

- **AC #8 commit-SHA backfill in `.claude/skills/running-benchmarks/SKILL.md`** — Story spec literal requires the SHA to be recorded in Completion Notes "after the story commits". Story 2-5 was reviewed pre-commit, so the current doc references Story 2-5 by name instead. Post-commit, edit the skill doc's "Genre-stratified output" subsection to pin the expected stratification to the actual commit SHA. `.claude/skills/running-benchmarks/SKILL.md`.
- **`GenreAccuracyReporterTests.swift` coverage expansion** — AC #6's 8 cases do not exercise (a) ≥3-element tie-break to strict-weak-ordering the comparator, (b) `overallAcc1Percent == 0.0` and `== 100.0` boundaries of the `format(...)` precondition, (c) long (>20 char) or non-ASCII genre names that would expose silent truncation by `padding(toLength: 20, …)`. Pick up when a future corpus refresh introduces genres that cross these boundaries. `Tests/BoomBoomBoomKitTests/GenreAccuracyReporterTests.swift`.

## Deferred from: code review of 2-6-perf-baselines-file-per-run-redesign (2026-04-19)

**Reason:** Latent risks that cannot trigger with current ISO8601 formatter (UTC-only) or cosmetic/stability trade-offs acknowledged in the story dev notes.

- **`compactRecordedAt` non-UTC offset risk** — Stripping `-` and `:` from `recordedAt` would leave a `+` in the filename if the timestamp ever had a numeric timezone offset (e.g. `+05:30`). Cannot trigger today: formatter is hardcoded `secondsFromGMT: 0`. If the formatter is ever changed, validate that `compactRecordedAt` produces a filesystem-safe string. `PerformanceBenchmarkTests.swift` — `write` function.
- **`recordedAt` string sort breaks for non-UTC records** — `records.sorted { $0.recordedAt < $1.recordedAt }` is correct iff all timestamps are UTC ISO-8601 extended form. Same root cause as above; hypothetical future risk only. `PerformanceBenchmarkTests.swift` — `readHistory` return sort.
- **jq "Noise floor" median uses floor-index for even N** — `.[length/2|floor]` returns the lower-middle element rather than the average of two middle elements. Pre-existing methodology choice; not introduced by this story. `.claude/skills/running-benchmarks/SKILL.md` — "Noise floor" recipe.
- **Same-second `recordedAt` sort: `history.last` nondeterministic for rapid runs** — Two writes within the same UTC second produce identical `recordedAt` strings; Swift's stable sort preserves filesystem-enumeration order for equal keys, which is not meaningful. Cosmetic: the delta line reports a slightly off comparison. Spec explicitly uses read-before-write as the load-bearing correctness invariant, not sub-second ordering. `PerformanceBenchmarkTests.swift` — `readHistory` return sort.
- **Temp `.json.tmp` file lives in Dropbox-synced directory** — The old staging design kept temp files outside Dropbox. New design places the `.tmp` sibling directly in `_bmad-output/perf-baselines/`. Sub-millisecond lifetime on APFS means the risk of Dropbox racing with the rename is very low; `readHistory` ignores `.tmp` files; dev notes acknowledge this trade-off. Monitor if Dropbox conflict copies appear in the future. `PerformanceBenchmarkTests.swift` — `write` function.

## Deferred from: code review of 3-2-fine-grid-precision-fix (2026-04-25, Codex gpt-5.5 round)

Replaces an earlier same-day defer list; supersedes items now reclassified as `patch` (clamp gap, floor/ceil constants, `actualBPM` assertion, AC #2 body alignment).

### High-priority follow-up

- **Story 3-7: 96 kHz × 126 BPM candidate selection (HIGH)** — `refineCandidates` is called only on `[winner]` (`Sources/BoomBoomBoomKit/BPMAnalyzer.swift:314`), so when upstream candidate selection picks 125 BPM at 96 kHz × 126, refinement cannot recover the correct candidate. User-visible output ≈ 125.35 BPM. The test exclusion at `Tests/BoomBoomBoomKitTests/BPMAnalyzerTests.swift:430` masks this real wrong-BPM result. Fix candidates: refine top-K candidates and let downstream selection re-score, or add a tie-break in coarse candidate ranking specific to 96 kHz onset characteristics. Out of scope for Story 3-2 (fine-grid step only); tracked here pending the next epic-3 story.

- **Story 3-2: Icicle 0.3 BPM (PARTIAL on AC #3)** — Story 3-2 self-marked AC #3 as PARTIAL and ships with Icicle's absolute error at 0.3 BPM (vs the 0.05 BPM target). Corpus accuracy holds (OA300/GiantSteps unchanged). Future investigation: ACF interpolation has inherent peak-location bias; cubic Lagrange and tempogram-only approaches each regress either synthetic precision or real corpus. Either accept the 2% Acc1 user-visible bound or design a fundamentally different ACF interpolant.

### Lower-priority residual items

- **Tie-run handler treats broad shoulders as plateaus** — `1e-6` absolute eps on Float-normalized fused scores (max ~1) is ~8 ULPs; broad valid peaks with near-equal neighbors get classified as a tie-run and skip the quadratic fit. Heuristic; corpus accuracy holds today. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1084-1088`.
- **All-zero fused plateau bypasses tie-run handler** — when every fused score is exactly 0, `bestFused = 0` and strict `>` never sets `winnerIdx`; the full-window plateau returns `centerBPM` instead of the midpoint. Requires complete signal absence. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1061, 1070, 1079`.
- **Empty/negative scan window returns unclamped center** — when `centerBPM` is far enough outside `bpmRange` that `scanMax < scanMin`, the guard appends `centerBPM` unchanged instead of clamping or rejecting. Current callers always pass valid ranges. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1030, 1035`.
- **`centerIdx` not clamped to `[0, stepCount-1]` before distance comparisons** — used only in subtraction (no array index) so no out-of-bounds risk. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1121`.
- **NaN/Inf propagation: silent center/discrete fallback rather than rejection** — NaN comparisons fail, causing center fallback through normalization, fused product, and `denom < -epsD`. Defensive concern; not observed. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1055, 1064, 1173`.
- **Quadratic fit accepts very weak (low-signal) peaks** — `bestFused > 0` only requires a positive product after normalization; tiny tempogram/ACF products from noisy windows can be quadratically refined into precise-looking BPMs with false confidence. No minimum-score guard; corpus accuracy holds. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1048-1057, 1133-1158`.
- **`epsD = max(1e-6, 1e-6 * magnitudeMax)` floor rejects valid shallow peaks at fused-score scale** — at fused-score scale (~1) the floor dominates and the relative term is dead code; at very small magnitudes the floor could reject well-conditioned fits. Heuristic; corpus accuracy holds. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1166-1168`.
- **`tempogramSearchSteps` / override constants not validated against `stepCount`** — when `bpmRange` is narrower than 0.4 BPM the override search range collapses silently. Not exposed by current callers. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1021-1023, 1107-1118`.
- **Override gate constants (0.4/0.3/0.3 BPM) hard-coded to the diagnosed synthetic case** — by design and documented; OA300+GiantSteps validate the choice. Latent risk if onset rate, hop size, or window duration ever change. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1007-1014`.
- **Quadratic refinement changes output contract from 0.1 BPM grid to arbitrary BPM** — by design (the refinement IS the sub-BPM precision goal); doc comment updated to "sub-BPM resolution". Downstream callers must not assume grid alignment. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:913, 1146`.
- **Tempogram override can return outside its documented ±0.4 BPM search radius** — quadratic offset on a peak at exactly 4 grid steps from fused winner can push output to ~0.5 BPM. Single-step overshoot is well within the ±4 BPM scan window. `Sources/BoomBoomBoomKit/BPMAnalyzer.swift:1107, 1178, 1182`.

## Deferred from: code review of 4-1-boomboomboomkitml-package-structure (2026-05-04)

**Reason:** Hardening items for the `compile-model` Makefile target and per-target platform constraints, all in Story 4.5's scope. Story 4.1 is structurally inert and does not exercise the `compile-model` target.

- **`compile-model` Makefile robustness** — Multiple edge cases for the new target: (a) `xcrun coremlc` is unavailable on Command Line Tools-only installs (no full Xcode); (b) `[ ! -f ]` guard rejects `.mlpackage` directory format silently with "not found" message; (c) empty `ML_MODEL_INPUT=` produces confusing `Error: ML_MODEL_INPUT not found at .` diagnostic; (d) no stale `<name>.mlmodelc` cleanup before recompile (renamed inputs leave orphan dirs); (e) no symlink-follow guard on `ML_MODEL_OUT_DIR`; (f) no `[ -r ]` readability preflight; (g) `xcrun coremlc` failure prints raw output without remediation hint. Story 4.1's gating checklist does NOT exercise this target by spec design (DD #5). Address when Story 4.5 first runs `make compile-model` against a real `.mlmodel`. `Makefile:118-127`.
- **`xcrun coremlc` vs `xcrun coremlcompiler` tool selection** — Both binaries exist under XcodeDefault.xctoolchain. Either may be the canonical Apple-documented path; verify and standardize when Story 4.5 first invokes the target. `Makefile:127`.
- **Per-target `platforms:` constraint for BNNS / CoreML availability** — `BoomBoomBoomKitML` target inherits package-level `[.macOS(.v15)]`. Currently fine. If the package adds platforms (visionOS, watchOS, tvOS, iOS, or older macOS) the `import CoreML` and `BNNSGraph*` symbols may not resolve. No `@available` annotations on `BNNSTechnique` / `CoreMLTechnique` structs. Add per-target platform floor and `@available` guards when Story 4.5/4.6 lands the actual conformance shape against `BNNSGraphCompileFromFile` (macOS 14+) and `MLModel.init(contentsOf:configuration:)` (broader). `Package.swift:6,22-27`; `Sources/BoomBoomBoomKitML/BNNSTechnique.swift:8`; `Sources/BoomBoomBoomKitML/CoreMLTechnique.swift:8`.

## Deferred from: code review of 4-3-ml-technique-slot-wiring-and-tuple-to-struct-migration (2026-05-05)

**Reason:** Adversarial code review surfaced five real follow-ups that are out-of-scope for Story 4.3's plumbing-only delivery. Two map to existing follow-up stories (4-3b for perf-test methodology, 4.4 for ensemble validation, 4.5/4.6 for cancellation cooperation). Filed alongside the patches written into the story file.

- **Story 4.4 — `MLEvaluation` input validation against NaN/Inf and out-of-range bpm/confidence.** Today `combine` discards `mlEvaluation` (DD #5), so unvalidated values cannot propagate. Story 4.4's `EnsemblePolicy` cases will read the fields directly; without a failable init or precondition + clamp, a buggy conformer could surface NaN BPM via `AudioAnalysisResult`. Decision: add validation on `MLEvaluation.init` OR clamp in the policy switch. `Sources/BoomBoomBoomKit/DSPTechnique.swift` `MLEvaluation.init`.

  - **RESOLVED BY STORY 4.4 (2026-05-08):** Sanitized at the consumer (`EnsembleCombiner.combine`), NOT at `MLEvaluation.init`. Two-sentinel rule per DD #4: non-finite `bpm` (NaN, ±∞) treats the ML evaluation as a sentinel-abstain (combiner falls back to DSP, `mlAbstained: true` in the attached `EnsembleDecision`); finite-but-out-of-range `bpm` clamps to `60.0...200.0`. Confidence sentinel is independent: non-finite collapses to `0.0` (NOT abstain — bpm may still be valid); out-of-range clamps to `0.0...1.0`. `MLEvaluation.init` itself is unchanged (per DD #4: a failable init would be a breaking change to a struct that already shipped in Story 4-3). Tests: `EnsembleCombinerSanitizationTests` (6 boundary cases); existing `MLEvaluation` DocC saying "conformers should clamp predictions" stays correct as defense-in-depth. Close-out commit: see `git log --grep "Story 4-4: configurable EnsemblePolicy"`.

- ~~**Story 4.5/4.6 — Cancellation cooperation across `MLTechnique.evaluate` boundary.** No `options.isCancelled()` check between `MetadataCorroborator.apply` and `MLTechnique.evaluate` in `analyzeBPM`. Mock evaluate is sub-millisecond so unobservable today. A real BNNS or CoreML conformance with multi-second inference will hold the worker thread past `isCancelled` becoming true, violating the documented `analyzeBPM` cancellation contract. Decision: add a between-stage cancellation check in `analyzeBPM` AND/OR document the per-conformance responsibility. `Sources/BoomBoomBoomKit/AudioAnalysisService.swift:266-279`.~~

  - **RESOLVED BY STORY 4.5 (2026-05-14, SHA: 0b2d8dc):** Story 4.5 introduced the private static `AudioAnalysisService.evaluateMLIfActive(options:trace:)` helper. The post-review-pass-v2 spec-canonical semantic places the cancellation check AFTER the policy/ml/trace guard (so `.dspOnly` returns nil silently regardless of cancellation, preserving Story 4-4 AC #14's `RecordingMockMLTechnique.callCount == 0` invariant) and BEFORE `ml.evaluate(trace:)` is invoked. Story 4.6 (CoreML) inherits the helper as-is — the same gate fires regardless of which `MLTechnique` conformance is wired up. Cancellation latency contract: BNNSGraph inference is atomic from the consumer's perspective (no mid-inference cancellation hook); a cancelled task waits the full inference wall-clock (50-1000 ms typical for the bundled tempo CNN) before observing cancellation. ADR-1's per-window granularity is satisfied; finer mid-inference cancellation is documented as a separate future enhancement.

- **Story 4-3b — Perf-gate sequential-ordering bias (cache + thermal warmth).** Corpus-grain `mlMockOnAbstainPerf` runs baseline pass to completion before mock pass, so OS file cache and CPU thermal state are warmed when mock measurement begins. Biases `ratio = mockMean / baselineMean` downward — a real ~1.40x trace-build regression could land at ~1.15x measured. Interleave per-track (baseline, mock, baseline, mock, …) or randomize. Belongs in Story 4-3b alongside threshold tightening. `Tests/BoomBoomBoomKitBenchmarkTests/PerformanceBenchmarkTests.swift:439-462`.

- **Story 4-3b — Perf-gate uses arithmetic mean; switch to robust statistic.** Single slow outlier track skews the gate. Median or trimmed mean would flake less under CI noise. Lower-priority than the cache-warmth bias but belongs in the same scope. `Tests/BoomBoomBoomKitBenchmarkTests/PerformanceBenchmarkTests.swift:446-448`.

- **Story 4-3b — Threshold-prose arithmetic correction.** AC #7 amendment claims "25% headroom over the empirical ~1.22x floor" but 1.30 / 1.22 ≈ 1.066 (~6.5% headroom). The threshold itself was Codex-finalized; the prose math is wrong. Story 4-3b should rebase the threshold against measured floor data and document the chosen headroom correctly. Spec lines 162-176.

## Apple-library dependency-budget evaluation (party-mode 2026-05-05)

**Reason:** During Story 4.3 code review, the user opened the door to adopting `apple/swift-collections` or `apple/swift-service-context` if either would simplify the `BPMDiagnosticTrace.candidatesAfterBoost` empty-vs-absent disambiguation. Investigation concluded **neither library is a fit for that question** — but the evaluation surfaced potential future use cases worth tracking. Captured here so the investigation doesn't have to repeat. The zero-dependency posture (`project-context.md` §"Technology Stack & Versions") remains intact for now.

- **`swift-collections` — possible Story 4-3b lever.** `OrderedCollections.OrderedDictionary<Key, Value>` is a candidate replacement for the `subBandEnergies: [String: Float]` field whose typed-struct migration is the lead hypothesis in Story 4-3b's trace-build cost budget. Story 4-3b's current plan migrates to a named `SubBandEnergies` struct (kick/snare/crack/hihat fields) following the Story 3-3b typed-evidence precedent — `OrderedDictionary` is an alternative direction that preserves dict-shaped access while gaining insertion-order stability for trace serialization. Pre-1.0 / no-BC framing accepts either direction; pick based on whether 4-3b ablation reveals dict-shaped access patterns are load-bearing for future ML feature engineering. Lower-priority alternative: `HeapModule.Heap` for top-K candidate selection (currently sorts arrays; O(log k) per push vs O(n log n) sort). Minor at typical N≈10 candidates; not worth a dependency on its own.

- **`swift-service-context` — wrong tool, do NOT adopt.** `ServiceContext` provides a typed-key bag (`ServiceContextKey` protocol with associated `Value`), where lookup returns `Optional<Value>` — type-encoding "key-not-set vs key-set-to-empty." That part appeared to solve the empty-vs-absent question raised by code review of 4.3. **However:** the design center is W3C Baggage / OpenTelemetry cross-async context propagation via task-local values. `BPMDiagnosticTrace` is a synchronous single-shot diagnostic record, not telemetry propagated through async hops. Adopting it would (a) regress the typed-evidence migration discipline (`ClickCorrelationEntry`, `HarmonicRatioEvidence`, `SubBandVoteEvidence`, `DurationHintEvidence`, `BarCandidate`) toward key-bag semantics, (b) lose DocC discoverability of trace fields, (c) impose a dependency designed for cross-async-context propagation on a synchronous in-process pipeline. **The eventually-chosen resolution to the original empty-vs-absent question was Codex's Option 2** (Codex consult thread `019dfa81-a8b9-7bf3-b602-4f8c53916ab0`, finalized 2026-05-05): populate `candidatesAfterBoost` unconditionally inside `MetadataCorroborator.apply`'s no-tags branch by mirroring `result.candidates`. This preserves byte-identity on the disabled-policy path (the `bpm`/`confidence`/`candidates` fields of BPMResult are unchanged; only the trace's `candidatesAfterBoost` and `metadataPolicyUsed` fields are populated). The earlier "Option 4" framing of `metadataPolicyUsed` as a sibling Optional run-state predicate was rejected — Codex grounded against the working tree and verified `metadataPolicyUsed: MetadataPolicy = .default` is non-optional in `BPMDiagnosticTrace.swift:136`, so the proposed predicate doesn't predicate. Cross-checked via party-mode (Winston/Amelia/Mary/Siri picked Option 1 doc-rewrite; Codex picked Option 2 unconditional populate; Option 2 won on round-2 grounding).

- **Revisit triggers.** The zero-dependency posture should be revisited via a named story spec (NOT in code review) if any of: (a) Story 4-3b chooses `OrderedDictionary` over named struct for `subBandEnergies`; (b) a future story requires a Heap, Deque, or Trie that would otherwise demand re-implementing standard data structures from scratch; (c) Stories 4.5/4.6 BNNS or CoreML feature-engineering surfaces a need for typed-key payloads that the named-typed-evidence-struct discipline cannot economically express. None of these triggers fire at Epic 4 close-out as currently scoped.

- **Cross-check sources.** Party-mode round 2026-05-05 (Winston, Amelia, Mary, Siri, Codex consult thread `019dfa81-a8b9-7bf3-b602-4f8c53916ab0`); `axiom:ask` Apple-API-precedent reply citing `URLSessionTaskMetrics`, `URLResourceValues`, `MKAnnotation`, `OSLogEntry`, `NSAttributedString.attributes(at:effectiveRange:)`. Files: `Sources/BoomBoomBoomKit/BPMDiagnosticTrace.swift:133` (`candidatesAfterBoost` field), `Sources/BoomBoomBoomKit/MetadataCorroborator.swift:62-65` (early-return short-circuit), `Sources/BoomBoomBoomKit/DSPTechnique.swift:240,270,284-285` (protocol DocC).

- **`swift-algorithms` — modest fit only; do NOT adopt for plumbing-level wins.** 6.3k stars, 27 algorithm modules: `AdjacentPairs`, `Chain`, `Chunked`, `Combinations`, `Compacted`, `Cycle`, `EndsWith`, `FirstNonNil`, `FlattenCollection`, `Grouped`, `Indexed`, `Intersperse`, `Joined`, `Keyed`, `MinMax`, `Partition`, `Permutations`, `Product`, `RandomSample`, `Reductions`, `Rotate`, `Split`, `Stride`, `Suffix`, `Trim`, `Unique`, `Windows`. Surveyed against current code:
  - **No fit for the DSP hot path.** Bulk numeric operations on `[Float]` go through vDSP (`vDSP_vmul`, `vDSP_rmsqv`, `vDSP_vrsum`, etc.) — sequence algorithms can't compete on contiguous-buffer perf and the `project-context.md` rule "every bulk numeric operation must use vDSP" rules them out at the buffer level.
  - **Possible fits in test-support / benchmark layers:**
    - `Keyed` or `Grouped(by:)` — `BoomBoomBoomKitTestSupport.GenreAccuracyReporter` currently builds `[String: [Track]]` manually; `Dictionary(grouping:by:)` from stdlib already covers this with no dependency. swift-algorithms version is more flexible only when the key is computed.
    - `Product` — benchmark `(VotingPolicy, threshold)` sweep currently uses nested for-loops; `product(VotingPolicy.allCases, thresholds)` would flatten one level. Cosmetic.
    - `RandomSample` — corpus sub-sampling for fast-cadence smoke benchmarks (analog to `make ablation-smoke`'s curated 16-combo subset). Currently the smoke list is hand-curated; a `randomSample(count:)` would change the determinism contract and is therefore **not** a drop-in.
    - `Combinations.allCombinations()` — `TechniqueSet.allDSPCombinations()` currently uses `2^7` bit-shift iteration (faster, clearer, locked by unit-test invariant `count == 128`). swift-algorithms `combinations(ofCount:)` returns subsets of a fixed size, NOT all subsets — wrong shape, no fit.
    - `Windows(ofCount:)` — sliding-window iteration. The DSP pipeline iterates window CHUNKS via vDSP indices; the multi-window candidate merge iterates `[BPMResult]`, not a sequence-window pattern. No load-bearing fit.
  - **Verdict:** none of the candidate sites is load-bearing enough to justify a dependency on its own. If a future story introduces N≥3 distinct usage sites (e.g., test-support utilities consolidating around `Grouped` + `Keyed` + `Product`), revisit then via a named story spec.

- **`swift-async-algorithms` — zero fit; do NOT adopt.** 3.6k stars. Modules cover async-stream composition (`Merge`, `Zip`, `CombineLatest`, `FlatMapLatest`), rate-limiting (`Throttle`, `Debounce`), buffering (`Buffer`, `MultiProducerSingleConsumerChannel`), reductions (`AsyncReductionsSequence`), timers (`AsyncTimerSequence`), and byte iteration (`AsyncBufferedByteIterator`). Surveyed against current code:
  - **Library is fundamentally synchronous.** `AudioAnalysisService.analyzeBPM(url:options:)` is a single throwing function. The DSP pipeline is sync end-to-end. The only async/await usage is the consumer-supplied `Task` that calls `analyzeBPM`, plus `withTaskGroup` for parallel track analysis in benchmarks (`AblationFullMatrixTests.fullAblationMatrix`, `OA300BenchmarkTests`). swift-async-algorithms does NOT replace `TaskGroup` — that remains stdlib territory.
  - **No async streams to compose.** The library has no `AsyncSequence` producers. Cancellation flows through the injectable `isCancelled: @Sendable () -> Bool` (synchronous predicate, default `Task.isCancelled`). Progress flows through `onProgress: (@Sendable (ProgressUpdate) -> Void)?` (synchronous callback). Both ADR-1 decisions explicitly prefer the synchronous predicate/callback model over `AsyncStream`.
  - **No event sources to throttle/debounce.** No UI events, no network streams, no file watchers — the library reads PCM once per `analyzeBPM` call.
  - **`AsyncBufferedByteIterator`** — irrelevant: `PCMBufferReader` uses `AVAudioFile` for audio decoding (Core Audio codecs), not raw byte iteration.
  - **Verdict:** zero candidate sites. swift-async-algorithms applies to event-driven / stream-oriented apps; this library is request-response synchronous. Do not investigate again unless the public API surface grows an `AsyncSequence`-shaped producer (e.g., a future "live BPM stream from microphone" use case — none currently planned).

- **Combined evaluation summary (4 Apple libraries surveyed 2026-05-05):**

  | Library | Fit | Adopt now? | Revisit trigger |
  |---|---|---|---|
  | `swift-collections` | Possible Story 4-3b lever (`OrderedDictionary`, `Heap`) | No | Story 4-3b chooses dict-shape over named struct, OR a future story needs a non-trivial data structure |
  | `swift-service-context` | Wrong domain (cross-async telemetry, not in-process diagnostics) | No | Never — close the door on this one |
  | `swift-algorithms` | Modest test-support fits only | No | N≥3 distinct usage sites surface in a single story |
  | `swift-async-algorithms` | Zero fit (library is synchronous) | No | Public API surface grows an `AsyncSequence` producer |

  **Net position:** zero-dependency posture (`project-context.md` §"Technology Stack & Versions") survives the 2026-05-05 evaluation intact. The cost of revisiting this evaluation is now amortized — future stories can consult this section before re-investigating.

## Deferred from: code review of 4-4-configurable-ml-ensemble-voting-policy (2026-05-08)

- **`clampBPM` silent garbage-in-garbage-out under `.highestConfidence`** — `EnsembleCombiner.clampBPM(_:)` maps `bpm: 999, confidence: 0.99` to `bpm: 200, confidence: 0.99`, beats DSP under `.highestConfidence`, and propagates to `AudioAnalysisResult` indistinguishable from a legitimate 200 BPM prediction. Spec DD #4 explicitly chose silent clamp; pre-clamp value is dropped. **Re-open trigger:** Story 4-5 BNNS calibration data shows model emitting out-of-range BPM in non-trivial fraction of corpus, OR debugger UI (Epic 5) needs to show "predicted X, clamped to Y".
- **`EnsembleDecision.selectedBPM` redundant with `BPMResult.bpm`** — invites silent drift if a future combiner change updates one but not the other. Spec DD #11 enumerated 6 fields; today the 9-cell trace test pins the invariant. **Re-open trigger:** drift observed in production trace data, OR Story 4-5/4-6 adds a path where the two values can legitimately differ.
- **`EnsembleDecision` is not `Codable` while nested `Winner: Codable`** — half-Codable creates a future trap when `BPMDiagnosticTrace` is made `Codable`. **Re-open trigger (UPDATED 2026-05-20, Story 5-4):** Story 5-4 deferred library-side `BPMDiagnosticTrace: Codable` to a future named story; the demo carries its own `TraceExport` Codable projection at `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/TraceExport.swift` (DD #2 + AC #10 — zero library changes). The original re-open trigger (Story 5-4) does NOT fire because no library-side promotion happened. The new re-open trigger: whichever future story decides to promote `BPMDiagnosticTrace` to `Codable` library-side will also need to add `Codable` to `EnsembleDecision`. Until then, this entry stays open with the updated cross-reference.
- **`EnsembleDecision` does not record pre-clamp ML BPM** — typed-evidence record is incomplete; debugger UI cannot reconstruct the raw model prediction. Spec DD #11 didn't list this field. **Re-open trigger:** Story 4-5 BNNS or Story 4-6 CoreML calibration evidence demands the raw value, OR Epic 5 demo app reveals the gap.
- **`Winner.dsp` overloads "DSP picked by policy" and "ML abstained on NaN bpm"** — downstream consumers must check `mlAbstained` to disambiguate. **Re-open trigger:** Story 4-5/4-6 or Epic 5 reveals consumers consistently writing `if winner == .dsp && mlAbstained { ... }` — that's the pattern that justifies a fourth Winner case (e.g., `.mlAbstained`).
- **`.highestConfidence` does not sanitize `dspWinner.confidence`** — combiner trusts DSP-side confidence is finite; a future regression that emits NaN DSP confidence (e.g., tag-boost arithmetic with NaN tag) silently propagates. Pipeline doesn't produce NaN today. **Re-open trigger:** any DSP-side path lands NaN confidence in production traces.
- **`MLPolicySweepTests` envelope/row JSON: snake_case + camelCase mix; no `CodingKeys`; no `.atomic` write option** — cosmetic + future-robustness gaps. **Re-open trigger:** an external consumer parses `4-4-ml-policy-sweep.json` (e.g., dashboard ingestion) and trips on the casing mix, OR a Swift toolchain default-keys change breaks byte-stability.
- **Story 4-4 runtime-evidence verification — RESOLVED 2026-05-09 at SHA `16e03e0`.** All five gates re-run post-patch to confirm corpus numbers held: `make perf-benchmark` (OA300 Acc1=58/82 Acc2=74/82 ✓, GiantSteps Acc1=537/661 Acc2=546/661 ✓, mock-on-abstain ratio 0.992x — well under 1.20x gate ✓, Δ vs prior baseline `be38d80` was `0.177s → 0.176s (-0.4%)`); `make benchmark` (OA300 default-intensity strict 2%: 58/82, 74/82 ✓); `make benchmark-giantsteps` (537/661, 546/661 ✓); `make ablation` (best combination `sharp+fine+vote` Acc1=55 ✓, baseline `fine+vote` Acc1=53); `make ml-policy-sweep` × 2 (run 1: 10.254s; run 2: 10.878s; `diff` exit 0 → byte-identical ✓ — HALT (c) determinism gate satisfied). The `4-4-ml-policy-sweep.json` artifact's accuracy counts were byte-identical to the pre-patch artifact at `be38d80`; only `snapshot_sha` changed (be38d80 → 16e03e0). New perf-baseline file at `_bmad-output/perf-baselines/Apple_M5_Max-26--Debug--20260509T011243Z--16e03e0--e3dbd9f1.json`. Branch `rterhaar/epic-4` is clear for merge from a Story 4-4 perspective.

## Deferred from: code review of 4-4b-tempo-classifier-training (2026-05-09) — chunk 3 of 4

- **MODEL_CARD GiantSteps split sums to 661 not the 664 cited in CLAUDE.md** — `MODEL_CARD.md:43-44` lists 595 train + 66 val from GiantSteps folds 02-10 / fold 01 (= 661). CLAUDE.md cites the GiantSteps Tempo Dataset as 664 EDM tracks. The 3-track delta is the labeled subset (3 tracks have malformed/missing labels). Doc polish only. **RESOLVED 2026-05-16 by Story 4-6 Branch C close-out:** the bundled `giantsteps_v1.mlmodelc` was removed; the GiantSteps split arithmetic now lives in MODEL_CARD's historical section below the new Status block. No current shipping claim relies on this count. A future Branch-A retrain that re-bundles a higher-quality model should refresh the arithmetic in a new Status entry.
- **MODEL_CARD OA300 row shows 82 tracks under a corpus named "OA300" without explaining the 218 unused** — `MODEL_CARD.md:45`. OA300 = 300 onset audio tracks of which only 82 carry ground-truth labels (project-internal corpus). Doc polish; consumers cloning main see 82 with no explanation of the corpus name. **Re-open trigger:** consumer issue/PR asking what OA300 is; OR Story X expands the labeled subset and the count changes.
- **License posture for trained-weights derivative work on GiantSteps + OA300 corpora needs counsel review** — `MODEL_CARD.md:134-138`. The bundled `giantsteps_v1.mlmodelc` is a derivative work; the "distributed under the same license as BoomBoomBoomKit" claim assumes the corpus licenses permit weight redistribution. GiantSteps is research-use; OA300 is project-internal. Out of code-review scope. **Re-open trigger:** pre-1.0 legal review (Epic 4 retrospective or pre-release counsel pass); OR a consumer files a license question on the bundled model.

## Deferred from: code review of 4-4b-tempo-classifier-training (2026-05-09) — chunk 2 of 4 (`tools/coreml-convert/`)

- **README "non-recommended accuracy default" framing too negative vs spec's "sensible default" posture** — `tools/coreml-convert/README.md:11,17`. README characterizes the bundled `giantsteps_v1.mlmodelc` as "measurably less accurate than the DSP pipeline" and "not a recommended accuracy default"; spec DD #0 / DD #11 frame it more neutrally as a "sanity demonstrator + sensible default" that consumers replace via BYOM. Framing call. **RESOLVED 2026-05-16 by Story 4-6 Branch C close-out:** superseded — the bundled-model framing is gone entirely from `tools/coreml-convert/README.md` (intro + Path A rewritten to BYOW-only voice). No "non-recommended accuracy default" wording survives.
- **`ReLU(inplace=True)` on sliced (non-contiguous) view in `MultiKernelConvBlock.forward`** — `tools/coreml-convert/reference_arch.py:694-705`. Even-kernel convs with `k//2` padding produce off-by-one widths that get cropped to a view; in-place ReLU on a view risks subtle eager-vs-traced numerical drift. Speculative until measured. **Re-open trigger:** numerical-equivalence test (P13 in chunk 2 review) catches drift, OR consumer reports trace/eager mismatch on a custom architecture.
- **`weights_only=True` rejects PyTorch 2.4-2.5 checkpoints containing schedulers / custom transforms / dataclasses** — `tools/coreml-convert/convert.py:369`. No fallback is the conservative security posture. Common research/training checkpoints (Lightning + custom Lambda transforms) hit `UnpicklingError`. **Re-open trigger:** multiple consumer reports of `UnpicklingError` on common training-framework outputs, OR a security model emerges that lets us safely fall back to `weights_only=False` (signed checkpoints, content-addressable storage, etc.).
- **README Path D is a Story 4-5 cross-link, not the MLX-trained worked example originally listed in DD #13 line 158** — `tools/coreml-convert/README.md:149-157`. Spec line 390 (later revision) explicitly accepts the cross-link form so this matches spec-as-written, but the original DD #13 line 158 promise of "MLX-trained" is not delivered. Listed for tracking only. **Re-open trigger:** Story 4-X lands an MLX-trained reference architecture and the README acquires a fourth genuine consumer path.

## Deferred from: code review of 4-4b-tempo-classifier-training (2026-05-09) — chunk 1 of 4 (`_bmad-output/ml-training/`)

- **Corpus quality: GiantSteps train+val data is 96 kbps MP3 (lossy)** — Project Lead noted 2026-05-09 that all GiantSteps audio is encoded at 96 kbps MP3, which is aggressive lossy compression that strips high-frequency content and introduces well-known transient smearing. The trained `giantsteps_v1.mlmodelc` reference model therefore inherits this quality ceiling regardless of architecture / hyperparameters. **Next training run should consider:** (a) higher-bitrate sources (Acid Pro / native CD-rips at 320 kbps or PCM), (b) supplementing GiantSteps with a higher-quality corpus, (c) explicitly augmenting with simulated MP3-encoding round-trips on lossless source material to make the model robust to multiple bitrate/codec combinations rather than fitted to a specific lossy variant. Especially relevant given onset-envelope quality on heavily-mastered material is already documented as the upstream weakness for DnB triplet failures (project-context.md). **Re-open trigger:** any future story planning a re-train (Story 4-X) MUST consult this entry; OR a higher-quality tempo corpus becomes available; OR consumer report indicates production accuracy on PCM/lossless input is materially better than benchmarks suggest.


## Deferred from: code review of 4-5-bnns-mltechnique-conformance (2026-05-13)

- **AC #5 `4-5-regression-snapshot.json` is metadata-only — letter of spec not met** — `_bmad-output/implementation-artifacts/4-5-regression-snapshot.json` was meant to be a per-track BPM snapshot captured at Task 1 and used as the byte-identity baseline. What shipped is a metadata pointer noting that per-track byte-identity is held via Story 4-3's `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/4-3-baseline-bpms.json` inherited fixture. The inertness contract IS satisfied via the inherited path; the spec's "captured at Task 1 with new per-corpus comparison" wording is a letter-vs-spirit mismatch. **Re-open trigger:** any future Story-4-X change to `BPMAnalyzer.computeMelOnsetEnvelopeWithSubBands` or `runPreCorroborationPipeline` that invalidates the Story-4-3 inherited baseline, OR a CI workflow change that wants a Story-4-5-specific snapshot file for archival.

## Deferred from: code review of 4-5-bnns-mltechnique-conformance — review-pass implementation (2026-05-14)

- **DN6 — Consumer-tunable `confidenceThreshold` / `marginConfidenceThreshold` on `BNNSTechnique`** — DD #10's gate values (`0.50` softmax-max + `0.10` margin) are calibrated for the lossy 96 kbps GiantSteps training distribution. Codex's review-pass meta-review recommended exposing them via `BNNSTechnique(modelURL:confidenceThreshold:marginThreshold:)` for consumers retraining on a higher-quality corpus. Pattern set, not yet implemented — review-pass time-box prioritized correctness fixes over API expansion. **Re-open trigger:** any consumer retraining on a HiFi corpus reports calibration mismatch (over-abstain or over-accept), OR Story 4-7+ surfaces calibration evidence demanding the override.

- **DN8 — Workspace caching on `BNNSGraphHandle`** — `inferTempoCNN` allocates and deallocates the BNNSGraph workspace per call. WWDC 2024 #10211 ("Support real-time ML inference on the CPU") emphasizes amortizing workspace allocation across calls. Today this is fine — `BNNSTechnique` runs at most once per `analyzeBPM` (DD #17), and per-call workspace is page-aligned + small. Codex deferred per "fix correctness first" framing. **Re-open trigger:** Instruments profile shows `BNNSGraphContextMake` + workspace allocate accounts for > 20% of `evaluate(trace:)` wall-clock; OR a Story 4-X reverses DD #17 to enable multi-window ML evaluation.

- **m18 — `EnsembleDecision.rawMLBpm: Double?` to surface ML's prediction even when DSP wins ensemble** — Today `EnsembleDecision` carries `selectedBPM` + winner enum but no separate `mlBPM` field. When `winner == .dsp` (including ML abstain), the impact report cannot distinguish "ML predicted X but lost to DSP" from "ML abstained entirely." Diagnostically lossy. Codex deferred from Unit 5 because adding the trace field requires expanding `EnsembleDecision` (signature surface change). **Re-open trigger:** Story 4.6 ML calibration evidence demands the raw prediction in the impact report; OR Epic 5 debugger UI needs to show "ML predicted X, DSP won with Y."

- **C6 negative-path fixture generation** — AC #13's `initThrowsOnMismatchedTensorNames` + wrong-shape + wrong-dtype tests require 3 malformed `.mlmodelc` fixtures. Per Risk #1 in the review-pass plan: this requires writing `_bmad-output/ml-training/build_malformed_fixtures.py` to construct broken CoreML models via direct `coremltools` API (NOT extending `tools/coreml-convert/convert.py`, which is consumer-facing and must not support malformed states). No `model.pt` checkpoint is currently committed; the fixture script needs to either retrain or hand-craft a minimal tempo-CNN. Time-boxed to 2 hours; exceeded. **Re-open trigger:** any future Story regenerates `model.pt`, OR pre-1.0 release counsel pass requires the HALT (i) gate to actually fire.

- **HALT (h) literal post-`vvlogf` byte-identity test** — The existing `retainedSpectrogramIsBitExactPostVvlogf` test in `MLFeatureFramesTests` compares retained envelopes between `captureMLFeatures: true/false` runs. The spec's letter (Story 4-5 AC #5) is element-wise byte-identity between the retained `logMelData` and the `logOutput` array immediately AFTER `vvlogf` and BEFORE the temporal-difference `vDSP_vsub`. Requires a new `@testable internal` hook on `BPMAnalyzer.computeMelOnsetEnvelopeWithSubBands` returning the intermediate `logOutput`. Deferred from review-pass Unit 6 because the indirect test (envelope byte-identity) already pins the retention path's regression-protection contract; the literal-letter test is a hardening gate, not a load-bearing invariant. **Re-open trigger:** any future change to `BPMAnalyzer.computeMelOnsetEnvelopeWithSubBands`'s log-mel retention path that the existing envelope-byte-identity test cannot detect (e.g., a future refactor that splits the function into separate `logOutput`-producing and envelope-producing helpers).

- **M11 multi-fixture byte-identity expansion** — `dspOnlyByteIdenticalWithBNNSAvailable` exercises one fixture (`Meta_Man.mp3`). Codex's review-pass meta-review recommended expanding to 3-5 fixtures of varying duration / sample rate to cover duration-dependent code paths (e.g., the `captureMLFeatures`-conditioned retention branch on short vs long clips). Deferred because the inertness contract is also pinned via the inherited Story 4-3 fixture pin (`Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/4-3-baseline-bpms.json`) which covers the full OA300 corpus. **Re-open trigger:** any Story 4-X change to `runPreCorroborationPipeline` whose effect could plausibly be duration-dependent (e.g., a new windowing strategy or a new pre-corroboration step gated on file duration).

- **`tools/coreml-convert/.venv/` lint exclusion** — Pre-existing Story 4-4b carry-over from the original close-out's Completion Notes. `.swiftlint.yml` should add `tools/coreml-convert/.venv` and `_bmad-output/ml-training/.venv` to `excluded` so `make lint` doesn't surface 157 violations from `coremltools/modelrunner/ModelRunner/ModelService.swift` (third-party Python deps that happen to ship Swift sources). Out of review-pass scope. **Re-open trigger:** any cleanup story that re-runs `make lint` at the Makefile level and wants a clean exit code.

## Story 4-5 review-pass v2 — deferred Edge Case Hunter findings (2026-05-14)

Items surfaced by the Edge Case Hunter pass during the post-review-pass-v2
sweep that are NOT being fixed in this commit. Each entry: 1-line summary +
trigger condition + Story 4.6 blocking status. Items marked `Blocks 4.6: no`
are safe to defer; items marked `Blocks 4.6: yes` must be re-opened before
4.6 enters dev.

- **EC#01 — TOCTOU between `FileManager.fileExists` and `BNNSGraphCompileFromFile`.**
  `BNNSTechnique.init(modelURL:)` checks file existence then opens via the
  C API. A delete-between-checks race throws `modelLoadFailed` rather than
  `modelResourceMissing`. **Trigger:** any consumer report of inconsistent
  error type under filesystem churn. **Blocks 4.6:** no (same surface in
  CoreML init).

- **EC#02 — `MLFeatureFrames.Equatable` returns false for NaN-bearing
  payloads vs themselves (Float NaN semantics).** Synthesized `Equatable`
  reflexivity violation when `logMelData` contains NaN. Pre-existing pattern
  documented for `SubBandEnergies` in the same file. **Trigger:** any test
  asserting `lhs == rhs` on a trace that could contain non-finite values
  (today's tests use deterministic finite inputs). **Blocks 4.6:** no.

- **EC#03 — `BNNSGraphContextDestroy` on a partially-initialized context.**
  Defer always fires; if `BNNSGraphContextMake` returned a context with
  non-zero `data` but zero `size` (or vice versa), the destroy call still
  runs. Framework-internal behavior unclear. **Trigger:** new SDK introduces
  half-initialized context as a documented state. **Blocks 4.6:** no
  (CoreML doesn't use this primitive).

- **EC#04 — Workspace pointer + size semantics on `workspaceSize == 0`.**
  When BNNS reports zero workspace, the code passes `nil` + `0`. If a future
  graph reports zero size but the framework still dereferences the pointer,
  a nil-deref ensues. **Trigger:** any failure in `BNNSGraphContextExecute`
  under a graph variant that previously worked. **Blocks 4.6:** no.

- **EC#05 — `RenamedTensors.mlmodelc` fixture corruption detection.**
  C1's fixture lacks a SHA256 manifest. Silent corruption during git
  merge or LFS round-trip wouldn't be caught by the existing existence
  check. **Trigger:** add manifest hashing when the fixture causes
  unexplained `initThrowsOnMismatchedTensorNames` flakes in CI.
  **Blocks 4.6:** no.

- **EC#06 — Raw-API guard's column-level scanner upgrade.** N11 documents
  the line-level scanner's known false-positive/false-negative shapes
  (banned-name embedded in `code() /* ... */ moreCode()` or `*/ realCode()`).
  Upgrade to a column-level state machine when a maintainer needs to
  mention a banned API by name in an inline comment. **Trigger:** first
  legitimate banned-name mention in source. **Blocks 4.6:** no.

- **EC#07 — `bnnsImpactReport` two-run determinism diff filter.** AC #8
  expects `make bnns-impact-report` twice with the same inputs to produce
  byte-identical JSON except for `latency_ms`. The post-N7 schema also
  varies `named_dnb_resolved_via_dsp_fallback` if any track flips
  between ML-win and DSP-fallback (timing-dependent at thresholds).
  **Trigger:** first CI run that observes the new field flipping between
  runs at the same SHA. **Blocks 4.6:** no.


## Story 4-5 review-pass v3 — Chunk 3 impact-report findings (2026-05-14)

Surfaced by the v3 4-chunk adversarial review (Chunk 3: Impact / Regression
Evidence). Verified directly against on-disk artifacts. These are real
findings but the v3 close-out judgment is: ship 4-5 as **"BNNS
infrastructure delivered; ML impact validation deferred to Story 4-6."**
Story 4-6 picks up the accuracy work with hard AC.

- **C1 — BNNS ML abstained on 100% of OA300 corpus tracks.** Verified at
  `_bmad-output/implementation-artifacts/4-5-bnns-impact-report.json`:
  `ml_acc1 = 0/82`, `named_dnb_resolved = 0/4`, `dsp_winner ==
  ensemble_winner` for every track, all 4 named DnB targets remain at
  ~113 BPM (half-tempo failure; ground truth 170 BPM). The motivating
  story case is NOT recovered. Per Codex Chunk 3 analysis: the 100%
  abstain hides 5 different failure modes (ML never ran / features empty
  / inference failed / decoded out of range / decoded plausible but
  two-gate rejected). Raw decoded BPM/confidence/secondMax/margin EXIST
  inside `BNNSTechnique.decodeLogits` at `:475-525` but are NOT surfaced
  through any diagnostic API. Cannot distinguish which mode is firing
  without instrumentation. **RESOLVED 2026-05-16 by Story 4-6 Branch C
  close-out:** diagnostic instrumentation landed (MLDiagnosticSnapshot +
  MLDiagnosticTechnique capability protocol + BNNSTechnique conformance).
  Threshold sweep at 0.00/0.00 disambiguated: every track reaches decode;
  `wrong_non_abstain_count = 54/82`; `softmax_max_p95 = 0.294`; bimodal
  predictions at 125/175 BPM regardless of input — Outcome D, model is
  genuinely too weak. Bundle pulled.

- **C2 — Bundle-pull decision deferred to Story 4-6.** Per Codex
  consult (thread `019e2975-7b50-78b1-80d2-d01b6eb73d2c`), if Story 4-6
  cannot demonstrate ≥ 2/4 named DnB tracks resolved, the bundled
  `Sources/BoomBoomBoomKitML/Resources/giantsteps_v1.mlmodelc` should
  be REMOVED from the main-shipping path (moved to develop-only
  `_bmad-output/ml-models/`). Codex's framing: "Pre-1.0 is exactly
  when to be strict about not shipping an ineffective bundled binary."
  **RESOLVED 2026-05-16 by Story 4-6 Branch C close-out:** Branch C
  fired. Bundle moved via `git mv` to `_bmad-output/ml-models/giantsteps_v1.mlmodelc/`;
  `Package.swift` BoomBoomBoomKitML target's `resources: [.copy("Resources")]`
  removed; `BNNSTechnique.bundledReferenceURL` hardcoded to nil literal.
  No bundled model ships in main as of this close-out. Re-open trigger
  filed below.

- **C3 — `_bmad-output/implementation-artifacts/4-5-regression-snapshot.json`
  naming and lifecycle are muddy.** Original c446069 captured a
  metadata-only snapshot with a self-documented note that the
  test-enforceable byte-identity gate is
  `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/4-3-baseline-bpms.json`
  (frozen, unchanged, still serves as the AC #5 byte-identity gate).
  Commit `4027b34` ("review pass v2: regenerate post-fix artifacts")
  rewrote the metadata stub into a real per-track JSON for
  cross-reference. The rewrite was honest but the file name suggests
  a "frozen baseline" that doesn't match the lifecycle. **Trigger:**
  Story 4-6 if revisiting the regression-snapshot infrastructure;
  otherwise low-priority rename/delete in a cleanup pass.
  **Blocks 4-6: no** — informational only, real byte-identity gate
  is elsewhere.

- **C4 — Impact-report HALT gate is develop-only, not in CI.**
  `make bnns-impact-report` fires `Issue.record` on `ml_acc1 = 0` but
  this only triggers when someone explicitly invokes the target.
  CI's `make test` never exercises the impact harness, so the
  100%-abstain artifact landed without observable CI failure.
  **Trigger:** Story 4-6. **Blocks 4-6: yes** — 4-6 should either
  promote the impact-report into a CI lane OR add a fast-running
  abstain-floor unit test.

- **C5 — DnB target list lacks DSP-correct control set.**
  `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/4-dnb-triplet-targets.json`
  has 4 tracks, all DSP-known-failures. No DnB tracks DSP gets right
  in the control set. The impact metric measures "ML rescues DSP's
  losses" but cannot detect "ML quietly broke 30 DnB tracks DSP got
  right." **Trigger:** Story 4-6. **Blocks 4-6: yes** — expand target
  set BEFORE re-running the impact report.

## Deferred from: Story 4-6 Branch C close-out (2026-05-16)

- **Story 4-6 re-open trigger (Task 15 verbatim).** If a future ablation pass shows >5% Acc1 regression on the previously-passing control set (`dsp_correct_controls` in `4-dnb-triplet-targets.json` v3), or if any additional control regresses (preserved → not-preserved), Story 4-6's bundle decision re-opens and Branch C is reconsidered with a follow-on story. Filed live for the entire pre-1.0 window. **Re-open trigger:** the literal condition above, OR a future Branch-A retrain story re-introduces a bundle (in which case this trigger gets archived as historical and a new symmetric trigger replaces it). The infrastructure path stays open because BNNSTechnique infrastructure shipped successfully — the gap is the model, not the integration architecture.

- **Squash-merge to main invariant — verify bundled-model path absence pre-merge.** Story 4-6 Branch C moved `Sources/BoomBoomBoomKitML/Resources/giantsteps_v1.mlmodelc/` to `_bmad-output/ml-models/giantsteps_v1.mlmodelc/` (develop-only) and removed the `resources: [.copy("Resources")]` line from `Package.swift`'s BoomBoomBoomKitML target. The CLAUDE.md release protocol relies on operator vigilance for the squash-merge to main. **Pre-squash check:** verify `_bmad-output/ml-models/giantsteps_v1.mlmodelc/` is NOT in the main-bound diff, AND verify `Sources/BoomBoomBoomKitML/Resources/` is absent in the main-bound tree. **Re-open trigger:** any future squash-merge to main that accidentally includes either path (would invalidate the Branch C close-out and ship an ineffective bundled binary back to main); OR a maintainer accidentally re-creates the Resources directory locally before squash-merge.

- **CoreML conformance dropped from Epic 4 (DD #10/#11).** Story 4-8 (`4-8-coreml-mltechnique-conformance`) was the planned follow-on if Branch A had fired. Under Branch C, no bundled model warrants a second runtime, and `CoreMLTechnique` symmetric-infra-with-consumer-supplied-URL adds no value over `BNNSTechnique` with consumer-supplied URL. The Story 4.1 `Sources/BoomBoomBoomKitML/CoreMLTechnique.swift` placeholder remains untouched on disk; Epic 4's scope ends at Story 4-7 (`4-7-spectral-flux-onset-dsp-variant`) + this close-out. **Re-open trigger:** a future Branch-A retrain that re-bundles a high-quality model AND a documented consumer ask for CoreML/ANE acceleration; under that combined condition, Story 4-8 unblocks and gets authored against the post-Branch-A baseline.

- **Story 4-7 `InlineArray<128, Float>` adoption opportunity (filed 2026-05-17 from axiom-swift validation).** Mel-band scratch buffers used inside `computeSpectralFluxOnsetEnvelope` (and the existing `computeMelOnsetEnvelopeWithSubBands`) are 128 Floats, known at compile time, used inside per-frame hot loops. This is textbook `InlineArray<128, Float>` material per `axiom-swift/skills/ownership-conventions.md:267-320` — stack-allocated, zero heap pressure, zero ARC traffic. **Not adopted in Story 4-7** because the rest of `BPMAnalyzer` uses `[Float]` arrays throughout, and adopting `InlineArray` for Story 4-7 alone would create inconsistency without measurable benefit (the existing scratch buffers are already allocated once outside the frame loop per ADR-3's buffer-reuse pattern). **Re-open trigger:** a future `make perf-benchmark` run shows measurable allocator-pressure regression on the spectral-flux path, OR a profiling pass on the energy-onset path identifies the scratch-buffer allocations as a hot spot. Either condition unblocks a project-wide `[Float]`→`InlineArray<N, Float>` refactor across `BPMAnalyzer`. Until then, stay consistent with the existing pattern.

- **Story 4-7 SuperFlux variant shipped inert (Branch B close-out 2026-05-17).** Per the per-track impact report at `_bmad-output/implementation-artifacts/4-7-super-flux-impact-report.json`: `namedDnBImproved=0/4`, `controlsPreserved=2/4`, `changedFinalBPM=82/82`. The Böck & Widmer 2013 SuperFlux frequency-axis max-filter (r=1, replicate-pad, paper-faithful parameters per Codex thread `019e36de`) moves every per-track BPM but does not resolve any of the four named DnB triplet failures and regresses two DSP-correct controls (Hellacopta + Darkgray Heart, both by ~0.5-1 BPM). Per AC #4 Branch B + HALT-(b), the `.superFluxOnset` case ships in `DSPTechnique` but is NOT added to any preset (`.optimal`, `.dnbOptimized`, `.clickAugmented` unchanged; `.full` auto-includes via `Set(allCases)`). Available for consumer experimentation via explicit opt-in. **Re-open trigger:** a future story produces a DnB-targeted refinement (e.g., sub-band flux, time-axis SuperFlux composition, complex-domain method) that EITHER resolves ≥1 of the four named tracks WITH controls preserved, OR an external community contribution shows improvement on a different corpus (genre-stratified evidence). Until then, the case is a building block for future onset-detection research and a paper-faithful Böck 2013 reference implementation for consumers who want to experiment.

- **Story 4-7 rescope workflow lesson (filed 2026-05-17 per Codex thread `019e36de` + party-mode Winston).** Story 4-7 was authored 2026-05-16 against the false premise that `BPMAnalyzer.swift:711-748` was "energy-based onset detection". The function actually implements log-mel spectral flux verbatim; the proposed Story 4-7 algorithm was a near-verbatim duplicate. The duplicate-algorithm finding survived axiom-skills validation (all 4 skills validated the *proposed* implementation correctly — they did not notice that the proposed implementation IS the existing implementation). Codex caught it during the final-pass spec review. **Workflow lesson for all future "new variant at a documented pipeline step" stories:** before drafting the algorithm DDs, the spec author MUST read the existing function body verbatim and quote the algorithm in the Dev Notes section, then explicitly state the algorithmic delta the new variant introduces vs the existing code. The bmad-create-story workflow Step 3 ("Read existing code being modified") already requires this — it just wasn't followed deeply enough on Story 4-7's first authoring pass. **Tighten:** Story-level checklist item — "If proposing a new variant at pipeline step N, paste the existing step-N body verbatim into Dev Notes and explicitly state how the proposed algorithm differs from the existing code." **Re-open trigger:** another story authored against a false-premise about existing code surfaces during pre-implementation review; that triggers a workflow-process retrospective.

## Deferred from: code review of 5-5-public-api-documentation-and-readme (2026-05-21)

- **`SubBandEnergies` doc comment references "Story-4-3b" but the work was Story 3-3b.** Pre-existing typo in `Sources/BoomBoomBoomKit/BPMDiagnosticTrace.swift:423` ("/// state as the pre-Story-4-3b `[String: Float] = [:]` shape."). The line is unchanged context in the Story 5-5 diff — not introduced by this story. **Re-open trigger:** a future story that touches `SubBandEnergies` doc content for any reason should fix the story number in the same edit; alternatively, a standalone doc-typo sweep across `Sources/` could batch this with any other historical story-ID typos.

## Deferred from: code review of 5-4-diagnostic-trace-visualization-and-export (2026-05-23)

- **W1 (already closed) — Security-scoped resource refcount imbalance in `analyze`/`exportTrace` paths.** PR #6 (Story 5-4 merge `353f5ff`) shipped `defer { url.stopAccessingSecurityScopedResource() }` without a matching `start...` on the NSSavePanel-returned URL (`AnalysisViewModel.swift:597-601` pre-W44) and an analogous imbalance on the `.onOpenURL` arm at `:200-201` (pre-W43). Both surfaced by the Story 5-4 `/bmad-code-review` adversarial + edge-case-hunter layers. **Status:** ALREADY CLOSED — Story 5-7 W43 (analyze) + W44 (exportTrace) landed in commit `558cf3c` apply the defensive `let didStart = url.startAccessingSecurityScopedResource(); defer { if didStart { url.stopAccessingSecurityScopedResource() } }` pattern at both sites. The diff under review predates the fix; current in-tree code is correct. Logged here for traceability — no further action.

- **W2 — `pbxproj` + `entitlements` dual source-of-truth for `ENABLE_USER_SELECTED_FILES` / `user-selected.read-write` with no lint guard.** Story 5-4 flipped both `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.entitlements:~7` (key `com.apple.security.files.user-selected.read-write` true) AND `Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo.xcodeproj/project.pbxproj:391,423` (`ENABLE_USER_SELECTED_FILES = readwrite`) — Story 5-4 Completion Notes documented that Xcode's build setting auto-injects the entitlement key, so the two MUST agree. No `make demo-lint` rule cross-checks the two; a future contributor flipping only one produces a contradictory built binary. **Re-open trigger:** add a `make demo-lint` cross-check (e.g., grep both files, fail if the entitlements value disagrees with the pbxproj build setting); or another story that touches this entitlement and gets bitten by the dual-source-of-truth. Cross-story `make demo-lint` enhancement work — not Story 5-4 territory.

- **W3 (already closed) — `@SceneStorage("traceInspectorPresented")` default `true` first-launch UX + missing `InspectorCommands` Control-Command-I toggle test coverage.** Story 5-4 shipped the inspector default-open per DD #4-B for first-launch discoverability; surfaced as a UX concern by the adversarial layer (user dismisses inspector → relaunches → state restored false → drops file → inspector stays hidden → user has no idea Export Trace just appeared). **Status:** ALREADY CLOSED — Story 5-6 W36/F03 flipped the default to `false` and made the toolbar Diagnostics button + ⌘⇧D keyboard shortcut the canonical surface, addressing the discoverability concern via an explicit affordance instead of default-on. The InspectorCommands keyboard-shortcut test coverage remains a gap but is now Story-5-6 territory.

- **W4 — `formatResultRow.nonFinite` case name semantics post-Story-5-3 + `AppError` migration test coverage gap.** Story 5-3 W31 re-open trigger ("localization lands... introduce typed `BannerError` enum") was silently satisfied by Story 5-4's typed-`AppError` enum migration (`AnalysisViewModel.swift:416-451`) — but Story 5-4 did NOT add migration tests asserting `errorDescription` matches the prior `String` text for `.dropMultiple`/`.dropUnsupportedType`/`.fileReadFailed`/`.noBPMDetected`/`.unexpected`. Only `.clipboardCopy` and `.dropEmpty` are exercised in the test bank. **Re-open trigger:** re-open W31 — silent display-text regression could land any time an `AppError.errorDescription` case body is touched. Address in a future Demo/UX hardening story by adding parameterized `errorDescriptionMatchesLegacyString` tests for each case, OR delete W31 explicitly when the team decides the legacy-text-match contract is not load-bearing.

- **W5 — `writeTraceJSON` synchronous MainActor encoding for large trace payloads.** `AnalysisViewModel.swift:549-572` encodes the `TraceExport` synchronously on the MainActor inside `writeTraceJSON(to:)`. At intensity 7 default the trace payload is small (single window + ~30 candidates) and encode is fast; at intensity 10 with many windows the JSON encode could noticeably block the UI but no measurements exist. **Re-open trigger:** add a perf measurement under `_bmad-output/perf-baselines/` first; if encode wall-clock exceeds ~50ms on the M5 Max reference hardware, hop to `Task.detached` for the encode step then re-marshal on MainActor for the write. Until measured, the synchronous encode is acceptable. Tracked as a future polish, not a Story 5-4 blocker.

## Deferred from: code review of 5-7-app-store-submission-readiness (2026-05-24 third pass)

- **W6 — `demo-archive` `rm -rf build/BoomBoomBoomKitDemo.xcarchive` trusts ignored workspace state — symlinked `build/` could trigger deletion outside the repo.** [`Makefile:75`] If `build/` is a symlink, the preflight resolves through it and deletes under the symlink target instead of under the repo. Codex Finding #2 (High but narrow). **Re-open trigger:** operator reports unexpected deletion outside `$(CURDIR)/build`. Conservative fix when revisited: refuse symlinked `build/`, create a real dir, delete only after verifying the resolved path is inside `$(CURDIR)/build`.

- **W7 (CLOSED 2026-05-24) — `demo-bump-build` parse regex whitespace tolerance.** Closed when the bump logic moved from a Makefile shell pipeline to `scripts/demo-bump-build.py` (operator directive after three rounds of churn on the shell form). The Python script's `LINE_RE` uses `\s*` for whitespace and is line-anchored via `re.MULTILINE`, so hand-edited pbxproj variants like `CURRENT_PROJECT_VERSION=1;` parse correctly. Reason for closure baked into the artifact, not into a future story.

- **W8 (CLOSED 2026-05-24) — `demo-bump-build` substitution anchoring.** Closed by the same Python extraction that closed W7. The Python `LINE_RE` is line-anchored (`^...$` with `re.MULTILINE`), so the substitution can never touch a quoted string or comment that happens to contain `CURRENT_PROJECT_VERSION = N;` verbatim. Risk class neutralized.

- **W9 — `@rm -rf build/BoomBoomBoomKitDemo.xcarchive` could destroy operator-edited xcarchive metadata.** [`Makefile:75`] Xcode Organizer can add dSYM lookup info / validation receipts to an existing xcarchive; the `rm -rf` discards them. Current behavior matches the `compile-model` sibling-target precedent. **Re-open trigger:** operator reports lost Organizer metadata between archive cycles. Workaround: move the xcarchive outside `build/` between bumps.

- **W10 — `.gitignore` `/build/*.xcarchive` is redundant — `/build/` already matches it.** [`.gitignore:174`] Cosmetic; the redundant line documents intent even if mechanically dominated by its parent. **Re-open trigger:** next general `.gitignore` housekeeping pass.

- **W11 — `PrivacyInfo.xcprivacy` shipping in the archive is not verified by an in-repo test.** [`Demo/BoomBoomBoomKitDemo/BoomBoomBoomKitDemo/PrivacyInfo.xcprivacy`] Story relies on Xcode 26's `PBXFileSystemSynchronizedRootGroup` auto-inclusion; no CI assertion confirms the manifest reaches `<built app>/Contents/Resources/PrivacyInfo.xcprivacy`. **Re-open trigger:** the operator-validation step at AC #6.6 (`xcodebuild -exportArchive` end-to-end) IS the verification gate; if it surfaces the manifest missing from the archive, promote this to a patch with an in-repo assertion.

- **W12 — `NSPrivacyAccessedAPICategorySystemBootTime` reason `35F9.1` may over-declare `ContinuousClock.now`.** [`PrivacyInfo.xcprivacy:23`] Apple's listed System Boot Time APIs are low-level uptime/absolute-time APIs; the documented mapping to `ContinuousClock` is not explicit. Over-declaration is Apple-side tolerant per R2 stance. **Re-open trigger:** Apple privacy report / archive upload validation flags this entry; remove if Apple deems it superfluous, or keep if validator accepts.

- **W13 — No automated regression test for the C1 or C2 sandbox-balance fix.** [`Tests/...AnalysisViewModelSmokeTest.swift`] Both fixes change runtime behavior on paths the smoke tests can't exercise (NSSavePanel / LaunchServices delivery). A future regression would pass CI silently. Testability limitation acknowledged. **Re-open trigger:** future story adds a programmatic save-path test harness, OR a regression is reported in user-facing behavior.

- **W14 — `AnalysisViewModel.swift:657` `deinit` cancels in-flight tasks but does not synchronously release security scopes.** Codex Finding #4. Swift's `defer` does run on cancellation, so the scope IS released — just asynchronously. Apple's documented refcount contract doesn't require synchronous release; the kernel-side scope has no side effect from a brief async hold. **Re-open trigger:** user reports stuck "in-use" indicators OR a real refcount asymmetry under sustained use.

- **W15 — Story 5-7 spec is enormous and self-contradictory across revisions.** [`_bmad-output/implementation-artifacts/5-7-app-store-submission-readiness.md`] KDD #1 has three revisions in one story; a POST-PIVOT NOTE tells auditors which paragraphs to ignore. Audit-trail-integrity choice (preserve historical bullets verbatim) is deliberate. **Re-open trigger:** retrospective or pre-1.0 final spec compaction pass.

- **W16 — `sprint-status.yaml` `last_updated` field is a multi-line essay wrapped in a YAML scalar.** [`_bmad-output/implementation-artifacts/sprint-status.yaml:1042+`] Structured-parser consumers receive a giant single string and lose semantic timestamping. **Re-open trigger:** a tooling script breaks parsing the field, OR the field's structural-parser contract becomes load-bearing for a dashboard/report.

## Deferred from: code review of 5-7-app-store-submission-readiness (2026-05-24 fourth pass — narrow scope on Python + Xcode metadata)

`/bmad-code-review` four-reviewer pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor + Codex MCP) over the narrow scope `scripts/demo-bump-build.py` (new, 122 LOC) + `Makefile` `demo-bump-build` one-liner + the new `xcshareddata/xcschemes/BoomBoomBoomKitDemo.xcscheme` + `BoomBoomBoomKitDemoTests/BoomBoomBoomKitDemo.xctestplan` (both Xcode-side-effect files from running tests interactively). 12 unique findings, 3 decision-needed pending user input, 3 patches authorized, 4 deferred below.

- **W17 (CLOSED 2026-05-24) — `scripts/demo-bump-build.py` lets `UnicodeDecodeError` / `PermissionError` / `OSError` escape as Python tracebacks instead of the documented exit-code contract.** Closed in PR #11 commit `99ce476` (P2 patch) after GitHub Copilot re-flagged on its automated review pass and Codex plan-review (threadId `019e5bde-f555-7c22`) pushed back on the defer rationale. Both I/O calls now wrap in `try / except`: `UnicodeDecodeError` → exit 1 (parse/validation), `OSError` → exit 2 (invocation). Docstring (lines 26-30) updated to reflect the expanded coverage.

- **W18 (CLOSED-PARTIAL 2026-05-24) — xctestplan placed inside the `BoomBoomBoomKitDemoTests/` synchronized-root-group; (a) navigator-duplicate via explicit `PBXFileReference` + main-group entry, AND (b) auto-included as a Resources build-phase entry of the Tests target.** Navigator-duplicate sub-concern (a) closed in PR #11 commit `380296b` (round-2 P3 patch) after GitHub Copilot re-flagged on its second automated review pass (comments `3295359355` + `3295359359`). The explicit `PBXFileReference` at pbxproj:27 and the main-group child entry at pbxproj:97 (referencing `13BC4F9A2FC3865D0046D014`) were both removed; the xctestplan continues to be visible via the `BoomBoomBoomKitDemoTests` `PBXFileSystemSynchronizedRootGroup` (pbxproj:50) and the scheme's `container:BoomBoomBoomKitDemoTests/BoomBoomBoomKitDemo.xctestplan` reference continues to resolve via the synced folder's filesystem path. Verified by `make demo-test` post-patch (TEST SUCCEEDED, all 81 tests passed). Bundle-bloat sub-concern (b) remains open — the xctestplan still gets copied into the Tests bundle's Resources phase every build because the synced root group has no `PBXFileSystemSynchronizedBuildFileExceptionSet`. **Re-open trigger for (b):** a future story adds membershipException scaffolding for the Tests target. Fix when re-opened: add a `PBXFileSystemSynchronizedBuildFileExceptionSet` for the Tests target that excludes `BoomBoomBoomKitDemo.xctestplan` from the Resources phase (modeled after the existing exception set for the app target's `Info.plist`).

- **W19 (CLOSED 2026-05-24) — xctestplan `PBXFileReference` uses `lastKnownFileType = text` instead of the canonical `wrapper.xctestplan`.** Closed in PR #11 commit `99ce476` (P3 patch) after GitHub Copilot re-flagged on its automated review pass and Codex plan-review pushed back on the defer rationale. `lastKnownFileType = wrapper.xctestplan` now set; Xcode-side normalization accepted the canonical value (verified by post-`make demo-test` git diff showing no rewrite to `text`). Xcode opens the file in the Test Plan editor on double-click instead of as raw JSON.

- **W20 (CLOSED 2026-05-24) — xcscheme uses mixed-case `Yes` for `queueDebuggingEnableBacktraceRecording` while every other boolean in the file uses `YES`.** Closed in PR #11 commit `99ce476` (P4 patch) after GitHub Copilot re-flagged on its automated review pass and Codex plan-review pushed back on carrying a 1-character defer entry. Single-character edit `Yes` → `YES` applied.

## Deferred from: code review of 5-6b-a11y-polish (2026-05-24)

`/bmad-code-review` three-reviewer pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor) over the 44-line uncommitted Demo-only diff for Story 5-6b. Auditor returned PASS on all six F-IDs and verified AC #3 (`git diff --stat Sources/ Tests/` both empty). 1 decision-needed surfaced to operator (F11 saturated-mode seam coverage in AC #4); 5 items deferred below.

- **5-6b-D1 — F12 idiomatic toggle trait alternative (`ContentView.swift:112`).** Spec-authorized `.accessibilityValue(Text("Shown"/"Hidden"))` form works. More idiomatic VoiceOver would come from `.accessibilityAddTraits(.isToggle)` (native "on/off" phrasing) or migrating to `Toggle { ... }.toggleStyle(.button)`. Out of 5-6b scope; polish for a future a11y pass.

- **5-6b-D2 — F12 verbose VoiceOver phrasing (`ContentView.swift:106-112`).** `.help` sets both tooltip AND VoiceOver hint on macOS; combined announcement is approximately "Diagnostics, Shown, button. Show / hide diagnostics, command shift D." Tightening would trade off the keyboard-shortcut tooltip discoverability. Polish for a future pass.

- **5-6b-D3 (CLOSED 2026-05-24) — F07 inaccurate technical claim in code comment (`StrategyBackground.swift:65-68`).** Closed in PR #12 commit `333f69f` after GitHub Copilot's automated review re-flagged the defect (comment ids `3295467366` + `3295467376`). The parenthetical "bare `Color(NSColor.foo)` resolves to the asset-catalog overload `Color(_ name:bundle:)` and silently returns a placeholder" was iOS-flavored lore that doesn't hold on macOS — the asset-catalog init takes a `String`, so the bare form actually resolves to the unlabeled `Color.init(_ color: NSColor)` overload, not an asset lookup. Comment now states only the substantive point (dynamic colors adapt Light/Dark + Increased Contrast); spec parallel mentions at `5-6b-a11y-polish.md` lines 22 + 54 cleaned up the same way.

- **5-6b-D4 — Pre-existing: `resultView` `secondaryMetadataRow` has no `.lineLimit` (`ContentView.swift:372-376`).** With the new full-height `.topTrailing` anchoring, a sufficiently long `row.fileName` would wrap and push the BPM hero downward. The risk pre-dates 5-6b (rows have always been unbounded) and grows slightly with the new anchoring. A `.lineLimit(1).truncationMode(.middle)` would harden this; belongs in a 5-x polish story.

- **5-6b-D5 — F14 VoiceOver drop-action dead-zone (`EmptyStateView.swift:24-25`).** The new hint "Drag an audio file here to analyze" promises an interaction VoiceOver-only users cannot perform (macOS VoiceOver has no drag-and-drop gesture). A ⌘O / File → Open menu equivalent would be the standard alternative. Pre-existing demo limitation; not in 5-6b scope.

## Deferred from: code review of persist-merge-strategy-preference (2026-05-25)

`/bmad-quick-dev` three-reviewer pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor) over the 108-line Demo-only diff that introduced UserDefaults persistence for the merge-strategy dropdown. Acceptance Auditor returned PASS on all 7 ACs and confirmed Demo-only scoping. 2 deferred items below; remaining findings (most claiming the demo "silently overrides the library default" or "should use schema versioning") were rejected as either by-design per the approved spec or hypothetical hardening that the project's "don't design for hypothetical future requirements" rule explicitly bars.

- **persist-merge-D1 — Concurrent app instances share the `com.robbyt.BoomBoomBoomBPM` UserDefaults domain.** Dev build (Xcode `make demo-build`) and archived build (`make demo-archive`) both read/write the same plist at `~/Library/Containers/com.robbyt.BoomBoomBoomBPM/Data/Library/Preferences/...`. Last-writer-wins; no `NSDistributedNotificationCenter` observer wakes a sibling instance. Dogfooding scenario only — production users run one instance. Re-open trigger: a future story adds a second persisted preference and starts surfacing user-facing confusion. Fix when re-opened: register an observer on `UserDefaults.didChangeNotification` (or `NSUbiquitousKeyValueStore.didChangeExternallyNotification` if cross-device sync ever lands) and refresh `viewModel.options.mergeStrategy` accordingly.

- **persist-merge-D2 — `CandidateMergeStrategy` rawValues are not documented as SemVer-stable.** The library's `public enum CandidateMergeStrategy: String, CaseIterable, Sendable, Hashable` at `Sources/BoomBoomBoomKit/CandidateMergeStrategy.swift:19` makes no rawValue stability commitment. A library rename (e.g., `quorum → clusterQuorum`) would silently send every operator's persisted pref through the self-heal path back to `.quorum` on next launch — handled correctly today but the user loses their selection without notice. Re-open trigger: the library renames or removes a `CandidateMergeStrategy` case, OR a future story persists additional library-enum-backed options. Fix when re-opened: introduce a schema-version key alongside the rawValue (e.g., `preferredMergeStrategy.schemaVersion = "1"`), and on version-bump apply a rename map before consulting `CandidateMergeStrategy(rawValue:)`. Pre-1.0 framing in `CLAUDE.md` makes silent self-heal acceptable for now.

## Deferred from: code review of story-6.3 (2026-05-28)

`/bmad-code-review` four-layer pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor + Codex-backed blind hunter) over the uncommitted Story 6.3 source/test diff (4 code files, ~371 lines; KDD-A6 Stage 2 pool-construction relocation). Edge Case Hunter and Codex both returned clean (byte-inertness proven on every reachable path); Acceptance Auditor confirmed all 9 ACs satisfied. 0 patch, 2 deferred below; ~11 Blind-Hunter context-free items dismissed as false positives (gate equivalence, `input.policy == options.metadataPolicy`, `BPMResult` rebuild, `Sendable`).

- **6-3-D1 — Stage-1 naming tokens inside Stage-2 code (`AudioAnalysisService.swift:807` `"stage1-eval-deferred"`; `MetadataCorroborator.swift:373` / `WeightedSignal.swift` `fileMetadataStage1TraceOnlyDefault`).** The relocated `buildStage2SignalPool` and the new `MetadataCorroborator.signalParticipationEntries` helper carry Stage-1-named string literals and a Stage-1-named constant. Correct THIS story per DD #6 — byte-identity mandates verbatim preservation, and renaming would change `signalParticipationTrace` content. Re-open trigger: Story 6.4 (KDD-A6 Stage 3) carrier flip swaps byte-equality for semantic parity, at which point the Stage-1 tokens can be renamed to Stage-neutral names without violating a floor. Fix when re-opened: rename alongside the 6.4 `merge` carrier-type flip + byte→semantic test swap.

- **6-3-D2 — Empty-candidate `.dsp` sentinel + no-accepted-tags `.absent` metadata edges lack a dedicated executing assertion (`AudioAnalysisService.swift:793`; `MetadataCorroborator.swift:332`).** The non-nil-result-but-no-candidates `.dsp` sentinel (`.abstained(.sourceSpecific("no-candidates"))`) and the policy-enabled-but-all-tags-rejected `.fileMetadata` `.absent` branch are both verbatim lifts and provably inert by inspection, but no fixture drives them at runtime. `SignalPoolTests.{dspPerSourceContract,mlAbsentWhenTechniqueNil,metadataAbsentWhenDisabled}` cover the common paths; these two edges rest on code-reading. Pre-existing — inherited from Story 6.1 where the sentinel/absent branches were introduced. Re-open trigger: Story 6.4 byte→semantic test swap (natural home for the two missing fixtures). Fix when re-opened: add a no-candidates fixture (non-nil `BPMResult` with empty `candidates`) and an all-tags-rejected metadata fixture, asserting the expected single sentinel/absent entry. **(2026-05-29, 6.4b review: the byte→semantic swap moved to Story 6.5 under the B-cascade, so this re-open trigger now resolves to 6.5, not 6.4b. Likewise 6-3-D1 above.)**

## Deferred from: code review of story-6.4b (2026-05-29)

`/bmad-code-review` five-layer pass (Blind Hunter + Edge Case Hunter + Acceptance Auditor + Codex-backed Blind Hunter + dedicated byte-inertness verifier) over the staged 6.4b source/test diff vs baseline `8907fe1` (`EnsembleCombiner` removal/inline + `MetadataCorroborator` `git mv` into `SignalPool/`). Byte-inertness UNANIMOUS PASS, independently re-verified (clean build + 33/33 migrated tests; 4 byte-floor tests untouched-green). 3 patch findings handled at review time; ~13 byte-inert confirmations + 4 nits dismissed. 1 deferred item below.

- **6-4b-D1 — `project-context.md:164` overstates the asserted OA300 CI floor (≥58/74) vs the actual unconditional `#expect` gate (≥57/73 at `OA300BenchmarkTests.swift:104/108`).** Pre-existing documentation drift, NOT introduced by the 6.4b diff; Story 6.4 DD #11 consciously scoped it OUT ("flag, not fix — surface to operator separately"). Live measurement (58/74) is held at zero delta and exceeds both the real (57/73) and the overstated (58/74) floor, so there is no CI risk today. Re-open trigger: any story that touches the OA300 floor doc or the `#expect` gate. Fix when re-opened: align `project-context.md:164` to the asserted gate (≥57/73), or — if the intent is to ratchet the floor — bump the `#expect` assertions to 58/74 and update both in the same commit. **(2026-05-30, Story 6.5a: the doc side is RESOLVED — `project-context.md:164` now states the CI gate ≥57/73 + a measurement note. The `#expect`-ratchet option remains open.)**

## Deferred from: Story 6.5a (2026-05-30)

- **6-5a-D1 — The epic root-file ceiling AC is INCOHERENT: `find Sources/BoomBoomBoomKit -maxdepth 2 -type f | wc -l ≤ 35` cannot be satisfied by subdir promotion.** `maxdepth 2` counts root files (depth 1) AND files one level deep in subfolders (depth 2), so moving a root file into a new subfolder keeps it at depth-2 — the count does NOT drop (only pushing to depth-3 reduces it). Live count is **43** after Story 6.5a's +4 files (was 39 = 18 root + 12 FeatureSubstrate + 9 SignalPool); ≤35 is unreachable by promotion. The architecture's real intent (per "the 18 top-level files are the promotion candidates") is to bound ROOT clutter = **maxdepth-1**. Story 6.5a flagged this rather than doing a futile physical promotion (5-file churn on a byte-inert story without satisfying the AC). Re-open trigger: any story needing the ceiling enforced, or an Epic 6 retro. Fix when re-opened (operator/architecture decision): (a) reinterpret as `find … -maxdepth 1 -type f -name "*.swift" | wc -l ≤ N` (root .swift currently 21), pick a realistic N + promote a cohesive ≥5-file group (e.g. the file-metadata family `MetadataPolicy`/`MetadataSource`/`MetadataBPMEvidence`/`HarmonicRatio`/`FileMetadataReader` → `Metadata/`); OR (b) amend epics.md:576 to a realistic maxdepth-2 number; OR (c) accept the breach + remove the AC.
- **6-5a-D2 — `architecture.md:902` names an `MLExecutionPolicy.allCases` invariant that cannot exist** — an associated-value enum (`.whenDSPConfidenceBelow(Double)`) cannot conform to `CaseIterable`, so `.allCases` is unsynthesizable. The Story 6.5a `MLExecutionPolicy` type is correct (no `CaseIterable`); the architecture line is stale. Re-open trigger: the Story 6.5b SoT-reconciliation pass (which already updates the project-context.md merge-frozen subsections). Fix when re-opened: replace the `.allCases` invariant with a pattern-based case-count note, or drop the line.

## Deferred from: code review of story-7.1 (2026-05-31)

`/bmad-code-review` 4-layer quorum (Blind Hunter + Edge Case Hunter + Acceptance Auditor + Codex blind-hunter) over the Story 7.1 develop-only Python changeset vs baseline `effb1d54`. Auditor confirmed all 10 ACs SATISFIED. ~11 quorum-agreed correctness/robustness fixes applied at review time (see the story's Review Follow-ups). 1 design item deferred below; the report-only fingerprint's residual inconsistent-naming near-dup flags are surfaced to the operator's KDD-B4 signoff (not deferred work — they are review evidence).

- **7-1-D1 — The Tony train/val/leaveArtistOut split is deterministic per-input but NOT incrementally stable under corpus growth.** `dataset.py:build_tony_splits` greedily accumulates held-out components to a `ceil(0.10*len(clean))` target, so adding/relabeling one track shifts `len(clean)` → `target_heldout` → which components are held out → the entire train/val/leaveArtistOut membership. Edge Case Hunter (2026-05-31). NOT a 7.1 defect (no AC requires cross-relabel stability), but it means the leave-artist-out held-out eval set is not stable across relabels — which matters once Stories 7.5/7.6 want a FIXED held-out eval to compare model versions against. **Re-open trigger:** Story 7.5 (training v2) or 7.6 (FR-18/FR-23) needing a stable held-out slice across reruns. Fix when re-opened: hold out by a stable hash bucket of the canonical artist key (`hashlib_md5_int(artist) % 100 < 10`, the helper already exists and is currently used only for legacy external-split bucketing) instead of greedy count-accumulation, so adding tracks does not reshuffle existing held-out membership.

## Deferred from: pre-dev review of story-7.3 (2026-06-02)

Pre-dev review (axiom:ask + Codex spec-review thread `019e86c8` + bmad-party-mode Mary/Winston/Amelia/John); all 5 `approve-with-changes`, ~20 fixes applied to the spec before dev. 1 scope item evicted below.

- **7-3-D1 — The weak-label `strong+weak` 2×2 sensitivity matrix is EVICTED from Story 7.3 scope.** The original 7.3 spec carried an optional 2×2 (strong-only vs strong+weak supervised × the two init schemes) using the 2,172 `secondarySupervised` weak labels via a `lambda_weak` loss term. John (PM, party-mode 2026-06-02) flagged it as gold-plating: NO downstream story (7.5/7.6/7.7) consumes a weak-label-sensitivity result, and Codex flagged it conflicts with AC10's strong-only headline ("pooled trainable set" ambiguity). The `secondarySupervised` pool is still consumed by Story 7.3 as masked-mel pretraining AUDIO (label-free), so 7.2 AC6 ("secondarySupervised consumed by the harness") is satisfied via the audio path — the LABELS are what's deferred. **Re-open trigger:** a future story that actually needs to know whether adding the 2,172 weak DSP+tag-agreement labels (octave-risk per 7.2 DD #6) to supervised training helps — e.g. if the masked-mel arm wins KDD-B2 and 7.7's FR-24 net-benefit analysis wants to disentangle "the unlabeled pool helped via SSL" from "the weak labels would have helped supervised." Fix when re-opened: implement the `strong+weak` row per the recipe Codex specified (separate `lambda_weak` loss term ≈0.25 with warmup, capped strong:weak batch ratio 1:1–1:2 never natural sampling, weak-confidence normalized within the weak batch, octave-robust loss, `lambda_weak` tuned once on val then frozen, applied IDENTICALLY to both arms) — as its own story with acceptance criteria, NOT smuggled as optional scope.

## Deferred from: Story 7.4 (2026-06-03)

Story 7.4 wired the 241 Marginal-tier tracks into the three named uses
(failure-categorization lens, disagreement-geometry block, post-bundle
watchlist) + the FR-14 exclusion gate + the KDD-B3 trigger->artifact map. One
known blind spot is tracked below (the idTag fork was RESOLVED to keep-null +
this item, per Codex thread `019e8c00`, over John's wire-via-mutagen
recommendation: the categorizer must run corpus-free, and ID3 BPM is an
FR-15-forbidden model input — the codebase already records structurally-absent
signals as such, e.g. `grid_spacing` at `corpus_diagnostics.py:116`).

- **7-4-D1 — `idTagBPM` is emitted `null` in `marginal-failure-categorization.json` / `marginal-watchlist.json`; the ID3/`tmpo` BPM producer is unwired, so `metadataConflict` categorization may under-count tag-driven conflicts.** The labeler never wired a per-track ID3 `TBPM` / MP4 `tmpo` BPM key (`scripts/tony-tunes-labels.py:64` — the `audio_metadata` signal carries weight 0.8 but is "not yet wired"), so no `idTagBPM` value exists to feed categorization; the `failureCategory` rule consults only the 3 populated BPM-bearing signals (dsp/rekordbox/grid) + cluster geometry. A tag-vs-audio conflict that ONLY an embedded ID3 BPM would reveal is therefore invisible to the `metadataConflict` category on this corpus. NOT wired in 7.4 because (a) the categorizer is contractually corpus-free (JSON only — a mutagen pass needs the 241 audio files on disk, the same host-dependence DD #5's proximity has), and (b) ID3 BPM is an explicit FR-15-forbidden MODEL input (`label-tier-policy-v1.md`), so even diagnostic use blurs a boundary the project keeps sharp. **Re-open trigger:** Story 7.6 post-bundle regression surfaces metadata-driven failures that the populated-signal categorization did not anticipate (i.e. a watchlist row whose v2 prediction fails in a way an ID3 tag would have predicted). Fix when re-opened: add an OPTIONAL operator-run mutagen pass (mirroring the `--fingerprint-fill` operator-only pattern) that backfills `idTagBPM` into the categorization records from the on-disk audio, re-derives `metadataConflict` with the 4th signal, and records the delta — as its own scoped step, never folded into the corpus-free core.

## Deferred from: code review of story-7.4 (2026-06-03)

Both items are REAL BUT LATENT on the operator-only `--fingerprint-fill` proximity path — corpus-clean on every dev-agent run (Marginal proximity is `null`+`insufficientCoverage` until an operator backfills fingerprints), confirmed by empirical probe during review. Deferred because they are forward-hardening of a path no AC exercises today, not 7.4 defects.

- **7-4-D2 — The use-(b) proximity coverage gate enforces only the Marginal-coverage floor + `bool(strong_vecs)`, never the Strong-coverage floor it reports.** `corpus_diagnostics.py:608` computes `coverage_ok = marg_cov_frac >= MARGINAL_PROXIMITY_COVERAGE_FLOOR and bool(strong_vecs)` — a single Strong vector satisfies the Strong side, so a pathological `1/333` Strong cache could emit numeric `meanNearestStrongDistance` with `coverageMet: true` even though the nearest-Strong baseline + standardization are over the Strong cohort. Spec DD #5 pinned the gate on Marginal coverage, so this matches spec; the realistic post-`--fingerprint-fill` state (Strong cache ~96% covered when Marginal hits 80%) makes it near-unreachable. **Re-open trigger:** an operator runs `--fingerprint-fill` and the geometry block emits numeric proximity for downstream consumption (Story 7.6 error-analysis, KDD-B3 trigger 3). Fix when re-opened: add `and len(strong_vecs) / strong_total >= MARGINAL_PROXIMITY_COVERAGE_FLOOR` to `coverage_ok` so the Strong floor the block already reports is enforced.
- **7-4-D3 — `nearest_strong_distances` has no guard against dimension-mismatched or NaN fingerprint vectors.** `marginal_failure_categorize.py:358-383`: `np.stack` over differing-length vectors raises an uncaught `ValueError` (a stale-dimension cache entry that survived the `__method__` check), and a NaN in any supplied vector propagates to a NaN cosine distance that `json.dumps` serializes as the non-standard `NaN` token, breaking strict JSON consumers. Both confirmed reachable by execution during review; neither fires today (the 52-dim `librosa-mfcc-chroma-timbral-v2` cache is method-pinned and clean). **Re-open trigger:** same as 7-4-D2 (any `--fingerprint-fill` run feeding real vectors through the proximity path). Fix when re-opened: assert a single shared dimension across the union and drop/skip non-finite vectors before `np.stack`, surfacing the dropped count rather than crashing or emitting invalid JSON.

## Deferred from: code review of 7-5-substrate-bound-tempocnn-training-v2-multi-seed (2026-06-04)

Second adversarial pass (Codex Blind Hunter + Edge Case Hunter + Acceptance Auditor). The two convergent BLOCKERs were patched in-pass; the three items below were deferred.

- **7-5-D1 — Pre-existing Swift one-float OOB heap read in `BNNSTechnique` resample.** `BNNSTechnique.swift:625` clamps `controlVector[W-1]` to `.nextDown`; when float32 accumulation lands the last control entry on (or one ULP above) an exact integer `< F-1`, `vDSP_vlint` reads `A[floor]` and `A[floor+1]`, and at the F-1 boundary `floor+1` can equal `F` → a one-float over-read past the mel-band row. Introduced Epic 4 (`f8ddbbe`), main-bound, NOT this diff; benign in practice (vlint over-read rarely faults and the Story 7.5 parity reconstruction does not reproduce it). **Re-open trigger:** a dedicated BNNS-runtime-hardening story, or any crash/ASan report from the BNNSGraph inference path. Fix when re-opened: clamp `floor` to `F-2` (not just the control value) so `floor+1 <= F-1` unconditionally.
- **7-5-D2 — `feature_distribution_sanity` ratio-outlier tolerance is loose for a conservative guard.** `train_v2_artifacts.py:235` tolerates `max(1, n//16)` = 8 outlier bands at n=128 before `passed` flips false; eight bands >3× (or <1/3×) off is a meaningful substrate-vs-librosa distribution shift the guard waves through. Deferred as a tuning judgment — tightening risks false alarms before real distributions are observed. **Re-open trigger:** the operator's first full-run `feature-distribution-sanity.json` over real tracks; retune the threshold against the observed per-band ratio spread.
- **7-5-D3 — `_octave_fold` returns NaN/≤0 inputs unchanged, contradicting its "fold into [lo,hi)" docstring.** `train_v2_artifacts.py:108` is safe only because every caller pre-filters with `isfinite and > 0`; a future caller that forgets the pre-filter would feed an unfolded NaN/negative into agreement arithmetic. Deferred — defensive hardening, no live failure path. **Re-open trigger:** any new caller of `_octave_fold`. Fix when re-opened: reject (not pass through) non-finite/≤0 with a documented sentinel.

## Deferred from: code review of 7-5-substrate-bound-tempocnn-training-v2-multi-seed (2026-06-04 third pass)

Third adversarial pass (Codex Blind Hunter + Edge Case Hunter + Acceptance Auditor). Four patches applied in-pass (build_marginal_stability ≤0/unknown-seed consistency, feature_distribution_sanity librosa finite-check, FR-15 ledger extension); two items deferred below.

- **7-5-D1 (RE-CONFIRMED) — Python↔Swift resample last-column divergence on UPSAMPLING.** The third-pass Blind Hunter + Edge Case Hunter independently re-surfaced the resample boundary as a *parity* concern (not just an OOB read): `feature_substrate_v2.py:110` clamps `min(accumulated, F-1)` then `nextafter`, while `BNNSTechnique.swift:625` clamps only `controlVector[W-1].nextDown` with no `min(., F-1)` floor, so when float32 accumulation drifts the last control index above `F-1` (reachable only for upsampling, F in [32, 511]) Swift over-reads `A[F]` and the two implementations interpolate the last column of every mel band from different reference values. This is the same root as the existing **7-5-D1** (Swift one-float over-read): the real Tony training corpus is F ≫ 512 (always DOWNsampling, where Python and Swift AGREE via the B1 float32-accumulation fix), and the F ≤ 496 parity fixtures pass at 2.81e-5. No separate entry — the fix is the same 7-5-D1 fix (clamp Swift's `floor` to `F-2`), which also closes the upsampling parity gap.
- **7-5-D4 — `compute_split_version_hash` fails open on a missing `tony`/`train`/`val` split.** `train_v2_artifacts.py:44-58`: lenient `.get("tony", {})` / `.get("train", [])` / `.get("val", [])` defaults produce a valid canonical-JSON sha256 over EMPTY structures instead of rejecting a malformed or wrong-shaped `corpus_splits.json` — so a recorded `splitVersionHash` could look valid while standing for nothing. Deferred: a malformed split fails loudly in the operator's training loader, and `audit-corpus-splits.py` gates split integrity upstream, so the hash function is never the first line of defense — defensive-only robustness. **Re-open trigger:** any non-operator consumer computing the hash without the upstream audit gate. Fix when re-opened: raise `ValueError` if `tony`, `tony.train`, or `tony.val` is absent rather than defaulting to empty.

## Deferred from: code review of 7-6 (2026-06-04)

- **7-6-D1 — `dnbSentinelsCorrect` (of 4) is not computed separately from `dnbSentinelsExpanded` (of 12).** AC1(a) names both an of-4 (original `4-dnb-triplet-targets.json`) and an of-12 (`12-dnb-sentinels-expanded.json`) count, but `evaluate_fr18.evaluate_seed` tags all sentinels `corpus="sentinel"` and computes only the of-12 + the Wilson-over-resolved bound. The Wilson 95% lower bound over the resolved sentinels is the BINDING close-out gate (AC1a / architecture gate (a)); the of-4 vs of-12 split is informational. Computing the of-4 separately would require threading an `isOriginal` flag through the main-bound `FR18EvaluationHarnessTests` per-track schema (the originals vs expanded distinction lives Python-side in `build_fr18_input.resolve_sentinels`). **Re-open trigger:** the operator's first real FR-18 run wants the of-4-originals subscore broken out (e.g. to triage which original triplet regressed). Fix when re-opened: tag original-sentinel input rows with `isOriginal: true` in `build_fr18_input` and pass it through the producer schema, then report both counts. Develop-only; corpus-clean today (the Wilson gate is unaffected).

## Deferred from: Story 8.1 (2026-06-10)

Story 8.1 shipped the chart-ready `analyzeLUFS(url:options:) -> LUFSReport?` reshape (momentary/short-term series on the 100ms grid, EBU Tech 3342 LRA, BS.1770-5 Annex 2 true-peak). Three items deferred, the first two by spec design (DD #7 / DD #10), the third discovered during DD #12 reference-tolerance measurement.

- **8-1-D1 — True-peak (and integrated loudness) are computed post-mono-mixdown; per-channel measurement deferred to the Story 8.2 `DecodedAudio` seam.** BS.1770-5 true-peak is the max over channels BEFORE mixdown, so the mono pipeline UNDERSTATES inter-sample peaks — the dangerous direction for a −1 dBTP delivery ceiling. The DocC on `LUFSReport.maxTruePeakDBTP` carries the "not suitable for delivery-compliance certification" warning. **Re-open trigger (corrected by Story 8-2 DD #11, 2026-06-10 — the "Story 8.2 per-channel carrier" language was aspirational; KDD-S2 defines `DecodedAudio` as MONO):** a future KDD-amendment story introducing a per-channel decode carrier (none scheduled). Fix when re-opened: run `LUFSAnalyzer.measureTruePeak` per channel pre-mixdown and take the max; integrated loudness's BS.1770 channel-weighted sum (G-weights) rides the same seam.
- ~~**8-1-D2 — `analyzeLUFS` has no cooperative cancellation; runs to completion (DD #10).**~~ **RESOLVED (Story 8-2 DD #8/#9, 2026-06-10).** `LUFSOptions.isCancelled: @Sendable () -> Bool = { Task.isCancelled }` landed (exact mirror of the BPM Options field). Checkpoints: url path — before decode (decode count 0 on pre-cancel) and after decode / before measurement; decoded path — before measurement. Service-level only; analyzers still never throw. Test-locked in `LUFSCancellationTests`.

## Deferred from: code review of 8-1-lufs-public-api-and-lufsreport-promotion (2026-06-10)

- **8-1-D4 — A single NaN/±Inf sample silently poisons `maxTruePeakDBTP` to the −100 silence-floor sentinel while the rest of the report reads normally.** The loudness path is per-block NaN-robust (blocks containing NaN gate out; integrated/momentary still measure), but the true-peak fold is all-or-nothing: `vDSP_maxmgv` over the raw signal propagates NaN into `maxAbs` (`LUFSAnalyzer.swift:459`), `max(maxAbs, phaseMax)` retains it (`:503`), and `guard maxAbs > 0` is false for NaN → returns `blockLoudnessFloor` (`:508`); a +Inf sample yields +Inf dBTP which `LUFSReport.init` clamps to −100. Net: a one-sample-corrupt loud Float32 WAV (decodable by AVFoundation) reports true peak −100 dBTP — the documented below-measurable floor — with no failure indication. Triggers only on corrupt float input; fix design is non-obvious (pre-pass non-finite sanitization costs an extra O(n) sweep vs a result-level guard that must choose an honest sentinel). **Re-open trigger (corrected by Story 8-2 DD #11, 2026-06-10 — KDD-S2 defines the carrier as mono; no per-channel seam exists in 8.2):** a future KDD-amendment story introducing a per-channel decode carrier (none scheduled), or any consumer report of implausible −100 dBTP on non-silent material. Fix when re-opened: detect non-finite `maxAbs` and surface it distinctly (nil report or documented NaN-in-input sentinel) rather than impersonating silence.

- **8-1-D3 — The bundled 96 kHz K-weighting coefficients deviate −0.477 LU at 997 Hz.** Discovered measuring DD #12 reference tolerances: the 96k high-shelf bilinear derivation yields +0.214 dB at 997 Hz vs the 48k prototype's +0.691 dB (verified analytically against the biquad transfer function; pre-existing — present in the pre-8.1 analyzer and barely inside the old ±0.5 cross-rate test). Coefficients are FROZEN by the AC6 byte-identity contract, so per the DD #12 measure-and-record rule the 96k reference test pins ±0.1 around the MEASURED offset (−0.4767) instead of silently widening the nominal tolerance. 48k (+0.00002) and 44.1k (−0.001) hold ±0.1 around nominal. **Re-open trigger:** a dedicated K-weighting-accuracy story (or any consumer-reported 96k absolute-loudness complaint). Fix when re-opened: re-derive the 96k high-shelf via a correct bilinear pre-warp of the analog prototype, re-baseline the 96k byte-identity literals in `LUFSByteIdentityTests` in the SAME commit (synthesized-only — 96k has no fixture literals), and tighten the 96k reference test to ±0.1 around nominal.

## Deferred from: Story 8-2 (2026-06-10)

Story 8-2 shipped the shared-decode seam (`readDecodedAudio` producer, `analyzeBPM(decoded:)`/`analyzeLUFS(decoded:)` overloads, `decodeOnce` funnel, AudioCodec/PrimingInfo reshape). Dispositions per DD #11: RESOLVED 8-1-D2 (cancellation) and W63 (by removal — see the struck entries above); RE-DEFERRED 8-1-D1 and 8-1-D4 with corrected triggers (the "Story 8.2 per-channel carrier" language was aspirational — KDD-S2 defines `DecodedAudio` as mono).

- **W62 considered and NOT FIXED (trigger unmet).** A `DecodedAudio.init` finiteness sweep would put an O(n) `allSatisfy` on the hot production decode path for a corrupt-float-only failure mode. The new `readDecodedAudio` producer inherits the same posture deliberately. W62's existing re-open trigger stands unchanged.

## Deferred from: code review of 8-2-shared-decode-wiring-three-analyzers-one-decodedaudio (2026-06-11)

- **8-2-D1 — AAC-family `AudioFormatID`s beyond `kAudioFormatMPEG4AAC` tag `.unknown`.** `PCMBufferReader.codec(forFormatID:)` maps exactly the six DD #7-enumerated IDs; HE-AAC (`kAudioFormatMPEG4AAC_HE`/`_HE_V2`), AAC-LD, and AAC-ELD fall to `default → .unknown` even though the public `AudioCodec` enum presents `.aac` as "the AAC case". Spec-conformant (the closed mapping is DD #7's letter; `.unknown` is the honest fallback and `TrimState.unknown` rides along correctly), so no behavior is wrong — only coarser provenance than a consumer might expect. **Re-open trigger:** a consumer report of an AAC-variant file that should tag `.aac`, or Story 8.5's priming-query work touching the same switch. Fix when re-opened: extend the switch with the AAC-family IDs (provenance-invariance test already locks that nothing downstream branches on the tag).

## Deferred from: code review of 8-4-davies-plumbley-beat-tracking-dp-and-beatgridanalyzer (2026-06-13)

- **8-4-D1 — Consumer-facing octave-guidance API for `BeatGrid` (`doubled()` / `halved()`), deferred to a future provenance-aware grid-editing layer.** Operator idea during code review: rather than have the library guess the octave, expose easy transforms so a DJ/producer can coax the grid to 2× (interpolate a beat midway between each pair) or ÷2 (decimate every other beat), tempo scaled accordingly. Codex-consulted 2026-06-13 (thread 019ec20d): advised AGAINST adding to the core analyzer now — `doubled()` *fabricates beats that were never detected*, which forces synthetic `strength`/`confidence` semantics, downbeat behavior, boundary handling, and Codable/Hashable expectations onto the core result type — real 1.0 API debt for a use case with no consumer yet. Story 8.4 instead snaps the reported `estimatedTempo` scalar to the BPM-stage octave (option C2) while keeping detected `beats` honest; the octave-disambiguation authority stays in the BPM stage. **Re-open trigger:** a real consumer need for DJ-style grid manipulation (e.g. the demo `BeatGridTimelineView` FR-39, or an external integrator). Design when re-opened: an explicit grid-*editing*/normalization layer — NOT a core-analyzer method — carrying beat provenance, e.g. `enum BeatOrigin { case detected, interpolated }` on `BeatTimestamp` (or a parallel field), so interpolated beats are never mistaken for observed ones. Keep the core `analyzeBeatGrid` faithful-tracker contract intact.

- **8-5-D1 — FR-30 codec-priming `TrimState.declared` provenance query + the empirical AVAudioFile-pre-trim verification test, deferred from Story 8.5 to Story 8.7.** Operator decision 2026-06-13 (party-mode review, John's scope argument; Codex thread `019ec2a4`; Apple TN2258). Story 8.5 lands the FR-30 *alignment contract* (`BeatTimestamp.presentationTime` is decoded-PCM-relative; no priming subtraction — `AVAudioFile`, layered on `ExtAudioFile` which per TN2258 removes priming/remainder frames, empirically presents already-trimmed PCM, so subtracting would double-correct). What's deferred: (a) the `kAudioFilePropertyPacketTableInfo` declared-trim query — which requires a SEPARATE `AudioFileOpenURL` + `AudioFileGetProperty` + `AudioFileClose` double-open (`AVAudioFile` does not expose its `AudioFileID`), `import AudioToolbox` (NOT AVFoundation — Siri caught the wrong-import claim), `OSStatus != noErr → .unknown` not `.knownNone`, LPCM short-circuit preserved, `defer`-close on every path, `AudioFilePacketTableInfo{mNumberValidFrames: Int64, mPrimingFrames: Int32, mRemainderFrames: Int32}` widened to `Int` with non-negative validation — the W63-named owner of that validation; (b) the additive `TrimState.declared(leadingFrames:trailingFrames:)` case (provenance ONLY, never subtracted from any timestamp); (c) the empirical pre-trim tripwire test (assert `decodedSampleCount ≈ mNumberValidFrames`, not `+ mPrimingFrames + mRemainderFrames`) — the only test that converts the "AVAudioFile pre-trims" assumption from prose to fact (Siri + Amelia). Rationale for deferral: this provenance feeds zero arithmetic in 8.5, touches a different file/API (`PCMBufferReader`/AudioToolbox), and belongs next to the priming-consuming AAC/MP3 hand-clicked acceptance oracle already scheduled for 8.7. While deferred, Story 8.5's DD #3 + the `PrimingInfo.swift:15-24` DocC must state the pre-trim mechanism as an EMPIRICAL ASSUMPTION, not a published Apple contract (`AVAudioFile` priming behavior is undocumented; only `ExtAudioFile` is documented). **Re-open trigger:** Story 8.7 spec-create. Also folds in `8-2-D1` (AAC-family `AudioFormatID` coarsening) — the same switch — and the LPCM-ness derivation should key off `processingFormat`, not the coarsened container `AudioFormatID`.

## Deferred from: party-mode review/validate/finalize of 8-5a-downbeat-detection (2026-06-14)

- **8-5a-D1 — Reported (ungated) real-audio downbeat fire-rate / correctness metric over OA300, deferred from Story 8.5a to a follow-up validation story.** Party-mode PM agent (John) flagged that 8.5a's acceptance is synthetic-only — AC8 plants the downbeat phase into runtime-generated fixtures and AC7's perf is reported-not-gated — so nothing in the story can catch a "the 4-part abstain gate (AC4: ≥3 bars AND `winner ≥ 1.25·runnerUp` AND `confidence ≥ 0.4` AND multi-bar support in ≥`ceil(bars/2)` bars) is so conservative the estimator never fires on real tracks" failure. Concern is valid; Codex tie-break (2026-06-14, thread `019ec464`) adjudicated **defer, do not expand 8.5a**: the story's acceptance should stay focused on deterministic correctness + abstain behavior, and a reported corpus fire-rate is only meaningful once the estimator exists. Scope when re-opened: a `make downbeat-impact-report` env-gated benchmark (sibling of `beat-grid-impact-report` / `shared-decode-impact-report`) over OA300 emitting (a) the **fire-rate** — fraction of analyzable tracks where `detectDownbeats == true` yields `.detected` vs `.noneDetected` at default `.analysisWindow` coverage (cheap; catches "practically inert"); and (b) **correctness** — which additionally needs downbeat ground truth the OA300 BPM oracle does not currently carry, so the follow-up story must source/curate a downbeat-phase oracle (a small hand-annotated 4/4 subset, or a DAW-derived bar-start oracle from the `.dawproject` files) before correctness can be measured. **Re-open trigger:** Story 8.5a lands the estimator (DownbeatResult populated), OR a consumer (Hilbert) reports the downbeat firing too rarely on real sets. Keep it a REPORTED metric, not a pass/fail gate — abstain-conservatism is the design intent (a wrong downbeat on a live deck is worse than none), so a hard fire-rate floor would be premature.

## Deferred from: code review of 8-6-modelregistry-and-cryptokit-sha256-integrity (2026-06-14)

- **8-6-D1 — Symlink handling in `ModelRegistry.computeDigest` is unspecified across four sub-cases.** The dev pre-review (F3) made an explicit "no special-casing; `.mlmodelc` bundles do not contain symlinks" decision and the code fails closed, so nothing is silently *accepted* wrong — but four symlink behaviors are surprising: (a) a top-level bundle URL that is itself a symlink to a directory → `FileManager.enumerator(at:)` yields zero children (empirically confirmed) → registration throws the misleading `.unsupportedFormat: "contains no regular files to hash"` instead of hashing a legitimate symlinked bundle path; (b) a subdirectory replaced by a symlink-to-dir is not descended under `options: []` → its content is silently excluded from the digest with no error; (c) the same bundle reached via a symlinked path vs its real path produces two `standardizedFileURL` cache keys (standardization collapses `.`/`..` but does NOT resolve symlinks) → redundant re-hash + split entries for one model; (d) a symlink resolving to a regular file outside the bundle is hashed as its target bytes (documented at `ModelRegistry.swift:211-214`), so the digest depends on external mutable state and TOFU can pin arbitrary external files. **Fix when re-opened:** resolve the bundle root and key the cache via `url.resolvingSymlinksInPath().standardizedFileURL`, and decide a single policy for in-bundle symlinks (resolve-and-hash, or reject). E1 (top-level symlinked bundle → wrong error) is the most user-visible. **Re-open trigger:** a consumer report of a symlinked model path failing with the wrong error, or any symlink-aware model-management work. [source: bmad-code-review blind+edge]

- **8-6-D2 — TOCTOU between digest computation and model load (and within the multi-file walk).** `ModelRegistry.register` hashes the bytes at registration time and stores the mutable `url` in the entry (`ModelRegistry.swift:142`); a consumer is later handed `entry.url`, and the file(s) can be swapped before CoreML loads — the registry still reports an integrity-checked entry for bytes no longer on disk. Within `computeDigest` itself, a file can mutate between the `isRegularFile` stat loop (`:209-232`) and the `Data(contentsOf:)` read loop (`:246-256`), so the digest need not reflect a single consistent filesystem moment. This is inherent to any hash-then-use design and partially documented in the class `- Important:` block; not fixable without filesystem-level locking. **Re-open trigger:** a story that needs verified-at-load-time integrity (e.g. re-hashing immediately before handing the URL to CoreML, or an OS file-presence/immutability primitive). [source: bmad-code-review blind+edge]

- **8-6-D3 — Empty `identifier` is accepted; `lookup("")` returns the entry.** `ModelMetadata(identifier: "")` registers without complaint and `ModelRegistry.lookup(identifier: "")` returns a real entry (`ModelRegistryEntry.swift:134-167`), which would silently break the intended join with `MLEvaluation.modelIdentifier`. Minor robustness; not a spec requirement, and `MLEvaluation.modelIdentifier` is itself unvalidated/optional, so the join contract is already best-effort. A non-empty guard would need a new error case (`.invalidMetadata`?) not worth inventing now. **Re-open trigger:** a consumer relying on identifier-based joins, or any story that adds metadata validation. [source: bmad-code-review edge]

## Deferred from: code review of 8-8c (2026-06-20)

- **8-8-D1 — `tempoBPM()` has no BPM positivity/range guard.** `JAMSFile.tempoBPM()` returns the first non-nil `tempo` value with no positivity/range check, so a `0.0`/negative/`NaN`-via-`float('nan')` BPM would decode silently into `OA300Track.bpm`/`DAWOracleTrack.dawBpm`. Pre-existing (the flat decoder it replaced had the same gap); migrated values are benchmark-proven, so no current regression. **Re-open trigger:** a new ground-truth artifact, or any decode-time validation policy work. [source: bmad-code-review edge]
- **8-8-D2 — Swift/Python tempo-decode asymmetry.** `tempoBPM()` picks `data.first(where: value != nil)` while the Python readers (`jams_corpus.py`, `validate_jams`) read `data[0]` unconditionally. Latent — the migrator only ever writes a single tempo observation. **Re-open trigger:** any artifact that emits multi-observation tempo annotations. [source: bmad-code-review edge]
- **8-8-D3 — No `track_id` uniqueness guard for oa300/daw migration.** `track_id` derives from the title (oa300) / filename (daw) with no uniqueness check in the converter or `validate_jams`; two identically-titled tracks in different subdirs would collide. Only the DnB *consumer* enforces uniqueness. The `filename.hasPrefix(track_id + ".")` join relies on uniqueness the migration never guarantees. Latent; 8-8b scope. **Re-open trigger:** a duplicate-title collision observed in a benchmark, or join-integrity hardening. [source: bmad-code-review edge]
- **8-8-D4 — `jams_corpus.load_oa300_rows` tolerates missing required fields as None.** The Python OA300 reader maps `identifiers.get('basename')`/`file_metadata.get('title')`/`sandbox.get('genre')` into flat rows without validation, so a malformed entry yields `None` rows into downstream training/split logic. The Swift decoders fail loudly on the same fields; the committed OA300 JAMS is validated. **Re-open trigger:** training-pipeline robustness work over hand-edited corpora. [source: bmad-code-review blind]

## Deferred from: code review of 8-7 (#44 patch round) (2026-06-20)

All develop-only oracle/benchmark tooling; none in `Sources/`, none break the committed oracle/calibration (F 0.37). Re-open triggers below are the only times these bite (the oracle is gated behind the operator's accept-vs-reopen-8.4 decision).

- **8-7-D1 — `BEAT_GRID_LIMIT` floor-skip keyed on a raw env var.** An exported `BEAT_GRID_LIMIT` leaks the floor-skip into a bare `swift test --filter ...BeatGridFloorTests` (prints "SMOKE MODE" — a tell, not silent); the Python `--allow-missing-constant` guard (`filter-out 0`) parses malformed/negative values differently from the Swift `Int(...) > 0` skip. **Robust fix:** gate the skip on a coverage signal (`n_missing_constant` / evaluated-vs-corpus count) in the accuracy JSON rather than the env var. **Re-open trigger:** CI hardening of the beat-grid floor, or any operator report of an unexpectedly-skipped floor. [source: bmad-code-review blind+edge]
- **8-7-D2 — Stale `COLLISION_OVERRIDES` entries are silent no-ops.** A deleted/renamed override target or a resolved dedupe leaves dead config in `scripts/rekordbox-beats.py:232` with no warning (fail-safe; only maintainability drift). **Re-open trigger:** audio-root reorg, or override-config hygiene work. [source: bmad-code-review edge]
- **8-7-D3 — 200k-iteration loop guard truncates silently.** `beats_from_markers` (`:288`, `:303`) `break`s mid-grid without recording a refusal bucket; unreachable for realistic durations/tempos but inconsistent with the round's strict hard-fail-never-truncate posture. **Re-open trigger:** a pathological high-BPM/long-track marker observed, or strict-refusal-parity hardening. [source: bmad-code-review edge]
- **8-7-D4 — DD-*/Codex/Story jargon in main-bound benchmark test files.** Pre-existing internal-jargon comments in `JAMSDecoder.swift` / `BeatGridBenchmarkTests.swift` / `JAMSDecoderTests.swift` that the squash-merge protocol (whole-file `git rm` only) would land on `main`; #44 adds none. **Re-open trigger:** the develop→main squash-merge release scrub (strip internal jargon from main-bound test comments). [source: bmad-code-review auditor]

## Deferred from: code review of story-8-9 (2026-06-22)

- **8-9-D1 — Downbeat gate smoke-skip keyed on `limit > 0`, not actual partial coverage.** `BeatGridBenchmarkTests.swift:367` skips the downbeat floor whenever `BEAT_GRID_LIMIT > 0`; an oversized limit (>= corpus size) skips the gate despite full coverage. Extends the surface of **8-7-D1** (the F-measure floor uses the same env-var convention) — the robust fix is identical: gate the skip on a coverage signal (evaluated-vs-corpus / `n_fired` vs full-corpus count) rather than the raw env var. **Re-open trigger:** CI hardening of the beat-grid floors, or any operator report of an unexpectedly-skipped floor. [source: bmad-code-review blind+edge]
- **8-9-D2 — Full-run zero-fired-constant downbeat slice false-fails instead of skipping.** `BeatGridBenchmarkTests.swift:370` asserts `meanConstant >= downbeatCorrectnessFloor`; if NO constant-tempo track fires a downbeat on a full run, `meanConstant` defaults to 0 < floor and the gate false-fails rather than skipping a degenerate slice. Near-unreachable (~42 constant tracks fire on the real corpus) and the `meanConstant` default predates this diff. **Re-open trigger:** a corpus change that drops the fired-constant count to zero, or downbeat-gate hardening. [source: bmad-code-review edge]
- **8-9-D3 — Even-`n` "median" picks the upper-middle element.** `BeatGridBenchmarkTests.swift:389,416,449` use `sorted[count/2]`, which for even `n` is the upper-middle element, not the statistical median (`[1,100]` reports 100, not 50.5). All three are REPORTED-only diagnostics (the gate uses P95) over large strata, so the one-element offset is cosmetic; the :389 IBI median feeds `octaveClass` but the 4% band absorbs it. **Re-open trigger:** any decision to gate on the reported median, or a small-`n` stratum where the offset flips a bucket. [source: bmad-code-review blind]
- **8-9-D4 — `decompose_beatgrid.py` has no file-not-found / JSON-decode handling on its two `open()` calls.** An operator typo on `--accuracy` / `--reference` yields a raw traceback instead of a readable error. Develop-only one-shot spike; a traceback is acceptable operator feedback, so this stays deferred. (The other degenerate-input sub-items originally bundled here — empty `p95s`, empty `oracle ∩ accuracy` join, explicit `"basename": null` — were folded into PR #50 after Copilot re-flagged them: empty-`p95s` guard, empty-join fail-fast, and `acc_row.get("basename") or track_id` fallback respectively.) **Re-open trigger:** promotion of the spike into repeatable tooling. [source: bmad-code-review blind+edge; PR #50 Copilot]

## Deferred from: code review of story-8-10 (2026-06-25)

- **8-10-D1 — combSupport bin count varies with period across the accept comparison.** `BeatGridAnalyzer.swift:726` — `bins = max(8, round(period·2))` differs between seed and refined periods, so the strict `support_refined > support_seed` accept compares two differently-binned objectives; max-over-more-bins inflates support for longer (slower) periods on sparse bins → a slow-tempo tilt. Negligible on the `.fullTrack` spans the feature targets (~100 frames/bin); material only on near-min-span fits (~4 frames/bin), which the min-span (8-beat) + contrast (1.5) guards already make abstain-prone; monotonicity preserved. **Re-open trigger:** any further sub-0.1-BPM precision tuning, or a corpus showing a slow-tempo acceptance bias — fix is a period-independent bin count for the seed/refined comparison. [source: bmad-code-review edge+blind]
- **8-10-D2 — Refinement trace evidence uncaptured on `.window`/`.fullTrack` coverage.** `BPMAnalyzer.swift:696` — the full-track seam threads `refineBeatGridTempo` but passes the default no-op `refinementSink` and has no `trace` surface, so `BPMDiagnosticTrace.beatGridTempoRefinement` is never populated on the long-coverage path where refinement most often fires. Diagnostic-only (AC #9 impact JSON reads `grid.estimatedTempo` directly; DD #6 marks the trace splittable). **Re-open trigger:** wiring a trace/sink through `AudioAnalysisService.beatGrid(decoded:)` into the full-track seam, or any debugging session that needs refinement evidence on long coverage. [source: bmad-code-review edge] **RESOLVED (PR #80 / issue #71, 2026-07-01):** the combined `analyze(url:)`/`analyze(decoded:)` path now threads live `refinementSink`/`downbeatSink` through `AudioAnalysisService.beatGridStitchingTrace` into the full-track seam and stitches the evidence onto `CombinedAnalysisResult.bpm.trace`, so `beatGridTempoRefinement` (and `downbeatStrategy`) populate on every coverage when `enableTrace` is set.
- **8-10-D3 — AC #9 monotonic floor can false-alarm on support/oracle divergence.** `DAWOracleBenchmarkTests.swift:659` — `improved >= worsened` / `meanDriftOn <= meanDriftOff` assume support-better ⇒ oracle-closer; the reject-guard only guarantees support improvement vs the audio's own onset evidence, so a groove/swing/mistuned-label track could move tempo away from the DAW oracle and trip the floor as a false regression. Operator-chosen floor; empirically improved=2/worsened=0 on the 14-track subset. **Re-open trigger:** a benchmark run where this floor fails — investigate whether it's a genuine guard failure or a support/oracle divergence before weakening the assertion. [source: bmad-code-review edge]
- **8-10-D5 — RESOLVED (PR #51 review).** `lockMatrixPrecedence` `.bpmStage` cell was weakly distinguishing (within-2% assertion passed even if `.bpmStage` no-oped). The Codex bot then found the actual no-op bug this weak test hid (`.bpmStage` routed through the gated agreement check and silently no-oped on a within-octave >2% refit divergence). Both fixed together: `AudioAnalysisService.stageLockTempo` makes `.bpmStage` authoritative within an octave; the matrix cell now asserts EXACT stage-tempo equality, and a dedicated `applyTempoLockBpmStageOverridesDivergentGrid` test pins the 124-vs-120 override directly. [resolved: PR #51, codex thread 019f09e0]
- **8-10-D6 — `.bpm(value)` still no-ops on a within-octave >2% disagreement.** Distinct from the `.bpmStage` fix: a caller-supplied `.bpm(120)` against a grid tracking 124 (3.3% off, within one octave) is currently IGNORED — `octaveNormalizedLockTempo` returns nil on `.disagree` and the grid stays at 124. Arguably user-hostile (the user explicitly asked to lock to 120), but it is shipped Story 8-9 behavior and was deliberately kept gated (arbitrary caller input can be wrong-pulse/absurd; Codex endorsed keeping `.bpm` gated). The `applyTempoLockBpmStageOverridesDivergentGrid` test codifies this current behavior as the `.bpmStage`-vs-`.bpm` contrast. **Re-open trigger:** an operator product decision on whether a manual `.bpm` lock should apply on within-octave disagreement (and only reject >octave / non-finite), mirroring `stageLockTempo`. [source: codex thread 019f09e0]
- **8-10-D4 — Refine window asymmetric on minimal spans.** `BeatGridAnalyzer.swift:656` — when `spanFrames/seedPeriod ≈ refineMinBeats`, `maxPeriodBySpan ≈ seedPeriod` truncates `pHi` to ≈ seedPeriod while `pLo` keeps the full `(1−halfWidth)` reach, so the search can only speed the tempo up on near-min-span fits — a faster-tempo coverage bias in the marginal regime. Monotonicity preserved (accept still requires support improvement), so it under-corrects rather than mis-accepts. **Re-open trigger:** evidence of systematic under-correction of slow-tempo tracks on short coverage — fix is to abstain or restore symmetry when `maxPeriodBySpan < seedPeriod·(1+halfWidth)`. [source: bmad-code-review edge]

## External audit triage (GLM / Gemini / ChatGPT, 2026-06-27)

Three external LLM audits (`docs/{glm,gemini,chatgpt}-audit.md`) triaged against the source + two party-mode rounds + two Codex consults (thread `019f0af8`). Every verifiable DSP claim confirmed; several framings context-blind. Outcome: measure-first → the Phase 0 forensic harness (shipped, `spec-accuracy-forensics`); experiment cluster → **Epic 13** charter; octave/model work → existing **Epic 12** charter (forensics feed its step-1 gate).

- **AUDIT-DISMISS — context-blind / deliberate (no action).** (1) "Core algorithm hidden" (GLM) — it's `BPMAnalyzer.swift` ~2100 lines. (2) "No evaluation harness" (ChatGPT) — five exist; the real point ("not forensic") shipped as Phase 0. (3) "Causal DP = unforced error, switch to Viterbi" (Gemini) — the Ellis DP already does the offline global backtrace (`BeatGridAnalyzer.swift:179-210/248-254`); only variable-tempo tracking is a real residue. (4) "NIH metadata parsing, use AVAsset" (Gemini) — deliberate; AVFoundation metadata is async-only/deprecated on the platform floor; parsers bounds-checked. (5) "Octave correction nonexistent" (GLM) — `resolveOctaveAmbiguity` exists; the *conclusion* (octave is the OA300 lever) is data-confirmed. (6) Overflow "theater" / precondition crashes / @TaskLocal — low code nits. **Re-open trigger:** a specific dismissed item independently re-reported with evidence.
- **AUDIT-FORENSIC-FINDING (Phase 0.5 substrate).** `make accuracy-forensics` → `accuracy-forensics-{oa300,giantsteps}.json` (reproduces floors exactly). OA300: 24 misses = 16 octave + 2 triplet + 6 other; recall split 21 selection-bound / 3 absent / 17 at-rank-1 → octave/**selection** lever, not generation (old `TODO.md:12` 11/30-absent improved). GiantSteps: 124 misses = 98 other + 19 triplet + 7 octave; 104 selection-bound; confidence reliability monotonic (0.5→70%, 0.9→94%). **Re-open trigger:** Epic 12 step-1 (octave-aware decode) or Epic 13 Phase 0.5 (ceiling sweep) consuming the JSON.
- **AUDIT-PRODUCT (Epic 10/11).** Split `audioConfidence` / `metadataConfidence` / `finalConfidence` + an "ambiguous" result signal when top candidates are octave/triplet-related and close — addresses the "metadata is cheating" critique all three raised; serves the DJ-sync consumer; gate on the (now-existing) reliability curve. **Re-open trigger:** an Epic 10/11 result-surface story.
- **AUDIT-EXPERIMENT-REJECTED — DO NOT demote periodicity candidates to a slower octave, in ANY form (2026-06-28).** `spec-octave-resolver-symmetry` made `resolveOctaveAmbiguity`'s sub-band vote demote to the fundamental. FOUR variants benchmarked, ALL net-negative, all reverted: OA300 Acc1 58→ un-gate-symmetric 40 / strict-tie 39 / score-guard@0.8 54 / score-guard@0.95 56; GiantSteps 537→ 503 / 507 / 534 / 536. The faster-only ratchet is LOAD-BEARING compensation for ACF subharmonic bias — a signal periodic at `T` is also periodic at `2T`, so the slow octave autocorrelates ≥ the fundamental (measured ~25% stronger in all 4 bands for a 160 click; a strict slow win, not a tie). A score-plausibility guard (only demote when the slow octave's fusion score ≥ threshold × the fast candidate's) blocks the 0.70→0.009 ghost demote and cuts damage ~5×, but asymptotes to baseline FROM BELOW (0.8→−4, 0.95→−2) and never lifts — the slow octave is genuinely score-competitive on enough correct-fast tracks. A unanimous codex+agy+MIR-literature quorum was empirically wrong and conceded; the literature (Parncutt 1994; Moelants & van Noorden 1999; Gkiokas et al. 2012; Schreiber & Müller 2018/2019; Luck 2024 review, arXiv:2401.00209) prescribes a perceptual prior / learned classifier, NOT a raw periodicity vote. Full record + citations: `investigations/accuracy-ceiling-sweep-investigation.md` Follow-up 2026-06-28. **Next octave levers (must NOT trust raw periodicity):** (1) perceptual ~120 BPM resonance tiebreak; (2) beat-grid coherence (W53/W74); (3) learned octave classifier (Epic 12). **Re-open trigger:** an Epic 12 octave story adopting one of levers 1-3 — NOT any demote-the-candidate variant.

## Deferred from: Epic 8 PR-review wrap-up (issues #56–#72) — 2026-07-01

The 17-issue review of PR #37 (Epic 8, `rterhaar/epic-8`) landed as 11 PRs (#73–#83). Reviews + Copilot/triage passes across the batch surfaced these non-blocking follow-ups; captured here so they don't die in merged-PR threads (PRs merge into `rterhaar/epic-8`, not the `main` default branch, so their `Closes #NN` never auto-fired either).

- **W75 — `analyze(url:)` / `analyze(decoded:)` duplicate the ~5-line grid/trace stitch block.** `Sources/BoomBoomBoomKit/AudioAnalysisService.swift` (the two combined-analyze tails, ~:1682 and ~:1711). Both call `beatGridStitchingTrace(...)` then `resolveTempoAgreement` and build `CombinedAnalysisResult(bpm: tracedBPM, beatGrid: applyTempoLock(...))`, differing only `decoded` vs `capped`. Pre-existing — PR #80 (ticket #71) edited both sites identically but did NOT introduce it. Behavior-preserving cleanup: extract a shared `combine(grid:bpm:options:)` helper. **Re-open trigger:** any further edit to the combined-analyze tail (a third caller, or a change to the stitch/lock ordering) — factor the helper then. [source: PR #80 review + triage, 2026-07-01] **RESOLVED (PR #85, 2026-07-01):** extracted `private static func combineGridWithBPM(decoded:options:bpm:)`; both `analyze(url:)` and `analyze(decoded:)` now call it (differing only `decoded` vs `capped`), so the stitch/lock/agreement block lives in one place. Verified a verbatim move — no behavior change.
- **W76 — Public-contract changes from the batch lack a CHANGELOG/release-note entry.** Two shipped-to-`main` behavior changes need a one-liner wherever release notes eventually live: (1) #58 (PR #77) — non-positive/non-finite `maxSeconds` on `PCMBufferReader.readMonoSamples(from:)` now reads the FULL file (was: empty result); (2) #59 / Story 8-1 — `analyzeLUFS` returns `LUFSReport?` and THROWS `LUFSAnalysisError.unsupportedSampleRate` (was: `Double?` / silent `nil`). Repo has no CHANGELOG today; pre-1.0, low urgency. **Re-open trigger:** authoring release notes for the eventual epic-8 → `main` squash, or a consumer report of the changed behavior. [source: PR #77 + PR #81 reviews, 2026-07-01]
- **W77 — `pinnedCheckBypassesCache` test name + comments stale after the TOFU cache removal.** `Tests/BoomBoomBoomKitTests/ModelRegistryTests.swift:~294-308` (issue #60 / PR #78 removed the digest cache). The assertions (`digestComputationCount == 2`) are correct, but the `// MARK: - Pinned re-verify bypasses the cache` heading + "does not use the cache" comment reference a cache that no longer exists. Cosmetic — rename to e.g. `pinnedReVerifiesOnEveryRegister` to match the always-recompute vocabulary the rest of #78 adopts. **Re-open trigger:** the next edit to `ModelRegistryTests`, or a docs pass. [source: PR #78 review, 2026-07-01] **RESOLVED (PR #78, 2026-07-01):** applied on the #78 branch as a finalize commit — renamed to `pinnedReVerifiesOnEveryRegister` and reworded the heading/comment; no `cache` reference remains in the pinned test.
- **W78 — `.combined` downbeat still abstains (not `≥ .metricalAccent`) on a `.halfBarAmbiguous` off-grid metrical.** `Sources/BoomBoomBoomKit/StructuralDropAnalyzer.swift` `combine()` `.halfBarAmbiguous` arm (~:274, `guard let mPhase = metricalPhase, mPhase == phaseA || mPhase == phaseB else { return .noneDetected }`). Issue #61 (PR #73) applied the "a drop can't veto a valid metrical fire" fallback ONLY to the `.confident`-disagree arm; the half-bar arm still discards a valid metrical fire when metrical's phase lands off the drop's confidently-detected bar grid. Deliberately left (off a confident grid is the least-trustworthy metrical case, so the abstain is defensible and possibly better for net F). The one-line extension is ready: `guard let mPhase = metricalPhase else { return .noneDetected }; guard mPhase == phaseA || mPhase == phaseB else { return metrical }` + a `.halfBarAmbiguous`-disagree test. **Re-open trigger:** the operator beat-grid downbeat benchmark (`make benchmark-beatgrid`) shows `.combined` net all-track downbeat F < `.metricalAccent` (the monotonic floor added in PR #73 is the empirical arbiter). [source: PR #73 review + triage, 2026-07-01]

## Deferred from: code review of story-10.1 (2026-07-08)

- **W79 — `BookmarkPersistence.store(url:)` does not dedup by URL.** `Demo/BoomBoomBoomBPM/BoomBoomBoomBPM/BookmarkPersistence.swift:141` mints a fresh `UUID()` on every call, so the same model file added twice yields two map entries and `resolveAll()` returns two `(id, url)` tuples with identical `url` / different `id` — Story 10.2 would then register the same model twice (a duplicate row, not corruption). AC2 requires only "mint a stable UUID"; dedup is intentionally NOT a persistence-layer concern. Deferred to Story 10.2 (the model picker owns the add-model UX and can reject a re-add of a known file). **Re-open trigger:** Story 10.2 picker implementation, or a report of duplicate model rows after re-adding a file. [source: code review of story-10.1, 2026-07-08] **RESOLVED (Story 10.2, 2026-07-09):** `ModelCatalog.addFromDisk` rejects a re-added URL with a labeled `Reason: model-already-added` and `restore()` dedups `resolveAll()`'s duplicate tuples — both by `standardizedFileURL` — so the same file added twice is one row (locked by `ModelCatalogTests.addFromDiskDedups` + `restoreDedupsDuplicateURLs`).

- **W80 — `ModelCatalog` dedup is lexical (`standardizedFileURL`), so a symlink and its target register as two rows.** `Demo/BoomBoomBoomBPM/BoomBoomBoomBPM/ModelCatalog.swift` keys dedup/identity on `url.standardizedFileURL`, which does not resolve symlinks; adding `/Models/m.mlmodelc` and a symlink `/Models/link.mlmodelc` → same physical bundle yields two catalog rows with identical `identifier` + `digest`. Benign (a duplicate row, not corruption) and an uncommon demo flow, so deferred. One-line fix if it surfaces: key on `url.resolvingSymlinksInPath().standardizedFileURL` consistently (dedup, `sources`, `select`, `isSelected`, and the `List` id) — apply everywhere at once or the keys diverge. **Re-open trigger:** a report of duplicate rows for one physical model added via a symlink. [source: Story 10.2 code review (Edge Case Hunter), 2026-07-09]
- **W81 — the in-use model selection is session-scoped; it does not survive relaunch.** `Demo/BoomBoomBoomBPM/BoomBoomBoomBPM/ModelCatalog.swift` `selectedURL` starts `nil` and `restore()` re-lists persisted models but does NOT re-attach a `BNNSTechnique`, so after relaunch a previously-chosen model appears in the picker but is not loaded — the user must reopen the sheet and click "Use this model" each launch. AC5 promises list presence only, so this is intentional 10.2 scope (documented in the story artifact Dev Notes + the `selectedURL` doc comment). Follow-up (own small story): persist the selected model's URL-key per the demo UserDefaults convention (`object(forKey:)` three-branch self-heal, `AnalysisViewModel.init` merge-strategy precedent), and on restore, if the persisted key matches a restored entry, call `loadModel(at:)` + `select(url:)`. Carries the DD5 caveat — a launch-time `BNNSTechnique` compile on the main actor. **Re-open trigger:** operator/user asks for the chosen model to auto-load on launch. [source: Story 10.2 code review (robbyt), 2026-07-10]
- **W82 — W79's dedup is in-memory only; the UserDefaults persistence layer still accumulates duplicate/failed bookmarks.** `Demo/BoomBoomBoomBPM/BoomBoomBoomBPM/BookmarkPersistence.store(url:)` mints a fresh `UUID` per call and never dedups by URL (W79 deferred that to the picker), and `ModelCatalog.addFromDisk` dedups against the in-memory `entries` only. So duplicate bookmarks for the same URL persist in `UserDefaults` forever and re-resolve every launch, and a URL whose restore FAILED this launch (absent from `entries`) can gain a second persisted bookmark on a re-add. `restore()` dedups the resolved tuples so the picker shows one row, but the persistence layer bloats. Harmless at 1–20-model scale. Fix if it surfaces: dedup/prune at the persistence layer (e.g. `store` rejects or overwrites a URL-equal bookmark, or a periodic prune of unresolved keys). **Re-open trigger:** a report of accumulating/duplicate persisted bookmarks or slow launch from re-resolving dead bookmarks. [source: Story 10.2 code review summary (robbyt), 2026-07-10]
