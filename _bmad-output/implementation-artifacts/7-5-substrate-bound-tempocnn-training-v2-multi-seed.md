# Story 7.5: Substrate-bound TempoCNN training run on FeatureSubstrate.OnsetFeatures (v2, multi-seed)

Status: done

<!-- baseline_commit: 08bb1dc | branch: rterhaar/7-5 | epic_source: epics.md#L741-L783 -->
<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Spec validation history

- 2026-06-03 — Spec created via bmad-create-story (factual-claims grep @ `08bb1dc`). 13 reality-deltas surfaced and baked as DDs (see "Verified on-disk reality").
- 2026-06-03 — DD #1 (headline fork) RESOLVED by operator: **coordinate-bump both version strings to "v2"** (`OnsetFeatures.featureSetVersion` AND `BNNSTechnique.supportedFeatureSetVersion`, `"v1"→"v2"`). Rationale: **backwards compatibility is NOT a goal pre-1.0** — the only downside of the bump was that the develop-only `giantsteps_v1.mlmodelc` would be abstained-against, which is a BC concern and explicitly off the table (and that v1 scaffolding model is precisely what this run replaces). This is also the epic-faithful reading: `featureSetVersion` is the feature-contract marker ("v1" = Story 7.3's disposable librosa scaffolding pipeline; "v2" = the substrate-locked, FR-18-evaluable real contract); the multi-seed **model generation** rides on the filename + `MLEvaluation.modelIdentifier`, NOT on `featureSetVersion` (Winston — keep the two axes separate; DD #14).
- 2026-06-04 — Implemented via bmad-dev-story (Tasks 0–8) + post-dev implementation review (Codex diff-review + Blind Hunter + party-mode Winston/Amelia). Swift/main-bound side (featureSetVersion v2 bump via shared constant + 12 coupled flips + the `@_spi` `modelInputTensor` seam) + the develop-only parity foundation (dump-model-input CLI + feature_substrate_v2 + ml-parity stage 5) + the AC10 precondition gate + the v2 producers (metadata/stability/sanity) all GREEN. Review found + fixed 2 BLOCKER (B1 resample control-vector float32-accumulation divergence on long tracks; B2 `_octave_fold` +inf hang) + 6 SHOULD-FIX (S1 fail-closed legacy-GiantSteps guard, S2 gate token-strengthen, S3 seed-completeness, `@_spi`, version-pairing, length-clamp) — all regression-tested. Below-gate training-loop rewire + the full 3-seed MPS run remain operator-gated (DD #16). See the Dev Agent Record.
- 2026-06-03 — Pre-dev adversarial review: axiom:ask → axiom-testing (resolved empirically against the codebase) + Codex consult (read featurize + the test suite) + bmad-party-mode (Mary/Winston/Amelia/John). Convergent findings folded in (6 BLOCKER + several SHOULD-FIX). **Headline correction (Winston + Codex, DD #15): the parity boundary in AC1 was at the wrong seam.** `OnsetFeatures.logMelData` is the PRE-transform payload (frame-major, post-`log1p(100·x)`, PRE-z-score / PRE-transpose / PRE-resample); `BNNSTechnique.featurize` (`BNNSTechnique.swift:526-626`) then applies transpose→per-mel z-score→resample-to-W=512 to reach the `[1,1,128,512]` model-input tensor. Train/runtime identity (FR-21) is a property of the MODEL-INPUT tensor, not the raw envelope — AC1 re-pinned. **Other corrections:** AC8's `Tests/BoomBoomBoomKitMLTests/` target does not exist (→ `Tests/BoomBoomBoomKitTests/`); the abstain test already exists (`BNNSTechniqueTests.swift:107` `evaluateAbstainsOnVersionMismatch`, version-agnostic — no new test, instead flip the 4 coupled `"v1"` assertions); the `±1e-6` tolerance is unreachable (existing Stage 3/4 gate at 1e-4/1e-3 — DD #15); the KDD-B4 gate makes a full `--subset` smoke impossible so dev-done = "reach the gate and fail there with a named error" (DD #16); a smoke-scale feature-distribution sanity check vs the ablation's librosa features is now mandatory (Mary, AC13). The spec below reflects ALL of these — the earlier "2-line Sources + 1 new test" framing is retracted.

## Story

As a **library maintainer**,
I want **the final TempoCNN training run executed against the substrate-locked `FeatureSubstrate.OnsetFeatures` from Story 6.2, consuming the KDD-B2 ablation winner (`maskedMelPretrain`, hard-coded with the `kdd-b2-comparison-v1.md` citation), trained on the Tony Strong+Solid split (NOT the legacy GiantSteps fold-split `train.py` currently uses), with Swift↔Python parity verified at the model-input-tensor seam (`[1,1,128,512]`, post-`featurize`) to the harness tolerance (1e-4/1e-3; DD #15), across 3 distinct seeds (42/43/44), and per-seed `model_metadata.json` carrying the resolved feature-set version + `promotable: true` + `bundleEligibilityPendingFR18: true`**,
so that **the model under FR-18 evaluation in Story 7.6 trained on byte-identical features to what `BNNSTechnique.featurize` produces at runtime (FR-21 train/runtime parity), the 3-seed checkpoint family gives KDD-B3 reopen-trigger #5 its multi-seed stability evidence, and Guardrail 2 is respected (Story 7.3's librosa-ablation metrics do not transfer; this is the run that produces FR-18-transferable metrics)**.

## Scope split (READ FIRST — mirrors Story 7.3's dev/operator boundary)

This story has a **dev-agent harness deliverable** and an **operator-run heavy step**, exactly as Story 7.3 did:

- **Dev-agent owns (this story's "done"):** build + smoke-verify the complete substrate-bound training harness — the Swift model-input dump CLI, `feature_substrate_v2.py`, the parity-harness extension (4→5 stages, AC1), the rewired `train.py` (Tony split + `maskedMelPretrain` recipe + substrate features + multi-seed orchestration + the precondition gate), `train_runnerup.py`, the per-seed metadata schema, the determinism/reproducibility plumbing, the `marginal-watchlist-stability.json` producer, the smoke-scale feature-distribution sanity check (AC13), and the DD #1 `featureSetVersion` bump + the coupled-assertion flips (AC8). Smoke-verify every path on `--subset` + a single seed (`--epochs 1`). **Do NOT run the full 3-seed 60-epoch training** — that is the operator's heavy step. Because `train.py`'s own precondition gate (AC10) blocks on the still-`pending` KDD-B4 signoff, dev-done for the `train.py` path is **"a `--subset --epochs 1` invocation loads the Tony split, produces substrate v2 features, runs ONE forward pass, then reaches the KDD-B4 gate and aborts there with the named error"** — NOT a completed training run (DD #16). Everything UP TO the gate (loader, feature production, parity, one forward/backward step, metadata/log/stability schema emission on a hand-rolled or `--smoke`-flagged batch) is dev-verified.
- **Operator owns (gated, NOT dev-agent):** (1) the **KDD-B4 corpus signoff** — flipping `corpus-diagnostics-v1.md` `REVIEWER_SIGNOFF: pending → signed` (this gate currently BLOCKS `train.py` past AC10; see DD #10); (2) the **full 3-seed × 60-epoch training runs** on MPS producing `giantsteps_v2_seed_{42,43,44}.mlmodel` + per-seed metadata + `training_log.json` + `marginal-watchlist-stability.json`. These are surfaced as pending operator steps at story close, not dev-agent blockers. The 7.5b operator-signoff line ALSO cannot flip until Story 7.3 (the `maskedMelPretrain` winner source) is merged off `review` (John — cross-story chain).

**Internal phase gate (John's pre-emptive-split concern, handled in-place):** Tasks 1–3 (the parity foundation: Swift dump CLI + `feature_substrate_v2.py` + the 5th parity stage) are the highest-uncertainty, hard-prerequisite sub-phase. They MUST pass before Tasks 4+ (the training rewire) proceed. If the 5th parity stage cannot reach the documented tolerance (DD #15) after reasonable investigation, STOP and invoke the epic pressure-release valve — formally split into Story 7.5a (parity reconciliation) / 7.5b (training run); training MUST NOT proceed on a non-parity pipeline. This keeps one story while honoring the parity-first risk ordering.

## Acceptance Criteria

1. **(FR-21 / AC parity) Model-input-tensor parity, Swift↔Python — at the RIGHT seam (DD #15).** The parity boundary is the **post-`featurize` model-input tensor** (`[1, 1, 128, 512]`, mel-major, z-scored) — the tensor the BNNSGraph actually consumes — NOT the raw `OnsetFeatures.logMelData` envelope. `OnsetFeaturesBuilder.build` emits `logMelData` frame-major, post-`log1p(100·x)`, **pre-z-score / pre-transpose / pre-resample**; `BNNSTechnique.featurize` (`BNNSTechnique.swift:526-626`) then applies transpose (frame-major→mel-major, `:545`) → per-band z-score (`vDSP_normalize`, `:574`) → resample-to-`W=512` (`:585-608`). Train/runtime identity (FR-21) is a property of THAT tensor; verifying only the raw envelope would let the model train on a different distribution than it infers on. A new Swift CLI executable under `_bmad-output/ml-training/swift_feature_extractor/` (e.g. `dump-model-input`, depending on `BoomBoomBoomKit` like `tony-dsp-prepass`) dumps the post-`featurize` `[1,1,128,512]` tensor (single-sourcing `BNNSTechnique.featurize` so transpose/z-score/resample have exactly ONE implementation — do NOT reimplement in Python; DD #15) for a fixture set. `make ml-parity` gains a **5th stage** asserting the Swift-dumped model-input tensor vs what `feature_substrate_v2.py` hands the encoder match, with the **128×512 shape invariant asserted explicitly** (Winston — shape-lock so a mismatch fails loudly at parity, not at a training-time broadcast). **Tolerance: `1e-4` abs / `1e-3` rel — aligned with the existing log-mel/z-score stages (`test_feature_parity.py:45-48`), NOT `1e-6`** (which float STFT/log accumulation cannot hold end-to-end; demanding 1e-6 would trip the pressure-release on a tolerance artifact, not a real defect — DD #15). The raw-`logMelData` (Contract A) parity MAY be kept as a secondary cheaper assertion to isolate which side drifted. If the model-input-tensor stage cannot reach tolerance after reasonable investigation, training is blocked and the pressure-release split is invoked (internal phase gate).

2. **(KDD-B2 winner consumed) `train.py` runs the `maskedMelPretrain` recipe.** `_bmad-output/ml-training/train.py` is rewired to use the **`maskedMelPretrain`** variant — the choice **hard-coded** with a comment citing the `_bmad-output/ml-training/ablation/kdd-b2-comparison-v1.md` numeric outcome (`maskedMelPretrain`; Acc1 88 vs 88; `ECE_half_double` 0.5353 < 0.5542 over 92 qualifying tracks). The SSL recipe is REUSED from `ablation/masked_mel.py` + `ablation/train_masked_mel_pretrain.py` (encoder split, 15%/5-frame contiguous time-masking, MSE-on-masked-positions) — NOT re-invented. The runner-up `supervisedAugmented` recipe is preserved at `_bmad-output/ml-training/train_runnerup.py` (REUSE `ablation/train_supervised_augmented.py`) for follow-up retrain stories.

3. **(CRITICAL — Tony split, NOT GiantSteps) `train.py` trains on the Tony Strong+Solid split.** `train.py` MUST load from `corpus_splits.json` `tony.{train, val, leaveArtistOut}` (820/92/104 — REUSE the existing loader path in `ablation/ablation_common.py` + `corpus_common.load_tony_corpus`), NOT the legacy `dataset.build_splits()` GiantSteps fold-split it currently calls (DD #2). Marginal-tier tracks are excluded (`corpus_common.tier_for`; `TRAINABLE_TIERS = ("Strong","Solid")`); verified by `scripts/audit-corpus-splits.py --reject-marginal` returning clean before training.

4. **(Substrate features only — FR-15 at the boundary) The model input derives SOLELY from the substrate log-mel; z-score/resample are shape transforms, not new signals.** `feature_substrate_v2.py` is reviewed to confirm the tensor reaching the model derives ONLY from the substrate `OnsetFeatures.logMelData` payload (via the featurize transpose/z-score/resample of AC1) — no playlist, path, Rekordbox, artist, ID3-BPM, or DSP-candidate-score signal enters the model input. **FR-15 forbids new SIGNALS, not the mandatory shape transforms** (per-band z-score + resample-to-512): the masked-mel encoder + `masked_mel.py`'s mask-value-`0` assumption are calibrated to a z-scored distribution (`masked_mel.py:8-9`, mean≈0 post-z-score), so the z-score/resample MUST be applied or SSL pretraining silently degrades (Codex/Amelia). Story 7.5 does NOT reuse `ablation/ablation_features.py`'s librosa mel path (that was the v1-scaffolding feature source — DD #1/DD #13); the reuse from `ablation/` is the TRAINING loop / masked-mel / octave loss / augmentation, NOT the feature producer. The FR-15 audit grep (`ablation/fr15-audio-only-audit.md` ledger) is extended to `feature_substrate_v2.py` + `train.py` + the new Swift CLI; the ledger records that z-score/resample are non-signal shape transforms.

5. **(FR-16 octave-aware loss) Reused, not re-invented.** `train.py` uses the octave-aware loss from `ablation/octave_aware_loss.py` (the same documented per-class weighting from `ablation/octave-aware-loss.md`); no new loss formula is introduced.

6. **(Guardrail 3 — one declared WeightingProfile; HARD substrate constraint) `weightingProfile` is `"uniform"`.** Training declares exactly one `WeightingProfile`, passed explicitly at start (`--weighting-profile`), encoded in `model_metadata.json`. Because `OnsetFeaturesBuilder.build(decoded:weighting:)` implements ONLY `.uniform` and THROWS `FeatureSubstrateError.weightingNotYetImplemented` on `.subBandEmphasis` (per the substrate as shipped — DD #6), the declared profile for the substrate-bound v2 run MUST be `"uniform"`; `"subBandEmphasis"` is not selectable until a future substrate story implements it. The harness rejects (`ValueError`) if `--weighting-profile` is absent.

7. **(Per-seed metadata schema) `model_metadata.json` produced PER SEED.** For each seed in {42, 43, 44}, a sibling `model_metadata.json` is produced carrying: `architecture: "TempoCNN"`, `seed`, `epochCount`, `corpusVersionHash` (REUSE `ablation_common.compute_corpus_version_hash`), `splitVersionHash` (net-new — same canonical-JSON-sha256 approach over `corpus_splits.json` `tony.*` + `_provenance`; DD #8), `trainingVariant: "maskedMelPretrain"`, `weightingProfile: "uniform"`, `featureSetVersion: "v2"` (per DD #1's resolved coordinate-bump — must equal the bumped `OnsetFeatures.featureSetVersion`), `promotable: true` (flipped from Story 7.3's `false`), and `bundleEligibilityPendingFR18: true`. A `tests/` schema assertion locks these keys.

8. **(FR-21 runtime mismatch guard) The `featureSetVersion` bump + the coupled-assertion flips — NO new test.** Per DD #1, bump the **emitted value** `"v1"→"v2"` at `BPMAnalyzer.swift:939` (`OnsetFeatures`/`MLFeatureFrames` producer) AND `BNNSTechnique.supportedFeatureSetVersion` at `BNNSTechnique.swift:204` so the substrate-emitted version matches the model's expected version (the v2 model RUNS rather than abstaining). **Introduce a single shared producer constant** (e.g. `OnsetFeatures`/`FeatureSubstrate`-owned `featureSetVersion`) that both producers reference, so there is ONE source of truth — but **keep test assertions as `"v2"` literals** (a test that reads the constant it asserts tests nothing — Amelia). **No new test is added: the abstain guard is ALREADY covered** by `Tests/BoomBoomBoomKitTests/BNNSTechniqueTests.swift:107` `evaluateAbstainsOnVersionMismatch` (uses `"v999-FROM-THE-FUTURE"`, version-agnostic → survives the bump) and `BNNSTechniqueDiagnosticTests.swift:205` (`"v99"`). Instead, **flip the 4 pipeline-coupled `== "v1"` assertions** that now break: `FeatureSubstrateTests.swift:74`, `MLFeatureFramesTests.swift:340`, `:383`, `:463` (each asserts the substrate/pipeline-EMITTED version → must read `"v2"`). **Do NOT touch the ~45 carrier-arg `"v1"` literals** (`MLFeatureFrames(... featureSetVersion: "v1")` passed as an arbitrary init arg — inert; nothing asserts they equal the runtime version) — a blind `replace_all` would also corrupt unrelated `"v1"` strings (e.g. `giantsteps_v1` filenames, `schema_version`). Update the 6 stale doc-comments referencing `"v1"` (`MLDiagnosticSnapshot.swift:35`, `OnsetFeatures.swift:44`, `MLFeatureFrames.swift:22`/`:110`, `BNNSTechnique.swift:201`/`:332`). Net Tests/ delta: **0 new tests, 4 modified assertions; total count stays 483**. (DD #12)

9. **(FR-20 reproducibility) `training_log.json` + same-seed determinism.** Each run records `_bmad-output/ml-training/training_log.json` (or a per-seed sibling) with per-epoch loss + val Acc1 + val Acc2 + ECE. Deterministic data shuffling is verified by a second run with the same `--seed 42` producing byte-identical final-epoch loss to within `±1e-9` (smoke-scale verification by the dev-agent; full-scale by the operator).

10. **(KDD-B4 + substrate + flip preconditions) `train.py` aborts if any gate is unmet.** `train.py` begins with a precondition check that aborts with a NAMED error if any of: (a) the `OnsetFeatures` + `OnsetFeaturesBuilder` types are absent from `Sources/BoomBoomBoomKit/FeatureSubstrate/`; (b) `BPMSelectionPolicy.select(` (Story 6.5b KDD-A6 Stage-3 flip — the presence grep MUST be `func select(`, NOT `select(from:`, because the signature spans `BPMSelectionPolicy.swift:461-467` and a single-line match returns nothing — Codex); (c) the KDD-B4 corpus signoff is unmet — i.e. `scripts/audit-corpus-splits.py --check-gate` exits non-zero because `corpus-diagnostics-v1.md` is `REVIEWER_SIGNOFF: pending`. Gates (a)/(b) are satisfiable in the current `rterhaar/7-5` checkout (both types present in-tree — DD #11); gate (c) is OPEN today and is the operator's heavy-step blocker. The "must have merged on develop" epic wording is interpreted as "present in the checkout `train.py` runs against" (DD #11). **Dev-agent smoke note (DD #16):** since gate (c) hard-blocks, a `--subset` run reaches gate (c) and aborts there — that IS the dev-verified outcome for the gated path. A narrow `--smoke` seam MAY be added to exercise post-gate wiring on a subset, but it MUST force `--subset`, be loudly non-production, and MUST NOT silently disable the gate for the operator's run (Mary/Amelia/John — reach-the-gate-and-fail is the preferred dev-done; a bypass is only for observing post-gate plumbing).

11. **(KDD-B3 trigger #5 — multi-seed) 3 seeds, ≥2-of-3 aggregation contract wired.** The harness orchestrates `--seed 42`, `--seed 43`, `--seed 44`, each producing a checkpoint destined for `_bmad-output/ml-models/giantsteps_v2_seed_{42,43,44}.mlmodel` (the `.mlmodel` export is the operator heavy-step output; the dev-agent wires + smoke-verifies the orchestration and the per-seed metadata). `_bmad-output/ml-training/marginal-watchlist-stability.json` is produced, consuming the Story 7.4 `marginal-watchlist.json` typed `v2Prediction` stub (`{status:"pending-7.5", seeds:[], varianceBpm, octaveNormalizedAgreement, matchesExpected}`) and FILLING `seeds: [{seed, bpm, confidence}]` + `varianceBpm` + `octaveNormalizedAgreement` across the 3 seeds (`matchesExpected` stays `null` — Story 7.6 fills it). The seed-aggregation gate "≥ 2 of 3 seeds pass each FR-18 gate" is DOCUMENTED here as the contract Story 7.6's evaluator enforces (Story 7.5 produces the 3-seed evidence; 7.6 grades it).

12. **(Develop-only except the version bump + coupled-assertion flips) Diff scope.** All new + modified files live under `_bmad-output/ml-training/` (incl. `swift_feature_extractor/`), `scripts/`, and `Makefile`, EXCEPT the `Sources/`+`Tests/` footprint of the DD #1 `featureSetVersion` bump: (i) `Sources/` — the 2 emitted-value sites (`BPMAnalyzer.swift:939`, `BNNSTechnique.swift:204`) routed through ONE shared producer constant, plus 6 doc-comment updates; (ii) `Tests/BoomBoomBoomKitTests/` — the 4 coupled `== "v1"`→`"v2"` assertion flips (`FeatureSubstrateTests.swift:74`, `MLFeatureFramesTests.swift:340`/`:383`/`:463`). **No new test; total test count stays 483** (the abstain guard is already covered — AC8). The ~45 carrier-arg `"v1"` literals and the intentionally-wrong `"v99"`/`"v999"` mismatch literals are LEFT UNCHANGED. `make build` + `make test` green; `make py-lint` (ruff + ty) green with the new modules added to the ty allowlist; `make ml-parity` green at 5 stages. Run the bpm-diagnostic-trace skill's audit recipes if any trace field is touched (it is not, but the version bump is adjacent). No emojis; `uv run`, never `python3`.

13. **(Feature-distribution sanity at smoke scale — Mary, do-not-discover-at-full-run) Substrate-vs-librosa guard + per-seed gradeable schema.** Because the KDD-B2 winner (`maskedMelPretrain`) was selected on the ablation's **librosa** features but Story 7.5 swaps to **substrate** features, the dev-agent MUST capture, at smoke scale, a training-independent feature-distribution sanity check: on a fixed handful of tracks, assert the substrate model-input tensor's per-band mean/variance are in the same ballpark as the ablation's librosa features, no all-zero or saturated bands, and the shape/checksum (reuse the `inputFeatureChecksum` FNV-1a tripwire + `ml-parity` machinery) is as expected — emitted to `_bmad-output/ml-training/feature-distribution-sanity.json`. This proves the bytes the model will train on are not "a different planet" from the bytes the winner was chosen on, WITHOUT requiring training convergence. Additionally, the per-seed `training_log.json` + `marginal-watchlist-stability.json` schema MUST capture **per-track, per-seed** Marginal predictions + confidence (not just aggregates) so Story 7.6 can mechanically (a) measure cross-seed AGREEMENT on WHICH tracks (KDD-B3 trigger #5 "stable" = same tracks, not just a stable count) and (b) apply a **pre-declared** high-confidence threshold (named in `label-tier-policy-v1.md`'s trigger-#5 row, NOT chosen post-hoc in 7.6 — else it is the 7.4 null-graded-null trap). A `tests/` assertion locks this schema shape.

## Tasks / Subtasks

> **Phase ordering (internal gate):** Tasks 1–3 (parity foundation) gate Tasks 4+. If the 5th parity stage can't reach tolerance → STOP + pressure-release split.

- [x] **Task 0 — Enumerate the `featureSetVersion` blast radius BEFORE coding** (AC: 8, 12; DD #1, DD #12)
  - [x] Classified all 56 `"v1"` hits. **CORRECTION to the review's prediction:** the coupled set is **12 sites, not 4** — 4 EMITTED-version assertions (`FeatureSubstrateTests:74`; `MLFeatureFramesTests:340/383/463`) PLUS 8 `evaluate`-feeding carrier-args that the version guard makes coupled (`BNNSTechniqueDiagnosticTests:102/149/175`; `BNNSTechniqueTests:143/175/366/423/429` — these expect featurize to RUN past the guard; with `"v1"` vs a `"v2"` runtime they version-mismatch-abstain and the test goes vacuous/red). The Diagnostic pair `:102/:149` are NOT model-gated and FAILED the suite, confirming the coupling Codex under-counted. Inert (left `"v1"`): `MLFeatureFramesTests` + `FeatureSubstrateTests` init-invariant carrier-args; intentional mismatch literals `"v999-FROM-THE-FUTURE"`/`"v99"` left as-is.
- [x] **Task 1 — Swift model-input dump CLI** (AC: 1, 4; DD #4, DD #15)
  - [x] Promoted `BNNSTechnique.featurize` to a pure `public static func modelInputTensor(from:)` seam; the instance `featurize` delegates (runtime byte-identical, single source). Added the `dump-model-input` executable target (deps `BoomBoomBoomKit` + `BoomBoomBoomKitML`; `.package(path: "../../..")` resolves). It reads the existing `parity_signals/{sine,click}.f32`, runs the REAL `OnsetFeaturesBuilder.build(.uniform)` → `modelInputTensor`, and dumps `out/swift_stages/<name>_stage5_model_input.f32`. **Verified: `swift build` clean + `swift run dump-model-input` emits two 65536-float ([128,512]) `v2` tensors (262 KB each).**
- [x] **Task 2 — `feature_substrate_v2.py` loader** (AC: 1, 4; DD #3, DD #13, DD #15)
  - [x] Created `feature_substrate_v2.py`: numpy reconstruction of `featurize` — `extract_log_mel` (mel-major substrate envelope, reused) → featurize-faithful per-band z-score (population std + constant-row ZEROING, matching `vDSP_normalize` + the `stddev==0` guard; NOT `dataset.zscore_per_band`'s 1e-8-floor variant) → `vDSP_vlint`-matching resample-to-512 (float32 control ramp + `nextDown` clamp). Architecture **(B)** chosen (Python reconstructs; parity verifies) over (A) because the masked-mel recipe needs per-epoch feature-space access; documented. This REPLACES `ablation/ablation_features.py`'s librosa mel — critically it uses **resample-to-512** (matching the runtime), NOT the legacy `crop_or_loop_to_target` the GiantSteps ablation used (that train/runtime mismatch is exactly what FR-21 fixes).
- [x] **Task 3 — Parity harness 5th stage (model-input tensor, shape-locked)** (AC: 1; DD #15)
  - [x] Added stage 5 to `test_feature_parity.py` at **1e-4/1e-3**, explicit `[128,512]` reshape (loud fail on shape/dump mismatch), sine=informational / click=binding-gate masking. **`uv run python test_feature_parity.py` → OVERALL PASS; stage-5 click `max_abs 1.13e-4` within element-wise abs-OR-rel.** Parity reached — no pressure-release split needed.
- [~] **Task 4 — Rewire `train.py` (gate + weighting-profile DONE/verified; below-gate loop operator-gated)** (AC: 2, 3, 5, 6, 10; DD #2, DD #6, DD #10, DD #11, DD #16)
  - [x] **AC10 precondition gate** `check_substrate_preconditions()` runs FIRST in `main()`: (a) substrate types present, (b) `func select(` present (NOT `select(from:` — spans lines), (c) KDD-B4 signoff via the same `REVIEWER_SIGNOFF: signed` contract as `--check-gate` (fails CLOSED on != 1 marker). **VERIFIED: `train.py --subset 4 --epochs 1 --weighting-profile uniform` aborts with `SubstratePreconditionError: AC10(c): KDD-B4 corpus signoff is pending` — (a)/(b) passed in-tree, (c) is the operator blocker = the DD #16 dev-done.**
  - [x] `--weighting-profile` REQUIRED + `choices=["uniform"]` (substrate `.subBandEmphasis` throws — DD #6). `env_metadata.feature_set_version` "v1"→"v2".
  - [~] **Below-gate loop rewire — operator-gated, NOT dev-executable.** The Tony-loader swap (DD #2) + `maskedMelPretrain` recipe (`ablation/masked_mel.py`) + `feature_substrate_v2` features + the fail-fast `(128, 512, float32, z-scored)` contract assert are documented at the `build_splits` call as the operator's full-run rewire reusing the 7.3-verified ablation machinery. Execution NEVER reaches the loop in the dev env (the AC10 gate aborts first — DD #16), so this is correctly deferred to the operator's signed run; the legacy GiantSteps loader is retained ONLY as the pre-signoff placeholder with a loud swap-before-signed-run note.
- [x] **Task 5 — `train_runnerup.py` + per-seed metadata + multi-seed contract** (AC: 2, 7, 9, 11; DD #8, DD #14)
  - [x] `train_runnerup.py` — thin single-source delegation to `ablation/train_supervised_augmented.py` (the runner-up; no copy-paste drift). `train_v2_artifacts.build_seed_metadata` — per-seed schema (`promotable: true`, `bundleEligibilityPendingFR18: true`, `featureSetVersion: "v2"`, `modelGeneration: "v2"` per DD #14, `splitVersionHash` via `compute_split_version_hash`, seeds `{42,43,44}`). Rejects non-`uniform` profile + unknown seeds. Unit-tested.
- [x] **Task 6 — `marginal-watchlist-stability.json` producer (per-track per-seed)** (AC: 11, 13; DD #9)
  - [x] `train_v2_artifacts.build_marginal_stability` fills the 7.4 `v2Prediction` stub PER TRACK across seeds: `seeds:[{seed,bpm,confidence}]` + `varianceBpm` + `octaveNormalizedAgreement` (octave-fold + 4% tol → "stable" = same track modulo octave, Mary); `matchesExpected` stays null (7.6). Unit-tested incl. octave-equal agreement + missing-prediction fallthrough.
- [x] **Task 7 — `featureSetVersion` bump + coupled-assertion flips (NO new test)** (AC: 8, 12; DD #1, DD #12)
  - [x] Added the shared constant `MLFeatureFrames.currentFeatureSetVersion = "v2"`; `BPMAnalyzer.swift:939` + `BNNSTechnique.supportedFeatureSetVersion` now reference it (single source of truth). Updated 6 doc-comments (`MLFeatureFrames:111/124` retain explanatory `"v1"`→`"v2"` narrative). Flipped the 12 coupled sites (4 emitted-version assertions + 8 evaluate-feeding carrier-args) to `"v2"`; left inert init-invariant carrier-args + the `"v999"`/`"v99"` intentional mismatch literals. `evaluateAbstainsOnVersionMismatch` (`"v999"`) + the `"v99"` diagnostic test pass unchanged. NO new test. **`swift build` clean + `make test` 483/105 GREEN.**
- [x] **Task 8 — Feature-distribution sanity + gates + smoke** (AC: 12, 13)
  - [x] `train_v2_artifacts.feature_distribution_sanity` (substrate model-input per-band mean/var vs librosa; flags all-zero/saturated bands + mean-ratio outliers) — pure function, unit-tested. (The on-disk `feature-distribution-sanity.json` emission over real tracks is part of the operator's gated full run — the substrate features there require the corpus; the producer + its guard logic are dev-verified.)
  - [x] Gates GREEN: `train.py --subset --weighting-profile uniform` reaches the KDD-B4 gate + aborts with the named error (DD #16); `make ml-parity` 5 stages PASS (click binding gate `max_abs 1.13e-4`); `make build` clean; `make test` 483 (no new test); `make py-lint` (ruff + format + ty) green with `feature_substrate_v2.py` + `train_v2_artifacts.py` in the ty allowlist; full ablation pytest 75 passed (61 + 14 new); `git diff --stat Sources/` = bump (5 files incl. the `modelInputTensor` seam) + `Tests/` = 4 coupled-assertion files.
  - [x] 5th stage runs under `make ml-parity`; ty-allowlist updated.

### Review Findings

> Second adversarial code-review pass (2026-06-04, `/bmad-code-review`) — Codex Blind Hunter (diff-only) + Edge Case Hunter (diff + project) + Acceptance Auditor (diff + spec). The two BLOCKERs below were CONVERGENT (Blind Hunter + Edge Case Hunter independently) and were NOT caught by the first implementation review. Acceptance Auditor: all 13 ACs satisfied, main-vs-develop placement clean, operator-gated remainder correctly deferred (not flagged). **All 4 patches APPLIED + regression-tested; 3 items deferred (7-5-D1/D2/D3 in deferred-work.md).** Re-verified GREEN: ablation pytest 84 passed (+3 new regressions), `make py-lint` (ruff+format+ty) clean, `make ml-parity` 5-stage PASS (stage-5 click binding gate 2.81e-5, unchanged — the F=496 click is where the min-clamp reproduces prior behavior). Zero Swift touched this pass, so the Swift gates hold. **Implementation NOTE (the fix was subtler than the finding):** clamping Python to Swift's accumulated value naively OOB-crashed on UPSAMPLING (F=200/496, where drift is +, accumulated lands above F-1) — the real fix is `min(accumulated, F-1)` then `nextDown`, which matches Swift on long tracks (drift −, the parity win) AND stays in-bounds where Swift over-reads (deferred 7-5-D1). Empirically probed: F=20000 drift −0.059, F=60000 drift −0.285, F≤496 drift +tiny.

- [x] [Review][Patch] Python resample last-column clamp diverges from Swift on long tracks — FR-21 parity gap [feature_substrate_v2.py:97] — Swift clamps `controlVector[W-1] = controlVector[W-1].nextDown` (one ULP below its float32-ACCUMULATED last value, `BNNSTechnique.swift:625`); Python clamps to `np.nextafter(F-1, -inf)` (one ULP below the IDEAL `F-1`). The body accumulation matches (B1 fix), but the last element uses two different reference values, diverging as accumulation drift grows: F=20000 frac Δ≈0.059, F=60000 frac Δ≈0.285. The F≤496 parity fixtures cannot exercise it; on real Tony tracks (F=10k–60k) column 511 of every mel band trains (numpy) on a different value than the runtime infers (Swift). Fix: `control[width-1] = np.nextafter(control[width-1], np.float32(-np.inf))` (clamp Python's OWN accumulated value).
- [x] [Review][Patch] build_marginal_stability emits invalid-JSON `Infinity` varianceBpm + false octave-agreement on a non-finite seed prediction [train_v2_artifacts.py:153-155] — a single blown-up seed BPM (inf) flows through `pvariance` → `varianceBpm: Infinity` (bare `Infinity` is not valid JSON; Swift `JSONDecoder`/strict `json.loads` reject it, breaking Story 7.6's consumer), and `octave_normalized_agreement` silently drops the inf seed and returns `True` (claims agreement while 1 of 3 seeds is garbage). The B2 fix guarded the hang but not the serialization. Fix: exclude non-finite seed BPMs from variance (null if <2 finite), serialize a non-finite seed bpm as null, and return `None` (not `False`) agreement when <2 finite seeds survive (folds in Blind Hunter F2).
- [x] [Review][Patch] Strengthen large-F resample regression to assert the accumulated-value clamp [ablation/tests/test_train_v2_artifacts.py:205] — `test_resample_control_vector_large_F_stays_linear` only checks numpy self-consistency (`< 1.0` frame vs `np.linspace`); it green-lights even the divergent last column. Add an assertion pinning `control[width-1]` to `nextafter` of the accumulated value (catches the BLOCKER-1 class, not just the body).
- [x] [Review][Patch] feature_distribution_sanity: flag non-finite-variance bands + fix docstring over-claim [train_v2_artifacts.py:200-250] — docstring claims "saturated bands" detection that isn't implemented; a `nan`/`inf` VARIANCE band passes (only zero-variance and non-finite MEAN are scanned). Add a non-finite-variance scan and align the docstring.
- [x] [Review][Defer] Pre-existing Swift one-float OOB heap read in BNNSTechnique resample when the clamped last control index floors to an integer ≤ F-1 [BNNSTechnique.swift:625] — deferred, pre-existing (Epic 4 `c120bc2`, main-bound, NOT this diff; benign vlint over-read, but worth a dedicated follow-up since it ships).
- [x] [Review][Defer] feature_distribution_sanity ratio-outlier tolerance (`n//16` = 8 bands @ n=128) is loose for a guard meant to be conservative [train_v2_artifacts.py:235] — deferred, tuning judgment (tightening risks false alarms; revisit against real distributions during the operator run).
- [x] [Review][Defer] _octave_fold returns NaN/≤0 unchanged (body contradicts "fold" docstring); safe only because every caller pre-filters [train_v2_artifacts.py:108] — deferred, currently safe (defensive hardening, no live failure path).

> Third adversarial code-review pass (2026-06-04, `/bmad-code-review` — Blind Hunter via **Codex** per operator directive, diff-only; Edge Case Hunter diff+project; Acceptance Auditor diff+spec). Blind Hunter + Edge Case Hunter CONVERGED on the resample last-column divergence (already deferred 7-5-D1) and on a `train_v2_artifacts.py` fail-open cluster the second pass's finiteness hardening did not fully close. Acceptance Auditor: 12 of 13 ACs fully satisfied, file placement clean, `featureSetVersion` bump correct + single-sourced; ONE literal AC4 sub-clause unmet (the FR-15 ledger extension). 7 findings dismissed as noise (the v2-assertion "no producer change" blind artifact — `make test` 483 GREEN proves `OnsetFeaturesBuilder` transitively emits v2; the `_provenance` list-order, even-seed median, all-silent-band, SIGNOFF-doc-match, runner-up exit-code, and non-4-byte-read findings — all either fail-closed-by-design, unreachable, or working as intended).

- [x] [Review][Patch] APPLIED — `build_marginal_stability` variance includes ≤0 BPM while octave-agreement excludes it — inconsistent sample sets [train_v2_artifacts.py:169-171] — `finite_bpms` filters only `None` (non-finite), keeping finite ≤0 values, so `varianceBpm = pvariance({0.0, 120, 60})` is computed over a 0 that `octave_normalized_agreement` (which folds only `b > 0`) drops — variance and agreement disagree on which seeds count. Unreachable today (decode `bpm = 30 + argmax` ≥ 30), same latency class as 7-5-D3; fix = apply the `> 0` filter to `finite_bpms` so both stats use one positivity rule.
- [x] [Review][Patch] APPLIED — `build_marginal_stability` accepts unknown seeds into "filled-7.5" and pollutes variance/agreement [train_v2_artifacts.py:154-182] — `seeds_payload` iterates `sorted(preds)` and `status` only checks for MISSING `{42,43,44}`, so a stray seed 99 is serialized AND `missing_seeds == []` → `filled-7.5`, while its BPM pollutes `finite_bpms`. Sibling `build_seed_metadata` already REJECTS unknown seeds (`seed not in SEEDS → ValueError`); align by restricting `preds` to `SEEDS` at the top. Reachable only via operator seed-set error (per-seed metadata gen would also fail), but the sibling-inconsistency is a fresh-code defect.
- [x] [Review][Patch] APPLIED (extended to librosa VARIANCE too, per Codex plan-review) — `feature_distribution_sanity` does not finite-check the librosa reference stats (substrate side is checked, librosa is not) [train_v2_artifacts.py:238-248] — a `nan`/`inf` librosa mean flows through `lm = abs(...)` → `denom = max(nan, 1e-9)` (returns `nan`) → `ratio = nan`, and `nan > tol` / `nan < 1/tol` are both False → no outlier flagged; the non-finite scans inspect `substrate_band_stats` only, so `passed` can stay True with a garbage reference. The guard's whole purpose is to catch substrate-vs-librosa distribution breaks; add a symmetric `nonFiniteLibrosaBands` scan. Reachable only if librosa extraction is itself broken.
- [x] [Review][Patch] APPLIED — FR-15 audit ledger NOT extended to the v2 files — literal AC4 sub-clause unmet [ablation/fr15-audio-only-audit.md] — AC4 / Task 4 / DD #13 require the FR-15 ledger be "extended to `feature_substrate_v2.py` + `train.py` + the new Swift CLI; the ledger records that z-score/resample are non-signal shape transforms." The ledger was not touched (last commit `08bb1dc` = Story 7-4; absent from the diff). The SUBSTANCE of AC4 is satisfied (no new signals enter the model input; richly documented in the new modules' docstrings) — this is the documentation-ledger half of the AC. Fix = append the three v2 files to the ledger with the shape-transform rationale.
- [x] [Review][Defer] Python↔Swift resample last-column divergence on UPSAMPLING (F < 512) — subsumed by 7-5-D1 [feature_substrate_v2.py:110 vs BNNSTechnique.swift:625] — convergent Blind Hunter + Edge Case Hunter finding: Python clamps `min(accumulated, F-1)` then `nextDown`; Swift clamps only `controlVector[W-1].nextDown` (no `min(., F-1)`), so on float32-accumulated upsampling drift above `F-1` Swift over-reads `A[F]` while Python stays in-bounds at `A[F-1]`, diverging on the last column of every band. The real Tony corpus is F ≫ 512 (always DOWNsampling, where the two AGREE via the B1 fix) and the F ≤ 496 parity fixtures pass at 2.81e-5; the residual is exactly the deferred Swift over-read (7-5-D1). No new entry — see 7-5-D1.
- [x] [Review][Defer] `compute_split_version_hash` fails open on a missing `tony`/`train`/`val` split [train_v2_artifacts.py:44-58] — lenient `.get("tony", {})` / `.get("train", [])` defaults produce a valid sha256 over empty structures instead of rejecting a malformed `corpus_splits.json`, so the recorded reproducibility hash would be hollow rather than loud. Deferred: a malformed split fails loudly in the operator's loader and is gated by `audit-corpus-splits.py` upstream, so the hash leniency is never the first line of defense — defensive-only.

## Dev Notes

### Context — where this sits in Epic 7

**Pool flow:** 7.1 (splits/tiers/diagnostics) → 7.2 (expansion pool) → 7.3 (ablation, winner=`maskedMelPretrain`) → 7.4 (Marginal wiring) → **7.5 (substrate-locked v2 winner run — THIS)** → 7.6 (FR-18 promotion gate; consumes the 7.5 checkpoints + the 7.4/7.5 watchlist) → 7.7 (FR-24 net-benefit + BYOW export). Story 7.5 is the FIRST Epic-7 story that touches `Tests/` (AC8) and the first that runs the REAL training (Stories 7.1–7.4 were diagnostics/wiring; Story 7.3's ablation used a standalone librosa feature pipeline that does NOT transfer — Guardrail 2). Story 7.5 swaps in the substrate-locked `FeatureSubstrate.OnsetFeatures` so the trained model sees byte-identical features to `BNNSTechnique.featurize` at runtime (FR-21).

### The three non-negotiable guardrails (epic, lines 587/726/759)

1. **v1 features were scaffolding/diagnostics only** — Story 7.3's ablation metrics DO NOT transfer to this run (different feature producer; DD #1/DD #13). This run produces the FIRST FR-18-transferable metrics.
2. **`featureSetVersion` bumps invalidate FR-18 metrics** — central to DD #1. Do not bump casually.
3. **One declared `WeightingProfile` per trained model** — forced to `"uniform"` by the substrate (DD #6).

### Verified on-disk reality (factual-claims grep @ 08bb1dc)

| Epic/spec assumption | On-disk reality | Action |
|---|---|---|
| `featureSetVersion: "v2"` is encoded in `model_metadata.json` and the runtime checks it | TWO coupled strings are BOTH literally `"v1"`: `OnsetFeatures.featureSetVersion` (assigned at `BPMAnalyzer.swift:939`) → flows to `MLFeatureFrames.featureSetVersion`, and `BNNSTechnique.supportedFeatureSetVersion` (`BNNSTechnique.swift:204`, `internal`). The runtime guard (`BNNSTechnique.swift:364`) ABSTAINS when they differ. A model recorded as "v2" under a "v1" runtime would never run. | **DD #1 (headline fork) — RESOLVED: coordinate-bump both strings `"v1"→"v2"`.** BC is not a goal pre-1.0, so the abstained-against develop-only `giantsteps_v1.mlmodelc` is acceptable (this run replaces it). `featureSetVersion` is the generational feature-contract marker ("v1"=7.3 librosa scaffolding, "v2"=substrate-locked real). 2-line `Sources/` change. |
| `train.py` is the final training entrypoint and trains on the corpus | `train.py` loads the **LEGACY GiantSteps** fold-split via `dataset.build_splits(verify=True)` (`dataset.py:152`; fold01=val, folds 02-10=train) — NOT Tony. The Tony loader lives in `corpus_common.load_tony_corpus` + `ablation/ablation_common.py` (used by the 7.3 ablation), never by `train.py`. | **DD #2.** Rewire `train.py` to the `corpus_splits.json` `tony.*` split; REUSE the existing ablation/corpus_common loaders (do not rebuild). |
| `feature_substrate_v2.py` exists as the Python feature loader | **MISSING.** | **DD #3.** Net-new file consuming the Swift OnsetFeatures dump. |
| The Swift CLI fixture extractor already emits the model-input tensor | `swift_feature_extractor/` has `dump-fixture` (no `BoomBoomBoomKit` dep — re-implements DSP, mel stages only), `tony-dsp-prepass` (BBBKit dep), `bnns-probe`. **None emit the post-featurize tensor.** | **DD #4 + DD #15.** Net-new `dump-model-input` target (BBBKit dep, like `tony-dsp-prepass`; single-sources `featurize`). |
| `make ml-parity` verifies the OnsetFeatures pipeline | `test_feature_parity.py` compares **4 mel-pipeline stages** (filterbank `.npz` / mel-power / log-mel `log1p(100·x)` / z-scored tensor), gated at **1e-4/1e-3** (`:45-48`) — it stops BEFORE the model-input tensor. | **DD #5 + DD #15.** Add stage 5 at the **model-input-tensor** seam (post-featurize `[1,1,128,512]`), tolerance **1e-4/1e-3** (NOT 1e-6 — unreachable for float STFT/log). If unreachable → pressure-release split. |
| `OnsetFeatures.logMelData` IS the model input (AC1 as first written) | **WRONG SEAM.** `OnsetFeaturesBuilder.build` (`:66-74`) emits `logMelData` frame-major, pre-z-score/pre-transpose/pre-resample. `BNNSTechnique.featurize` (`:526-626`) THEN does transpose (`:545`) → per-band z-score (`:574`) → resample-to-512 (`:585-608`). The model trains/infers on the POST-featurize tensor. | **DD #15.** Parity boundary = post-featurize model-input tensor; Swift dumps it (single-sourced featurize), Python does NOT reimplement. FR-15 z-score/resample are shape transforms, not signals (AC4). |
| AC8 test target `Tests/BoomBoomBoomKitMLTests/` | **Does not exist.** ML tests live in `Tests/BoomBoomBoomKitTests/`. The abstain test ALREADY exists (`BNNSTechniqueTests.swift:107` `evaluateAbstainsOnVersionMismatch`, `"v999-FROM-THE-FUTURE"`, version-agnostic). | **DD #12.** No new test; flip the 4 coupled `== "v1"` assertions (`FeatureSubstrateTests.swift:74`; `MLFeatureFramesTests.swift:340`/`:383`/`:463`). Path → `Tests/BoomBoomBoomKitTests/`. |
| The bump is "2 Sources lines, zero Tests churn" | 56 `"v1"` sites total; 2 emitted values + 6 doc-comments + 4 coupled assertions break; ~45 carrier-args + `"v99"`/`"v999"` mismatch literals are inert. | **DD #12.** Shared producer constant for the 2 values; literal `"v2"` in the 4 flipped assertions; NO blind `replace_all` (would corrupt `giantsteps_v1` / `schema_version`). |
| `train.py --subset` can smoke-verify end-to-end | `train.py`'s AC10 precondition hard-blocks on the still-`pending` KDD-B4 signoff → it CANNOT complete a subset run. | **DD #16.** Dev-done = reach the gate + abort with the named error; everything up to the gate is verified; optional loud `--smoke`-only post-gate seam (never silently disables the gate). |
| `weightingProfile` may be `"uniform"` OR `"subBandEmphasis"` (epic AC, line 759) | `OnsetFeaturesBuilder.build` implements ONLY `.uniform`; `.subBandEmphasis` THROWS `FeatureSubstrateError.weightingNotYetImplemented` (CLAUDE.md FeatureSubstrate roster). | **DD #6.** The v2 declared profile MUST be `"uniform"`; `subBandEmphasis` is not selectable until a future substrate story implements it. |
| `train_runnerup.py` exists | **MISSING.** | Net-new; REUSE `ablation/train_supervised_augmented.py` (the runner-up = `supervisedAugmented`). |
| `splitVersionHash` is an existing metadata field | The ablation schema has `corpusVersionHash` (via `compute_corpus_version_hash`) but NO `splitVersionHash`. | **DD #8.** Net-new; reuse the same canonical-JSON-sha256 approach over `corpus_splits.json` `tony.*` + `_provenance`. |
| `marginal-watchlist-stability.json` is produced by aggregating seeds | **MISSING** — Story 7.4 left a typed `v2Prediction` stub (`{status:"pending-7.5", seeds:[], varianceBpm:null, octaveNormalizedAgreement:null, matchesExpected:null}`, `_consumedBy:["7.5","7.6"]`) in `marginal-watchlist.json`. | **DD #9.** Net-new producer fills `seeds[]`+`varianceBpm`+`octaveNormalizedAgreement`; `matchesExpected` stays null (7.6). |
| KDD-B4 corpus signoff is complete (gates training) | `corpus-diagnostics-v1.md` carries `REVIEWER_SIGNOFF: pending` (`audit-corpus-splits.py --check-gate` exits non-zero). | **DD #10.** OPERATOR-OWNED gate — blocks the full training run, not the harness build. `train.py` enforces it via `--check-gate`. |
| Story 7.5 precondition "merged on develop" | The current `rterhaar/7-5` checkout HAS both `FeatureSubstrate.OnsetFeatures` (`Sources/BoomBoomBoomKit/FeatureSubstrate/`) AND `BPMSelectionPolicy.select(from:)` (`BPMSelectionPolicy.swift:461`). Epic 6 → develop squash-merge is operator-pending, but the TYPES are present in-tree. | **DD #11.** Interpret the gate as "present in the checkout `train.py` runs against" (executable, branch-agnostic); the develop merge is a release concern. |
| `ablation/ablation_features.py` is the feature source to reuse | It uses **librosa** (`librosa.effects.pitch_shift`, melspectrogram) — the v1-scaffolding feature pipeline, NOT parity-verified to the Swift runtime. | **DD #13.** Reuse the ablation TRAINING loop / masked-mel / loss / augmentation, but the FEATURE SOURCE is `feature_substrate_v2.py` (substrate parity), never librosa. |

### Reuse map (anti-wheel-reinvention — do NOT rebuild these)

| Need | Reuse from | Note |
|---|---|---|
| Tony split loader | `corpus_common.load_tony_corpus` + `ablation/ablation_common.py` (`CORPUS_SPLITS`, `compute_corpus_version_hash`, `build_metadata`) | The 7.3 ablation already loads `corpus_splits.json` `tony.{train,val,leaveArtistOut}` (820/92/104). |
| `maskedMelPretrain` recipe | `ablation/masked_mel.py` (`run_encoder`, masking, `masked_reconstruction_loss`) + `ablation/train_masked_mel_pretrain.py` + `ablation/ablation_train.py` (`finetune`) + `ablation/masked-mel-recipe.md` | The pinned SSL recipe (DD #13 of Story 7.3). |
| Runner-up recipe | `ablation/train_supervised_augmented.py` | → `train_runnerup.py`. |
| Octave-aware loss | `ablation/octave_aware_loss.py` + `ablation/octave-aware-loss.md` | FR-16; do not re-derive. |
| Model architecture | `model.py` `build_reference_model` (`TempoCNN`, 256-bin, `bpm = 30 + argmax`) | KDD-B1 architecture retained. |
| Tiering | `corpus_common.tier_for`, `TRAINABLE_TIERS = ("Strong","Solid")` | Marginal excluded. |
| Corpus/split hashing | `ablation_common.compute_corpus_version_hash` | + net-new `splitVersionHash` (same approach). |
| Runtime feature contract | `OnsetFeaturesBuilder.build(decoded:weighting:)`, `OnsetFeatures` (`Sources/BoomBoomBoomKit/FeatureSubstrate/`) | The substrate the Swift dump CLI invokes. |
| Runtime abstain guard | `BNNSTechnique.swift:364` + `supportedFeatureSetVersion` (`:204`) | The AC8 test target. |

### featureSetVersion mechanics (the DD #1 detail the dev MUST internalize)

Two coupled strings gate ML inference:
- `MLFeatureFrames.featureSetVersion` (← the producer constant at `BPMAnalyzer.swift:939`, currently `"v1"`) = what the runtime substrate EMITS.
- `BNNSTechnique.supportedFeatureSetVersion` (`:204`, currently `"v1"`) = what the trained model EXPECTS.
- `BNNSTechnique.evaluateInternal` (`:364`) abstains (`return (nil, nil)`) when they differ.

**Resolution (DD #1): bump BOTH to `"v2"` via ONE shared producer constant** so the substrate emits "v2" and the model expects "v2" → the model RUNS. BC is not a goal, so the develop-only `giantsteps_v1.mlmodelc` being abstained-against is acceptable (this run replaces it; nothing on `main` bundles a model — Story 4-6). The abstain guard is ALREADY tested (`evaluateAbstainsOnVersionMismatch`, `"v999"`, version-agnostic — survives the bump); **no new test is added** (DD #12). The only `Tests/` churn is flipping the 4 assertions that read the EMITTED version (`== "v1"` → `"v2"`).

### DD #14 — `featureSetVersion` ≠ model generation (Winston)

`featureSetVersion` is the **feature-contract** axis (does the byte layout / mel-FFT-log param set the model expects match what the substrate emits?). The **model generation** (multi-seed v2 vs the historical v1 model, or a future retrain on the SAME v2 features) rides on the `giantsteps_v2_seed_*` filename + `MLEvaluation.modelIdentifier` — NOT on `featureSetVersion`. Do not reach for `featureSetVersion` as a general "something changed" lever: a future same-features retrain would then have no clean version to bump. Bump `featureSetVersion` here ONLY because the feature contract genuinely changed (substrate-sourced ≠ Story-7.3 librosa-sourced).

### DD #15 — parity seam = the model-input tensor (Winston + Codex)

`featurize`'s transpose + per-band z-score + resample-to-512 sits BETWEEN the substrate envelope (Contract A: `OnsetFeatures.logMelData`) and the model input (Contract B: `[1,1,128,512]` z-scored mel-major). FR-21 train/runtime identity is a property of **Contract B**. Verify Contract B. The Swift `dump-model-input` CLI single-sources `BNNSTechnique.featurize` so transpose/z-score/resample have ONE implementation across runtime + dump (exposing featurize to the CLI makes it a single-source-of-truth seam — the spec accepts this access-surface change pre-1.0). `feature_substrate_v2.py` consumes the dumped tensor; it does NOT reimplement featurize. Default architecture (A): Swift dumps the final tensor, Python loads it → parity for the featurize transforms is trivially exact + FR-21 guaranteed; feature-space augmentation (if the recipe needs it) must then happen pre-substrate on audio. Tolerance for any Python-computed stage = 1e-4/1e-3 (the existing harness floor), never 1e-6.

### DD #16 — gate-vs-smoke (Mary/Amelia/John)

`train.py`'s AC10 KDD-B4 precondition hard-blocks while `corpus-diagnostics-v1.md` is `REVIEWER_SIGNOFF: pending` (operator-owned). So the dev-agent CANNOT complete a `--subset` training run. Dev-done for the gated path = "the run loads the Tony split, produces substrate v2 features, runs one forward pass, then reaches the gate and aborts there with the named error." A loud `--smoke`-only seam to observe post-gate plumbing is optional, MUST force `--subset`, and MUST NOT silently disable the gate for the operator's run.

### Project Structure Notes

- **Develop-only EXCEPT the DD #1 `featureSetVersion` footprint:** `Sources/` = 2 emitted-value sites (via one shared constant) + 6 doc-comments; `Tests/BoomBoomBoomKitTests/` = 4 coupled `== "v1"`→`"v2"` assertion flips (NO new test). Departure from the 7.1–7.4 zero-`Sources/`-zero-`Tests/` pattern; call it out in the dev record. No DSP/pipeline behavior change.
- Branch/PR: continue on `rterhaar/7-5` (already checked out); PR base `rterhaar/epic-7` — **never `develop`/`main`** (Epic 7 branch workflow). Operator owns the 1Password-signed commit (project convention — a clean code-review flips review→done; the signed commit/PR is surfaced as pending).
- No emojis anywhere (Makefiles/echo/artifacts). `uv run`, never `python3`, for BMad/ML scripts.
- The 3-seed `.mlmodel` exports land in `_bmad-output/ml-models/giantsteps_v2_seed_{42,43,44}.mlmodel` (develop-only; the `Sources/BoomBoomBoomKitML` target ships WITHOUT a bundled model per Story 4-6 — bundling is Story 7.6's promotion-gate outcome, not this story's).

### Testing standards

- Swift: Swift Testing (`import Testing`, `@Suite`/`@Test`/`#expect`/`#require`) — NOT XCTest. ALL ML tests live in `Tests/BoomBoomBoomKitTests/` (there is NO `BoomBoomBoomKitMLTests` target). AC8 adds NO new test — it flips 4 coupled `== "v1"` assertions in `FeatureSubstrateTests.swift` + `MLFeatureFramesTests.swift`; the abstain behavior is already covered by `BNNSTechniqueTests.swift:107`.
- Python: `pytest` under `_bmad-output/ml-training/` (schema assertions, determinism, parity). ruff + ty via `make py-lint`; add new modules to the `Makefile` ty allowlist.
- Smoke-scale only for the dev-agent: `--subset N --epochs 1`, single seed. Full 3-seed × 60-epoch is the operator heavy step (DD #10).

### References

- [Source: epics.md#L741-L783] — Story 7.5 ACs, FRs (FR-15/20/21), KDDs (B1/B2/B3-trigger-#5), pressure-release valve (7.5a/7.5b split; never drop below 2 seeds).
- [Source: epics.md#L587] — the three guardrails; Story 7.5 gates on 6.2 substrate + 6.5b runtime flip.
- [Source: _bmad-output/ml-training/ablation/kdd-b2-comparison-v1.md] — winner `maskedMelPretrain` (Acc1 88/88; ECE_half_double 0.5353<0.5542).
- [Source: _bmad-output/ml-training/ablation/masked-mel-recipe.md] — the pinned SSL recipe to reuse.
- [Source: Sources/BoomBoomBoomKitML/BNNSTechnique.swift:204,:364] — `supportedFeatureSetVersion` + abstain guard (AC8 target; the existing abstain test is `Tests/BoomBoomBoomKitTests/BNNSTechniqueTests.swift:107`).
- [Source: Sources/BoomBoomBoomKitML/BNNSTechnique.swift:526-626] — `featurize` transpose(`:545`)/z-score(`:574`)/resample-to-512(`:585-608`) — the parity seam (DD #15, AC1).
- [Source: _bmad-output/ml-training/test_feature_parity.py:45-48] — existing 4-stage tolerances (1e-4/1e-3) the 5th stage aligns to.
- [Source: Sources/BoomBoomBoomKit/FeatureSubstrate/OnsetFeatures.swift] — `OnsetFeatures` + `OnsetFeaturesBuilder` (`.uniform` only; `.subBandEmphasis` throws).
- [Source: _bmad-output/ml-training/corpus-diagnostics-v1.md] — `REVIEWER_SIGNOFF: pending` (KDD-B4 gate, DD #10).
- [Source: _bmad-output/ml-training/marginal-watchlist.json] — the 7.4 `v2Prediction` stub Story 7.5 fills (DD #9).
- [Source: _bmad-output/implementation-artifacts/7-4-marginal-tier-name-how-its-used-wiring.md] — prior-story patterns + the Marginal watchlist join contract.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (bmad-dev-story)

### Debug Log References

- `swift build` clean; `make test` 483/105 GREEN after the `featureSetVersion` bump + 12 coupled flips.
- `swift run dump-model-input` → two 65536-float ([128,512]) `v2` model-input tensors at `out/swift_stages/`.

### Completion Notes List

**DONE + verified (Swift / main-bound side + parity-dump foundation):**
- **Task 0 + Task 7 — `featureSetVersion` "v1"→"v2" bump (main-bound).** Shared constant `MLFeatureFrames.currentFeatureSetVersion`; `BPMAnalyzer.swift:939` + `BNNSTechnique.supportedFeatureSetVersion` reference it. 6 doc-comments updated. **Coupled-flip count was 12, not 4** (the review/Codex under-counted): 4 emitted-version assertions + 8 `evaluate`-feeding carrier-args (`BNNSTechniqueDiagnosticTests:102/149/175`, `BNNSTechniqueTests:143/175/366/423/429`) that the version guard couples — the non-gated Diagnostic pair `:102/:149` failed the suite and confirmed it. NO new test (the abstain guard is already covered by `evaluateAbstainsOnVersionMismatch`). `make test` 483/105 green.
- **Task 1 — public parity seam + Swift dump CLI.** `BNNSTechnique.modelInputTensor(from:)` (pure `public static`, single-sources the runtime transpose/z-score/resample-to-512); new develop-only `dump-model-input` SPM target dumps the post-featurize `[128,512]` tensor. Built + ran green.

**DONE + verified (develop-only Python training harness):**
- **Task 2/3 — substrate↔runtime parity.** `feature_substrate_v2.py` (numpy reconstruction: `extract_log_mel` mel-major → featurize-faithful z-score [population std + constant-row zeroing] → `vDSP_vlint`-matching resample-to-512). `test_feature_parity.py` gains stage 5 (post-featurize `[128,512]` vs the Swift dump) at **1e-4/1e-3**. `make ml-parity` → OVERALL **PASS** (click binding gate `max_abs 1.13e-4`). Critically it RESAMPLES to 512 (matching runtime), NOT the legacy `crop_or_loop_to_target` the GiantSteps ablation used — that train/runtime mismatch is the FR-21 bug this story fixes.
- **Task 4 — AC10 gate + weighting-profile (verified); below-gate loop operator-gated.** `check_substrate_preconditions()` aborts with `SubstratePreconditionError` on the pending KDD-B4 signoff (DD #16 dev-done); `--weighting-profile` required+`uniform`-only. The Tony-loader + masked-mel + substrate-feature loop rewire is documented as the operator's gated full-run path (reuses 7.3 ablation; unreachable in dev because the gate aborts first).
- **Task 5/6/8 — pure-Python downstream contracts (unit-tested, 14/14).** `train_v2_artifacts.py`: `build_seed_metadata` (per-seed schema, promotable:true, featureSetVersion "v2", modelGeneration "v2" per DD #14, splitVersionHash), `compute_split_version_hash` (order-independent), `build_marginal_stability` (per-track per-seed v2Prediction fill, octave-normalized agreement, matchesExpected→7.6), `feature_distribution_sanity` (substrate-vs-librosa guard). `train_runnerup.py` (single-source delegation to the ablation runner-up arm).

**Gates (all green):** `swift build` clean · `make test` 483/105 · `make ml-parity` 5 stages PASS · `make py-lint` (ruff+format+ty) green · ablation pytest 81 passed · `train.py` gate aborts with the named error.

**Implementation review (post-dev) — Codex diff-review + Blind Hunter + party-mode Winston/Amelia.** 2 BLOCKER + 6 SHOULD-FIX/NICE found and FIXED + regression-tested:
- **B1 (Blind Hunter):** `resample_to_width_vlint` used `np.arange(width)*step` (multiply) but Swift `vDSP_vramp` ACCUMULATES in float32 → diverged on long tracks (Tony F=10k–60k; the sine/click parity signals F≤496 couldn't catch it = real feature corruption on real audio). Fixed: float32 accumulation matching `vDSP_vramp`. Regression test (linear ramp @ F=20000 stays linear).
- **B2 (Codex+Blind Hunter):** `_octave_fold` infinite-looped on `+inf` (reproduced hang). Fixed: `math.isfinite` guard + `octave_normalized_agreement` filter. Regression test (no hang, returns False).
- **Winston:** the click residual `1.13e-4` (razor-thin, passing only on rel) was float64-vs-float32 mean/std accumulation — forced float32 reduction → residual now **`2.81e-5`** (~4× under tolerance).
- **S1 (all three — load-bearing landmine):** `train.py` still had the legacy GiantSteps `build_splits` reachable below the gate → an operator who signs off without the loop rewire would silently train a mismatched model. Fixed: **fail-closed** — `raise NotImplementedError` unless `--allow-legacy-giantsteps`; the v2 substrate loop is the operator's explicit wire-up.
- **S2 (Blind Hunter):** AC10(b) `func select(` substring could match a comment → now requires `func select(` AND `UnifiedSignalPool`.
- **S3 (Codex):** seed-completeness — `filled-7.5` now requires all 3 seeds; partial → `incomplete-7.5` + `missingSeeds` (7.6 won't grade off a partial set).
- **Amelia #3:** `modelInputTensor` is now `@_spi(FeatureParity)` (out of the stable public API; CLId reaches it via `@_spi` import) — Epic 6↔8 seam convention.
- **Amelia #4:** Swift↔Python version pairing pinned (`feature_substrate_v2.FEATURE_SET_VERSION` + test). **Amelia #2:** `train_runnerup` delegation smoke test. **N1:** `feature_distribution_sanity` length-mismatch now FAILS (was vacuous-pass on empty).
- **Deferred (noted, non-blocking):** Winston #2 (a real-audio binding parity fixture — the B1 fix + float32 tightening address the numerical risk; a real-DnB gate is a worthwhile follow-up before the operator's multi-seed run); N2 (watchlist `track_id` dedup).

**Operator-owned (per DD #16, NOT dev-agent):** the KDD-B4 corpus signoff; the below-gate Tony-loader/masked-mel loop swap; the full 3-seed × 60-epoch MPS runs producing `giantsteps_v2_seed_{42,43,44}.mlmodel` + per-seed metadata + `training_log.json` + the on-disk `marginal-watchlist-stability.json` / `feature-distribution-sanity.json`.

### File List

- `Sources/BoomBoomBoomKit/FeatureSubstrate/MLFeatureFrames.swift` (M — added `currentFeatureSetVersion = "v2"`; doc)
- `Sources/BoomBoomBoomKit/BPMAnalyzer.swift` (M — emit `MLFeatureFrames.currentFeatureSetVersion`)
- `Sources/BoomBoomBoomKit/FeatureSubstrate/OnsetFeatures.swift` (M — doc `"v1"`→`"v2"`)
- `Sources/BoomBoomBoomKit/MLDiagnosticSnapshot.swift` (M — doc `"v1"`→`"v2"`)
- `Sources/BoomBoomBoomKitML/BNNSTechnique.swift` (M — `supportedFeatureSetVersion` → constant; `modelInputTensor(from:)` public seam; docs)
- `Tests/BoomBoomBoomKitTests/FeatureSubstrateTests.swift` (M — `:74` assertion `"v2"`)
- `Tests/BoomBoomBoomKitTests/MLFeatureFramesTests.swift` (M — `:340/:383/:463` assertions `"v2"`)
- `Tests/BoomBoomBoomKitTests/BNNSTechniqueDiagnosticTests.swift` (M — `:102/:149/:175` carrier-args `"v2"`)
- `Tests/BoomBoomBoomKitTests/BNNSTechniqueTests.swift` (M — `:143/:175/:366/:423/:429` carrier-args `"v2"`)
- `_bmad-output/ml-training/swift_feature_extractor/Sources/dump-model-input/main.swift` (A — new CLI)
- `_bmad-output/ml-training/swift_feature_extractor/Package.swift` (M — new `dump-model-input` target)
- `_bmad-output/ml-training/feature_substrate_v2.py` (A — v2 model-input producer; Task 2)
- `_bmad-output/ml-training/test_feature_parity.py` (M — stage 5 model-input parity; Task 3)
- `_bmad-output/ml-training/train.py` (M — AC10 precondition gate + `--weighting-profile` + v2 metadata; Task 4)
- `_bmad-output/ml-training/train_v2_artifacts.py` (A — per-seed metadata + stability + sanity producers; Tasks 5/6/8)
- `_bmad-output/ml-training/train_runnerup.py` (A — runner-up delegation; Task 5)
- `_bmad-output/ml-training/ablation/tests/test_train_v2_artifacts.py` (A — 14 unit tests; Tasks 5/6/8)
- `Makefile` (M — `feature_substrate_v2.py` + `train_v2_artifacts.py` added to the ty allowlist)
