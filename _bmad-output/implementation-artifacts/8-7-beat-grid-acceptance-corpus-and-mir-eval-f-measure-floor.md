---
baseline_commit: f5f2574c9c399467f874f93fc093b50e95f8b11b
---

# Story 8.7: Beat-grid acceptance corpus + benchmarks — F-measure floor + beat-position tolerance + downbeat validation

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Dependency status (resolved — dev is unblocked)

**The Story 8-5 contract this story baselines against has merged; no task is gated on an unmerged dependency.** PR #38 (Story 8-5: long-file sync stability, BPM/beat-grid consistency, grid anchor) merged into `rterhaar/epic-8` on 2026-06-14 (commit `e2dea60`, in this branch's history), followed by #39 (8-5a downbeat), #40 (8-5b beat-grid hardening), and #41 (8-6 ModelRegistry). The `BeatGrid` + `tempoAgreement` consistency contract and the FR-29 drift behavior are stable, so the accuracy ACs (AC4/AC5/AC6/AC7) baseline against a merged, fixed contract. (Historical note: this story was drafted while #38 was still in review; the per-task "gate on #38" annotations have been removed now that it is merged.)

## Story

As a **library maintainer**,
I want **beat-grid accuracy validated against the OA300 corpus (DAW-oracle ground truth) plus a stratified DnB subset — F-measure floor and beat-position tolerance committed as numeric ACs, AND a real-audio validation of the Story-8.5a downbeat detector (fire rate, correctness-when-fired, abstain rate)**,
so that **Epic 8 cannot close without committed beat-grid accuracy gates equivalent to the FR-10 BPM accuracy floors, future regressions surface immediately, and the opt-in downbeat feature that reaches a live DJ deck stops being "trust me, it's conservative" and becomes a measured number.**

## Acceptance Criteria

1. **JAMS beat-oracle generation (develop-only).** A new develop-only script `scripts/dawproject-beats.py` (sibling of `scripts/dawproject-bpm.py`) extracts per-beat positions from the existing OA300 `.dawproject` source and emits `daw-oracle-beats.json` as a **multi-track JAMS corpus** (DD-9): a top-level `{ "entries": [ <JAMS object>, … ] }` wrapper — matching the repo's existing multi-track precedent at `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/12-dnb-sentinels-expanded.json` — where **each `entries` element is a standalone-valid JAMS object** (top-level `file_metadata` + `annotations`, exactly as single-file JAMS specifies). Per entry: `file_metadata` carries `identifiers` (filename) + `artist`/`title`/`duration`; `annotations` carries one annotation with `namespace: "beat"` and a sparse observation list (per-beat `time` + a `value` = bar-phase position `1 ... beatsPerBar` where `1` marks a downbeat, **`null` when the bar-phase is unknown — never `0`/absent**, per JAMS `beat`-namespace semantics — + `confidence` + zero `duration`); `annotation_metadata.curator.{name,email}` + `annotation_metadata.data_source = "DAW manual placement"` document provenance. The wrapper must NOT leak corpus-level fields into the per-track objects (keeps a future split-to-N-`.jams` lossless — DD-9). `make oracle-generate-beats` regenerates it via `uv run scripts/dawproject-beats.py` and writes to `${OA300_CORPUS_PATH}/daw-oracle-beats.json` (NOT committed — corpus-dir artifact, mirroring `daw-oracle.json`). The `jams` Python dependency is added to `_bmad-output/ml-training/pyproject.toml` (develop-only, NOT shipped to `main`). [Source: epics.md#Story-8.7 (lines 1156-1158); scripts/dawproject-bpm.py precedent; DD-2, DD-6]

2. **Swift JAMS decoder (ships to main, in the benchmark target).** A Swift JAMS decoder lands at `Tests/BoomBoomBoomKitBenchmarkTests/Helpers/JAMSDecoder.swift` (~100-150 LOC of `Codable` structs matching the JAMS schema) — **NOT** in `BoomBoomBoomKitTestSupport` (which is `public` and would leak a JAMS decoder onto downstream consumer test packages — DD-7). Top type is `JAMSCorpus { entries: [JAMSFile] }` (the multi-track wrapper, DD-9); each `JAMSFile { fileMetadata, annotations }` is standalone-valid JAMS. It decodes `file_metadata` + `annotations` and supports the `tempo`, `beat`, and `tag_open` namespaces (enough for our annotation surface — `beat` for 8.7, `tempo` for 8.8's migrated artifacts), exposing each beat observation's `time` + `value` (bar-phase, **optional**: `JAMSValue?` — `null` round-trips as `nil`) + `confidence`. It **rejects an unknown namespace with a typed `JAMSDecodingError`** rather than silently defaulting. The decoder + the Python generator (Task 1) + the F-measure sidecar (Task 3) MUST agree byte-for-byte on the `annotations[].data` beat-time field path — that path is the real Swift↔Python parity surface (DD-9). A small committed JAMS fixture (`Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/jams-decoder-sample.json`) + a non-env-gated `JAMSDecoderTests` suite exercise round-trip decode + the unknown-namespace rejection. [Source: epics.md#Story-8.7 (lines 1164-1166); DD-7]

3. **Beat-grid F-measure benchmark via mir_eval sidecar.** A new env-gated test `Tests/BoomBoomBoomKitBenchmarkTests/BeatGridBenchmarkTests.swift` + `make benchmark-beatgrid`: with `OA300_CORPUS_PATH` set, the Swift test runs `analyzeBeatGrid(url:options:)` (or `analyze(url:options:)`) per track, emits per-track estimated beat positions to a temp `.json` in JAMS shape, then invokes a develop-only Python sidecar `_bmad-output/ml-training/eval-beatgrid.py` that calls `mir_eval.beat.f_measure(reference_beats, estimated_beats)` (±70 ms tolerance) against the JAMS reference `${OA300_CORPUS_PATH}/daw-oracle-beats.json`; per-track F-measure is logged to `_bmad-output/implementation-artifacts/8-7-beat-grid-accuracy.json`. **Coverage-parity (DD-10) is an asserted in-test invariant, NOT a `>= N` floor:** the count of evaluated tracks equals the count of analyzable OA300 fixtures; every analyzable fixture appears in the evaluated set exactly once (an absent one FAILS the test); every oracle row maps to a real analyzable track; each evaluated track has a non-empty `beats[]`. This replaces the `dawOracle.count >= 20` smoke-test pattern (`DAWOracleBenchmarkTests.swift:91`) — a floor measured over an unpinned population is theater. The `mir_eval` dependency is added to `_bmad-output/ml-training/pyproject.toml` (develop-only — consistent with the `tools/coreml-convert/` DD #13 scope that confines Python tooling to `_bmad-output/` and `tools/coreml-convert/`). `mir_eval` is pinned to a major version in `pyproject.toml` (the epic's version-pin guard). [Source: epics.md#Story-8.7 (lines 1140-1142); DD-6, DD-10]

4. **Measure-then-floor on the corpus F-measure (NOT a blind 0.75).** The committed corpus floor is **derived from a measured calibration run, not asserted blind.** Task 5 runs the benchmark once against the current (post-#38) beat-tracker, records the actual mean OA300 F-measure in `8-7-beat-grid-accuracy.json` and in this story's Completion Notes, and the locked Swift `#expect` floor is set at `measured − margin` (margin ≈ 0.03, the FR-10 BPM-floor precedent of committing the floor below the measured value). **The calibration run that establishes `measured` MUST satisfy the AC3 coverage-parity invariant (DD-10) — a partial calibration population invalidates the lock**, otherwise `measured − 0.03` is anchored to a flattering subset and the floor is permanently soft. The epic's `≥ 0.75` is the **published target**, not a measured fact (DD-1): if the measured mean is `≥ 0.75`, lock the gate at `0.75`; if it is below, lock the gate at `measured − margin` AND invoke the pressure-release valve (document the actual number, reopen Story 8.4 for refinement — do NOT silently weaken to fit). [Source: epics.md#Story-8.7 (line 1142) + Pressure-release valve; FR-10 BPM-accuracy-floor precedent; DD-1]

5. **Stratified DnB subset floor (measure-then-floor, target ≥ 0.65).** The DnB subset is defined by an **explicit, committed genre predicate over the corpus** (the OA300 `genre == "drum-and-bass"` rows — `Sources/BoomBoomBoomKitTestSupport/CorpusTracks.swift:20` carries `genre`), NOT a hand-listed file that could be cherry-picked (DD-10). Its membership and minimum size are asserted in-test; every subset filename must exist in BOTH the OA300 ground truth and the beat oracle. The committed fixture `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/beat-grid-dnb-subset.json` records the resolved membership for review, but the predicate is the source of truth. Mean F-measure on the subset is measured, reported track-by-track in `8-7-beat-grid-accuracy.json`, and the `#expect` floor is committed at `measured − margin` with the epic's `≥ 0.65` as the published target (lower floor for the harder genre per KDD-C1). Same pressure-release + calibration-population discipline as AC4 if below target. [Source: epics.md#Story-8.7 (lines 1144-1146); DD-1, DD-10]

6. **FR-29 P95 drift gate (uses the real `tempoAgreement`/`gridOrigin` API).** On OA300 tracks ≥ 5 minutes long, the benchmark measures last-beat absolute drift vs the `gridOrigin`-extrapolated expectation `gridOrigin.presentationTime + (60.0/estimatedTempo) · (n − gridOrigin.beatIndex)` — the anchor can sit at **any** beat index, NOT beat 0 (`BeatGrid.swift:104` only guarantees `gridOrigin.beatIndex` indexes a real `beats` entry), so the extrapolation MUST offset by `gridOrigin.beatIndex`; this is the documented mid-track contract — NOT trusting each `beats` entry. The **P95 absolute drift ≤ 30 ms** is locked as a Swift Testing `#expect`. The cross-reference to the consistency contract uses the **actual shipped field `BeatGrid.tempoAgreement: TempoAgreement`** (`.notCompared`/`.agree`/`.octaveEquivalent(factor:)`/`.disagree`) — the epic's `tempoAgreedWithBPMStage: Bool?` name is stale (DD-4). [Source: epics.md#Story-8.7 (lines 1148-1150); Sources/BoomBoomBoomKit/BeatGrid.swift:74,89,100; DD-4]

7. **Downbeat validation — Story-8.5a fire rate, correctness-when-fired, abstain rate (folded-in scope).** A new section of the benchmark runs `analyze(url:options:)` with `Options.detectDownbeats = true` over the corpus (corpus-wide where the JAMS bar-phase oracle is derivable; otherwise scoped to the DnB subset — DD-5) and emits `_bmad-output/implementation-artifacts/8-7-downbeat-accuracy.json` with three measured quantities: (a) **fire rate** — fraction of evaluated tracks where `beatGrid.downbeats` is `.detected(estimate:)` rather than `.noneDetected` — **REPORTED, not gated** (8.5a is conservative-by-design; a low fire rate is acceptable); (b) **downbeat-phase correctness-when-fired** — measured over **ALL** detected downbeats, not just the first: the precision of `estimate.beats` (every detected bar start, `DownbeatEstimate.swift:93`) against oracle downbeats (non-nil `value == 1`) within ±70 ms, requiring sustained recurring-phase agreement across the track (a grid that nails downbeat #1 then drifts phase by bar 8 is the exact "wrong downbeat is worse than none" failure and MUST score as incorrect) — **GATED** at a measured floor (`measured − margin`), because this is the safety-critical metric (8.5a's thesis: precision-when-fired is what must hold). First-detected-only would measure onset, not the sustained phase a DJ deck actually trusts; (c) **abstain rate** — fraction `.noneDetected` — **REPORTED**. No blind floor anywhere; measure first, then commit the one gate. [Source: operator party-mode decision 2026-06-14; Sources/BoomBoomBoomKit/AudioAnalysisService.swift:340 (`detectDownbeats`), :1528 (`analyze` URL overload; decoded overload :1574); DownbeatResult.swift:46; DownbeatEstimate.swift:93; DD-5]

8. **Combined wall-clock is already gated — reference, don't duplicate.** The epic's "combined analysis ≤ 1.25× BPM-only" perf check is **already implemented** by Story 8-4's `make beat-grid-impact-report` (per-format +grid ≤ 1.25× BPM-only) and Story 8-5's `make combined-analyze-impact-report` (shared-decode gate). This story does NOT re-implement a perf gate; it cites those targets and confirms they still pass with `detectDownbeats` wired (the downbeat path is opt-in and only forces sub-band envelopes, never BPM selection). [Source: epics.md#Story-8.7 (lines 1152-1154); Makefile `beat-grid-impact-report`/`combined-analyze-impact-report`; DD-8]

9. **README + MODEL_CARD epic-close assembly (ships to main).** The README carries coherent sections for `analyzeLUFS` (added 8.1), `analyzeBeatGrid` (ensure/assemble), and `ModelRegistry` (added 8.6); `MODEL_CARD.md` references the beat-grid acceptance floor (the measured F-measure + the ±70 ms tolerance + the corpus). Public-facing prose only — no story numbers, no BMAD/KDD/JAMS-internal jargon beyond what a consumer needs, no aubio. [Source: epics.md#Story-8.7 (lines 1160-1162); memory: feedback_no_aubio_in_public, feedback_no_business_jargon_in_commits]

10. **No Epic-6 promotion; no regression / BPM byte-identity; ship discipline.** Per Mary's seam-mitigation rule, this story promotes **no** Epic-6 internal surface — it is a benchmark/corpus + epic-close story, and its README work assembles already-public surfaces (DD-3); the spec states this explicitly so a reviewer does not flag a missing promotion. No `Sources/` pipeline edits, so the default `analyzeBPM` path is byte-identical by construction; `make test` (the non-benchmark suite) stays green; `make fmt` idempotent; `make lint` clean (only the pre-existing `LUFSAnalyzer.swift:135` baseline). Ship split: Swift test infra (`JAMSDecoder.swift`, `BeatGridBenchmarkTests.swift`, the committed fixtures, the Makefile targets) ships to `main`; Python sidecars (`scripts/dawproject-beats.py`, `_bmad-output/ml-training/eval-beatgrid.py`), the pyproject deps, and the accuracy JSON reports stay on develop. `make benchmark-beatgrid` fails loudly on a main-only checkout (its Python sidecar is develop-only — the intentional `ml-*`-target convention; consumers don't run it). [Source: epics.md#Story-8.7 seam note; CLAUDE.md ship rules; DD-3]

## Tasks / Subtasks

- [x] **Task 1 — JAMS beat-oracle generator + pyproject deps** (AC: 1)
  - [x] Add `mir_eval` and `jams` to `_bmad-output/ml-training/pyproject.toml` `dependencies` (develop-only). Pin `mir_eval` to a major version (epic version-pin guard). Run `make ml-train-deps` (or `uv sync`) to lock.
  - [x] Create `scripts/dawproject-beats.py` (develop-only, stdlib + `jams`; `uv run` shebang block like `scripts/dawproject-bpm.py`). Reuse the `.dawproject` ZIP + `project.xml` parsing from `dawproject-bpm.py`; derive per-beat times from warp markers + `Tempo`, and per-beat **bar-phase** (`value` `1 ... beatsPerBar`) from the arrangement's time-signature / bar grid where available (DD-5: if bar-phase is not reliably derivable corpus-wide, emit beats with `value = null` — NOT `0` (per JAMS `beat` semantics) — and mark the track so AC7 scopes downbeat eval to the hand-verified subset).
  - [x] Emit the **multi-track corpus** (DD-9): top-level `{ "entries": [...] }`, one standalone-valid JAMS object per track, each with one `beat`-namespace annotation, sparse observation list (`time`, `value` (null when bar-phase unknown), `confidence`, `duration = 0`), `file_metadata.identifiers` = filename, `annotation_metadata.curator.{name,email}` from the git user config (CLAUDE.md), `annotation_metadata.data_source = "DAW manual placement"`. Hard-fail (do NOT warn-and-emit, contrast `dawproject-bpm.py:180`) if a `.dawproject` clip cannot be matched to a known fixture — a partial oracle must not silently produce a consumable file (AC4 coverage-parity, DD-10).
  - [x] Add `make oracle-generate-beats` (Makefile; mirrors `oracle-generate`) → `uv run scripts/dawproject-beats.py ... > ${OA300_CORPUS_PATH}/daw-oracle-beats.json`. Develop-only target; document it fails loudly on a main-only checkout.
- [x] **Task 2 — Swift `JAMSDecoder.swift` + unit fixture** (AC: 2)
  - [x] Create `Tests/BoomBoomBoomKitBenchmarkTests/Helpers/JAMSDecoder.swift` (new `Helpers/` dir). `Codable` structs: `JAMSCorpus { entries: [JAMSFile] }` (the multi-track wrapper, DD-9), `JAMSFile { fileMetadata, annotations }` (standalone-valid JAMS), `JAMSFileMetadata { identifiers, artist?, title?, duration? }`, `JAMSAnnotation { namespace, data: [JAMSObservation], annotationMetadata }`, `JAMSObservation { time, value: JAMSValue?, confidence?, duration }`. The `namespace` decodes into an enum supporting `.tempo` / `.beat` / `.tagOpen`; an unknown raw value throws `JAMSDecodingError.unknownNamespace(String)` (NOT in `BoomBoomBoomKitTestSupport` — keep it benchmark-target-internal, DD-7).
  - [x] `value` is **optional** and namespace-polymorphic (`tempo` → BPM Double; `beat` → bar-phase Int/Double; `null` → `nil`). Model it as `JAMSValue?` with a custom single-value-container `Codable` (decode number; JSON `null`/absent → `nil`). This resolves the prior optional-vs-required ambiguity: `value` is ALWAYS `JAMSValue?`, never a non-optional field. Downbeat eval (AC7/Task 6) treats only non-nil `value == 1` as an oracle downbeat. One decoder round-trips `tempo` (8.8) and `beat` (8.7).
  - [x] Commit `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/jams-decoder-sample.json` (tiny hand-written corpus: `entries` with one JAMS object, one `beat` annotation, two observations — one with `value: 1`, one with `value: null`) + add `JAMSDecoderTests` (`import Testing`; NOT env-gated): assert (a) round-trip decode of times/values incl. the `null` → `nil` case, (b) a **decode→encode→decode identity** `#expect` (the estimated-JAMS encode path Task 4 relies on — covers `null`, `beat`, `tempo` variants, F9), and (c) `JAMSDecodingError.unknownNamespace` on a bogus namespace.
- [x] **Task 3 — Python F-measure sidecar `eval-beatgrid.py`** (AC: 3)
  - [x] Create `_bmad-output/ml-training/eval-beatgrid.py` (develop-only). CLI: `--estimated <jams.json> --reference <daw-oracle-beats.json> --out <accuracy.json> [--subset <dnb-subset.json>]`. Per track: load both JAMS, extract beat times, call `mir_eval.beat.f_measure(ref, est)` with the ±70 ms `f_measure_threshold`. Emit aggregate mean + per-track F-measure to the `--out` JSON; emit a separate subset mean when `--subset` is passed.
  - [x] `uv run`-compatible; ruff-clean (covered by the `make py-lint` ruff glob over `_bmad-output/ml-training/` + `scripts/`). **For `ty`: `ty check` is an explicit file LIST in the Makefile (`Makefile:333`), NOT a glob — add `eval-beatgrid.py` (and `scripts/dawproject-beats.py` from Task 1) to that `ty check` command** so they are actually type-checked; otherwise the "ty-clean" claim is vacuous (DD-11).
- [x] **Task 4 — `BeatGridBenchmarkTests.swift` + `make benchmark-beatgrid`** (AC: 3, 6)
  - [x] Create `Tests/BoomBoomBoomKitBenchmarkTests/BeatGridBenchmarkTests.swift` (env-gated on `OA300_CORPUS_PATH`, init throws if unset — mirror `OA300BenchmarkTests`/`DAWOracleBenchmarkTests` init pattern). Load the JAMS reference via `JAMSDecoder`. Per track: `analyzeBeatGrid(url:options:)`, collect `beats[].presentationTime`, write a JAMS-shaped estimated file to a temp dir (encode via the same `Codable` `JAMSDecoder` structs — they round-trip both ways). **Orchestration boundary (DIRECTIVE — DD-12):** the Swift test emits the estimated JAMS to a known path; `make benchmark-beatgrid` then runs `eval-beatgrid.py` as a second step (the FR18EvaluationHarnessTests make-orchestration precedent). Do NOT `Process`-invoke Python from inside the Swift test — it couples a main-bound test to a develop-only script + a Python venv and is CI-fragile. The Swift test asserts coverage-parity (AC3/DD-10) on the corpus it iterates; the F-measure floor `#expect` reads `eval-beatgrid.py`'s emitted accuracy JSON. Document the wiring in Completion Notes.
  - [x] FR-29 drift sub-test (AC6): for OA300 tracks ≥ 5 min, compute last-beat absolute drift vs the `gridOrigin`-extrapolated expectation `gridOrigin.presentationTime + (60.0/estimatedTempo) · (lastIndex − gridOrigin.beatIndex)` — offset by the anchor's `beatIndex`, which is NOT guaranteed to be 0 (`Sources/BoomBoomBoomKit/BeatGrid.swift:104`); `#expect` P95 ≤ 30 ms.
  - [x] Add `make benchmark-beatgrid` (Makefile; mirror `benchmark`/`beat-grid-impact-report` env-threading). Document the develop-only Python-sidecar dependency.
- [x] **Task 5 — Calibration run → commit measured floors** (AC: 4, 5)
  - [x] Run `make benchmark-beatgrid` against OA300 once on the post-#38 tree. Record actual mean OA300 F-measure + DnB-subset mean in `8-7-beat-grid-accuracy.json` and in Completion Notes.
  - [x] Build the stratified DnB subset `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/beat-grid-dnb-subset.json` from the OA300 DnB tracks (filenames + reference).
  - [x] Lock the two `#expect` floors at `measured − 0.03` (target `0.75` corpus / `0.65` DnB; if measured ≥ target lock at target). If measured < target, write `_bmad-output/implementation-artifacts/8-7-pressure-release.md` (actual number + the Story-8.4-reopen recommendation) — do NOT weaken silently.
- [x] **Task 6 — Downbeat validation** (AC: 7)
  - [x] Add a downbeat section to `BeatGridBenchmarkTests` (or a sibling `DownbeatBenchmarkTests`): `analyze(url:options:)` with `options.detectDownbeats = true`, read `result.beatGrid?.downbeats`. Derive oracle downbeats from the JAMS `beat` observations where `value == 1`.
  - [x] Compute + emit `8-7-downbeat-accuracy.json`: fire rate (reported), abstain rate (reported), correctness-when-fired computed over **ALL** of `estimate.beats` vs oracle downbeats (precision within ±70 ms + sustained recurring-phase agreement — NOT first-detected-only; `DownbeatEstimate.swift:93` carries every detected bar start + `phaseIndex`).
  - [x] `#expect` correctness-when-fired ≥ `measured − margin` (the only downbeat gate). Scope to the DnB subset if corpus-wide bar-phase is not reliably derivable (DD-5); state the scope in the test doc comment + Completion Notes.
- [x] **Task 7 — README + MODEL_CARD (ships to main)** (AC: 9)
  - [x] Ensure README has coherent `analyzeLUFS` / `analyzeBeatGrid` / `ModelRegistry` sections (add the `analyzeBeatGrid` section if 8.4/8.5 did not; verify the other two read cleanly). Public prose only.
  - [x] Add the beat-grid acceptance floor (measured mean F-measure, ±70 ms tolerance, OA300 corpus) to `MODEL_CARD.md`.
- [x] **Task 8 — Gates + ship audit** (AC: 8, 10)
  - [x] `make fmt` idempotent; `make lint` baseline-only; `make test` (non-benchmark suite) green; `make py-lint` clean for the two new Python files — **after** adding both to the `ty check` explicit file list (`Makefile:333`), since that command does not glob (DD-11).
  - [x] Confirm the perf gates still pass: `make beat-grid-impact-report` + `make combined-analyze-impact-report` (no new perf gate authored here — AC8).
  - [x] Ship audit: `git diff` confirms Swift test infra (`JAMSDecoder.swift`, `BeatGridBenchmarkTests.swift`, fixtures, Makefile targets) + README + MODEL_CARD are main-bound; `scripts/dawproject-beats.py`, `_bmad-output/ml-training/eval-beatgrid.py`, the pyproject deps, and the accuracy JSONs are develop-only. Confirm `Package.swift` is untouched (zero new SPM deps).

## Dev Notes

### OPERATOR RE-SCOPE 2026-06-15 — beat oracle is the Rekordbox grid, not the OA300 dawproject (DD-13 … DD-17)

During dev the operator redirected the oracle source away from the OA300 `.dawproject` (the spec-as-written source) to **Tony's Rekordbox-gridded corpus**. Decision recorded via `AskUserQuestion`: F-measure oracle = "Tony Rekordbox grids"; downbeat oracle = "Tony Rekordbox Battito". This is an authorized amendment to AC1/AC3/AC4/AC5/AC7. The OA300 dawproject path is dropped from this story.

- **DD-13 — Oracle source = Rekordbox `<TEMPO>` beat grid.** The corpus is the Rekordbox XML export at `/Users/rterhaar/Dropbox/tony-tunes/05092026.xml` (`TONY_XML`) against the on-disk audio tree `/Users/rterhaar/Dropbox/tony-tunes` (`TONY_AUDIO_ROOT`). Per-track beats come from the `<TEMPO Inizio Bpm Metro Battito>` markers: each marker defines a grid segment `[Inizio_i, Inizio_{i+1})` with beats at `Inizio_i + k·(60/Bpm_i)` and bar-phase advancing from `Battito_i`; the last segment extends to `TotalTime`; the first marker back-fills to `t ≥ 0`. **All 1719 gridded tracks are 4/4** (verified — `Metro` is uniformly `4/4`). 1224 are single-anchor (constant tempo, pure anchor+BPM extrapolation), 495 are multi-anchor (hand-adjusted — closely-spaced markers ARE individual hand-placed beats, a genuine per-beat oracle in variable sections). The generator is `scripts/rekordbox-beats.py` (NOT `dawproject-beats.py` — honest naming for the real source), reusing the XML parse + basename audio-resolution from `scripts/tony-tunes-survey.py`.
- **DD-14 — `Battito` IS the JAMS bar-phase `value` (authoritative, never null).** Rekordbox `Battito ∈ {1,2,3,4}` is the beat-in-bar; `Battito == 1` is the downbeat. This populates the JAMS `beat`-namespace `value` directly — so the DD-5 "`value = null` corpus-wide + hand-verified-subset fallback" never fires. The downbeat oracle (AC7) is fully populated corpus-wide, which is strictly stronger than the dawproject could give.
- **DD-15 — Half-time tagging → octave-normalized F-measure is the gated metric.** ~77% of the corpus (1035/1348 resolved tracks) is tagged at half-time (80 BPM ≈ 160-BPM DnB; a known Tony-corpus property — see memory project_tony_bpm_truth_labeling). Our beat tracker tracks the full tempo, so raw `mir_eval.beat.f_measure` would be dominated by a 2× beat-density mismatch (precision ≈ 0.5) and measure tagging convention, not tracking quality. The sidecar therefore computes **both** a raw F-measure AND an **octave-normalized** F-measure (up/down-sample the estimated beats to the oracle's tempo octave when the two tempos are ~2×/~½× — the same octave tolerance the BPM Acc2 metric already uses). The committed `#expect` floor gates the **octave-normalized** mean (the DJ-sync-relevant number, consistent with `TempoAgreement.octaveEquivalent`); raw is reported alongside. Pure measure-then-floor (DD-1) still governs the locked value.
- **DD-16 — AC5 DnB-subset floor is folded into the main floor.** The XML `Genre` is empty on 88% of tracks (1518/1719), so a committed genre-predicate DnB subset (AC5 as written) is not constructible. The corpus is DnB-dominant by construction (Tony's library), so the corpus floor IS effectively the DnB floor. AC5's separate `≥ 0.65` sub-floor is dropped; the single corpus floor (AC4) carries the intent. Stated in Completion Notes.
- **DD-17 — Corpus = gridded ∧ on-disk-resolved ∧ analyzable; coverage-parity preserved.** ~1348 tracks have a `<TEMPO>` grid AND resolve on disk (373 are cloud-only/missing and are excluded by construction, not by survivorship). The DD-10 coverage-parity invariant still holds against THIS denominator: the generator hard-fails if a gridded+resolved track yields no beats; the Swift benchmark asserts every oracle entry maps to an analyzed track and vice-versa. The benchmark is env-gated on `TONY_XML` + `TONY_AUDIO_ROOT` (mirroring `OA300_CORPUS_PATH` gating). FR-29 drift (AC6) scopes to Tony tracks ≥ 5 min.

- **DD-18 — Octave-tolerant downbeat correctness.** The half-time reality (DD-15) applies to downbeats too: our 4/4 bars at the full tempo are 2× as frequent as Rekordbox's half-time bars. So downbeat correctness-when-fired is the octave-tolerant F (max over the estimate's tempo octaves) of our downbeats vs the oracle `Battito == 1` set, scored over ALL detected downbeats (sustained recurring-phase agreement — a grid that nails downbeat #1 then drifts phase scores as incorrect). Gated; fire-rate + abstain-rate reported.
- **DD-19 — Gated metrics scope to constant-tempo (single-anchor) tracks.** Tony's corpus has 354 variable-tempo (multi-anchor) tracks; our tracker is documented constant-tempo-only (`BeatGrid` DocC) and 8.5a's downbeat estimator assumes constant tempo + fixed 4/4. Evaluating a constant-tempo tracker against a variable-tempo oracle is out-of-domain, so all three gated metrics (F-measure, FR-29 drift, downbeat correctness) scope to the constant-tempo subset (910 tracks); variable-tempo numbers are reported, not gated. Coverage-parity (DD-10/DD-17) is still asserted over the FULL resolved corpus (we analyze all 1264; we only GATE on the constant subset). The oracle carries a `constant_tempo` flag (marker-count == 1) in the JAMS top-level `sandbox` — relocated there from `file_metadata` so each entry passes the real `jams` library's `jams.load(validate=True, strict=True)` (its typed `FileMetadata` rejects unknown keys); threaded into the Swift decoder (`JAMSFile.constantTempo`) + the Python sidecar (`entry["sandbox"]`).
- **DD-20 — Compare the EXTRAPOLATED grid, not raw beats (and the same for downbeats).** The grid a consumer syncs to is the `gridOrigin` + `estimatedTempo` extrapolation, NOT the raw detected beats (`BeatGrid` DocC: "extrapolate from the anchor, don't trust every beat"). Rekordbox's single-anchor grid IS such a constant extrapolation, so the apples-to-apples comparison is extrapolated-vs-extrapolated. The benchmark emits, as the estimated beats, our grid reconstructed from `gridOrigin` over the raw-beat index span; downbeats are likewise extrapolated from the first detected downbeat at the bar period. (Emitting raw beats instead measured 0.14 F; the extrapolated grid measured 0.37 — the raw-beat per-onset jitter the DocC warns about was corrupting the comparison.) The RAW last beat is still used for the FR-29 drift metric (that metric's entire point is "how far do raw beats stray from the extrapolation").
- **DD-22 — Post-implementation Codex diff-review fixes (measurement correctness).** A `codex:diff-review` pass flagged that the estimated grid was extrapolated only over the RAW detected-beat count (deflates recall when the tracker trims the intro/outro) and that `detectDownbeats: true` repoints `gridOrigin` to the first downbeat (`BeatGridAnalyzer.swift:354`), making the beat-grid anchor inconsistent between fired and non-fired tracks. Both fixed: the grid now extrapolates over the FULL `[0, duration]` span (`BeatMatch.extrapolate`), and the benchmark runs TWO passes — `detectDownbeats: false` for the stable-anchor beat-grid F-measure + FR-29 drift, `detectDownbeats: true` for the downbeat metric. Also: standard P95 index (`ceil(0.95·n)−1`) + a reverse-coverage warning in `eval-beatgrid.py` (oracle-rows-missing-from-estimated, recorded as `n_missing_constant`). Re-calibrated: F-measure 0.3655 → **0.3718** (full-span helped marginally), downbeat + drift unchanged — confirming the fixes were methodological, not number-changing. Codex's `rekordbox-beats.py` boundary math, `BeatMatch` one-to-one matcher, and `JAMSDecoder` null handling were reviewed clean. Round 2 (no merge blockers): separated a Pass-B downbeat *analysis failure* (a defect where Pass A succeeded) from a legitimate `.noneDetected` *abstain* — locked at zero via an `#expect` (measured `n_pass_failures == 0`), so the abstain rate can't be silently inflated; added a defensive `period.isFinite` guard to `extrapolate`.
- **DD-21 (outcome) — All gated metrics land far below the epic's published targets; pressure-release fired.** Measured (constant-tempo, ±70 ms): F-measure octave-normalized **0.372** (target 0.75), downbeat correctness-when-fired **0.144** at a **4.3%** fire rate, FR-29 P95 drift **1652 ms** (target 30 ms). Floors locked at `measured − margin` (0.33 / 0.10 / 2.0 s ceiling) as regression nets; `8-7-pressure-release.md` documents the actuals + recommends reopening Story 8.4 for tracker refinement. NOT silently weakened to fit (DD-1). This low-but-honest result IS the story's deliverable — the epic's 0.75/0.65/30 ms were unmeasured placeholders, and 8.7 is where they became measured numbers.

The README/MODEL_CARD (AC9) and the no-`Sources/`-edit byte-identity discipline (AC10) are unaffected by the re-scope. The Swift `JAMSDecoder` design (AC2/Task 2) is unchanged — it decodes the same JAMS `beat`/`tempo` shape regardless of which tool emitted it.

### Why this is the epic-closing story

Story 8.4 shipped a beat-tracker, 8.5 a consistency contract, 8.5a a downbeat detector — three new beat-grid behaviors with **no accuracy floor guarding any of them** (the same anti-pattern as a public type with no producer). This story is the regression net: it commits numeric F-measure + drift + downbeat gates equivalent to the FR-10 BPM-accuracy floors, so the epic cannot close on unproven beat-grid surface. It is also where the opt-in downbeat feature — which reaches a live DJ deck when a consumer flips `detectDownbeats = true` — gets a measured fire-rate/correctness number instead of the design-intention word "conservative".

### Sequencing vs Story 8.8 (operator decision 2026-06-14)

8.7 and 8.8 stay **separate**; 8.7 comes first. 8.7 **owns the shared JAMS plumbing** (the `JAMSDecoder.swift` + the `jams`/`mir_eval` pyproject deps); 8.8 (the one-time migration of `daw-oracle.json` / `oa300-ground-truth.json` / `4-dnb-triplet-targets.json` to JAMS) **reuses** that decoder later. Critically, 8.7 reads its **own** `daw-oracle-beats.json` (from `make oracle-generate-beats`), NOT 8.8's migrated `daw-oracle.json` — so 8.7 is self-contained and does not depend on 8.8 (verified in the pre-spec grep — DD-2). 8.8 is the natural next story after this one.

### Decisions / factual corrections to the epic ACs (READ BEFORE CODING)

From the mandatory pre-spec grep of the actual codebase + the operator party-mode reflection (2026-06-14):

- **DD-1 — The F-measure floors (0.75 / 0.65) are UNMEASURED placeholders, not baselines.** The grep found zero `mir_eval`/`jams` in `pyproject.toml` and zero existing beat-grid F-measure measurement anywhere in the repo. So the epic's `≥ 0.75` (OA300) and `≥ 0.65` (DnB) were written before the beat-tracker existed and have never been measured. Treat them as **published targets**, and commit the locked `#expect` floors from a measured calibration run (`measured − 0.03`), exactly as the FR-10 BPM-accuracy floors were set from measured ablation rather than guessed. The epic's pressure-release valve (document actual + reopen 8.4, don't weaken silently) is the contingency if measured < target. This is the single highest-risk AC in the epic — a wrong numeric floor is either un-passable or toothless. [memory: feedback_factual_claims_grep_pre_story, feedback_direct_about_failure]
- **DD-2 — JAMS schema is coherent across 8.7/8.8; 8.7 does not depend on 8.8.** One JAMS container shape; 8.7 emits the `beat` namespace and its decoder reads `tempo` + `beat` + `tag_open`; 8.8 emits the `tempo` namespace into the migrated artifacts. No two-schema drift. 8.7's benchmark reads its own `daw-oracle-beats.json`, generated by 8.7's own `make oracle-generate-beats` — there is no read dependency on 8.8's migration. This confirms "8.7 first, separate from 8.8."
- **DD-3 — No Epic-6 surface is promoted by this story.** 8.7 is a benchmark/corpus + epic-close story. The README AC assembles sections for already-public surfaces (`analyzeLUFS` public since 8.1, `analyzeBeatGrid` since 8.4/8.5, `ModelRegistry` since 8.6); nothing internal is promoted. Per Mary's seam-mitigation rule, the spec states this explicitly so a reviewer does not flag a missing promotion — a benchmark story legitimately promotes nothing.
- **DD-4 — `tempoAgreedWithBPMStage: Bool?` is stale; the shipped field is `tempoAgreement: TempoAgreement`.** Story 8.5 shipped the richer 4-case enum `.notCompared` / `.agree` / `.octaveEquivalent(factor:)` / `.disagree`, defined in its OWN file `Sources/BoomBoomBoomKit/TempoAgreement.swift:38-58` (the `BeatGrid.tempoAgreement` field that holds it is at `BeatGrid.swift:99`). The epic-8 8.5 + 8.7 ACs predate that reshape and name a `Bool?`. AC6's drift cross-reference and any consistency-rate cross-reference use the real enum (`!= .disagree` is the "sync-usable" predicate, per `make consistency-rate-oa300`). Do not reintroduce a `Bool?`. [Source: Sources/BoomBoomBoomKit/TempoAgreement.swift:38-58; Sources/BoomBoomBoomKit/BeatGrid.swift:99,104]
- **DD-5 — Downbeat oracle: bar-phase from the DAW grid, with a hand-verified-subset fallback.** The JAMS `beat` namespace carries a per-beat `value` = bar-phase (`1` = downbeat). `dawproject-beats.py` derives it from the arrangement time-signature / bar grid where available. If corpus-wide bar-phase is not reliably extractable from the `.dawproject` source, scope the downbeat metric (AC7) to the committed DnB subset (the genre where downbeat matters most for DJ launch quantization, and small enough to hand-verify, mirroring the 8.5 hand-clicked `playback-alignment-oracle.json` precedent) and report corpus-wide only where derivable. State the chosen scope in the test doc comment + Completion Notes. `mir_eval` has no dedicated downbeat metric, so AC7 computes phase-correctness directly (not via `mir_eval`).
- **DD-6 — Python tooling split.** `scripts/dawproject-beats.py` lives in `scripts/` (develop-only, sibling of `dawproject-bpm.py`). `eval-beatgrid.py` lives in `_bmad-output/ml-training/` (develop-only, alongside the other eval sidecars). Both pull `jams`/`mir_eval` from `_bmad-output/ml-training/pyproject.toml`. None ships to `main` — consistent with the DD #13 exception that scopes shipping Python to `tools/coreml-convert/` only. The Makefile `benchmark-beatgrid` + `oracle-generate-beats` targets live in the Makefile (ship to main) but fail loudly on a main-only checkout, the established `ml-*`-target convention.
- **DD-7 — `JAMSDecoder` is benchmark-target-internal, never `BoomBoomBoomKitTestSupport`.** `BoomBoomBoomKitTestSupport` is a `public` SPM product consumed by downstream test packages; putting a JAMS decoder there leaks an MIR-format decoder onto consumers. It lives in `Tests/BoomBoomBoomKitBenchmarkTests/Helpers/` (a new dir). Reject unknown namespaces with a typed error rather than silently defaulting (the epic is explicit on this). [Source: epics.md#Story-8.7 (line 1166)]
- **DD-8 — Do not author a new combined-analysis perf gate.** The "≤ 1.25× BPM-only" check already exists as Story 8.4's `make beat-grid-impact-report` and Story 8.5's `make combined-analyze-impact-report`. AC8 references them; re-implementing a third wall-clock gate here would duplicate coverage and drift from the canonical numbers. Just confirm they pass with `detectDownbeats` wired.
- **DD-9 — JAMS corpus shape: `{ "entries": [ <standalone-valid JAMS object> ] }` wrapper (pinned here, inherited by 8.8).** Standard JAMS describes ONE audio file per object (top-level `file_metadata` + `annotations`); a single such object cannot carry per-track metadata for the whole OA300 corpus. The repo ALREADY chose a multi-track `entries` wrapper at `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/12-dnb-sentinels-expanded.json:16` — extend that one convention rather than introducing a second dialect (one-`.jams`-per-track), which would force 8.8 to bridge a seam. Discipline that keeps a future standards-pure split lossless: each `entries` element is a standalone-valid JAMS object, and corpus-level fields never leak INTO the per-track objects (split = `entries.map { write($0) }`). Pin the wrapper key, the `annotations[].data` beat-time field path (the real Swift↔Python parity surface — the decoder, `dawproject-beats.py`, and `eval-beatgrid.py` must agree byte-for-byte), and reserve named per-track slots NOW for the fields 8.8's legacy oracles need (the DAW-verified vs Rekordbox dual-truth distinction; triplet targets) even though 8.7 does not populate them. These are develop-only ground-truth artifacts (JSON stays off `main`; only the decoder ships), so the loss of off-the-shelf `jams.load()` interop is a property we neither ship nor need. The AC4 coverage gate also rides on this: `JAMSCorpus.entries.count` is the clean, unambiguous coverage assertion (vs globbing a directory). [Source: 12-dnb-sentinels-expanded.json:16; party-mode architect 2026-06-14; reference_dawproject_bpm dual-truth]
- **DD-10 — The accuracy floors are toothless without a pinned denominator (coverage-parity).** An F-measure of 0.78 means nothing unless the population is pinned: `DAWOracleBenchmarkTests.swift:91` only asserts `dawOracle.count >= 20` and `dawproject-bpm.py:180` only WARNS on unmatched clips while still emitting JSON — compose them and a partial oracle silently feeds a gate that measures a survivorship-selected favorable subset. So coverage-parity is an asserted in-test invariant (AC3), the oracle generator hard-fails on unmatched clips (Task 1), the DnB subset is a committed genre predicate not a cherry-pickable list (AC5), AND the one-time calibration run that sets `measured` must itself satisfy coverage-parity (AC4) — else `measured − 0.03` locks to a flattering number and the floor is permanently soft. An un-gameable floor is the whole value of this story; a green check over an unknown population is worse than no floor. [Source: DAWOracleBenchmarkTests.swift:91; dawproject-bpm.py:180; CorpusTracks.swift:20; party-mode analyst 2026-06-14; memory: feedback_factual_claims_grep_pre_story]
- **DD-11 — `make py-lint` does NOT ty-check new Python files automatically.** `ty check` in `Makefile:333` is an explicit file LIST (ruff is a glob). New sidecars `scripts/dawproject-beats.py` + `_bmad-output/ml-training/eval-beatgrid.py` must be added to that `ty check` command or the "ty-clean" ship claim is vacuous. ruff already covers them via its `.`/`../../scripts/` glob.
- **DD-12 (orchestration directive) — prefer the make-two-step over Process-from-test.** The benchmark should emit estimated JAMS to a known path and let `make benchmark-beatgrid` run `eval-beatgrid.py` as a second step (the `FR18EvaluationHarnessTests` precedent), rather than `Process`-invoking Python from inside the Swift test. Process-from-test couples the Swift test to a Python runtime + venv resolution (CI-fragile) and muddies the main-vs-develop split (a main-bound test would shell to a develop-only script). The epic phrased both as dev's-choice; this review makes the make-two-step the directive. [Source: axiom-testing guidance 2026-06-14; Makefile FR18 precedent]

### Source-tree placement + ship discipline

- **Ships to `main`:** `Tests/BoomBoomBoomKitBenchmarkTests/Helpers/JAMSDecoder.swift`, `Tests/BoomBoomBoomKitBenchmarkTests/BeatGridBenchmarkTests.swift`, `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/{jams-decoder-sample.json, beat-grid-dnb-subset.json}`, the new `Makefile` targets (`benchmark-beatgrid`, `oracle-generate-beats`), `README.md`, `MODEL_CARD.md`. The benchmark target ships to main per CLAUDE.md ("Tests/ — Unit tests + the env-gated benchmark target"). [CLAUDE.md ship table]
- **Stays on develop:** `scripts/dawproject-beats.py`, `_bmad-output/ml-training/eval-beatgrid.py`, the `pyproject.toml` `jams`/`mir_eval` deps, `_bmad-output/implementation-artifacts/8-7-beat-grid-accuracy.json`, `8-7-downbeat-accuracy.json`, this story file, `sprint-status.yaml`, any `8-7-pressure-release.md`.
- **Corpus-dir (not committed):** `${OA300_CORPUS_PATH}/daw-oracle-beats.json` (mirrors the un-committed `daw-oracle.json`).
- **README discipline:** public-facing — no story numbers, no BMAD/KDD/JAMS-internal jargon beyond what a consumer needs, no aubio. [memory: feedback_no_aubio_in_public, feedback_no_business_jargon_in_commits]
- **Zero new SPM deps:** `Package.swift` untouched. `mir_eval`/`jams` are develop-only Python deps, not SPM.

### Existing patterns to mirror (read these first)

- **`Tests/BoomBoomBoomKitBenchmarkTests/DAWOracleBenchmarkTests.swift`** — env-gated init (`ProcessInfo...["OA300_CORPUS_PATH"]`, throw if unset), `Bundle.module` fixture loading, corpus-dir JSON loading (`daw-oracle.json` via `appendingPathComponent`), per-filename lookup map. The exact template for `BeatGridBenchmarkTests` init.
- **`Tests/BoomBoomBoomKitBenchmarkTests/OA300BenchmarkTests.swift`** — env-gating + the `.enabled(if:)` trait pattern + per-track iteration over the corpus.
- **`scripts/dawproject-bpm.py`** — `.dawproject` ZIP + `project.xml` parsing, warp-marker → BPM derivation, `--match`/`--json` modes, `uv run` shebang. The base for `dawproject-beats.py` (extend to per-beat times + bar-phase).
- **`Makefile` targets `benchmark`, `beat-grid-impact-report`, `combined-analyze-impact-report`, `oracle-generate`** — env-threading (`OA300_CORPUS_PATH`, `GIT_SHA`), `swift test --filter`, the develop-only-tooling-in-Makefile convention.
- **`Sources/BoomBoomBoomKit/BeatGrid.swift`** — the real `BeatGrid` shape (`beats`, `downbeats: DownbeatResult`, `estimatedTempo`, `tempoAgreement: TempoAgreement`, `gridOrigin: BeatGridAnchor?`, `coverage`, `schemaVersion`) + the `gridOrigin`-extrapolation contract documented in its DocC (use for the AC6 drift expectation).
- **`Sources/BoomBoomBoomKit/AudioAnalysisService.swift`** — `analyzeBeatGrid(url:options:)` (`:1387`, decoded overload `:1435`) + `analyze(url:options:) -> CombinedAnalysisResult?` (`:1528`, decoded overload `:1574`) + `Options.detectDownbeats` (`:340`).

### Testing standards

- Swift Testing (`import Testing`, `@Suite`/`@Test`/`#expect`/`#require`) — NOT XCTest. `JAMSDecoderTests` is non-env-gated (runs in CI). `BeatGridBenchmarkTests` is env-gated on `OA300_CORPUS_PATH` (skipped without corpus).
- No `sleep()` (testing-auditor discipline). Per DD-12 the Swift test does NOT `Process`-invoke Python; `make benchmark-beatgrid` runs `eval-beatgrid.py` as a second step. The make target captures the sidecar's stdout/stderr and fails with its error text; on a main-only checkout (develop-only sidecar absent) the target fails loudly — the established `ml-*`-target convention.
- The benchmark must NOT be wired into the default `make test` (which filters `BoomBoomBoomKitTests`); it runs only via `make benchmark-beatgrid`.

### Latest tech information

- **`mir_eval`** (MIR-research standard, `craffel/mir_eval`, MIT): `mir_eval.beat.f_measure(reference_beats, estimated_beats, f_measure_threshold=0.07)` — the ±70 ms window is `f_measure_threshold` in seconds. Pin the major version in `pyproject.toml` (epic guard); `f_measure` has been API-stable across `0.x`.
- **JAMS** (`marl/jams`, ISC): the `beat` namespace observation `value` is the metrical position (1-indexed; `1` = downbeat); `tempo` namespace value is BPM. The Swift decoder only needs the subset of the schema we emit/consume.

### Project Structure Notes

- New directory `Tests/BoomBoomBoomKitBenchmarkTests/Helpers/` (first helper). Confirm the benchmark test target's `Package.swift` resource/source globbing already picks up new `.swift` under the target (it does — sources are globbed) and that `Fixtures/` resources are copied (the existing `.json` fixtures confirm the rule).
- No `Sources/` changes. No `BPMDiagnosticTrace` touch (so the 5-recipe trace audit is N/A, but state "trace untouched" in Completion Notes for parity with sibling stories).

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-8.7 (lines 1132-1170)] — the eight epic ACs, FR-34, the JAMS/mir_eval adoption, the JAMSDecoder placement, the pressure-release valve.
- [Source: _bmad-output/planning-artifacts/epics.md#Story-8.8 (lines 1172-1202)] — the downstream migration story that reuses this story's JAMSDecoder + Python deps.
- [Source: _bmad-output/planning-artifacts/epics.md (line 27, JAMS adoption decision 2026-05-26)].
- [Source: Sources/BoomBoomBoomKit/BeatGrid.swift:53-142] — real `BeatGrid` surface incl. `gridOrigin: BeatGridAnchor?` (anchor `beatIndex` contract at `:104`, AC6 drift). The `TempoAgreement` enum + its 4 cases live in `Sources/BoomBoomBoomKit/TempoAgreement.swift:38-58` (DD-4), NOT in `BeatGrid.swift`.
- [Source: Sources/BoomBoomBoomKit/DownbeatResult.swift:34-46, DownbeatEstimate.swift:93] — `DownbeatResult.detected(estimate:)` + `DownbeatEstimate` (every detected bar start + `phaseIndex`, AC7 all-downbeats precision).
- [Source: Sources/BoomBoomBoomKit/AudioAnalysisService.swift:340,1387,1528] — `detectDownbeats`, `analyzeBeatGrid`, `analyze -> CombinedAnalysisResult?` (decoded overloads at :1435 / :1574).
- [Source: Tests/BoomBoomBoomKitBenchmarkTests/DAWOracleBenchmarkTests.swift] — env-gated oracle-benchmark init template.
- [Source: scripts/dawproject-bpm.py] — `.dawproject` parser precedent for `dawproject-beats.py`.
- [Source: _bmad-output/ml-training/pyproject.toml] — develop-only Python dep home (add `jams`/`mir_eval`).
- [Source: Makefile `beat-grid-impact-report`, `combined-analyze-impact-report`, `consistency-rate-oa300`, `oracle-generate`] — perf-gate reuse (DD-8) + target conventions.
- [Source: CLAUDE.md] — main-vs-develop ship rules; the DD #13 Python-tooling scope; design constraints.
- [Source: operator party-mode reflection 2026-06-14] — 8.7-first sequencing + downbeat-validation fold-in + #38 dev-gate.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (bmad-dev-story), 2026-06-16/17.

### Debug Log References

- `make oracle-generate-beats` → 1264 tracks, 652,189 beats, all 4/4, 0 tracks with 0 downbeats, 84 duplicate-path collapsed.
- `make benchmark-beatgrid` calibration (release, full corpus, 2026-06-17): see Completion Notes for the locked numbers + `_bmad-output/implementation-artifacts/8-7-pressure-release.md`.

### Completion Notes List

**Operator re-scope (oracle source).** During dev the operator redirected the beat oracle from the OA300 `.dawproject` (the spec-as-written source) to Tony's Rekordbox-gridded corpus, for both the beat-position F-measure and the downbeat-phase oracle (decided via `AskUserQuestion`). The spec's AC1/AC3/AC4/AC5/AC7 are amended accordingly — see DD-13…DD-21 in Dev Notes. The OA300/dawproject path is dropped from this story; the JAMS plumbing (decoder + sidecar deps) is unchanged and still serves 8.8.

**What shipped.**
- `scripts/rekordbox-beats.py` (develop-only) — parses `<TEMPO Inizio Bpm Metro Battito>` markers into a multi-track JAMS `{ "entries": [...] }` beat oracle; `Battito` populates the bar-phase `value` (1 = downbeat) authoritatively. Hard-fails on a gridded+resolved track yielding zero beats; dedups by physical audio path so coverage-parity holds. `make oracle-generate-beats` writes the develop-only, gitignored oracle.
- `Tests/.../Helpers/JAMSDecoder.swift` (ships to main, benchmark-target-internal) — `Codable` JAMS structs (`JAMSCorpus`/`JAMSFile`/`JAMSAnnotation`/`JAMSObservation`/`JAMSValue?`), `beat`/`tempo`/`tag_open` namespaces, typed `JAMSDecodingError.unknownNamespace`. `JAMSDecoderTests` (non-env-gated, 4 tests, run via `swift test --filter BoomBoomBoomKitBenchmarkTests.JAMSDecoderTests`) cover round-trip, decode→encode→decode identity, null→nil, and unknown-namespace rejection.
- `_bmad-output/ml-training/eval-beatgrid.py` (develop-only) — `mir_eval.beat.f_measure` (±70 ms), raw + octave-normalized, constant-tempo-scoped gated metric. Pinned `mir_eval>=0.7,<1.0` (the `jams` lib was intentionally NOT added — the `{entries}` wrapper isn't `jams.load()`-able; dead dep avoided). Both new Python files are ruff- AND ty-clean and added to the `make py-lint` `ty check` list (DD-11).
- `Tests/.../BeatGridBenchmarkTests.swift` (ships to main) — env-gated on `BEAT_GRID_ORACLE`+`BEAT_GRID_OUT_DIR`; one full-track `analyzeBeatGrid` pass per track yields the extrapolated-grid beats (F-measure), extrapolated downbeats (AC7), and raw-beat FR-29 drift. Asserts coverage-parity. `BeatGridFloorTests` (separate suite) asserts the F-measure floor from the sidecar JSON. `make benchmark-beatgrid` is the DD-12 make-two-step (Swift emit → Python eval → Swift floor).

**Measured calibration (2026-06-17, release, 1264 tracks / 910 constant-tempo, ±70 ms).** All gated metrics scope to constant-tempo tracks (DD-19); the estimated grid is the anchor+tempo EXTRAPOLATION, not raw beats (DD-20).
- **F-measure (octave-normalized, constant): 0.3718** → floor **0.33**. (raw all 0.2622; octave all 0.3721.) [full-span extrapolation + stable anchor, post-Codex-review — DD-22]
- **Downbeat correctness-when-fired (octave-tolerant, constant): 0.1444** over 42 fired tracks → floor **0.10**. Fire rate **4.3%** (54/1264), abstain 95.7% (both REPORTED).
- **FR-29 P95 last-beat drift: 1652 ms** (median 176 ms) → regression ceiling **2.0 s**.

**Pressure-release (DD-1 / DD-21).** All three are FAR below the epic's published targets (0.75 / 0.65 / 30 ms). The targets were unmeasured placeholders written before the beat tracker existed; this story is where they became measured numbers. Floors locked at `measured − margin` as regression nets, NOT silently weakened to fit. `8-7-pressure-release.md` documents the actuals + analysis (per-track ±97 ms phase offsets with 50–100 ms within-track spread = fine tempo/phase disagreement; Rekordbox is an auto-analyzed reference; real-world DnB material) + recommends reopening Story 8.4 for tracker refinement. AC5's separate DnB sub-floor is folded into the main floor (DD-16 — XML genre is empty on 88% of tracks; the corpus is DnB-dominant).

**Trace untouched.** No `BPMDiagnosticTrace` field changes (the 5-recipe trace audit is N/A). No `Sources/` edits → `analyzeBPM` byte-identical by construction (AC10); `Package.swift` untouched (zero new SPM deps).

**Gates.** `make fmt` idempotent; `make lint` baseline-only (`LUFSAnalyzer.swift:135` TODO); `make test` 689/689 green; `make py-lint` clean (ruff + ty incl. both new files). Perf gates (AC8) are unaffected by construction (no `Sources/` changes); `make combined-analyze-impact-report` re-confirmed.

**Pending user action.**
- Operator commit + PR into `rterhaar/epic-8` (1Password GPG signer).
- `/bmad-code-review` on a separate-LLM cadence.
- **Decision the operator owns:** whether the measured beat-grid accuracy (F 0.37 / downbeat 0.14 / drift 1.65 s, all far below the published 0.75/0.65/30 ms targets) is acceptable to close Epic 8 with regression-net floors + the pressure-release recommendation, OR whether to reopen Story 8.4 to refine the beat tracker before closing. Surfaced per the DD-1 pressure-release valve.

### File List

**Ships to main:**
- `Tests/BoomBoomBoomKitBenchmarkTests/Helpers/JAMSDecoder.swift` (new)
- `Tests/BoomBoomBoomKitBenchmarkTests/JAMSDecoderTests.swift` (new)
- `Tests/BoomBoomBoomKitBenchmarkTests/BeatGridBenchmarkTests.swift` (new)
- `Tests/BoomBoomBoomKitBenchmarkTests/Fixtures/jams-decoder-sample.json` (new)
- `Makefile` (new targets `oracle-generate-beats`, `benchmark-beatgrid`; `ty check` list; `BEAT_ORACLE_JAMS`/`BEAT_GRID_ACCURACY_OUT_DIR` vars)
- `README.md` (Beat grid → Accuracy subsection)
- `MODEL_CARD.md` (Beat-grid acceptance section)
- `.gitignore` (develop-only beat-grid artifact patterns)

**Stays on develop:**
- `scripts/rekordbox-beats.py` (new)
- `_bmad-output/ml-training/eval-beatgrid.py` (new)
- `_bmad-output/ml-training/pyproject.toml` + `uv.lock` (`mir_eval` dep)
- `_bmad-output/implementation-artifacts/8-7-beat-grid-accuracy.json` (new, report evidence)
- `_bmad-output/implementation-artifacts/8-7-downbeat-accuracy.json` (new, report evidence)
- `_bmad-output/implementation-artifacts/8-7-pressure-release.md` (new)
- `_bmad-output/implementation-artifacts/8-7-...md` (this story), `sprint-status.yaml`

**Develop-only, gitignored (regenerable, machine-specific paths):**
- `_bmad-output/ml-training/tony-corpus/rekordbox-beats.jams.json` (oracle, `make oracle-generate-beats`)
- `_bmad-output/implementation-artifacts/8-7-estimated-beats.jams.json` (benchmark intermediate)

### Review Findings

Code review 2026-06-18 (`bmad-code-review`; Codex blind-hunter thread `019ede1b-b380-70c3-b18a-9ae76e271921` + self-pass edge-case/acceptance, JAMS-stale-aware). Scope: consolidated `f5f2574..5c99d3f` (Story 8-7 #42 + JAMS-schema-compliance #43), develop-only logic chunk. **All findings are in develop-only oracle/benchmark tooling — none in `Sources/`, none shipped to `main`. No defect in already-merged shipping code.**

**Resolution 2026-06-19** (branch `rterhaar/8-7-review-patches`): all 6 findings (1 decision + 5 patch) applied, Codex-reviewed (thread `019ede1b`, all six verdicts CORRECT/REFINE), gated (`make fmt`/`py-lint`/`swiftlint` baseline-only/`make test` 689/689, JAMS decoder 7/7 incl. a new parity test).

Decision-needed:

- [x] [Review][Decision] Basename-only audio join may attach a grid to the wrong physical file [scripts/rekordbox-beats.py] — **RESOLVED: operator chose HARD-FAIL** (Codex-refined to content-hash-gated: byte-identical duplicates collapse to a deterministic pick — provably the same audio, not a guess — while genuinely-different content hard-fails). **Benchmark regeneration surfaced 4 GENUINELY-ambiguous collisions** the old silent-pick code papered over: `Todd Bucher - Proem.wav` (3 different-content copies), `Todd Bucher - Onyx.wav` (3), `Ron Mercy - Junkin Da Trunk.mp3` (×2 TrackIDs, Bass Music vs Drum and Bass). `make oracle-generate-beats` now refuses until these are resolved. **Follow-up operator decision:** dedupe the audio root for these 4, OR switch the resolver to Rekordbox `Location`-matching (the authoritative source — would auto-resolve all 4). The committed oracle (1264 tracks) is untouched; existing F 0.37 stands; re-measure is gated on this.

Patch:

- [x] [Review][Patch] Missing `TotalTime` truncates a single-marker oracle [scripts/rekordbox-beats.py] — hard-fail on `total_time is None` for resolved gridded tracks. **Refined post-test:** the original `<= max_inizio` clause false-flagged 8 benign tracks where the last marker sits a few tenths past a whole-second-rounded `TotalTime` (the marker IS the track end; the grid is complete) — narrowed to `None` only.
- [x] [Review][Patch] Intermediate segment drops its own marker beat [scripts/rekordbox-beats.py] — break condition is now `if k > 0 and t >= cutoff - 1e-9`; every `<TEMPO Inizio>` marker's own `k==0` beat is always emitted (subsumes the old last-segment special case).
- [x] [Review][Patch] `BEAT_GRID_LIMIT` subset could pass the full floor [Makefile / eval-beatgrid.py / BeatGridBenchmarkTests.swift] — shipped both (Codex): `eval-beatgrid.py` gains `--allow-missing-constant` (missing constant-tempo rows now FATAL by default), Makefile passes it only when `BEAT_GRID_LIMIT` is set, and `BeatGridFloorTests` reports-but-skips the floor under `BEAT_GRID_LIMIT`.
- [x] [Review][Patch] `Battito`/`Metro` unvalidated [scripts/rekordbox-beats.py] — `parse_tracks` records malformed/out-of-range `Battito` and non-empty non-4/4 `Metro`; a resolved gridded track carrying any hard-fails (no quiet skip).
- [x] [Review][Patch] Swift/Python `constant_tempo` parity [JAMSDecoder.swift] — custom `init(from:)` on `JAMSFileMetadata` + `JAMSSandbox` decodes `constant_tempo` with `try?` (present non-bool → nil, matching Python), locked by `JAMSDecoderTests.nonBoolConstantTempoDecodesTolerantly`.

Dismissed (1): the Swift greedy two-pointer matcher (`BeatMatch.score`, used for the gated downbeat F) differs from `mir_eval`'s optimal matching — acceptable by design for sparse downbeats at ±70 ms; the 0.10 floor is an explicit coarse regression net (the gated *beat* F-measure uses real `mir_eval`).

### Review Findings — #44 patch round (2026-06-20)

Code review (`bmad-code-review`) of the previously-unreviewed PR #44 (`5c99d3f..b94cdb4`) — the patch round that resolved the 2026-06-18 findings, whose 2nd/3rd commits added new work (operator-curated collision overrides, non-finite tempo rejection, collision-hash guards) outside the original review scope. Three layers: Blind Hunter (Codex thread `019ee6c3`, diff-only), Edge Case Hunter (diff + project), Acceptance Auditor (diff + spec). All agents were told the code had already been through several review/refinement passes (spec deviations expected; the bar is correct code, not spec compliance). **All findings are in develop-only oracle-generation tooling (`scripts/rekordbox-beats.py`, `_bmad-output/ml-training/eval-beatgrid.py`) or the env-gated benchmark harness — none in `Sources/`, none break the committed oracle/calibration (F 0.37), and the central anti-toothless-floor invariant (DD-10/DD-17) was independently confirmed to hold for the full CI run.** 2 patch · 4 defer · 3 dismissed.

Patch:

- [x] [Review][Patch] Present-but-inconsistent `total_time` silently extends the grid past the declared track end [scripts/rekordbox-beats.py:270] — P1 narrowed the hard-fail to `total_time is None` only; a present `total_time` smaller than the last marker's `inizio` falls through (`end = … else start`, `cutoff = start`), the last segment emits just its k=0 beat, and back-fill still fills `[0, inizio]`, so a grid extending past the declared end is emitted (≥1 beat ⇒ the zero-beat gate misses it). Consistent with the round's "hard-fail loudly, never silently truncate" posture; latent (no committed-corpus track currently hits it). **FIXED** (Codex-refined): `main()` now hard-fails when `total_time < max_inizio - duration_slop`, `duration_slop = max(1.0, min(one_bar, 4.0))` — the 1 s floor keeps whole-second rounding benign above 240 BPM, the 4 s cap keeps the guard live at very slow tempos; measured-inert (0 fires) on the committed 1264-track oracle. [source: edge]
- [x] [Review][Patch] Collision-override returns the selected path without re-checking its digest [scripts/rekordbox-beats.py:234-240] — the unreadable-candidate guard (`sha256_file` OSError → `<unreadable:…>` sentinel) is bypassed by the override branch: `override in str(p)` selects on the path string and returns it even when the selected path's digest is the unreadable sentinel, contradicting the stated "unreadable → hard-fail" principle. Very narrow (requires an operator-verified override target to become unreadable). **FIXED**: `resolve_path` now re-checks the override-selected path's cached digest and falls through to the ambiguous hard-fail when it is the unreadable sentinel. [source: blind]

Deferred:

- [x] [Review][Defer] `BEAT_GRID_LIMIT` floor-skip keyed on a raw env var [BeatGridBenchmarkTests.swift:457 / Makefile / eval-beatgrid.py] — an exported `BEAT_GRID_LIMIT` leaks the floor-skip into a bare `swift test` run (prints "SMOKE MODE" — not entirely silent), and the Python `--allow-missing-constant` guard (`filter-out 0`) parses malformed values differently from the Swift `Int(...) > 0` skip. Robust fix: gate the skip on an actual coverage signal (`n_missing_constant` / evaluated-vs-corpus count) instead of the env var. Develop-only test harness; has a print tell. [source: blind+edge]
- [x] [Review][Defer] Stale `COLLISION_OVERRIDES` entries are silent no-ops [scripts/rekordbox-beats.py:232] — a deleted/renamed override target or a resolved dedupe leaves dead config with no warning (fail-safe; only maintainability). [source: edge]
- [x] [Review][Defer] 200k-iteration loop guard truncates silently [scripts/rekordbox-beats.py:288,303] — `break`s mid-grid without recording a refusal bucket; unreachable for realistic durations/tempos, but inconsistent with the strict-refusal posture. [source: edge]
- [x] [Review][Defer] DD-*/Codex/Story jargon in main-bound benchmark test files [JAMSDecoder.swift, BeatGridBenchmarkTests.swift, JAMSDecoderTests.swift] — pre-existing internal-jargon comments that the squash-merge protocol (whole-file `git rm` only) would land on `main`; #44 adds none. Belongs to the eventual release scrub. [source: auditor]

Dismissed (3): (1) duplicate beat at exactly-coincident markers — the `1e-3` dedup (`:310`) collapses the time-duplicate; a phase conflict on coincident markers is malformed input [blind]; (2) negative-`Inizio` first marker "drops the downbeat" — **false positive**: the phase advances correctly through the `t < 0` skip (`:280-286`), and a downbeat before `t = 0` is legitimately outside `[0, duration]`, so in-track bar-phases are correct [edge, claimed High]; (3) `override in str(p)` matching the audio-root path — an over-match selects >1 candidate → safe hard-fail [edge].

### Change Log

- 2026-06-19 — `/bmad-code-review` on the consolidated `f5f2574..5c99d3f` (#42 + #43). Codex blind-hunter (thread `019ede1b`) + self-pass → 1 decision + 5 patch + 1 dismissed, all develop-only tooling (zero `Sources/`). All 6 applied on branch `rterhaar/8-7-review-patches` (separate follow-up PR → `rterhaar/epic-8`), each Codex-reviewed: D1 content-hash-gated basename hard-fail; P1 missing-`TotalTime` hard-fail (refined post-test to `None`-only after it false-flagged 8 benign last-marker-past-rounded-TotalTime tracks); P2 always-emit-marker-beat; P3 `--allow-missing-constant` + `BEAT_GRID_LIMIT` floor-skip; P4 Battito/Metro hard-fail; P5 tolerant `constant_tempo` decode (+ parity test). Gates: fmt clean, py-lint clean, swiftlint baseline-only, `make test` 689/689, JAMS decoder 7/7. **Benchmark re-run (oracle regen): the hardening now hard-fails on 4 genuinely-ambiguous basename collisions** (`Todd Bucher - Proem`/`Onyx`, `Ron Mercy` ×2 — different-content files the old silent-pick code grid-attached blind). Zero `Sources/` change ⇒ estimated beats byte-identical ⇒ committed oracle + F 0.37 stand; clean re-measure gated on resolving those 4 (dedupe audio root OR `Location`-match resolver — open operator decision). Status: review (PR unmerged + open decision).
- 2026-06-14 — Story drafted from epics.md#Story-8.7 + operator party-mode reflection. Pre-spec grep corrections recorded as DD-1 (F-measure floors are unmeasured placeholders → measure-then-floor), DD-2 (JAMS schema coherent, 8.7 self-contained vs 8.8), DD-3 (no Epic-6 promotion), DD-4 (`tempoAgreedWithBPMStage: Bool?` stale → `tempoAgreement: TempoAgreement`). Folded in downbeat validation (AC7) per operator decision; gated dev-start on PR #38 (Story 8-5) merge for the accuracy ACs. Status: ready-for-dev.
- 2026-06-17 — PR #42 Copilot review: made the oracle audio-path resolution deterministic across machines (`build_basename_index` sorts each basename's collision candidates) — `rglob` traversal order is filesystem-dependent and on-disk basename collisions exist (42 collisions, 11 with a sorted-vs-unsorted pick difference), so an unsorted list let the chosen audio (hence the calibrated floors) vary by machine. Re-calibrated: GATED numbers unchanged (F 0.3718, downbeat 0.1444, drift 1652 ms); one duplicate-content track flipped fired→abstain (fire-rate 4.4%→4.3%). Committed accuracy JSONs refreshed.
- 2026-06-17 — Implemented (claude-opus-4-8). **Operator re-scope mid-dev: beat oracle source switched from the OA300 `.dawproject` to Tony's Rekordbox-gridded corpus** (`AskUserQuestion`), for both F-measure and downbeat phase. Recorded as DD-13…DD-21. Shipped `scripts/rekordbox-beats.py` (Rekordbox `<TEMPO Battito>` → JAMS oracle), `JAMSDecoder.swift` + `JAMSDecoderTests`, `eval-beatgrid.py` (`mir_eval`, octave-normalized), `BeatGridBenchmarkTests` + `BeatGridFloorTests`, `make oracle-generate-beats`/`benchmark-beatgrid`, README/MODEL_CARD accuracy disclosure. Key dev findings: the comparison must use the anchor+tempo EXTRAPOLATED grid, not raw beats (DD-20, raised F 0.14→0.37); gated metrics scope to constant-tempo tracks (DD-19); half-time tagging requires octave-normalized F (DD-15/DD-18). Calibration: F-measure 0.366 / downbeat 0.144 (4.4% fire) / drift P95 1652 ms — all far below the published 0.75/0.65/30 ms targets; floors locked at measured−margin (0.33/0.10/2.0 s) + `8-7-pressure-release.md` recommends reopening Story 8.4 (DD-1, not silently weakened). Gates: fmt idempotent, lint baseline-only, test 689/689, py-lint clean, perf gates unaffected (no `Sources/` edits) + combined-analyze re-confirmed. Status: review.
- 2026-06-15 — Validation pass (Codex blind-hunter + axiom:ask + BMAD party-mode review: Amelia/Winston/Mary/John) on branch `rterhaar/8-7-story-review`. Applied: removed the now-stale PR-#38 blocking dependency (merged `e2dea60`) + per-task gate annotations; corrected the FR-29 drift formula to offset by `gridOrigin.beatIndex` (anchor is not beat 0 — AC6/Task 4); pinned the JAMS corpus schema to a `{ "entries": [...] }` wrapper matching the repo's existing multi-track precedent (DD-9, AC1/AC2/Task 1/Task 2); added coverage-parity as an asserted invariant + oracle hard-fail + DnB genre-predicate + calibration-population rule to close the toothless-gate loophole (DD-10, AC3/AC4/AC5/Task 1); made downbeat correctness-when-fired score ALL detected downbeats with recurring-phase agreement, not just the first (AC7/Task 6); fixed `value: JAMSValue?` (null-not-zero, resolves the optional-vs-required contradiction) + added an encode round-trip test (Task 2); flagged that `ty check` is an explicit file list so new Python files must be added (DD-11, Task 3/Task 8); made the make-two-step the orchestration directive over Process-from-test (DD-12, Task 4); corrected drifted `AudioAnalysisService` line citations + the `TempoAgreement` file location (`TempoAgreement.swift`, not `BeatGrid.swift`). Status: ready-for-dev.
