---
baseline_commit: 2fac43fb7621ac09d059241fb9a782a2fa853a07
---

# Story 7.7: Confidence-calibration verification + reproducibility artifact + BYOW export + KDD-B2 semi-supervised net-benefit gate

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **library maintainer**,
I want **the final Story 7.5 v2 model exported to CoreML for BYOW consumption (FR-19), the FR-25 prior-bundle-anchored calibration verification recorded with the documented `eceHalfDoubleFloor: 0.10`, the FR-24 semi-supervised net-benefit ablation gate evaluated (maskedMelPretrain vs supervisedAugmented on OA300 + GiantSteps + sentinels), the FR-20 reproducibility record frozen at `gitTag: "epic-7-close"`, and a sealed stratified GiantSteps holdout (n=150) used as an iteration-leak tripwire against Story 7.6's iterated Acc1**,
so that **Epic 7 closes with a consumable `giantsteps_v2.mlmodel` at `_bmad-output/ml-models/` (re-bundled to `main` only if Story 7.6 said `decision: "bundle"` AND FR-25 calibration + the holdout-gap tripwire both clear), a smoke-tested consumer convert path, and a frozen record sufficient to retrain bit-identically.**

## Context & posture (READ FIRST)

This is a **develop-only Python story with ZERO `Sources/` + `Tests/` diff** — the cleanest-footprint story of Epic 7. It mirrors the dev/operator split of Stories 7.3 / 7.5 / 7.6, and it is the **Epic 7 close-out**: it consumes the operator-produced outputs of 7.5 (the v2 checkpoints) and 7.6 (the per-track FR-18 runtime dumps + the KDD-B5 decision), neither of which exists on disk yet.

**The dependency chain that gates the authoritative run (all operator-owned, currently OPEN):**
1. KDD-B4 corpus signoff — `corpus-diagnostics-v1.md:9` reads `REVIEWER_SIGNOFF: pending`. Blocks 7.5's `train.py`.
2. Story 7.5 full 3-seed × 60-epoch training run → `giantsteps_v2_seed_{42,43,44}.mlmodel` + per-seed `model_metadata.json` + `marginal-watchlist-stability.json`. **Plus** the supervisedAugmented runner-up checkpoint(s) (`train_runnerup.py`) that FR-24 needs.
3. Story 7.6 authoritative FR-18 evaluation → the `seed_*/` per-track runtime prediction dumps (which 7.7 partitions for the holdout gap) + `fr-18-evaluation.md` with the KDD-B5 `decision:` line.

Therefore:

- **Dev-agent scope** (this story's "done", done-with-operator-remainder like 7.5/7.6 — NOT review-and-hold) = ship + smoke-verify the *close-out harness*: the holdout sampler, the calibration-verification generator, the FR-24 net-benefit comparator, the holdout-gap evaluator, the reproducibility-freeze tool, the post-bundle-watchlist annotator, the Makefile targets, the `7-7-pressure-release.md` scaffold, the `.gitignore` pattern, and the develop-only matplotlib dependency. Smoke-verification runs against the develop-only prior bundle `giantsteps_v1.mlmodel` / `giantsteps_v1.mlmodelc` (a **negative control**) and/or pure-Python synthetic fixtures. Every generator **fails closed with a named error** when its real v2 input is absent.
- **Operator-owned remainder** (NOT dev tasks; surfaced as pending at close) = the real v2 export, the real calibration/net-benefit/holdout-gap numbers, the FR-20 freeze, and the decision-gated `main` squash-merge. All gated on the chain above.

**Develop vs main:** EVERYTHING dev ships is develop-only. Unlike 7.5 (the `featureSetVersion` bump) and 7.6 (the `FR18EvaluationHarnessTests` file), **Story 7.7 has NO main-bound `Sources/`/`Tests/` change** — the consumer convert tool (`tools/coreml-convert/`, which ships to main) is **unchanged** for v2 (the reference architecture — 256 bins, `[1,1,128,512]` NCHW, `30 + argmax` — is identical; v2 is a *feature-contract* version bump, not an architecture change; `reference_arch.py` needs no edit — verified). The only main-tracked file dev touches is `.gitignore` (adding the develop-only `giantsteps-holdout-v2.json` ignore pattern, which CLAUDE.md *requires* live there). `make test` stays **483/105**; `git diff --stat Sources/ Tests/` is **empty**. The conditional main-bound re-bundle (re-add `Sources/BoomBoomBoomKitML/Resources/giantsteps_v2.mlmodelc/` + `Package.swift` `resources: [.copy("Resources")]` + `MODEL_CARD.md`) is **operator-owned AND `decision: "bundle"`-gated** — it is documented here as the operator path, not implemented by dev (the real decision needs the operator FR-18 run, and the current smoke decision is `byow`).

## Pre-dev verification (MUST run before writing code)

Three load-bearing assumptions; each has a fallback baked into the ACs. Confirm against `rterhaar/7-7` HEAD before implementation:

1. **The consumer convert path works against the reference arch unchanged.** Run `make ml-convert` (it converts the develop-only `model.pt` v1 checkpoint via `tools/coreml-convert/convert.py --arch reference --validate`) and `make ml-convert-tests` (the 20-test pytest suite). Both must pass on the v1 stand-in. This proves AC2's convert path needs no `tools/coreml-convert/` edit for v2 (the v2 checkpoint is the same TempoCNN/256-bin/`[1,1,128,512]` contract). Record the result. If the suite fails on the v1 stand-in, that is a pre-existing convert-tool regression to surface — NOT a 7.7 deliverable.
2. **The GiantSteps corpus carries a style/genre label for stratification.** The holdout AC wants "5 tempo bins × 3 GS style labels = 15 strata." `build_fr18_input.resolve_giantsteps` reads only `filename` + `bpm`/`tempo` from `giantsteps-tempo-ground-truth.json` — no style field confirmed. Inspect the actual GS ground-truth JSON (under `GIANTSTEPS_CORPUS_PATH`) for a genre/style/key field. **If absent, the sampler falls back to tempo-only stratification** (5 bins × 30 = 150) and records the degraded basis in the manifest — do NOT fabricate style labels (DD #7).
3. **Story 7.6's per-track GS dumps are partitionable by the holdout hash-list.** Confirm the 7.6 `seed_*/` per-track JSON schema carries a stable per-track key that the holdout manifest can join on (`trackId` and/or an audio hash). The holdout-gap evaluator (AC6) reads 7.6's dumps and removes the 150 holdout tracks — it does NOT re-run 7.6. Record the join key. (At dev time 7.6's real dumps don't exist; smoke uses synthetic dumps + a synthetic holdout list.)

## Acceptance Criteria

1. **BYOW export of the v2 model (FR-19) — develop-only target + load-AND-infer check.** A develop-only `make ml-export-v2` target (or a documented `ML_MODEL_*` override of `ml-export`) runs `export.py --checkpoint <v2.pt> --output ../ml-models/giantsteps_v2.mlmodel` (`export.py:40-42` already accepts `--output`; the existing `ml-export` recipe hardcodes `giantsteps_v1` — DD #3). `make compile-model ML_MODEL_INPUT=_bmad-output/ml-models/giantsteps_v2.mlmodel` produces `giantsteps_v2.mlmodelc`. **The load check is load-AND-infer, NOT load-without-throw** (axiom-ai/Winston): a botched bin-offset or transposed mel axis converts/compiles/loads clean and infers garbage ("conversion succeeded ≠ correct"). The check feeds one known fixture (a synthetic click track at a known BPM, or one corpus track) through the loaded `.mlmodelc` via `BNNSTechnique(modelURL:)` AND through `tools/coreml-convert/convert.py --validate` (AC2), asserting the argmax-decoded BPM agrees within one bin. **Which seed is exported (FR-18 is a ≥2-of-3 multi-seed gate, but the BYOW/bundled export is a SINGLE model):** the exported checkpoint is the **median-Acc1 seed of the promoted arm** (deterministic tiebreak: lowest seed number) — record the seed-selection rule + the chosen seed in the export report (John). **Dev smoke:** run the export+compile+infer path against the develop-only v1 `model.pt` to prove the plumbing works (v1 may abstain under the bumped `featureSetVersion` per 7.6 DD #11 — record which path the smoke exercised); the **real v2 export is operator-owned** (the v2 `.pt` does not exist). **Fail-closed is Makefile-enforced** — `export.py` raises a raw `FileNotFoundError` (`export.py:48`), so `ml-export-v2` adds a `[ ! -e "$(CHECKPOINT)" ]` guard mirroring `compile-model` (Amelia). [Source: epics.md#Story-7.7 AC1; export.py:37-48; Makefile `ml-export`/`compile-model`]

2. **Consumer convert-tool smoke (the only Python that ships to main) — unchanged for v2.** `make ml-convert` (invokes `tools/coreml-convert/convert.py --checkpoint <model.pt> --arch reference --output <tmp>.mlmodelc --validate`) passes and `make ml-convert-tests` reports zero failures. Story 7.7 makes **NO edit** to `tools/coreml-convert/` — the reference arch matches the v2 contract (DD #2); this AC is a *smoke that the existing tool still works* (against the v1 `model.pt` stand-in at dev time, against the real v2 `.pt` at operator time). [Source: epics.md#Story-7.7 AC2; tools/coreml-convert/convert.py; reference_arch.py:36-39,96-148]

3. **FR-25 calibration verification — side-by-side anchors, subset-N honesty, `eceHalfDoubleFloor: 0.10`, reliability PNG.** A develop-only `_bmad-output/ml-training/calibration_verification.py` consumes the **v2 checkpoint's** per-track runtime predictions (the 7.6 producer's `seed_*/` dumps for the FR-24-promoted arm — "final model" = the v2 checkpoint, which is **identical under bundle vs byow** because both run the same `.mlOnly` BNNS path, so calibration is *downstream of, not an input to*, the bundle/byow decision — DD #14 decision-DAG; resolves the apparent circularity John/Codex flagged) and produces `_bmad-output/ml-training/calibration-verification.md` reporting `eceOverall`, `eceHalfDouble`, a bimodality-125/175-reappearance row, and a reliability diagram saved as `_bmad-output/ml-training/calibration-reliability-v2.png`. **`softmaxMaxP95` is reported as a confidence-SHARPNESS diagnostic, explicitly NOT a calibration gate (Mary)** — a peaky model can be confidently mis-calibrated. **Each metric is side-by-side against the prior-bundle anchor (non-optional): `softmax_max_p95 v2 = X.XXX vs prior = 0.294`, `ECE_half_double v2 = X.XX (N=n) vs prior PRIOR_ECE_HALF_DOUBLE = 0.5`, plus the 125/175 row.** **Report the subset N beside every v2 metric AND the anchor's basis (Mary):** the half/double subset goes `inconclusive` at `N < 100` (`ECE_MIN_SUBSET_N`), and **`inconclusive` ≠ pass — it blocks (fail-safe to byow)**, never renders a `None` ECE as a false "0.0 vs 0.5". **Anchor-provenance honesty:** `softmax_max_p95 = 0.294` and "bimodal 125/175" are in `MODEL_CARD.md:11-12`; `PRIOR_ECE_HALF_DOUBLE = 0.5` is NOT in MODEL_CARD — it lives only in `evaluate_fr18.py:55` sourced from `4-6-...-bundle-decision.md`. The report must state whether `0.5` is a measured prior (with its N) or a worst-case sentinel; if a sentinel, the report says so plainly ("v2 < 0.5" against a theoretical-max sentinel is a floor check, not a measured improvement). The ECE/bimodality math + reliability bins are **imported from `evaluate_fr18.py`** (`compute_ece_half_double`, `bimodality_125_175`, the `PRIOR_*` constants, and `result["bins"]` for the PNG/JSON) — NOT re-implemented (DD #13). **Schema + abstain discipline (Codex/Amelia):** before metric calc, validate the producer invariant `mlAbstained == (fr18ModelBPM is None)` per row and explicitly filter `not r["mlAbstained"]` — `compute_ece_half_double` itself does NOT read `mlAbstained` (`evaluate_fr18.py:163-171` only checks finite `fr18ModelBPM`/`softmaxMax`), so abstain-exclusion is currently incidental and must be made explicit so a stale-finite-BPM-with-abstain row can't contaminate the subset. All metrics bind to `fr18ModelBPM` (= `decodedBPM`), never `result.bpm` (DD #9). The documented v2 floor is `eceHalfDoubleFloor: 0.10` (FR-18 gate d). **Calibration failure (or `inconclusive`) blocks Story 7.7 close even if Story 7.6 said `decision: "bundle"`** → pressure-release downgrade to `byow` (AC11/DD #14). [Source: epics.md#Story-7.7 AC (FR-25); evaluate_fr18.py:53-61,156-230; MODEL_CARD.md:11-12; 4-6-...-bundle-decision.md]

4. **Reliability-diagram dependency (matplotlib) is added develop-only.** matplotlib is **not currently** an `_bmad-output/ml-training/` dependency (confirmed; 7.3 deferred the PNG to 7.7). It is added to `_bmad-output/ml-training/pyproject.toml` + `uv.lock` (develop-only — ml-training never ships to main, so a heavy plotting dep is acceptable here). `calibration_verification.py` emits the PNG **and** the reliability-bin data as inspectable JSON (so the artifact survives a matplotlib-absent environment and the smoke can assert on the bins). A clear named error is raised if matplotlib is required but unavailable. [Source: ablation/compare_kdd_b2.py:13 ("PNG deferred to 7.7"); pyproject.toml]

5. **FR-24 semi-supervised net-benefit gate (both arms, external corpora) — crash-safe two-arm contract.** A develop-only `_bmad-output/ml-training/fr24_net_benefit.py` produces `_bmad-output/ml-training/fr-24-semi-supervised-net-benefit.md` reporting **Acc1 + ECE + tail-error P95 of `maskedMelPretrain` (the KDD-B2 winner, the trained v2) vs `supervisedAugmented` (the runner-up) on OA300 + GiantSteps + sentinels** — NOT the tony.val/LAO axes `ablation/compare_kdd_b2.py` already covers (DD #5). Per-corpus metrics **group rows by `r["corpus"]`** (Amelia). **Two-arm directory contract (Codex/Winston — `consume_predictions` RAISES `ValueError` on a duplicate seed `evaluate_fr18.py:559` and on two dirs sharing a checkpoint SHA `:562`, so co-mingling the two arms into one `FR18_PRED_DIR` is GUARANTEED to crash):** each arm gets its own namespace `FR18_PRED_DIR/<arm>/seed_<N>/` (`<arm> ∈ {maskedMelPretrain, supervisedAugmented}`); `fr24_net_benefit.py` calls `consume_predictions(root/<arm>)` once per arm, never globbing a co-mingled root; `fr18-produce` gains an `ARM=` override (or per-arm `FR18_PRED_DIR`) so the operator command is copy-pasteable (the literal per-arm commands go in AC12). **Net-benefit polarity (Codex/Mary/John — reconcile AC↔DD to the conservative reading):** promotion of the SSL variant requires improving OR preserving all FR-18 gates within tolerance (**Acc1 within ±2 tracks, ECE within +0.02, tail-error P95 within +0.5 BPM**); failure → **fall back to `supervisedAugmented`**. **If the runner-up checkpoint was not trained, FR-24 is `inconclusive` → net-benefit is UNPROVEN; the SSL variant must NOT be bundled-as-net-benefit-proven.** The honest outcome: the model still ships on its own FR-18 gates, but bundling it then requires an **explicit operator acknowledgment, recorded in the report, that net-benefit is unproven** (a named operator decision, not a silent auto-pass) OR training the runner-up to close the gate properly. All accuracy binds to `fr18ModelBPM` (DD #9). [Source: epics.md#Story-7.7 AC (FR-24); train_runnerup.py; kdd-b2-comparison-v1.* (winner=maskedMelPretrain); evaluate_fr18.py:553-565]

6. **Sealed stratified GiantSteps holdout (n=150) + stratified-subset stability check.** A develop-only `scripts/sample-giantsteps-holdout.py` samples **n=150** from the GiantSteps corpus with a **fixed seed + stratified-by-tempo-band + stratified-by-style-label** (5 tempo bins × 3 style labels = 15 strata × 10; tempo-only fallback of 5 × 30 per pre-dev #2 / DD #7). **Two artifacts, distinct leak surfaces (Codex/Winston — the prior "track-hash-list in JAMS format with per-track BPM" was self-contradictory):** (a) a local `_bmad-output/ml-training/giantsteps-holdout-v2.json` JAMS truth file (`tempo` namespace, per-track BPM, reuse `curate_sentinels.py:130-159`) that is **`.gitignore`'d** (never committed — operator-local convenience); (b) the **committed** `_bmad-output/ml-training/giantsteps-holdout-v2-manifest.md` carrying ONLY the selection script ref + GS corpus version + seed value + the **SHA256 digest of the sorted `{trackId, sha256(audio)}` hash-manifest** (no BPM in the committed artifact — the GS ground truth the gap evaluator needs already lives in the corpus). A develop-only `_bmad-output/ml-training/holdout_gap.py` (or `make holdout-gap`) reads **Story 7.6's existing `seed_*/` per-track GiantSteps dumps** (it does NOT re-run 7.6 — DD #6), joins on `trackId` (`resolve_giantsteps` emits `trackId = str(filename)` — pre-dev #3), removes the 150 holdout tracks to compute `Acc1_iterated` (over 661 − 150 = 511) and `Acc1_holdout` (over 150), and emits `_bmad-output/ml-training/giantsteps-holdout-gap.md`. **It calls the LEAF counters (`count_acc1`/`count_acc1_octave`) directly and does NOT route the 511-row split through `evaluate_seed`/`aggregate_seeds`** — those carry `EXPECTED_GIANTSTEPS = 661` and would mark the 511 split `inconclusive` (`evaluate_fr18.py:75,403` denominator landmine — Winston). **Honest framing (Mary — this is a within-corpus stability check, NOT a sealed-pre-iteration leak detector):** the 150 tracks were inside the same 661 corpus 7.6 tuned against, so a small gap is *necessary-but-not-sufficient* evidence against iteration-leak (a uniform leak yields a near-zero gap and passes). The gap is in **percentage-points** (NOT counts — 8/150 = 5.3pp ≠ 8/511 = 1.6pp), reported with the per-arm Wilson lower bounds; at n=150 the SE of the difference is ≈ 4.2pp, so **gap ≥ 8 pp (≈ 1.9σ) is an informational review tripwire, not a significance test** — it blocks Story 7.7 close until reviewed (do NOT downgrade the threshold to pass — AC11), and it is NOT comparable to 7.6 gate-(c)'s absolute `537/661` count framing. **The seal is temporal + structural, not a filename grep (Winston):** the holdout is sampled at 7.7-close, fixed-seed, SHA256-committed, and `holdout_gap.py` only ever *subtracts* its hashes from dumps produced before it existed — the reproducibility guard is "the committed seed + digest re-sample the same 150 hashes," NOT `grep "giantsteps-holdout-v2"` (which can't prove 7.3/7.5/7.6 never read the underlying tracks, and trips on the manifest/.gitignore/tests anyway). [Source: epics.md#Story-7.7 AC (holdout + gap); curate_sentinels.py:130-159; build_fr18_input.resolve_giantsteps; evaluate_fr18.py:75,403; corpus_common.file_sha256]

7. **FR-20 reproducibility freeze.** A develop-only **`make epic7-freeze` backed by a NEW `epic7_freeze.py`** (NOT by extending `train_v2_artifacts.build_seed_metadata` — that builder is per-seed and `raise`s on `seed not in SEEDS` at `train_v2_artifacts.py:79`, the wrong home for an epic-global close-out fact — Amelia) freezes `_bmad-output/ml-training/training_log.json` + `_bmad-output/ml-training/model_metadata.json` at close. The two **net-new epic-global** fields are stamped into the frozen `model_metadata.json` (and, because each per-seed metadata file records its own promotability, into all three seed files for audit join): `epic7CloseDecision: "bundle" | "byow"` and `gitTag: "epic-7-close"` (DD #8 — neither exists today). **`epic7CloseDecision` reuses 7.6's exact KDD-B5 literals `"bundle" | "byow"`** (`kdd_b5_decision` returns `Literal["bundle","byow"]`, `evaluate_fr18.py`), NOT the divergent `"bundled"/"byowOnly"` the epic prose used — so a freeze↔decision audit join matches on identical strings (John). Both files are committed to develop. **Dev smoke** seeds the fields with the smoke decision (`byow`); the **real freeze (the authoritative `epic7CloseDecision`) is operator-owned** (needs the operator FR-18 + FR-25 + holdout-gap outcome via the DD #14 decision-DAG). [Source: epics.md#Story-7.7 AC (FR-20); train_v2_artifacts.py:61-95; evaluate_fr18.py kdd_b5_decision; training_log.json]

8. **Post-bundle regression watchlist v2.** A develop-only `_bmad-output/ml-training/post_bundle_watchlist.py` produces `_bmad-output/ml-training/post-bundle-regression-watchlist-v2.json` — each of the 241 `marginal-watchlist.json` tracks annotated with the **final selected model's** actual `fr18ModelBPM` prediction + whether it matched `expectedFailureMode`/`verificationPredicate` (reuse 7.6's closed-enum `matchesExpected` logic — DD #13). 7.4's `v2Prediction.seeds[]` is filled by 7.5; 7.6 fills `matchesExpected`; 7.7's post-bundle watchlist is the *final-model* annotation against the bundled/BYOW selection. **7-4-D1 watch-only:** flag any row whose v2 prediction fails in a way an ID3 tag would have predicted as a reopen signal; do NOT wire ID3 reading (FR-15 boundary). [Source: epics.md#Story-7.7 AC (Story-7.4 use c); marginal-watchlist.json (241); deferred-work.md (7-4-D1)]

9. **`main`-branch ship discipline (operator + decision-gated; documented, not dev-implemented).** The story DOCUMENTS the conditional squash-merge: if 7.6 said `decision: "bundle"` AND FR-25 (AC3) + the holdout-gap (AC6) both clear, the operator squash-merge to `main` re-adds `Sources/BoomBoomBoomKitML/Resources/giantsteps_v2.mlmodelc/` (re-bundled from `_bmad-output/ml-models/`), reinstates `Package.swift`'s `resources: [.copy("Resources")]` on the `BoomBoomBoomKitML` target, and updates `MODEL_CARD.md` with the new accuracy disclosure; if `decision: "byow"`, the squash-merge updates `MODEL_CARD.md` ONLY (continued BYOW-only status). **Dev does NOT perform this** (the real decision needs the operator run; current smoke decision is `byow`). [Source: epics.md#Story-7.7 AC (main-ship); CLAUDE.md §"Stays on develop ONLY" + §"Release Process"; Package.swift:23-32]

10. **Develop/main placement + build gates + reproducibility guard.** All new files (`scripts/sample-giantsteps-holdout.py`, `calibration_verification.py`, `fr24_net_benefit.py`, `holdout_gap.py`, `epic7_freeze.py`, `post_bundle_watchlist.py`, all `*.md`/`*.json`/`*.png` reports, the Makefile targets) are **develop-only**. The matplotlib dep + `uv.lock` are develop-only (ml-training). `.gitignore` gains the **bare** pattern `giantsteps-holdout-v2.json` (NO leading slash — Git matches a slash-free pattern at any depth, so it ignores the file under `_bmad-output/ml-training/`; a leading-slash `/giantsteps-holdout-v2.json` would silently fail to ignore the real file = worst outcome — Codex/Winston). Adding a develop-only ignore pattern to the main-tracked `.gitignore` is allowed/required by CLAUDE.md and does NOT count against the `Sources/Tests` diff gate. **`git diff --stat Sources/ Tests/` is EMPTY**; `make build` clean; `make py-lint` (ruff + format + ty) clean — new stdlib/numpy-clean modules join the ty set, but the matplotlib-importing `calibration_verification.py` stays OUT of ty scope (torch-exclusion precedent in the Makefile `py-lint` note). **`make test` is the epic-7-branch baseline (`483/105` if 7.6's `FR18EvaluationHarnessTests` has landed on `rterhaar/epic-7`) — it is INHERITED, not a 7.7 deliverable (7.7 adds zero Swift); assert "unchanged vs the branch baseline," not a hardcoded count (John).** **Reproducibility guard (replaces the filename-grep theater — Winston):** the committed manifest's seed + SHA256 digest must re-sample the identical 150 hashes (a real determinism tripwire); the temporal/structural seal (sampled at 7.7-close, only subtracts hashes from pre-existing dumps) is the leak argument, documented in the manifest. [Source: CLAUDE.md decision tree + .gitignore develop-only patterns; Makefile `py-lint`]

11. **Pressure-release valve (monotone downgrade-only — DD #14 decision-DAG).** A `_bmad-output/implementation-artifacts/7-7-pressure-release.md` documents the override procedure. **The close-out decision is a strict one-way pipeline (DD #14): FR-24 selects the promoted arm → KDD-B5 (7.6) emits `bundle`/`byow` → FR-25 calibration + holdout-gap can ONLY downgrade `bundle`→`byow`, NEVER upgrade `byow`→`bundle`.** Downgrade-only removes the feedback edge that made the calibration input appear circular. If FR-25 calibration (AC3) fails or is `inconclusive` even after 7.6 said `bundle`, Story 7.7 flips to `byow`, appends an addendum to `_bmad-output/ml-training/fr-18-evaluation.md` citing the FR-25 override, and bundling does NOT proceed. **Holdout-gap ≥ 8 pp branch (John — give it an owned default so close-out cannot stall):** the threshold is NEVER downgraded to pass; the **default is accept-the-BYOW-outcome + record the gap in the pressure-release doc**; restarting the FR-18 evaluator on a fresh checkpoint (not tuned against the iterated corpus) requires an **explicit operator sign-off with a named reason**. Both outcomes are recorded; the freeze (AC7) stamps the final (possibly-downgraded) `epic7CloseDecision`. [Source: epics.md#Story-7.7 pressure-release valve; DD #14]

12. **Dev done-definition + fail-closed smokes + adversarial fixture tests + concrete operator handoff.** Dev-done (done-with-operator-remainder, like 7.5/7.6) = all harness pieces ship + smoke-verify GREEN with **pure-Python fixture tests** (no corpus/model) for every gate computation. The fixtures must be **adversarial, failing for the right reason** (Amelia/Codex):
   - FR-24 tolerance gate: pass + each fail mode (Acc1 / ECE / tail-P95 individually breaching) + runner-up-absent → `inconclusive` (not silent pass) + per-corpus grouping by `r["corpus"]`.
   - Holdout-gap tripwire: `gap = 7 pp` closes, `gap = 8 pp` blocks (pin the `>=` boundary exactly); a holdout hash NOT present in the synthetic `seed_*/` dumps → the evaluator **fails closed** (does not silently compute over 661); the partition removes exactly 150.
   - Stratified sampler: correct strata counts (15×10 or tempo-only 5×30) + seed-determinism (same seed → identical 150 hashes) + style-absent fallback.
   - Calibration renderer: a subset of `N < 100` surfaces **`inconclusive`** (NOT a `None` ECE rendered as a false "0.0 vs 0.5"); the anchor row is present; an **abstain row with a stale finite `fr18ModelBPM` + `mlAbstained:true` is EXCLUDED** from the ECE subset (proves the explicit `not r["mlAbstained"]` guard, since `compute_ece_half_double` won't drop it); the **no-matplotlib path** is exercised (JSON bins emitted) since `calibration_verification.py` is ty-excluded (AC10).
   - Metadata freeze: `epic7CloseDecision` (`"bundle"`/`"byow"` literals) + `gitTag: "epic-7-close"` written.

   Each generator **fails closed with a named error** when its real input is absent. The smoke seeds the operator-handoff: a checklist of the exact operator remainder with **literal copy-pasteable commands** — (i) the v2 export `make ml-export-v2 EPIC7_V2_CHECKPOINT=<median-seed v2 .pt>` + `make compile-model ML_MODEL_INPUT=...giantsteps_v2.mlmodel`; (ii) the **per-arm** FR-24 producer runs (`make fr18-produce ARM=maskedMelPretrain SEED=42/43/44 BNNS_MODEL_URL=...` and the same for `ARM=supervisedAugmented`, each into `FR18_PRED_DIR/<arm>/seed_<N>/`); (iii) `make holdout-gap` over 7.6's real dumps; (iv) `make epic7-freeze` + the DD #14 decision-DAG order; (v) the decision-gated `main` squash-merge. The **authoritative v2 numbers + final `epic7CloseDecision` + the `main` squash-merge are operator-owned**, gated on KDD-B4 signoff → 7.5 training → 7.6 evaluation. [Source: mirrors 7.5/7.6 dev/operator split; epics.md#Story-7.7]

13. **Epic 7 close-out bookkeeping.** On dev-done, `sprint-status.yaml` records 7-7 with the dev/operator split narrative (done-with-operator-remainder); the inherited deferred items (7-6-D1, 7-5-D1) are confirmed non-blocking for 7.7; and the `epic-7-retrospective` remains `optional` (operator-triggered after the operator close-out run). No epic-status flip is performed by dev (the epic flips to `done` only when all stories — including the operator remainder — complete). [Source: sprint-status.yaml; deferred-work.md]

## Tasks / Subtasks

- [x] **Task 0 — pre-dev verification (gate, do FIRST)** (AC: #2, #6)
  - [x] `make ml-convert` + `make ml-convert-tests` against the v1 `model.pt` stand-in — both GREEN (proves AC2 needs no convert-tool edit for v2). Record result.
  - [x] Inspect the GiantSteps ground-truth JSON for a style/genre field; record present-or-absent (drives the 15-strata vs tempo-only fallback — DD #7).
  - [x] Confirm the 7.6 `seed_*/` per-track JSON join key (`trackId` / audio hash) the holdout-gap evaluator partitions on. Record the field.

- [x] **Task 1 — stratified GiantSteps holdout sampler** (AC: #6, #10)
  - [x] `scripts/sample-giantsteps-holdout.py`: fixed-seed stratified n=150 (5 tempo bins × 3 style labels × 10; tempo-only 5 × 30 fallback when style absent — pre-dev #2/DD #7); local JAMS file (`tempo` namespace, reuse `curate_sentinels.py` hashing pattern) → `giantsteps-holdout-v2.json` (gitignored, never committed, DD #10).
  - [x] Commit `giantsteps-holdout-v2-manifest.md` = script ref + GS corpus version + seed + SHA256 digest of the sorted `{trackId, sha256(audio)}` hash-manifest (NO BPM committed, DD #10); add the **bare** `giantsteps-holdout-v2.json` pattern to `.gitignore` (no leading slash — AC10).
  - [x] Reproducibility guard (replaces the grep theater — DD #6/Winston): assert the committed seed + digest re-sample the identical 150 hashes.

- [x] **Task 2 — holdout-gap evaluator** (AC: #6, #11)
  - [x] `holdout_gap.py` / `make holdout-gap`: read 7.6's `seed_*/` GS per-track dumps, join on `trackId`, partition by the holdout hashes, compute `Acc1_iterated` (511) + `Acc1_holdout` (150) + gap (percentage-points + per-arm Wilson LB) via the **leaf** counters `count_acc1`/`count_acc1_octave` (NOT `aggregate_seeds` — 661-guard landmine, DD #6/Winston); emit `giantsteps-holdout-gap.md`. Gates bind to `fr18ModelBPM` (DD #9). NO 7.6 re-run (DD #6).
  - [x] Gap ≥ 8 pp → block (informational tripwire, ~1.9σ at n=150; do not downgrade — AC11); < 8 pp → close normally. Fail closed (named error) when 7.6 dumps absent OR a holdout hash is missing from the dumps (do not silently compute over 661).

- [x] **Task 3 — FR-25 calibration verification** (AC: #3, #4)
  - [x] `calibration_verification.py`: import `compute_ece_half_double` / `bimodality_125_175` / `PRIOR_*` / `result["bins"]` from `evaluate_fr18.py` (DD #13); validate the producer invariant `mlAbstained == (fr18ModelBPM is None)` + explicitly filter `not r["mlAbstained"]` before metric calc (DD #9); render `calibration-verification.md` with the non-optional side-by-side anchor table (report subset **N** beside every metric + the anchor basis; `softmaxMaxP95` labeled sharpness-diagnostic-not-gate; state whether `PRIOR_ECE_HALF_DOUBLE=0.5` is measured or a sentinel), `eceHalfDoubleFloor: 0.10`, **`inconclusive` (N<100) blocks → byow, never a false "0.0 vs 0.5"**.
  - [x] Add matplotlib to `_bmad-output/ml-training/pyproject.toml` + `uv.lock` (develop-only); emit `calibration-reliability-v2.png` + the reliability-bin JSON; named error if matplotlib required-but-absent. Keep `calibration_verification.py` OUT of the ty set (matplotlib import — AC10).

- [x] **Task 4 — FR-24 semi-supervised net-benefit gate** (AC: #5)
  - [x] `fr24_net_benefit.py`: consume both-arm per-track predictions from **arm-namespaced** `FR18_PRED_DIR/<arm>/seed_<N>/` via `consume_predictions(root/<arm>)` per arm (NEVER one co-mingled root — `consume_predictions` raises on dup-seed/shared-SHA, DD #15); group by `r["corpus"]` (OA300 + GiantSteps + sentinels); compute Acc1/ECE/tail-P95; apply tolerance gate (Acc1 ±2 / ECE +0.02 / tail-P95 +0.5); runner-up fallback. **`inconclusive` when runner-up absent → net-benefit UNPROVEN, no silent SSL bundle** (DD #5: bundling-as-net-benefit-proven needs explicit recorded operator ack). Add `ARM=` to the `fr18-produce` Makefile target. Emit `fr-24-semi-supervised-net-benefit.md`. Binds to `fr18ModelBPM` (DD #9).

- [x] **Task 5 — FR-20 reproducibility freeze** (AC: #7)
  - [x] `make epic7-freeze` backed by a NEW `epic7_freeze.py` (NOT the SEEDS-gated `build_seed_metadata` — DD #8/Amelia): freeze `training_log.json` + `model_metadata.json`; add net-new `epic7CloseDecision` (`"bundle"`/`"byow"` — 7.6's exact literals) + `gitTag: "epic-7-close"` (DD #8). Smoke writes `byow`; real value operator-owned via the DD #14 DAG.

- [x] **Task 6 — post-bundle regression watchlist v2** (AC: #8)
  - [x] `post_bundle_watchlist.py`: annotate the 241 marginal-watchlist tracks with the final-model `fr18ModelBPM` + `matchesExpected` (reuse 7.6's closed-enum logic) → `post-bundle-regression-watchlist-v2.json`. Flag 7-4-D1 reopen signals (no ID3 wiring).

- [x] **Task 7 — BYOW export + convert smoke + main-ship documentation** (AC: #1, #2, #9)
  - [x] `make ml-export-v2` (with a `[ ! -e "$(CHECKPOINT)" ]` Makefile guard mirroring `compile-model` — `export.py` only raises raw `FileNotFoundError`) → `giantsteps_v2.mlmodel`; `make compile-model ML_MODEL_INPUT=...v2.mlmodel` → `.mlmodelc`; the load check is **load-AND-infer** (one fixture through the loaded `.mlmodelc` AND `convert.py --validate`, argmax-decoded BPM agrees within one bin — axiom-ai/Winston, not load-without-throw). Record the seed-selection rule (median-Acc1 seed of the promoted arm). Smoke against v1 `model.pt`; real v2 export operator-owned.
  - [x] Confirm `make ml-convert` / `make ml-convert-tests` GREEN (no `tools/coreml-convert/` edit — DD #2).
  - [x] Document the decision-gated `main` squash-merge (Resources + Package.swift + MODEL_CARD.md vs MODEL_CARD-only) in the story / pressure-release; do NOT perform it (AC9).

- [x] **Task 8 — pressure-release + operator handoff + gates** (AC: #11, #12, #13)
  - [x] `7-7-pressure-release.md`: FR-25-fail and holdout-gap≥8 override procedures (flip to byow; no threshold downgrade).
  - [x] Seed the operator-handoff checklist (v2 export, both-arm FR-24 producer runs, holdout-gap over real 7.6 dumps, freeze + decision finalize, decision-gated squash-merge).
  - [x] Makefile targets (`holdout-sample`, `holdout-gap`, `calibration-verify`, `fr24-net-benefit`, `epic7-freeze`, `post-bundle-watchlist`, `ml-export-v2`) — develop-only, fail loudly on a main-only checkout (`ml-*` precedent).
  - [x] pytest fixture suite (AC12) for every pure-function gate.
  - [x] Final gate sweep: `make build` clean, `make test` 483/105, `git diff --stat Sources/ Tests/` empty, `make py-lint` clean, pytest GREEN, grep guard, all generators fail-closed when real inputs absent.

## Dev Notes

### Reuse-not-reinvent — `evaluate_fr18.py` is the calibration/accuracy engine
Story 7.6 already shipped the ECE / bimodality / Wilson / half-double-subset math and the prior-bundle anchor constants (`evaluate_fr18.py:53-58,119-230`). Story 7.7's calibration + net-benefit + holdout-gap generators **import** these — they are thin renderers/comparators over the same functions, NOT re-implementations (DD #13). The runtime-prediction producer is the 7.6 `FR18EvaluationHarnessTests` benchmark test (re-used per arm for FR-24; re-used for the final-model post-bundle watchlist) — no new Swift inference path. The JAMS-emit pattern is `curate_sentinels.py`. This keeps 7.7's footprint pure-Python-glue + one config dep.

### Key verified API facts (factual-claims grep @ HEAD on `rterhaar/7-7`)
- `export.py:37-42` — `--checkpoint` (default `model.pt`) + `--output` (default `giantsteps_v1.mlmodel`, **parameterized** — can emit v2). `make ml-export` (`Makefile:411-416`) hardcodes `--output ../ml-models/giantsteps_v1.mlmodel` → 7.7 needs a v2 target/override (DD #3).
- `tools/coreml-convert/convert.py` — `--checkpoint`/`--arch {reference,custom}`/`--output`/`--validate`/`--input-shape 1,1,128,512`; `reference_arch.py:36-39` (`BPM_BIN_COUNT=256`, min 30 max 285), `:96-148` (TempoCNN). Matches the BNNSTechnique runtime contract (`BNNSTechnique.swift` expectedMelBands 128 / targetWidth 512 / expectedBinCount 256 / bpmBinOffset 30.0). **convert.py does NOT read `model_metadata.json` or embed `featureSetVersion`** → v2 needs no convert-tool edit (DD #2). 20-test pytest suite at `tools/coreml-convert/tests/test_round_trip.py`. `coremltools>=8,<10` dep.
- `BNNSTechnique.init(modelURL: URL? = Self.bundledReferenceURL) throws` (`BNNSTechnique.swift:264`); `supportedFeatureSetVersion = MLFeatureFrames.currentFeatureSetVersion = "v2"` (`MLFeatureFrames.swift:129` — Story 7.5 bumped it; do NOT assert a literal).
- `evaluate_fr18.py:53-58`: `PRIOR_SOFTMAX_MAX_P95 = 0.294`, `PRIOR_ECE_HALF_DOUBLE = 0.5`, `BIMODAL_ANCHORS = (125.0, 175.0)`, `BIMODAL_ANCHOR_TOL_BPM = 3.0`. Functions `wilson_lower_bound` (:119), `in_half_double_subset` (:139), `compute_ece_half_double` (:156), `bimodality_125_175` (:210), `kdd_b5_decision` (returns `Literal["bundle","byow"]`).
- matplotlib is **NOT** an ml-training dependency (grep clean; `uv.lock` no matplotlib) — 7.3 deferred the reliability PNG to 7.7 (`ablation/compare_kdd_b2.py:13`). 7.7 adds it develop-only (DD #4).
- `model_metadata.json` current fields (v1 schema) + `train_v2_artifacts.build_seed_metadata` v2 fields (`:61-95`: `metadataSchema, architecture, trainingVariant, weightingProfile, featureSetVersion, modelGeneration, seed, epochCount, corpusVersionHash, splitVersionHash, harnessGitSha, promotable, bundleEligibilityPendingFR18`). **No `epic7CloseDecision` / `gitTag`** — net-new (DD #8). `training_log.json` exists (per-epoch + metadata block).
- `marginal-watchlist.json` = 241 records `{track_id, failureCategory, expectedFailureMode, verificationPredicate, tonyLabel, dspBPM, rekordboxBPM, gridBPM, v2Prediction{status:"pending-7.5", seeds:[], varianceBpm:null, octaveNormalizedAgreement:null, matchesExpected:null}}`.
- `corpus_common.py`: `canonical_ratio` (:78), `OCTAVE_RATIO_TOL=0.05`, `HARMONIC_RATIO_TOL=0.03`, `file_sha256` (:199), `tier_for` (:108). `corpus_splits.json` giantsteps split = 595 (legacy fold) — **NOT** the 661-track benchmark corpus; the holdout samples the real GS corpus via `GIANTSTEPS_CORPUS_PATH` (661 active), 661 − 150 = 511 iterated (DD #6).
- `_bmad-output/ml-models/`: `giantsteps_v1.mlmodel/` + `giantsteps_v1.mlmodelc/` exist (smoke negative control); NO `giantsteps_v2.*`. `_bmad-output/ml-training/model.pt` = v1, 1.3 MB (the convert/export smoke stand-in). `_bmad-output/ml-training/fr18-predictions/` does NOT exist yet (operator produces the `seed_*/` dirs).
- `Package.swift:23-32` — `BoomBoomBoomKitML` target has NO `resources: [.copy("Resources")]` (removed Story 4-6 Branch C); no `Sources/BoomBoomBoomKitML/Resources/` dir. Re-add is operator + decision-gated (AC9).
- KDD-B4 signoff `corpus-diagnostics-v1.md:9` = `REVIEWER_SIGNOFF: pending` (gates the whole operator chain).

### Inherited deferred work
- **7-6-D1** — `dnbSentinelsCorrect` (of 4) is not split from `dnbSentinelsExpanded` (of 12); Wilson-over-resolved is the binding gate. Informational; does not block 7.7. [`deferred-work.md`]
- **7-5-D1 (RE-CONFIRMED)** — the Python↔Swift resample last-column divergence is upsampling-only (F ≤ 511); the FR-18/holdout corpora are long tracks (F ≫ 512 → downsampling, Swift/Python agree). Does NOT affect 7.7's evaluated predictions. [`deferred-work.md`]
- **7-4-D1** — `idTagBPM` unwired; watch the post-bundle watchlist (AC8) for tag-driven failure signals, do NOT wire ID3 (FR-15 boundary).

### Design Decisions

- **DD #1 — the v2 model does not exist yet ⇒ dev/operator split (mirrors 7.3/7.5/7.6).** KDD-B4 signoff is pending → 7.5's training run hasn't produced `giantsteps_v2_seed_*.mlmodel` (nor the supervisedAugmented runner-up FR-24 needs) → 7.6's authoritative FR-18 run + KDD-B5 decision + `seed_*/` dumps don't exist. **Resolution:** dev ships + smoke-verifies every close-out generator against the v1 negative control + pure-Python fixtures; the authoritative export/calibration/net-benefit/holdout-gap/freeze + the decision-gated squash-merge are operator-owned, surfaced as pending at close. Every generator fails closed (named error) when its real input is absent.

- **DD #2 — Story 7.7 has ZERO `Sources/`/`Tests/` diff; the convert tool is unchanged for v2.** v2 is a *feature-contract* version bump (`featureSetVersion` "v1"→"v2", done in 7.5), NOT an architecture change — the TempoCNN/256-bin/`[1,1,128,512]`/`30+argmax` reference contract is identical. `convert.py` does not read `model_metadata.json` or embed `featureSetVersion`. **Resolution:** AC2 is a smoke that the existing main-bound convert tool still works (against the v1 `model.pt` stand-in / the real v2 `.pt` at operator time); no `tools/coreml-convert/` edit. The only main-tracked file dev touches is `.gitignore` (a develop-only ignore pattern CLAUDE.md requires there). The 7.5/7.6 main-bound-file pattern does NOT recur here.

- **DD #3 — `make ml-export` hardcodes `giantsteps_v1`; `export.py` is parameterized.** `export.py --output` already accepts an arbitrary path. **Resolution:** add a develop-only `ml-export-v2` target (or document the `--output ../ml-models/giantsteps_v2.mlmodel` override) — do not mutate the existing `ml-export` recipe's default (other targets/precedent depend on it). The real v2 export needs the operator's v2 `.pt`.

- **DD #4 — matplotlib is a develop-only addition.** The reliability-diagram PNG was explicitly deferred from 7.3 to 7.7, and matplotlib is not yet a dep. **Resolution:** add it to `_bmad-output/ml-training/pyproject.toml` + `uv.lock` (develop-only — ml-training never ships to main, so a heavy plotting dep is acceptable). The generator emits the PNG **and** the reliability-bin JSON (inspectable without matplotlib; the fixture test asserts on the bins, not the PNG bytes). `calibration_verification.py` stays out of the ty-checked set (matplotlib import — torch-exclusion precedent).

- **DD #5 — FR-24 net-benefit is on the EXTERNAL corpora (OA300 + GiantSteps + sentinels), not tony.val/LAO; runner-up-absent does NOT silently bundle the SSL variant.** `ablation/compare_kdd_b2.py` already compared the two arms on tony.val + leave-artist-out (the KDD-B2 winner selection). FR-24 is a *different* gate: net benefit on the bundle-deciding corpora. **Resolution:** `fr24_net_benefit.py` re-uses the 7.6 producer once per arm (per-arm dir namespace — DD #15), groups by `r["corpus"]`, computes Acc1/ECE/tail-P95, applies the ±2/+0.02/+0.5 tolerance gate. **Polarity fix (Codex/Mary/John — the AC↔DD contradiction is resolved to the conservative reading):** the gate's whole purpose is to PROVE the SSL expansion was worth it; an `inconclusive` that lets the SSL variant bundle anyway defeats FR-24. If the `supervisedAugmented` runner-up (`train_runnerup.py`, operator-trained) is absent, FR-24 is `inconclusive` → **net-benefit is UNPROVEN**. The model may still ship on its own FR-18 gates, but bundling it as net-benefit-proven then requires an **explicit operator acknowledgment recorded in the report** (a named decision, not a silent auto-pass) OR training the runner-up to close the gate. "Net-benefit-proven" is never claimed without the comparison.

- **DD #6 — the holdout is a within-corpus stability check (NOT a sealed-pre-iteration leak detector); it partitions 7.6's existing GS dumps with no 7.6 re-run.** `corpus_splits.json` giantsteps = 595 (a legacy training fold), NOT the 661-track benchmark corpus; the sampler reads the real GS corpus (`GIANTSTEPS_CORPUS_PATH`, 661 active). 7.6 evaluated the FULL 661 (it predates and never reads the holdout). **Resolution:** `holdout_gap.py` reads 7.6's `seed_*/` per-track GS predictions (join on `trackId`), removes the 150 holdout hashes → `Acc1_iterated` (511) vs `Acc1_holdout` (150), both from the *same* operator run, via the **leaf** counters (NOT `aggregate_seeds`, whose `EXPECTED_GIANTSTEPS=661` guard would reject the 511 split — Winston). **Honesty (Mary):** because the 150 tracks were inside the same 661 corpus 7.6 tuned against, this measures *within-corpus subset dispersion*, not true iteration-leak — a small gap is necessary-but-not-sufficient (a uniform leak yields a near-zero gap). The gap is in **percentage-points** with per-arm Wilson LBs; at n=150 the difference-SE ≈ 4.2pp, so gap ≥ 8pp (≈ 1.9σ) is an **informational review tripwire, not a significance test**, and is NOT comparable to 7.6's 537/661 counts. The seal is **temporal + structural** (sampled at close, fixed-seed, SHA256-committed, only subtracts hashes from pre-existing dumps) — NOT a filename grep. No re-evaluation of 7.6.

- **DD #7 — style-label stratification falls back to tempo-only if GS carries no style field.** The 15-strata design needs a GS style/genre label that `resolve_giantsteps` does not currently read. **Resolution:** pre-dev #2 inspects the GS ground truth; if a style field exists, 5 tempo × 3 style × 10; else tempo-only 5 × 30, recorded as the degraded basis in the manifest. Never fabricate labels.

- **DD #8 — `epic7CloseDecision` + `gitTag` are net-new `model_metadata.json` fields, written by a separate `epic7_freeze.py`.** Neither exists in the current v1 schema nor `train_v2_artifacts.build_seed_metadata` (which is per-seed + `raise`s on `seed not in SEEDS` at `train_v2_artifacts.py:79` — wrong home for an epic-global fact, Amelia). **Resolution:** a NEW `epic7_freeze.py` stamps `epic7CloseDecision` + `gitTag: "epic-7-close"` into the frozen metadata (and into the 3 per-seed files for audit join). **`epic7CloseDecision` reuses 7.6's exact literals `"bundle" | "byow"`** (`kdd_b5_decision` → `Literal["bundle","byow"]`), NOT the epic prose's `"bundled"/"byowOnly"`, so a freeze↔decision join matches identical strings (John). Dev smoke writes `byow`; the authoritative value is operator-set after the FR-25 + holdout-gap outcome via the DD #14 downgrade-only DAG.

- **DD #9 — calibration/holdout/net-benefit metrics bind to `fr18ModelBPM` (= `decodedBPM`), never `result.bpm` (inherit 7.6's decodedBPM-binding decision).** Under `.mlOnly` an abstaining model returns the DSP fallback as `result.bpm`; reading it would mislabel DSP accuracy as model accuracy. **Resolution:** all 7.7 accuracy/calibration reads the 7.6 per-track schema's `fr18ModelBPM` field; abstain = model-incorrect for accuracy, and **explicitly excluded from the calibration subset via a `not r["mlAbstained"]` guard** (the imported `compute_ece_half_double` only drops non-finite `fr18ModelBPM`/`softmaxMax`, NOT `mlAbstained` — `evaluate_fr18.py:163-171` — so the exclusion must be made explicit + schema-validated against the invariant `mlAbstained == (fr18ModelBPM is None)`, Codex/Amelia). All AC/Task citations of this decision read **DD #9** (the prior draft mis-cited it as "DD #12").

- **DD #10 — two holdout artifacts (local JAMS gitignored, committed hash-digest manifest); bare `.gitignore` pattern.** The earlier "track-hash-list in JAMS format with per-track BPM" was self-contradictory (Codex/Winston) — a JAMS-with-BPM file IS the answer key, not a hash list. **Resolution:** (a) the local `giantsteps-holdout-v2.json` is a JAMS truth file (operator convenience) that is **`.gitignore`'d, never committed**; (b) the committed `giantsteps-holdout-v2-manifest.md` carries the script ref + corpus version + seed + the **SHA256 digest of the sorted `{trackId, sha256(audio)}` hash-manifest** — no BPM in the committed artifact (the GS ground truth the gap evaluator needs already lives in the corpus). The `.gitignore` pattern is the **bare** `giantsteps-holdout-v2.json` (NO leading slash — matches at any depth; a `/`-anchored form would silently fail to ignore the real file). Adding a develop-only ignore pattern to the main-tracked `.gitignore` is required by CLAUDE.md and does not count against the `Sources/Tests` diff gate.

- **DD #11 — FR-25 failure / holdout-gap≥8 are conservative downgrade-only override gates (see DD #14 for the DAG).** They can downgrade 7.6's `bundle` to `byow` at 7.7 close, never the reverse; the holdout-gap threshold is never downgraded to force a pass (epic-explicit). **Resolution:** `7-7-pressure-release.md` documents both override paths + the gap≥8 owned default (accept-BYOW + record; restart needs named operator sign-off — John); the freeze records the final (possibly-downgraded) `epic7CloseDecision`; an addendum is appended to `fr-18-evaluation.md`.

- **DD #12 — Epic 7 close-out is gated on the full operator chain; dev does NOT flip the epic to `done`.** The epic flips to `done` only when the operator remainder completes (real export + numbers + decision + squash-merge). **Resolution:** dev sets 7-7 done-with-operator-remainder, leaves `epic-7` `in-progress` and `epic-7-retrospective` `optional`; the operator triggers the retrospective after close-out.

- **DD #13 — reuse `evaluate_fr18.py`, do not reinvent (the calibration/accuracy engine).** The ECE, bimodality, Wilson, half/double-subset math and the `PRIOR_*` anchor constants already shipped in 7.6 (`evaluate_fr18.py:53-61,119-230`). **Resolution:** `calibration_verification.py` / `fr24_net_benefit.py` / `holdout_gap.py` IMPORT `compute_ece_half_double`, `bimodality_125_175`, `in_half_double_subset`, the `PRIOR_*` constants, `result["bins"]` (for the reliability PNG/JSON), and the leaf counters `count_acc1`/`count_acc1_octave` — they are thin renderers/comparators, never re-implementations. The runtime-prediction producer is the 7.6 `FR18EvaluationHarnessTests` benchmark (re-used per arm). The JAMS-emit pattern is `curate_sentinels.py` (hashing only — not the BPM payload, DD #10). Verified signatures: `compute_ece_half_double(rows: list[dict]) -> dict` (reads `fr18ModelBPM`/`softmaxMax`/`groundTruthBPM`/`dspCandidatesTop3`), `bimodality_125_175(preds: list[float], tol=3.0) -> dict` (flat BPM list — extract first). All AC/Task citations of the reuse rule read **DD #13** (the prior draft cited "DD #13" before this DD existed).

- **DD #14 — the close-out decision is a strict monotone DAG (resolves the calibration circularity).** Codex/Winston/John flagged AC3 reading "final-model" dumps while AC11 lets calibration flip the decision — an apparent input↔output loop. **Resolution:** order is one-way — (1) FR-24 selects the promoted arm [model identity]; (2) KDD-B5 (7.6 gates on that arm) → `bundle`/`byow`; (3) FR-25 calibration + holdout-gap may **only downgrade `bundle`→`byow`, never upgrade**; (4) `epic7CloseDecision` = step-3 output. The downgrade-only invariant removes the feedback edge. The key fact that makes it non-circular: **under `.mlOnly` the bundled and byow model are the SAME v2 checkpoint, so the per-track predictions calibration reads are identical regardless of the bundle/byow outcome** — "final model" = the v2 checkpoint, fixed at step 1; calibration consumes it, does not choose it (John).

- **DD #15 — two-arm FR-24 producer needs a crash-safe directory namespace.** `consume_predictions` (`evaluate_fr18.py:549-565`) RAISES `ValueError` on a duplicate seed (`:559`) and on two dirs sharing a checkpoint SHA (`:562`), so running both arms into one `FR18_PRED_DIR` is guaranteed to crash (Codex/Winston — the prior draft's "re-use the producer once per arm" was not just under-specified, it was broken). **Resolution:** arm-namespaced `FR18_PRED_DIR/<arm>/seed_<N>/` (`<arm> ∈ {maskedMelPretrain, supervisedAugmented}`); `fr24_net_benefit.py` calls `consume_predictions(root/<arm>)` once per arm; `fr18-produce` gains an `ARM=` override so the operator command is copy-pasteable (literal commands in AC12).

### Project Structure Notes
- New develop-only Python: `scripts/sample-giantsteps-holdout.py` (sibling of the other develop-only `scripts/*.py`), `_bmad-output/ml-training/{calibration_verification,fr24_net_benefit,holdout_gap,epic7_freeze,post_bundle_watchlist}.py` (+ pytest under `_bmad-output/ml-training/ablation/tests/`). Reports under `_bmad-output/ml-training/`; `7-7-pressure-release.md` under `_bmad-output/implementation-artifacts/`.
- Develop-only dep: matplotlib in `_bmad-output/ml-training/pyproject.toml` + `uv.lock`.
- Main-tracked but develop-only-pattern: one line in `.gitignore`.
- ZERO `Sources/`/`Tests/`/`tools/coreml-convert/` change.
- PR base `rterhaar/epic-7` (never develop/main). Branch `rterhaar/7-7`.

### References
- [Source: _bmad-output/planning-artifacts/epics.md#Story-7.7 (lines 833-879); epics changelog lines 25-26 (n=150 holdout, gap≥8 tripwire)]
- [Source: _bmad-output/implementation-artifacts/7-6-fr18-promotion-gate-and-fr23-octave-consistency-report.md (dev/operator split + DD #12 decodedBPM binding + evaluate_fr18 reuse)]
- [Source: _bmad-output/implementation-artifacts/7-5-substrate-bound-tempocnn-training-v2-multi-seed.md (v2 checkpoints + featureSetVersion v2 + train_runnerup.py + per-seed metadata)]
- [Source: _bmad-output/ml-training/evaluate_fr18.py:53-58,119-230 (PRIOR_* anchors + ECE/bimodality/Wilson/kdd_b5_decision)]
- [Source: _bmad-output/ml-training/export.py:37-42; Makefile `ml-export`/`ml-convert`/`ml-convert-tests`/`compile-model`/`ml-pipeline`]
- [Source: tools/coreml-convert/convert.py + reference_arch.py:36-39,96-148; tools/coreml-convert/tests/test_round_trip.py (20 tests); pyproject.toml (coremltools>=8,<10)]
- [Source: Sources/BoomBoomBoomKitML/BNNSTechnique.swift:264; Sources/BoomBoomBoomKit/FeatureSubstrate/MLFeatureFrames.swift:129; Package.swift:23-32]
- [Source: _bmad-output/ml-training/corpus_common.py:74-88,108,199; corpus_splits.json (giantsteps=595); marginal-watchlist.json (241); curate_sentinels.py:130-159 (JAMS); train_v2_artifacts.py:61-95; ablation/compare_kdd_b2.py:13 (PNG deferred)]
- [Source: MODEL_CARD.md:11-12 (softmax_max_p95=0.294, bimodal 125/175); _bmad-output/ml-training/corpus-diagnostics-v1.md:9 (KDD-B4 REVIEWER_SIGNOFF: pending)]
- [Source: _bmad-output/ml-models/ (giantsteps_v1.mlmodel/.mlmodelc present; no v2); _bmad-output/ml-training/model.pt (v1 stand-in)]
- [Source: CLAUDE.md §"Release Process" (develop vs main; .gitignore develop-only patterns; squash-merge protocol); Makefile `py-lint` (ty-exclusion precedent)]
- [Source: _bmad-output/implementation-artifacts/deferred-work.md (7-6-D1, 7-5-D1, 7-4-D1)]

## Dev Agent Record

### Agent Model Used

claude-opus-4-8

### Debug Log References

- `make py-lint` — ruff check + ruff format-check + ty all clean (the 4 stdlib/numpy modules `holdout_gap.py`, `fr24_net_benefit.py`, `epic7_freeze.py`, `post_bundle_watchlist.py` + `scripts/sample-giantsteps-holdout.py` joined the ty set; `calibration_verification.py` stays OUT — lazy matplotlib import, AC10).
- `uv run pytest ablation/tests/` — **133 passed** (was 109; +24 `test_epic7_close.py`).
- End-to-end CLI smoke (synthetic 3-seed + two-arm dumps under `/tmp`): `calibration_verification.py` (report + reliability PNG rendered via matplotlib), `holdout_gap.py` (median gap → close), `fr24_net_benefit.py` (two-arm → evaluated), `epic7_freeze.py` (stamped `epic7CloseDecision`/`gitTag`), `post_bundle_watchlist.py` (real 241-track marginal-watchlist annotated). Sampler fails closed with `HoldoutSampleError` when `GIANTSTEPS_CORPUS_PATH` unset.
- `make build` clean; `make test` **483/105** (unchanged epic-7 baseline — zero Swift touched); swiftlint 1 pre-existing `LUFSAnalyzer:94` TODO baseline, 0 serious.
- `git diff --stat Sources/ Tests/` — **empty** (zero-diff contract holds).

### Completion Notes List

Implemented the develop-only Epic-7 close-out harness, ZERO `Sources/`/`Tests/` diff (DD #2). All gate logic reuses `evaluate_fr18.py` (DD #13) — thin renderers/comparators, no re-implementation. Dev/operator split mirrors 7.3/7.5/7.6: dev ships + smoke-verifies; the authoritative v2 numbers + final `epic7CloseDecision` + the decision-gated `main` squash-merge are operator-owned (gated on KDD-B4 signoff → 7.5 training → 7.6 eval).

Pre-dev review (axiom:ask → axiom-ai/coreml-conversion + Codex thread 019e9611 + party-mode Mary/Winston/Amelia/John) hardened the spec 12→15 DDs before coding. Notable in-code resolutions: the FR-24 two-arm dirs are namespaced `FR18_PRED_DIR/<arm>/seed_<N>/` because `consume_predictions` raises on dup-seed/shared-SHA (DD #15, verified at `evaluate_fr18.py:559,562`); the holdout-gap uses the leaf counters (not `aggregate_seeds`, whose 661-guard would reject the 511 split — DD #6); calibration explicitly filters `not mlAbstained` + validates the producer invariant before metric calc (DD #9); the 7-4-D1 reopen signal fires on `matches is None` (undecidable for lack of a tag), not `False` (caught + fixed in the first test run).

OPERATOR-OWNED REMAINDER (surfaced as pending, NOT dev tasks): see `7-7-pressure-release.md` operator checklist — real v2 export, both-arm FR-24 producer runs, holdout sample + gap, calibration numbers, the FR-20 freeze with the real decision, and the decision-gated squash-merge. The 1Password-signed commit + PR base `rterhaar/epic-7` are operator-owned per convention.

### File List

NEW (develop-only):
- `scripts/sample-giantsteps-holdout.py` (Task 1)
- `_bmad-output/ml-training/holdout_gap.py` (Task 2)
- `_bmad-output/ml-training/calibration_verification.py` (Task 3)
- `_bmad-output/ml-training/fr24_net_benefit.py` (Task 4)
- `_bmad-output/ml-training/epic7_freeze.py` (Task 5)
- `_bmad-output/ml-training/post_bundle_watchlist.py` (Task 6)
- `_bmad-output/ml-training/ablation/tests/test_epic7_close.py` (24 fixture tests)
- `_bmad-output/implementation-artifacts/7-7-pressure-release.md` (Task 8)

MODIFIED:
- `Makefile` (Task 7/8) — `ml-export-v2`, `holdout-sample`, `holdout-gap`, `calibration-verify`, `fr24-net-benefit`, `epic7-freeze`, `post-bundle-watchlist` targets; `ARM=` override on `fr18-produce`; 5 new modules in the `py-lint` ty set.
- `_bmad-output/ml-training/pyproject.toml` + `uv.lock` — develop-only `matplotlib>=3.8` (DD #4).
- `.gitignore` — bare `giantsteps-holdout-v2.json` pattern (DD #10).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 7-7 → review.

### Change Log

- 2026-06-05 — DEV (bmad-dev-story): implemented the develop-only close-out harness (6 modules + 24-test fixture suite + 7 Makefile targets + matplotlib dep + .gitignore + pressure-release doc), zero Sources/Tests diff. Gates GREEN: py-lint clean, ablation pytest 133 (+24), end-to-end CLI smoke (PNG rendered, freeze stamped, 241-track watchlist annotated, sampler fail-closed), make build clean, make test 483/105 unchanged, git diff Sources/Tests empty. Status in-progress → review. Operator-owned remainder per 7-7-pressure-release.md.
- 2026-06-05 — Pre-dev review (axiom:ask → axiom-ai/coreml-conversion + Codex spec-review thread 019e9611 + party-mode Mary/Winston/Amelia/John). 3 BLOCKER + 13 SHOULD-FIX folded in; spec grew 12→15 DDs. HEADLINE fixes: (1) **FR-24 polarity reconciled** — `inconclusive`/runner-up-absent no longer silently bundles the SSL variant (AC5/DD #5); (2) **two-arm producer was BROKEN, not just under-specified** — `consume_predictions` raises on dup-seed/shared-SHA (`evaluate_fr18.py:559,562`), now arm-namespaced `FR18_PRED_DIR/<arm>/seed_<N>/` + `ARM=` override (AC5/AC12/DD #15); (3) **calibration circularity resolved** via the monotone downgrade-only decision-DAG + the "same `.mlOnly` checkpoint" insight (AC3/AC11/DD #14). SHOULD-FIX: AC1 load→load-AND-infer parity (axiom-ai); holdout reframed stability-check-not-leak-detector + percentage-point units + ~1.9σ caveat + leaf-counter 661-guard bypass (Mary/Winston); holdout artifact split (gitignored JAMS local vs committed hash-digest) + bare `.gitignore` pattern + grep-theater dropped (Winston); explicit `not mlAbstained` filter + schema validation (Codex/Amelia); `epic7_freeze.py` separate from the SEEDS-gated builder (Amelia); `epic7CloseDecision` literals reconciled to 7.6's `"bundle"/"byow"` (John); ECE≈0.5 anchor citation fixed to `evaluate_fr18.py:55` + sentinel-vs-measured honesty (Codex/Mary); seed-selection rule for the single export (John); adversarial fixtures pinned (AC12); DD cross-references renumbered (decodedBPM = DD #9, reuse = DD #13 — prior draft mis-cited "DD #12"/"DD #13").

### Review Findings (code-review 2026-06-05 — Blind Hunter + Codex Blind Hunter + Edge Case Hunter + Acceptance Auditor)

Four adversarial layers (the operator-requested extra blind hunter run via Codex). Triage: **0 decision-needed / 8 patch / 0 defer / 6 dismiss**. All 8 patches applied + regression-tested (ablation pytest 133→136, +3).

- [x] [Review][Patch] **FR-24 fails OPEN with empty `perCorpus`** (Codex BH + Edge Case, convergent BLOCKER) `fr24_net_benefit.py` — `net_benefit_gate` returned `netBenefitProven: True` with zero comparable corpora (overall init True, loop skips all), violating the DD #5 polarity. Fixed: `proven = overall and compared > 0`; `evaluate` returns `inconclusive`+`requiresOperatorAck` when both arms present but no comparable corpus rows. +regression test.
- [x] [Review][Patch] **`key` in STYLE_KEYS stratifies on musical key** (Blind Hunter + Acceptance Auditor) `scripts/sample-giantsteps-holdout.py` — GiantSteps is the Tempo+Key dataset; harmonic `key` is not a style label, would wrongly engage style+tempo stratification, violating DD #7. Fixed: removed `"key"` (kept style/genre/subgenre).
- [x] [Review][Patch] **Stratification weakened by global sort+trim** (Codex BH + Blind Hunter, BLOCKER) `scripts/sample-giantsteps-holdout.py` — `sorted(...)[:n]` + "other" excluded from strata_keys lexically dropped strata-selected picks. Fixed: quota distributes n across populated strata (remainder to first), no global trim, "other" is a real stratum.
- [x] [Review][Patch] **`expectedFailureMode` always null** (Edge Case Hunter) `post_bundle_watchlist.py` — `_load_watchlist` strips the field. Fixed: `_load_expected_failure_modes()` reads the raw `marginal-watchlist.json`; smoke now populates 241/241. +regression test.
- [x] [Review][Patch] **Holdout cardinality unenforced** (Codex BH + Edge Case) `holdout_gap.py` — a duplicate trackId would inflate the holdout subset. Fixed: `partition` raises when `len(holdout) != len(holdout_ids)`; dropped the dead `EXPECTED_ITERATED`. +regression test.
- [x] [Review][Patch] **Non-finite JSON risk** (Codex BH) — added `allow_nan=False` to every report writer (calibration/fr24/holdout_gap/post_bundle/sampler/freeze).
- [x] [Review][Patch] **`_median_model_bpm` accepts non-finite** (Codex BH) `post_bundle_watchlist.py` — added `math.isfinite` filter.
- [x] [Review][Patch] **`epic7_freeze` did not touch `training_log.json`** (Edge Case + Auditor) — AC7/docstring name it; now stamps both files. Plus octave-Acc1 added to the gap report (Auditor AC6 prose alignment) and the AC1/AC12 `CHECKPOINT=` → `EPIC7_V2_CHECKPOINT=` prose fix.

Dismissed (6, verified correct): net-benefit inequality directions (Acc1 `>=r-2` / ECE `<=r+0.02` / tail `<=r+0.5`) — correct improve-or-preserve; `compute_gap` sign + pp units — correct; Makefile `$(if $(ARM),…)` — correct; `_quantile_edges`/`tempo_bin` off-by-one — none (bisect-right); all-abstain calibration NaN — safe (`percentile([])` returns 0.0, `bimodality_125_175` handles n=0, ECE inconclusive); all imported `evaluate_fr18` signatures/constants match.

Re-verified GREEN: ablation pytest 136 (+3 regression), `make py-lint` (ruff+format+ty incl. 5 new modules) clean, end-to-end CLI smoke (FR-24 fail-closed + stratification rewrite + expectedFailureMode 241/241), `git diff Sources/ Tests/` empty, grep guard clean, holdout JSON gitignored. Clean review after patches → review → done (operator remainder per `7-7-pressure-release.md`).

### Codex diff-review (2026-06-05, post code-review) — 4 findings, all applied

Codex (thread 019e9660) re-reviewed the patched diff. 1 BLOCKER + 2 SHOULD-FIX + 1 NICE, all applied + 1 regression test (pytest 136→137); it confirmed the 8 prior patches correct and dismissed the inequality-direction / closure / Makefile-ARM / tempo-bin items.

- [x] [Review][Patch] **FR-24 lacked a per-corpus denominator guard** (BLOCKER) `fr24_net_benefit.py` — the empty-`perCorpus` fix was correct but a partial/tiny operator corpus (e.g. a 5-track OA300) still counted as comparable and could mis-promote (the raw `Acc1 ±2` tolerance is only meaningful at full corpus size). Added `MIN_DENOMINATOR = {oa300: 82, giantsteps: 661, sentinel: 1}` (reusing `evaluate_fr18.EXPECTED_*`, mirroring the 7.6 denominator BLOCKER fix); a corpus below its denominator is recorded in `belowDenominator` and NOT counted as compared → all-below → `inconclusive`. +regression test.
- [x] [Review][Patch] **holdout_gap duplicate-anywhere + sealed-count guards** (SHOULD-FIX) `holdout_gap.py` — `partition` now rejects a duplicate trackId in EITHER subset (`len(by_id) != len(gs_rows)`, was holdout-only); `_load_holdout_ids` enforces the sealed `n=150` (a truncated holdout file now fails closed). +regression test.
- [x] [Review][Patch] **post_bundle fail-closed on empty prediction root** (SHOULD-FIX) `post_bundle_watchlist.py` — a root with no marginal predictions would annotate every track `v2ModelBPM=None` and "succeed"; now raises `RuntimeError` when zero marginal rows are present.
- [x] [Review][Patch] **strata-count wording** (NICE) `scripts/sample-giantsteps-holdout.py` — docstring corrected: the `"other"` catch-all means up to 5×4=20 populated strata, not exactly 15; the n=150 quota distributes across whatever is populated.

Re-verified GREEN: ablation pytest 137, `make py-lint` clean, CLI smoke (fr24 full-denominator → evaluated; fr24 tiny-corpus → below-denominator excluded; post-bundle no-marginal → fail-closed), `git diff Sources/ Tests/` empty.

### Copilot review (PR #31, 2026-06-05) — 3 comments, all valid, all applied

All three verified against code (none false positives); develop-only, low-to-medium severity.

- [x] [Review][Patch] **FR-24 sentinel-only fail-open + inaccurate comment** (substantive) `fr24_net_benefit.py` — `MIN_DENOMINATOR` hardcoded `sentinel: 1` while the comment claimed it mirrors `EXPECTED_*`; a sentinel-only run (oa300/giantsteps below denominator) could decide `netBenefitProven` on n<=12 evidence. Fixed: corrected the comment (sentinel is best-effort by design, k<12 per AC13) + `net_benefit_gate` now requires >=1 FIXED corpus (`oa300`/`giantsteps`) compared before `netBenefitProven`; `evaluate` returns `inconclusive` when `fixedCorporaCompared == 0`. +regression test (138).
- [x] [Review][Patch] **`detect_style_key` doc-code mismatch** (minor) `scripts/sample-giantsteps-holdout.py` — docstring said "majority" but the condition is `>= 0.5 * len` (accepts exactly half); docstring corrected to "at least half (>= 50%)".
- [x] [Review][Patch] **`ml-export-v2` relative-path inconsistency** (minor) `Makefile` — the `[ ! -e ]` check ran repo-root-relative while `export.py --checkpoint` ran ml-training-relative after `cd`; wrapped `$(EPIC7_V2_CHECKPOINT)` with `$(abspath …)` on both sides (default is already absolute → no-op there).

Re-verified GREEN: ablation pytest 138, `make py-lint` clean, CLI smoke (fr24 full-denominator → evaluated/proven; sentinel-only → inconclusive; Makefile relative override → absolute `--checkpoint`), `git diff Sources/ Tests/` empty.
