# Commit Conventions

Gloria's knowledge of this repository's commit style, tag format, and project structure.

## Message Format

(Populated by SC scan — e.g., "conventional commits with feat/fix/chore prefixes")

## Tag Scheme

(Populated by SC scan — e.g., "v1.{epic}.{bugfix}-{build}" or "tagless repo")

## Story/Epic ID Notation

(Populated by SC scan — e.g., "uses hyphen format 12-4, dot format not observed")

## Artifact Paths

(Populated by SC scan — e.g., "_bmad-output/implementation-artifacts/")

## Notes

*This file holds meta-knowledge about conventions — never git output or commit data.*
